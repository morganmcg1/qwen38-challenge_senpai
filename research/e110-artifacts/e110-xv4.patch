diff --git a/Vendor/mlx-swift/Source/Cmlx/mlx-generated/quantized.cpp b/Vendor/mlx-swift/Source/Cmlx/mlx-generated/quantized.cpp
index 36c45a49..f3d26477 100644
--- a/Vendor/mlx-swift/Source/Cmlx/mlx-generated/quantized.cpp
+++ b/Vendor/mlx-swift/Source/Cmlx/mlx-generated/quantized.cpp
@@ -1033,13 +1033,14 @@ METAL_FUNC void qmv_fast_crossrow_affine4_g64_wide(
             simd_lid * values_per_thread + 4 * i;
         thread float xc[4];
         if (DIRECT_NIBBLES) {
-          xc[0] = static_cast<float>(xm[0]);
-          xc[1] = static_cast<float>(xm[1]);
-          xc[2] = static_cast<float>(xm[2]);
-          xc[3] = static_cast<float>(xm[3]);
+          const vec<T, 4> xv = *reinterpret_cast<const device vec<T, 4>*>(xm);
+          xc[0] = static_cast<float>(xv[0]);
+          xc[1] = static_cast<float>(xv[1]);
+          xc[2] = static_cast<float>(xv[2]);
+          xc[3] = static_cast<float>(xv[3]);
           // Preserve the incumbent BF16 expression tree used for the affine
           // bias correction; only the qdot nibble extraction changes.
-          sums[m] += xm[0] + xm[1] + xm[2] + xm[3];
+          sums[m] += xv[0] + xv[1] + xv[2] + xv[3];
         } else {
           sums[m] += load_vector<T, float, 4, 4>(xm, xc);
         }
diff --git a/Vendor/mlx-swift/Source/Cmlx/mlx/mlx/backend/metal/kernels/quantized.h b/Vendor/mlx-swift/Source/Cmlx/mlx/mlx/backend/metal/kernels/quantized.h
index 48739953..8c56e0e4 100644
--- a/Vendor/mlx-swift/Source/Cmlx/mlx/mlx/backend/metal/kernels/quantized.h
+++ b/Vendor/mlx-swift/Source/Cmlx/mlx/mlx/backend/metal/kernels/quantized.h
@@ -1020,13 +1020,14 @@ METAL_FUNC void qmv_fast_crossrow_affine4_g64_wide(
             simd_lid * values_per_thread + 4 * i;
         thread float xc[4];
         if (DIRECT_NIBBLES) {
-          xc[0] = static_cast<float>(xm[0]);
-          xc[1] = static_cast<float>(xm[1]);
-          xc[2] = static_cast<float>(xm[2]);
-          xc[3] = static_cast<float>(xm[3]);
+          const vec<T, 4> xv = *reinterpret_cast<const device vec<T, 4>*>(xm);
+          xc[0] = static_cast<float>(xv[0]);
+          xc[1] = static_cast<float>(xv[1]);
+          xc[2] = static_cast<float>(xv[2]);
+          xc[3] = static_cast<float>(xv[3]);
           // Preserve the incumbent BF16 expression tree used for the affine
           // bias correction; only the qdot nibble extraction changes.
-          sums[m] += xm[0] + xm[1] + xm[2] + xm[3];
+          sums[m] += xv[0] + xv[1] + xv[2] + xv[3];
         } else {
           sums[m] += load_vector<T, float, 4, 4>(xm, xc);
         }
