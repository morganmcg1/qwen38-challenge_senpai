## (0) Probe validity: E27 anchors and control gates

  E27 measured NA=2..5 at r=4 independently. If these four numbers do not
  reproduce, the probe is wrong and nothing below counts.

  cell             E27   E36  match
  xship_na2          62    62   True
  xship_na3          83    83   True
  xship_na4         104   104   True
  xship_na5         125   125   True
  NA=6 r=4 (E27 known-BAD)       regs=144 acc_spill=True  (E27: 144, spills)

  generated body at r=4,v=16 vs shipped template, NA=2..5: [62, 83, 104, 125]
  gate_validation_failures: none
  anchors reproduce: True


## (a) The grid: peak_live_regs / private_bytes, S = accumulator spill

### grid_relaxed

| NA | r1 v8 | r1 v16 | r1 v32 | r1 v64 | r2 v8 | r2 v16 | r2 v32 | r2 v64 | r3 v8 | r3 v16 | r3 v32 | r3 v64 | r4 v8 | r4 v16 | r4 v32 | r4 v64 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 2 | 35/0 | 38/8 | 38/16 | 38/32 | 56/0 | 46/16 | 46/32 | 59/64 | 54/12 | 54/24 | 58/48 | 81/96 | 63/16 | 62/32 | 72/64 | 64/160 |
| 3 | 46/0 | 49/8 | 49/16 | 49/32 | 59/8 | 61/16 | 61/32 | 64/64 | 72/12 | 72/24 | 72/48 | 88/96 | 83/16 | 83/32 | 83/64 | 85/160 |
| 4 | 56/0 | 60/8 | 60/16 | 60/32 | 76/8 | 76/16 | 76/32 | 76/64 | 90/12 | 90/24 | 90/48 | 95/96 | 104/16 | 104/32 | 104/64 | 106/160 |
| 5 | 72/4 | 71/8 | 71/16 | 71/32 | 91/8 | 91/16 | 91/32 | 91/64 | 108/12 | 108/24 | 108/48 | 108/96 | 125/16 | 125/32 | 125/64 | 127/160 |
| 6 | 83/4 | 82/8 | 82/16 | 82/32 | 106/8 | 106/16 | 106/32 | 106/64 | 126/12 | 126/24 | 126/48 | 126/96 | 144S/112 | 144S/128 | 144S/160 | 146S/256 |
| 7 | 94/4 | 93/8 | 93/16 | 93/32 | 121/8 | 121/16 | 121/32 | 121/64 | 144/12 | 144/24 | 144/48 | 144/96 | 157S/128 | 157S/144 | 157S/176 | 159S/272 |
| 8 | 105/4 | 104/8 | 104/16 | 104/32 | 136/8 | 136/16 | 136/32 | 136/64 | 168S/108 | 168S/120 | 168S/144 | 168S/192 | 177S/144 | 177S/160 | 177S/192 | 179S/288 |
| 9 | 116/4 | 115/8 | 115/16 | 115/32 | 151/8 | 151/16 | 151/32 | 151/64 | 187S/120 | 187S/132 | 187S/156 | 187S/204 | 197S/160 | 197S/176 | 197S/208 | 199S/304 |
| 10 | 127/4 | 126/8 | 126/16 | 126/32 | 166/8 | 166/16 | 166/32 | 166/64 | - | - | - | - | - | - | - | - |
| 11 | 138/4 | 137/8 | 137/16 | 137/32 | 181/8 | 181/16 | 181/32 | 181/64 | - | - | - | - | - | - | - | - |
| 12 | 148/0 | 156/0 | 126/16 | 126/32 | 264/0 | 196/16 | 196/32 | 196/64 | - | - | - | - | - | - | - | - |

### coverage_preserving

| NA | r1 v8 | r1 v16 | r1 v32 | r1 v64 | r2 v8 | r2 v16 | r2 v32 | r2 v64 | r4 v8 | r4 v16 | r4 v32 | r4 v64 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 2 | 36/0 | 39/8 | 39/16 | 39/32 | 59/0 | 49/16 | 49/32 | 62/64 | 63/16 | 62/32 | 72/64 | 64/160 |
| 3 | 48/0 | 51/8 | 51/16 | 51/32 | 64/8 | 66/16 | 66/32 | 69/64 | 83/16 | 83/32 | 83/64 | 85/160 |
| 4 | 59/0 | 63/8 | 63/16 | 63/32 | 83/8 | 83/16 | 83/32 | 83/64 | 104/16 | 104/32 | 104/64 | 106/160 |
| 5 | 76/4 | 75/8 | 75/16 | 75/32 | 100/8 | 100/16 | 100/32 | 100/64 | 125/16 | 125/32 | 125/64 | 127/160 |
| 6 | 88/4 | 87/8 | 87/16 | 87/32 | 117/8 | 117/16 | 117/32 | 117/64 | 144S/112 | 144S/128 | 144S/160 | 146S/256 |
| 7 | 100/4 | 99/8 | 99/16 | 99/32 | 134/8 | 134/16 | 134/32 | 134/64 | 157S/128 | 157S/144 | 157S/176 | 159S/272 |
| 8 | 112/4 | 111/8 | 111/16 | 111/32 | 151/8 | 151/16 | 151/32 | 151/64 | 177S/144 | 177S/160 | 177S/192 | 179S/288 |
| 9 | 124/4 | 123/8 | 123/16 | 123/32 | 168/8 | 168/16 | 168/32 | 168/64 | 197S/160 | 197S/176 | 197S/208 | 199S/304 |

threadgroup memory references across all 262 cells: {0} (the crossrow family declares none, at any values_per_thread)


## (a2) Max spill-free NA, by rows_per_simd and values_per_thread

| arm | r | v=8 | v=16 | v=32 | v=64 |
|---|---|---|---|---|---|
| grid_relaxed | 1 | 12 | 12 | 12 | 12 |
| grid_relaxed | 2 | 12 | 12 | 12 | 12 |
| grid_relaxed | 3 | 7 | 7 | 7 | 7 |
| grid_relaxed | 4 | 5 | 5 | 5 | 5 |
| coverage_preserving | 1 | 9 | 9 | 9 | 9 |
| coverage_preserving | 2 | 9 | 9 | 9 | 9 |
| coverage_preserving | 4 | 5 | 5 | 5 | 5 |

  (12 is the largest NA compiled; cells reading 12 are '>=12, untested above')


## (d) Register model in three dimensions

  The headline does not need a fit. Hold (arm, NA, r) fixed and vary only
  values_per_thread; the register count is the SAME NUMBER. Delta against
  the v=16 column, over every spill-free cell:

  arm                      NA range    v=8   v=32   v=64
  grid_relaxed                 2..9 10/1.08 10/0.54 27/2.77
  grid_relaxed                 4..9 4/0.50 0/0.00 5/0.50
  coverage_preserving          2..9 10/1.40 10/0.50 13/1.20
  coverage_preserving          4..9 4/0.64 0/0.00 2/0.29

  (max / mean absolute delta in registers. NA=2,3 are where the allocator
  does small-cell things: they carry the whole max. From NA=4 up, every
  spill-free cell at r<=2 is bit-identical across v=8..64.)

  Per (r, v) line fits, over spill-free NA >= 4, falling back to all
  spill-free NA when that leaves under 3 points (r=4, i.e. E27's own ladder).

   r   v  intercept   slope  max|resid|   NA used
   1   8      12.54   11.54        2.68     4..10
   1  16      16.00   11.00        0.00     4..10
   1  32      16.00   11.00        0.00     4..10
   1  64      16.00   11.00        0.00     4..10
   2   8      16.00   15.00        0.00     4..10
   2  16      16.00   15.00        0.00     4..10
   2  32      16.00   15.00        0.00     4..10
   2  64      16.00   15.00        0.00     4..10
   3   8      18.00   18.00        0.00      4..7
   3  16      18.00   18.00        0.00      4..7
   3  32      18.00   18.00        0.00      4..7
   3  64      27.50   16.50        2.00      4..7
   4   8      21.30   20.70        0.40      2..5
   4  16      20.00   21.00        0.00      2..5
   4  32      33.00   18.00        4.00      2..5
   4  64      22.00   21.00        0.00      2..5

  Is the slope a function of values_per_thread at all?
    r=1: slopes across v=8..64 = [11.54, 11.0, 11.0, 11.0]  spread = 0.536
    r=2: slopes across v=8..64 = [15.0, 15.0, 15.0, 15.0]  spread = 0.000
    r=3: slopes across v=8..64 = [18.0, 18.0, 18.0, 16.5]  spread = 1.500
    r=4: slopes across v=8..64 = [20.7, 21.0, 18.0, 21.0]  spread = 3.000

  slope(r) = 8.00 + 3.30*r   max |residual| = 0.40
  E32 fitted 8.36 + 3.19*r on the same kernel. The small shift is E32's r=1
  fit having included NA=11,12, where the allocator leaves the linear regime
  (156 regs at NA=12/v=16 with ZERO allocas, 126 at v=32). That is what gave
  E32's r=1 row its 5.45 residual. The mechanism reading is unchanged and
  slightly better: 8.00/3.30 = 2.42 against the 5:2 = 2.50 ratio of
  x-side floats (a0..a3 + sums) to per-row floats (acc + partial) per NA.

  Two-dimensional form, with values_per_thread carrying zero weight:
    regs(NA, r, v) = intercept(r) + (8.00 + 3.30*r)*NA + 0.00*v

  Advisor's prediction, scored:
    'slope becomes 8.36*(vpt/16) + 3.19*r' -> slope(r=2,v=32) = 23.10
    predicted regs(NA=6, r=2, v=32) = 16.0 + 23.10*6 = 155 (advisor quoted ~196)
    MEASURED                                                 = 106
    measured regs(NA=6, r=2, v=16) from E32                  = 106
    -> the x-side term does NOT scale with values_per_thread. FALSIFIED.

  What values_per_thread actually costs: private (alloca) bytes.

   r   v  stage_bytes   r*v/2  match
   1   8            4       4   True
   1  16            8       8   True
   1  32           16      16   True
   1  64           32      32   True
   2   8            8       8   True
   2  16           16      16   True
   2  32           32      32   True
   2  64           64      64   True
   4   8           16      16   True
   4  16           32      32   True
   4  32           64      64   True
   4  64          128     128   True

  staging model private_bytes = rows_per_simd * values_per_thread / 2 : exact in every cell = True
  (uint16 packed[r][v/4]: r * v/4 words * 2 bytes. NA-independent.)

  The two resources are separable with no cross term:
    registers      = f(NA, rows_per_simd)          -- values_per_thread free
    private bytes  = rows_per_simd * vpt / 2       -- NA free


## (c) Host-side legality

  Frozen host dispatch (backend/metal/quantized.cpp, NOT in editablePaths):
    :246-259  bn=8, bk=32, group_dims(32,2,1), grid_dims(M, ceil(N/8), B)
    :259      bool fast = N % bn == 0 && K % 512 == 0;
    :992      the same gate again for gather_qmv
  Nothing host-side derives a size, offset or thread count from
  values_per_thread or bytes_per_lane. The grid is a function of (M, N, B)
  only, so raising values_per_thread does NOT disturb the output geometry
  that killed rows_per_simd in E32. It changes K-loop striding only.

  But `K % 512 == 0` is the ONLY alignment the frozen host guarantees, and
  512 == 16 * SIMD_SIZE is exactly the vpt=16 block size. The crossrow
  k-loop `for (k = 0; k < in_vec_size; k += block_size)` has no K tail and
  no bounds check; `_m` is a tail over M (inputs), never over K.

| N | K | projection | K%512 | K%1024 | K%2048 | v=8 | v=16 | v=32 | v=64 |
|---|---|---|---|---|---|---|---|---|---|
| 34816 | 5120 | fused gate+up | 0 | 0 | 1024 | ok | ok | ok | **OVERRUN** |
| 16480 | 5120 | GDN fused in_proj | 0 | 0 | 1024 | ok | ok | ok | **OVERRUN** |
| 14336 | 5120 | fused q+k+v | 0 | 0 | 1024 | ok | ok | ok | **OVERRUN** |
| 248320 | 5120 | lm_head | 0 | 0 | 1024 | ok | ok | ok | **OVERRUN** |
| 5120 | 6144 | o_proj / GDN out_proj | 0 | 0 | 0 | ok | ok | ok | ok |
| 5120 | 17408 | down_proj | 0 | 0 | 1024 | ok | ok | ok | **OVERRUN** |
| 5120 | 10240 | MTP head fc | 0 | 0 | 0 | ok | ok | ok | ok |
| 2048 | 5120 | KV-only pack (narrow pair kernel) | 0 | 0 | 1024 | ok | ok | ok | **OVERRUN** |

  every scored K covered:  v=8: True  v=16: True  v=32: True  v=64: False
  K=5120 and K=17408 are 1024-aligned but NOT 2048-aligned, and they carry
  gate+up, QKV, GDN in_proj, lm_head and down_proj. values_per_thread=64 is
  a hard coverage wall on the highest-traffic shapes in the model.

  Group-coverage cap, independent of K: one scale/bias is fetched per lane
  per k-block at `simd_lid / (64/values_per_thread)`, so a lane must stay
  inside one 64-element affine group. Upstream writes the same constraint as
  `scale_step_per_thread = group_size / values_per_thread` (quantized.h:768),
  which is 0 -- a division by zero -- for values_per_thread > 64.


## (c2) The wall that is not in the register file: lane->K partition

  values_per_thread does not just make loads wider. It repartitions K across
  lanes, which reassociates the FP32 sum. The shipped kernel states order
  preservation as its OWN safety case, in its own header:

    quantized.h:966  '...the K accumulation order and simd_sum are unchanged
                      for every output element.'
    quantized.h:821  the same sentence for the narrow pair kernel.

  That claim is true today because the two paths partition K identically:
    quantized.h:786   qmv_fast_impl   x += tid.x*in_vec_size
                                           + simd_lid*values_per_thread
    quantized.h:1020  crossrow wide   xm = x + ... + k
                                           + simd_lid*values_per_thread + 4*i
  Both are values_per_thread=16, so lane L owns [k+16L, k+16L+16) in both.

  Raising values_per_thread in the crossrow kernel alone breaks that:
    - M==1 and M>9 fall through to qmv_fast_impl (quantized.h:2016); the
      switch at :1920 only covers ntg.x in 2..9. So within ONE candidate run
      the same (x, W) product would be summed in two different orders.
    - the narrow pair kernel (:873-877) and the N<1024 path keep vpt=16.
    - the pinned serial leg is M==1 throughout, i.e. entirely vpt=16, and it
      is the token stream the candidate has to match.
  scarletbright's shipped vpt=32 has none of this exposure: it is the 2-bit
  draft readout, which quantized.h:1068-1082 documents as proposal-only and
  exactly reranked afterwards. Reassociation there cannot change a token.
  On the wide crossrow verify path there is no such escape.

  Row-blocked NA does not have this problem. For a given output element the
  row-blocked form runs the identical k sequence into the identical acc[r];
  only which simdgroup pass computes it moves. E32's axis is
  order-preserving by construction. This one is not.


## (b) COMPOSITION VERDICT: best legal (NA, values_per_thread) at r=2

| M | shipped IPG/passes | best legal NA | passes | widest K-legal vpt | bytes_per_lane | k-blocks K=5120 | regs | spill | recommended vpt |
|---|---|---|---|---|---|---|---|---|---|
| 6 | 3/2 | 6 | 1 | 32 | 16 | 5 | 117 | clean | **16 (unchanged)** |
| 9 | 5/2 | 9 | 1 | 32 | 16 | 5 | 168 | clean | **16 (unchanged)** |

  Register headroom at those cells is NOT the binding constraint:
    M=6, NA=6, r=2 row-blocked, v= 8: 117 regs, 8 private bytes, clean
    M=6, NA=6, r=2 row-blocked, v=16: 117 regs, 16 private bytes, clean
    M=6, NA=6, r=2 row-blocked, v=32: 117 regs, 32 private bytes, clean
    M=6, NA=6, r=2 row-blocked, v=64: 117 regs, 64 private bytes, clean
    M=9, NA=9, r=2 row-blocked, v= 8: 168 regs, 8 private bytes, clean
    M=9, NA=9, r=2 row-blocked, v=16: 168 regs, 16 private bytes, clean
    M=9, NA=9, r=2 row-blocked, v=32: 168 regs, 32 private bytes, clean
    M=9, NA=9, r=2 row-blocked, v=64: 168 regs, 64 private bytes, clean

  Plain answer for thorfinn: he can have NA, he cannot usefully have
  values_per_thread, and the reason is NOT contention.
    axis 1, row-blocked NA   : free in registers, order-preserving -> ship it
    axis 2, values_per_thread: free in registers (0 cost at every cell in
                               this grid), capped at 32 by K coverage, and
                               blocked by the kernel's own exactness claim
  The axes compose perfectly in the resource he was worried about and do not
  compose at all in the one nobody costed. Rung 1 of E33 needs no change.


## (e) Falsification: why did the shipped kernel choose 16?

  Required, because v=32 IS spill-free and IS K-legal on every scored shape.
  Four candidate reasons were checked; two are real, one is real but does not
  transfer, one is dead.

  1. REAL, and it is the whole reason upstream picked 16. The generic
     qmv_fast_impl materialises `thread U x_thread[values_per_thread]`
     (quantized.h:774) -- values_per_thread FP32 registers per lane, live
     across the whole row loop. 16 there costs 16 registers/lane; 32 costs
     32. The single literal that sets it is `packs_per_thread = bits == 2 ?
     1 : 2` (quantized.h:761), uncommented, and the bits==2 special case
     exists precisely to hold vpt at 16 when pack_factor doubles. So the
     upstream invariant is '16 values per lane', and it is a register
     argument -- for a kernel that stages all 16.
     It does NOT transfer to the crossrow kernel, which never materialises
     x_thread: it re-reads 4 activations per staged word into a0..a3
     (quantized.h:1014-1035). That is exactly why this grid measures zero
     register cost. The crossrow kernel inherited 16 by copy, not by
     analysis: `git log -S 'constexpr int values_per_thread = 16'` on
     quantized.h returns only Validate/Accept submission snapshots, and
     `git log -S packs_per_thread -- Vendor/` returns only the initial
     squashed MLX import.
  2. REAL and binding here: 512 == 16 * SIMD_SIZE is exactly the block size
     the frozen host gate `K % 512 == 0` guarantees. 16 is the largest lane
     width that needs no self-guard. See (c).
  3. REAL but not about 16: `qdot` for bits in {3,5,6} advances its weight
     pointer cumulatively inside the loop and is only correct for vpt <= 16
     (quantized.h:216-218, 247-248, 267-269). bits==4 -- our path -- uses the
     generic form and needs only values_per_thread % 4 == 0.
  4. DEAD: no reduction assumes a lane covers a quarter of a group. simd_sum
     (:1059) reduces one scalar per (row, input) over 32 lanes and is
     vpt-independent; quad_sum (:742) is only reachable at K in {64,128}
     (quantized.cpp:1385-1387), which no scored shape hits. Coalescing is not
     a reason either: bytes_per_lane goes 8 -> 16 contiguous per lane, which
     is a better burst, not a worse one.

  So: a real reason exists, it is reason 1, and it is a reason for the
  GENERIC kernel that was never re-derived for the crossrow one. Reason 2 is
  a real reason for the crossrow kernel and survives. I did not have to
  invent anything, and I did not find a coalescing, shuffle-width or
  group-size-16 interaction, because there is none.


## Primary metric

  e36/max_spill_free_NA_at_values_per_thread_32, r=2 row-blocked (thorfinn's form): 9  (at v=16: 9)
  e36/max_spill_free_NA_at_values_per_thread_32, r=4 shipped form: 5  (at v=16: 5)
  e36/max_spill_free_NA_at_values_per_thread_32, r=2 grid-relaxed, model extension: 12  (at v=16: 12)

  The delta against the shipped ceiling of 5 is entirely E32's row
  blocking. values_per_thread contributes exactly 0 to it at every cell.
