# Acknowledgements for research/frontier-revert-gate.sh
#
# One line per file our submission packages that differs from the live
# leaderboard tip:
#
#     path | STATUS | why
#
# STATUS must be exactly one of:
#
#   INTENTIONAL-REPLACEMENT
#       We have READ the frontier's copy of this file, we know which of its
#       lines our overlay deletes, and we accept that. Requires a reason of at
#       least 40 characters naming what is being dropped.
#
#   MUST-REBASE
#       We have NOT reconciled our copy with the frontier's. Our overlay would
#       delete accepted work we have not read. This is a NAMED BLOCKER: the gate
#       fails while any entry carries it. It exists so the condition is visible
#       and attributable, not so it can be waved through.
#
# Blank lines and lines beginning with # are ignored.
#
# ---------------------------------------------------------------------------
# Status as of ledger 162. THE REBASE IS DONE. Four of the six files that were
# MUST-REBASE at ledger 161 now match upstream/main byte for byte and have left
# this file entirely, which is the correct end state for an ack: a reconciled
# file needs no waiver, it needs no diff.
#
#   TAKEN FROM THE TIP (no longer differ, entries deleted):
#     Qwen35RuntimeWeights.swift        crown hunk 1  (128 -> 512)
#     RuntimeStartupMemoryPolicy.swift  crown hunks 2,3 (overwrite=1, 512/50)
#     quantized.cpp                     E27 dropped
#     quantized.h                       E27 dropped
#
# The two entries that remain are deliberate. For BOTH of them I ran
# `git blame` on the TIP's copy before writing the word INTENTIONAL, because the
# gate's question is not "did we change this file" but "whose accepted work do
# we delete". The answer decides the verdict, and for both files the lines we
# delete are traceable to the PRISTINE CHALLENGE IMPORT (David Tai, 5d029178,
# 2026-08-14) or to an inherited bot commit -- not to a recent promotion. That
# is a materially different act from reverting the crown, and last turn I could
# not tell the two apart because I had not blamed a single line.
# ---------------------------------------------------------------------------

Sources/MLXFastModel/Qwen36MTPBlockSession.swift|INTENTIONAL-REPLACEMENT|We delete the tip's stop-token early-exit (its two sites plus the reachedStopToken field). Blame on the tip puts `if stopTokens.contains(primary)` at ^5d029178 -- David Tai's pristine challenge import -- so this is inherited code, NOT frontier work: `git diff 1cb1f43a upstream/main` on this file is 0 insertions / 19 deletions, i.e. the tip's own last edit here DELETED a promotion. E26 proves the exit kills the session (pendings nil'd, next round throws .notBegun, both legs capped at emitted index 302) and research/crown_leg_decomposition.py proves it is score-NEUTRAL on ranked: effective_mean_draft_len agrees to full repr on all 8 prompts across base, ours and the crown, and base and crown BOTH carry the exit, so it never fires inside the 512-token window. Pure insurance, zero measured cost. This fix has been made four times and lost five; Tests/MLXFastTests/QwenMTPSyncContentGateTests.swift is the executable content assertion that keeps it. We also ADD BACK fkiene's 19-line verify-concat JIT warm (promoted at 1cb1f43a, 3.24417896624589 vs 3.24326223889754 base, +0.0283 %), which ofou's stale-base whole-file overlay deleted and which is therefore absent from the tip; it sits in warmAllDepthShapes, outside the timed window. The rest is round-granularity trace instrumentation behind MLX_QWEN_MTP_TRACE.

Vendor/mlx-swift-lm/Libraries/MLXLLM/Models/Qwen35.swift|INTENTIONAL-REPLACEMENT|We delete two copies of the tip's `switch i { case 0,1,9,19,29,39,49,57: }` decode-ladder dispatch and replace them with a Set<Int> membership test whose default rung set is byte-identical to that switch, so the shipped schedule is unchanged and MLX_QWEN_MTP_LADDER makes it overridable for attribution runs. Blame on the tip puts those lines at 86fb1f02 (yukon-autoresearch bot, 2026-08-18 14:56) -- an inherited bot commit, not a mechanism anyone promoted for its own sake. Cost is bounded and its SIGN is favourable: the lookup is 64x per forward, the serial leg pays it once per token while the MTP leg pays it once per ~3.5 tokens, so d(raw_p) = c*(mtp - serial*forwards_per_tok)/mtp^2 > 0. At the modelled c = 1 us/step that is +0.00025 % -- a gain of ~1/400 sigma_score, not a cost. Kept because it is how E29(c) attribution runs are performed; it is the one instrument here that lives at LAYER granularity, and the rule I am applying is that instrumentation may live at round granularity freely but must justify itself at layer granularity.
