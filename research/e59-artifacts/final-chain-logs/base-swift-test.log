[0/3] Write swift-version--58304C5D6DBC2206.txt
[0/1] Planning build
[1/1] Compiling plugin CudaBuild
[2/2] Compiling plugin GenerateManual
[3/3] Compiling plugin GenerateDoccReference
Building for debugging...
[3/9] Write swift-version--58304C5D6DBC2206.txt
[4/8] Compiling quantized.cpp
[5/10] Write Objects.LinkFileList
[7/10] Linking mlxfast-runtime-worker
[8/10] Applying mlxfast-runtime-worker
[9/10] Linking mlxfast-challenge-devPackageTests
Build complete! (16.46s)
Test Suite 'All tests' started at 2026-08-20 05:47:05.946.
Test Suite 'All tests' passed at 2026-08-20 05:47:05.948.
	 Executed 0 tests, with 0 failures (0 unexpected) in 0.000 (0.002) seconds
◇ Test run started.
↳ Testing Library Version: 1902
↳ Target Platform: arm64e-apple-macos14.0
◇ Suite SetupDFlashTargetIdentityTests started.
◇ Suite "Fused residual RMSNorm bit-identity" started.
◇ Suite "DFlash gates-only artifact leak" started.
◇ Test transformRejectsNonMLXCompressedAndGlobalScaleSchemas() started.
◇ Test attachDerivedBenchmarkOracleRejectsABaseCaseShorterThanTheTimedDecodeWindow() started.
◇ Test setupRejectsCommandLineToolsWithFullXcodeInstructions() started.
◇ Test semanticGPQAGateRetriesTransportFailuresAndFailsInfraNotSemantic() started.
◇ Test cliSupportsHiddenGPQAGateAttachment() started.
◇ Test correctnessAcceptsOnlyExactTopLogitTies() started.
◇ Suite QwenMTPScoringSemanticsTests started.
◇ Test hashWeightsDirectoryIsIndependentOfWeightsPathButSensitiveToContent() started.
◇ Suite "Qwen-MTP trusted-context guards" started.
◇ Test transformKeySelectionDropsRotaryInvFreqOnlyForLaguna() started.
◇ Test safetensorsRejectsUInt64MaxHeaderLengthWithoutTrapping() started.
◇ Test defaultTransformedWeightsLimitIsTwentyFiveGiB() started.
◇ Test setupReferenceCacheStampUsesUniqueAtomicTemporaryFiles() started.
◇ Test failedScoreRedactsCorrectnessTokenMismatchByDefault() started.
◇ Test benchmarkWorkflowProbesAndEnforcesRuntimeWorkerSandbox() started.
◇ Test quantizedSwitchLinearForwardsNVFP4GatherSemanticsWhenRuntimeTestsAreEnabled() started.
◇ Test localIterateScorePublishesCLIUsableEstimatedScore() started.
◇ Test setupReferenceCacheStampRejectsSameSizeImmediateMutation() started.
◇ Test editableDecodeDelayHookIsFullyRemoved() started.
◇ Test loadGoldenCasesAcceptsValidFixture() started.
◇ Test goldenModelProvenanceIsStrictAndPinned() started.
◇ Test safetensorsCopySubsetRejectsForgedValidatedHeaderWithoutMutatingDestination() started.
◇ Test qwenHybridCachePositionAcceptsRecurrentCachesPinnedAtZero() started.
◇ Suite QwenMTPNoopReferenceProvenanceTests started.
◇ Suite QwenMTPTrackNamingTests started.
◇ Suite "DFlash enablement interlock" started.
◇ Suite BenchmarkSafetyTests started.
◇ Suite QwenQMVCostCurveTests started.
◇ Test transformRejectsPoolsideLagunaRouterMissingCorrectionBias() started.
◇ Test setupDerivesTheMetadataDownloadListFromTheSelectedManifest() started.
◇ Test offlineRunnerProvesNetworkIsBlockedBeforeRunningCommand() started.
◇ Test submissionStaticReviewPromptCoversMeasurementStructureExploitation() started.
◇ Test dflashWorkerAppliesStartupMemoryPolicyBeforeItsModelLoads() started.
◇ Test transformRejectsPoolsideQuantizationOverrides() started.
◇ Suite QwenMTPBackboneLayoutTests started.
◇ Test safetensorsCopySubsetRebindsPublicHeaderToMatchingSource() started.
◇ Test rankedJobRunsPublicBehaviorGateBeforeHiddenGates() started.
◇ Test theTransformCacheKeyCoversTheReferenceIdentity() started.
◇ Suite "DFlash local run lock" started.
◇ Suite QwenDraftTop32SelectionTests started.
◇ Suite QwenQMVParityTests started.
◇ Suite QwenMTPMetallibAvailabilityTests started.
◇ Suite "DFlash R2 key probe workflow" started.
◇ Test benchmarkScoreChecksComponentFloors() started.
◇ Suite QwenMTPWeightRenameTests started.
◇ Suite QwenMTPTrackScopedConstantsTests started.
◇ Suite "Fused residual RMSNorm source gate" started.
◇ Suite QwenMTPFixedWindowSourceGuardTests started.
◇ Test benchmarkScriptFailsWhenScorePayloadFails() started.
◇ Test setupDownloadHeartbeatCountsInProgressPartialBytes() started.
◇ Suite E57SdpaChunkDispatchCountTests started.
◇ Suite QwenMTPHeadManifestTests started.
◇ Suite "R2 request execution" started.
◇ Test checkpointIndexToolsReturnsUniqueSortedSafetensorShards() started.
◇ Suite "DFlash private material binding" started.
◇ Suite "DFlash dry run reaches its own gate" started.
◇ Suite QwenMTPHeadProvenanceGateTests started.
◇ Suite LocalDFlashScriptSurfaceTests started.
◇ Suite QwenMTPGoLiveRunbookTests started.
◇ Test setupReferenceCacheStampRejectsMutationAfterHashBeforePublication() started.
◇ Test ngramSelfSimilaritySeparatesRecentPredictionFromOptimisticUpperBound() started.
◇ Test loadGoldenFixtureRejectsDuplicateLayeredCorrectnessNames() started.
◇ Test setupReferenceCacheStampRejectsManifestSwapAfterVerification() started.
◇ Suite QwenMTPOptionalEditablePathTests started.
◇ Suite "R2 SigV4 signing" started.
◇ Test participantDocsExposeDefaultCLIInstallDirectory() started.
◇ Suite Qwen35ReferenceParityTests started.
◇ Test mlxTensorBridgeCreatesArrayWhenRuntimeTestsAreEnabled() started.
◇ Test qwen35PreflightFixtureHasExactPinnedTensorInventory() started.
◇ Test safetensorsCopySubsetRejectsSourceAsDestinationWithoutMutatingIt() started.
◇ Test semanticGPQAGateParsesOpusVerdictShapesAndFailsUnparseableCaseClosed() started.
◇ Test coolGateWarnsOnceWhenGpuTelemetryLooksImplausible() started.
◇ Test setupReferenceCacheStampRejectsManifestSwapDuringFastValidation() started.
◇ Test nonWorkerBenchmarkRejectsBehaviorGatesBecauseTTFTRequiresWorker() started.
◇ Test workerStderrDrainCapsUnterminatedLine() started.
◇ Test setupRetainsFailedMetallibStateInSynchronousAndParallelModes() started.
◇ Suite ParentToolSandboxTests started.
◇ Suite QwenDraftReadoutExactnessTests started.
◇ Suite QwenMTPDepthFreedomTests started.
◇ Suite E58DispatchStormTests started.
◇ Suite ShellGapRegressionTests started.
◇ Suite QwenMTPFixedWindowTests started.
◇ Suite QwenMTPRollbackContractTests started.
◇ Suite QwenMTPPayloadSchemaTests started.
◇ Test traceProtocolCarriesRequestedTopKAndExpectedTokenDiagnostics() started.
◇ Suite "DFlash redactor kind coverage" started.
◇ Test runtimeWorkerClientCloseEscalatesPastIgnoredTerminate() started.
◇ Suite NVFP4QuantizedMMTests started.
◇ Test residentModelGuardPatternCoversTheDFlashSubcommands() started.
◇ Test runtimeWorkerClientTimesOutStalledRequest() started.
◇ Suite QwenMTPLaunchInvariantTests started.
◇ Suite "DFlash golden provisioning" started.
◇ Test writeScorePayloadKeepsTokenStepSeparateFromLayerFailures() started.
◇ Test benchmarkGoldenOracleFieldsAreValidatedOnLoad() started.
◇ Test transformRejectsReferenceAsOutputWithoutMutatingCheckpoint() started.
◇ Suite DFlashTrustedEditableCouplingTests started.
◇ Test benchmarkScriptPrintsLocalSummaryWithBaselineComparison() started.
◇ Test lagunaConfigRejectsBehaviorChangingXSMutations() started.
◇ Suite DFlashHiddenGoldenOccupancyTests started.
◇ Suite QwenMTPSyncContentGateTests started.
◇ Test prefillFailsWhenMoreThan5PercentFast() started.
◇ Test benchmarkKeepsZeroExpertStreamingSchemaWithoutStreamingMachinery() started.
◇ Suite QwenDraftReadoutGatePremiseTests started.
◇ Test terminatedLocalRunReapsTheModelHoldingProcessTree() started.
◇ Test localIteratePhaseHeartbeatFiresWhileBlockedAndStopsAfterCancel() started.
◇ Suite "DFlash golden R2 keys" started.
◇ Suite QwenMTPHeadDeclarationTests started.
◇ Test setupReferenceCacheStampDoesNotModifyReadOnlyCheckpoint() started.
◇ Test attachDerivedBenchmarkOracleReproducesTheSerialEraPrecedentRule() started.
◇ Test runtimeWorkerPinnedConfigurationRejectsAlternateQuantizationSchemas() started.
◇ Test pinTrustedHarnessScriptDetectsTamper() started.
◇ Test runtimeWorkerPinnedConfigurationRejectsWrongQwen35Fields() started.
◇ Test materializedTensorRejectsMismatchedShapeAndBytes() started.
◇ Test setupCompatibilityLinkFailurePropagatesThroughConditionalCaller() started.
◇ Test verifyTrustedSourceScopeScriptDetectsScopeTamper() started.
◇ Test lagunaWeightLoaderExplicitlyRejectsNonMLXQuantizationSchemas() started.
◇ Test metallibBuilderRegeneratesStaleCMakeCache() started.
◇ Test lagunaCorrectnessGeneratesGreedyTokensWithGrowingContext() started.
◇ Test trustedBenchmarkWorkflowGuardAllowsOnlyPermittedBranches() started.
◇ Test setupVerifiedDownloadMarkerRejectsManifestABA() started.
◇ Test denseTensorStoreRejectsUnsafeShardPath() started.
◇ Test loadGoldenCasesRejectsMissingVersion() started.
◇ Test attachDerivedBenchmarkOracleLeavesEveryOtherSectionUntouched() started.
◇ Test attachDerivedBenchmarkOracleAcceptsTheExactTimedWindowBoundary() started.
◇ Test lagunaConfigRejectsChangedArchitecture() started.
◇ Test loadGoldenFixtureAcceptsLayeredCorrectnessGates() started.
◇ Test safetensorsCopySubsetRejectsDuplicateNamesWithoutMutatingDestination() started.
◇ Test transformRejectsIndexTensorMissingFromShardHeaderBeforeCreatingOutput() started.
◇ Test transformFailurePreservesExistingOutputAndRemovesStagingDirectory() started.
◇ Test loadGoldenFixtureRejectsHalfCalibratedBenchmarkBaselines() started.
◇ Test localIterateSummaryEmitsSpeedupsAndEstimatedScore() started.
◇ Test localIterateScoreSanitizesNonfiniteFailureMetricsForJSON() started.
◇ Test transformRuntimeConfigCaptureDoesNotRereadChangedSource() started.
◇ Test benchmarkScriptFailsFastWithGuidanceWhenGoldenIsMissing() started.
◇ Test attachDerivedBenchmarkOracleOutputLoadsThroughTheStrictFixtureLoader() started.
◇ Test theTransformCacheDefinitionsStayExtractable() started.
◇ Test safetensorsCopySubsetRejectsReplacedValidatedSourceAndPreservesDestination() started.
◇ Test benchmarkWorkflowsPinTrustedSourceScopeBeforeBuilding() started.
◇ Test benchmarkPreflightRejectsMissingSemanticTensor() started.
◇ Test runtimeWorkerWatchdogAtomicCancellationDisarmsTimer() started.
◇ Test benchmarkPreflightRejectsMissingBenchmarkOracle() started.
◇ Test transformRejectsQuantizedLagunaExpertStackMissingScales() started.
◇ Test qwenChatTemplateMatchesTheCheckpointSidecarPlainPath() started.
◇ Test editableSurfaceByteBudgetEnforcesCapsAtWorkerLaunch() started.
◇ Test setupAllowsOnlyOneCompetingStaleLockRecoverer() started.
◇ Test trustedSandboxRebindsExecPermissionToTheRuntimeWorker() started.
◇ Test lowMemoryPlanWritesNoFeatureDefaultsAndAnnouncesRankedParity() started.
◇ Test cliSupportsFreeRunGateAttachmentCoveringTimedDecodeOffsets() started.
◇ Test checkpointIndexToolsRejectsUnsupportedShard() started.
◇ Test runtimeWorkerOrphanReaperStaysQuietWhileTheParentIsAlive() started.
◇ Test attachDerivedBenchmarkOracleCarriesNoPerPromptBaselines() started.
◇ Test transformRejectsExistingRegularFileOutputWithoutReplacingIt() started.
◇ Test benchmarkScriptResolvesThePinnedQwenReference() started.
◇ Test benchmarkRestoresFansWhenAbortedAfterBoost() started.
◇ Test metallibBuilderReclaimsOnlyDeadOrBrokenAtomicLockGenerations() started.
◇ Test scoreMetricsDecodeOlderPayloadWithoutWeightsIntegrityFields() started.
◇ Test benchmarkScriptRejectsMultipleScoreObjectsOnStdout() started.
◇ Test setupAndMetallibStatFallbackDiscardsFailedProbeOutput() started.
◇ Test qwen35TransformRejectsAlternateQuantizationSpecs() started.
◇ Test checkpointIndexBuildRejectsDuplicateTensorNamesAcrossShards() started.
◇ Test setupUsesDownloadedMetalToolchainIdentifierForCompilerProbe() started.
◇ Test loadGoldenCasesRejectsNamesWithControlCharacters() started.
◇ Test metallibBuilderSerializesConcurrentProcesses() started.
◇ Test transformRejectsUnsafeIndexShardBeforeCreatingOutput() started.
◇ Test compareTeacherForcedWithWorkerUsesSerialTeacherForcedSteps() started.
◇ Test transformSelectsTextTowerTensorsAndDropsVisionTensors() started.
◇ Test timedDecodeChargesOneValidatedSeedForward() started.
◇ Test benchmarkWorkflowPinsTrustedLayerCountForFinalValidation() started.
◇ Test safetensorsCopySubsetUsesThrowingDestinationCreate() started.
◇ Test transformRequiresMatchingExplicitPoolsideQuantizationBlocks() started.
◇ Test transformRejectsCheckpointWithoutTextTowerTensorsBeforeCreatingOutput() started.
◇ Test prefillAcceptsAtReferenceAndModestlyFaster() started.
◇ Test setupScriptDefaultsToFastReferenceMirror() started.
◇ Test workerPhaseStartsResetTrustedAllocatorBeforeForwardSetup() started.
◇ Test setupSerializesSharedReferenceCacheMutationAndRechecksAfterWaiting() started.
◇ Test metallibFingerprintScriptAndSwiftTwinAgree() started.
◇ Test semanticBehaviorGateRequiresPromptAndReferenceAnswer() started.
◇ Test benchmarkScoreUsesWeightedBaselineSpeedups() started.
◇ Test safetensorsCopySubsetPreservesBytesForUnsortedTensorNames() started.
◇ Test qwen36PublicConfigFixturePinsExactArtifactSemantics() started.
◇ Test endOfTurnTruncationCutsTheFinishedTurnTail() started.
◇ Test loadGoldenFixtureStaleBenchmarkOracleErrorMentionsPrecomputedFixture() started.
◇ Test benchmarkPreflightRejectsMalformedGolden() started.
◇ Test safetensorsRejectsBooleanAndFloatingPointIntegerFields() started.
◇ Test transformRejectsSourceMetadataMutationsBeforePublishingOutput() started.
◇ Test ngramSelfSimilarityReportsNoHitsForUniqueContinuation() started.
◇ Test localDecodeProgressIntervalUsesExpectedCadence() started.
◇ Test participantDocsDescribeTheirOwnTrackScoreAndFloor() started.
◇ Test workerStderrDrainForwardsRedactedLinesAndKeepsTailForDiagnostics() started.
◇ Test startupMemoryPolicyUses64GiBAsFullProfileBoundary() started.
◇ Test safetensorsCopySubsetRejectsSourceMutationBeforePublishAndRemovesTemporaryFile() started.
◇ Test phaseStartAllocatorResetLeavesExactlyEmptyCacheWhenRuntimeTestsAreEnabled() started.
◇ Test qwenChatTemplatePreClosesTheThinkingBlock() started.
◇ Test correctnessPreflightHonorsConfiguredWeightsByteLimit() started.
◇ Test nvfp4NibbleOrderAndE4M3ScaleBytesMatchMLXContractWhenRuntimeTestsAreEnabled() started.
◇ Test referenceCacheProbeWorkflowIsManualAndExperimental() started.
◇ Test attachDerivedBenchmarkOracleRejectsAGoldenWithNoBaseCases() started.
◇ Test correctnessCheckGatesFalseSkipsGateLoopsAndReportsBaseCaseCountAlone() started.
◇ Test hashWeightsDirectoryRejectsHardlinks() started.
◇ Test submissionStaticReviewOversizeFailureExplainsStaleCloneDeletions() started.
◇ Test poolsideLagunaPublicConfigFixturePinsExactArtifactSemantics() started.
◇ Test setupReclaimsOnlyDeadOrBrokenAtomicLockGenerations() started.
◇ Test naxSplitKBF16MatchesRegularNAXWhenRuntimeTestsAreEnabled() started.
◇ Test decodeFailsGainBeyond5Percent() started.
◇ Test transformVerifierAcceptsFreshSubmittedTransformOutputAndIgnoresLocalCacheMarkers() started.
◇ Test setupAcceptsLegacyReferenceDirectoryAsItsOwnCompatibilityPath() started.
◇ Test qwen36ConfigContractDigestMatchesTheReferenceManifest() started.
◇ Test transformRejectsOutputThatContainsWorkingDirectory() started.
◇ Test qwenHybridCachePositionRejectsDivergentFullAttentionOffsets() started.
◇ Test benchmarkLocalIterateModeUsesPublicFixtureAndNonOfficialScore() started.
◇ Test setupDownloadPublishesVerifiedPartialAtomically() started.
◇ Test benchmarkGatesOnlyRunSkipsSpuriousSemanticCaptureFailure() started.
◇ Test benchmarkScriptTransformOnlyStopsBeforeAnyMeasurement() started.
◇ Test setupScriptCoordinatesCacheAndMetallibState() started.
◇ Test workerComputesExpectedTokenDiagnosticsOutsideReturnedTopSubset() started.
◇ Test localIterateProjectedDecodeSecondsPerTokenConvergesToChargedMean() started.
◇ Test frozenDependencyGraphAssertionDetectsManifestDrift() started.
◇ Test setupRejectsZeroMutationLockStaleThreshold() started.
◇ Test trustedParentStartsPhaseTimerBeforeWorkerResetRequest() started.
◇ Test loadGoldenFixtureRejectsMalformedBenchmarkOracle() started.
◇ Test pairedBaselineOverrideParsesTrustedEnvironmentFailClosed() started.
◇ Test benchmarkScriptLocalSummarySkipsQuietlyWithoutTimingMetrics() started.
◇ Test materializedTensorDecodesSupportedValues() started.
◇ Test benchmarkScriptForwardsLocalIterateDefaultsToSwiftBenchmark() started.
◇ Test localIteratePrefillStatusReportsPerTokenAndSpeedup() started.
◇ Test publicDiagnosticsAreCoarsenedWhileRankingStaysPrecise() started.
◇ Test qwen36TensorInventoryFixturePinsAllPublicHeaders() started.
◇ Test evaluateTimedRunMatchesSeparateScoreSpeedupAndBandChecks() started.
◇ Test theQwenMTPRunnerRefreshesTheTransformAndGatesTheModelFamily() started.
◇ Test setupFailureCleanupTerminatesMetallibProcessGroup() started.
◇ Test benchmarkCliSupportsSkippableBenchmarkCorrectness() started.
◇ Test benchmarkFailsFastWhenSetupArtifactsAreMissing() started.
◇ Test cliSupportsBenchmarkOracleAttachmentForRankedGoldens() started.
◇ Test lagunaConfigRejectsAnyQuantizationOverride() started.
◇ Test robustReferenceRejectsTooFewOrInvalid() started.
◇ Test benchmarkScriptFallsBackToCacheWhenReferenceSymlinkIsBroken() started.
◇ Test localModesGuardAgainstDoubleModelResidency() started.
◇ Test bufferedFileLineReaderPreservesBufferedLinesAndEOFFragment() started.
◇ Test benchmarkWindowConstantsAreFrozen() started.
◇ Test qwenChatTemplateEmbedsThePromptVerbatim() started.
◇ Test lagunaConfigRequiresMatchingExplicitQuantizationBlocks() started.
◇ Test correctnessPreflightAcceptsPublicGoldenWithoutBenchmarkOracle() started.
◇ Test decodeMeasurementInvokesNoPhaseVaryingEditableHook() started.
◇ Test qwen35TransformRejectsInventoryAndPackingDrift() started.
◇ Test benchmarkScriptRejectsPathFlagsBeforeForwardingToSwiftBenchmark() started.
◇ Test lagunaConfigLoadsPinnedPoolsideNVFP4Geometry() started.
◇ Test endOfTurnTruncationNeverReturnsAnEmptyCapture() started.
◇ Test decodeFailsRegressionBeyond2Percent() started.
◇ Test safetensorsCopySubsetRejectsMissingRequestedTensor() started.
◇ Test lagunaConfigAcceptsAbsentOrExplicitNullNullableArtifactFields() started.
◇ Test benchmarkRebuildsWhenParticipantSourcesAreNewerThanTheBuiltWorker() started.
◇ Test workerStderrDrainCapsRetainedTail() started.
◇ Test qwenFullAttentionIntervalMatchesThePinnedLayerSchedule() started.
◇ Test loadGoldenCasesRejectsDuplicateCaseNames() started.
◇ Test benchmarkWindowFreezeDocMatchesConstants() started.
◇ Test runtimeWorkerRejectsPublicContractFieldsPreviouslyIgnoredByDecoder() started.
◇ Test rankedScoreSemanticsAreUnchangedByLocalEstimatedScore() started.
◇ Test localIterateScoreFailsForUnusableTimings() started.
◇ Test benchmarkPreflightAcceptsRequiredArtifacts() started.
◇ Test benchmarkTimingChargesDecodeSetupAndSeparatesWorkers() started.
✘ Test submissionStaticReviewPromptCoversMeasurementStructureExploitation() recorded an issue at BenchmarkScriptTests.swift:1199:5: Expectation failed: (contract → "<!-- SENPAI-CAMPAIGN-BEGIN -->
# Qwen 3.8 Native-MTP Challenge Agent Guide

This repository has one ranked task: make Qwen 3.8 27B native-MTP decode
faster on Apple Silicon without changing a single emitted token. The ranked
track is `qwen3.8-27b-mtp-v1`.

Retained files from earlier tracks are historical prior art only. Their
commands, model geometry, editable surface, correctness rules, scoring, and
runner configuration do not apply here. Operate this repository only as the
Qwen track named above.

## Senpai Campaign

For coordinated research in `morganmcg1/qwen38-challenge_senpai`, read
[`senpai/program.md`](senpai/program.md) and
[`senpai/experiment-runbook.md`](senpai/experiment-runbook.md) before assigning
or running experiments. The campaign ledger and exact frontier pins are in
[`senpai/campaign-ledger.md`](senpai/campaign-ledger.md) and
[`senpai/frontier-state.json`](senpai/frontier-state.json).

The intended remotes are:

```text
origin    https://github.com/morganmcg1/qwen38-challenge_senpai.git
upstream  https://github.com/Layr-Labs/qwen-3.8-mtp-challenge
```

`upstream` must have push URL `DISABLED`. Never run `yukon sync`,
`yukon sync --harness-only`, or `yukon reset` in this maintained checkout;
those commands are for plain solver checkouts and can reset campaign history or
repoint `origin`. Use the repository-local `sync-organizer-frontier` skill for
organizer policy and promoted-snapshot refreshes.

Record the exact campaign `BASE_SHA` and organizer `UPSTREAM_SHA` for every
experiment. Preserve unrelated user work, campaign files, and dirty-worktree
changes. Generated weights, caches, score files, downloaded artifacts, and
hidden material never belong in Git.

## Authority Order

This repository still contains historical comments and bring-up prose. Resolve
contract questions from the live enforcing sources in this order:

1. [`benchmark.json`](benchmark.json): track identity, commands, editable
   paths, optional paths, budget policy, and scoring fields.
2. [`fixtures/qwen3_8_27b_mtp_track.json`](fixtures/qwen3_8_27b_mtp_track.json):
   artifact pins, timed prompt pool, fidelity semantics, and calibration.
3. [`.github/workflows/qwen-mtp-ranked-benchmark.yml`](.github/workflows/qwen-mtp-ranked-benchmark.yml):
   ranked execution, runner label, and enforced environment values.
4. [`docs/qwen-mtp-editable-surface.md`](docs/qwen-mtp-editable-surface.md):
   rationale for the editable/trusted boundary.
5. [`TASK.md`](TASK.md): concise participant-facing summary.

For mutable solver behavior, inspect the current `BASE_SHA` source and
`mtp-head.manifest.json`. Narrative descriptions of the original starting tree
do not override the current implementation. If authorities disagree, stop and
identify the exact enforcing site before editing or assigning work.

## Mission And Score

The official score is a decode-only paired speedup anchored at serial `1.0`:

```text
for each hidden prompt p:
  raw_p = mean(pinned-baseline depth-0 serial seconds/token)
          / mean(candidate seconds/token)

published score = median(raw_1 ... raw_8)
```

The ranked legs come from different workspaces. The trusted, prebuilt baseline
workspace produces the serial numerator; the submitted candidate workspace
produces the MTP denominator. Candidate-editable code cannot change the ranked
serial numerator, so any compliant reduction in candidate MTP time improves
the affected `raw_p`. Never subtract a local serial-path share such as
`psi_serial` when pricing official value. The local same-build harness has a
different causal graph and must be labeled separately. Run
`senpai/verify-ranked-score-boundary.sh` before using a derived score model.

All eight hidden prompts are measured. Because eight is even, the median is the
mean of the two central ordered values. There is no no-op normalization and no
prompt lottery. The published median must be at least `0.90` and no greater
than the `5.0` plausibility ceiling.

Each ranked leg processes a 512-token seed followed by 512 parent-counted
decode tokens, and the seed work is charged in the timed leg. Prefill is not a
separate score, but work removed from the charged seed still improves the
result. The ranked run uses one thermally gated serial/candidate pair per
prompt unless the trusted contract changes.

The trusted parent offers a per-round ceiling. Candidate policy may choose
from 0 through 8 drafts independently on every round. Drafting nothing is
legal and degenerates to serial decode; it is a control, not the optimization
goal. Acceptance rate alone is not the objective: deeper drafting wins only
when extra committed tokens repay head, target-verification, rejected-tail,
and state-management costs across varied prompts.

The organizer's original calibrated tree scored about `0.994`. That is a
historical starting-line measurement, not the current campaign frontier. Read
the live Yukon promoted row and `senpai/frontier-state.json` before comparing or
submitting work.

## Target And Current Scored Path

The fixed target is the organizer's MLX 4-bit affine/group-64 conversion of
Qwen 3.8 27B:

```text
backbone  EigenLabs/Qwen3.8-27B-4bit
          @ eda45ab47f465d08d6558f0353a2346e2eb9d5b3
upstream  Qwen/Qwen3.8-27B
          @ 1d4bf0f2ff6012fd82039f2fa52739d0dd7c60c0
```

The organizer-pinned default proposal head is the separate bf16 MTP
artifact. A candidate may instead declare an immutable remote or in-branch head
through `mtp-head.manifest.json`; inspect that file at `BASE_SHA` for current
head provenance. The target checkpoint remains fixed regardless of the head.

The Qwen tower is a dense hybrid architecture:

- 64 layers: 48 Gated DeltaNet recurrent layers and 16 full-attention layers;
- hidden size 5,120 and vocabulary 248,320;
- full attention with 24 query heads, 4 KV heads, and head dimension 256;
- affine 4-bit/group-64 backbone weights and dense MLPs.

The `Qwen35*` source names and `qwen3_5_text` configuration label describe the
architecture family embedded in the Qwen 3.8 artifact; they do not mean this
repository targets Qwen 3.5 or 3.6.

The scored worker loads the transformed checkpoint through `LLMModelFactory`,
attaches the selected head, warms legal shapes, and serves rounds through
[`Sources/MLXFastModel/Qwen36MTPBlockSession.swift`](Sources/MLXFastModel/Qwen36MTPBlockSession.swift).
A round conceptually:

1. commits the pending primary token;
2. lets candidate policy choose 0...8 drafts within the offered maximum;
3. runs the MTP head chain to propose them;
4. verifies `[primary] + drafts` with the fixed target in a batched forward;
5. accepts the longest correct prefix;
6. keeps, restores, replays, or repairs target and head state as required;
7. reports actual rows and exact top-two evidence to the trusted parent.

The timed target model is the vendored `Qwen35TextModel`/`Qwen35Model` in
[`Vendor/mlx-swift-lm/Libraries/MLXLLM/Models/Qwen35.swift`](Vendor/mlx-swift-lm/Libraries/MLXLLM/Models/Qwen35.swift),
together with the editable MTP session. Directory-wide editability does not
prove reachability: for example, the separate `Qwen35FastEngine` path is not
the MTP worker's current target path. Trace the call path before optimizing.

### Current frontier is already optimized

Do not treat historical defaults as current behavior or rediscover mechanisms
already in the promoted snapshot. At this guide's 2026-08-16 rewrite:

- `Qwen36MTPBlockSession.init` installs an adaptive cost-model policy; the
  nearby K=1 property initializer and constant-depth comments are historical
  fallback/bring-up text, not the instantiated policy;
- the campaign `mtp-head.manifest.json` declares a 4-bit/group-64 proposal
  head rather than using the organizer-pinned bf16 default;
- persistent head history, K/V-only history flush, device-resident drafting,
  exact rollback/replay, and scored-shape warmups are present;
- target verification has exact multi-row affine4 QMV sharing, fused GDN and
  full-attention preparation, and an exact two-stage top-two reducer;
- proposal selection already uses a compact vocabulary and a custom selector;
- the layer loop already carries an `asyncEval` ladder for narrow widths.

These are mutable solver facts. Recheck source after every frontier sync. See
[`senpai/laguna-to-qwen-speedup-map.md`](senpai/laguna-to-qwen-speedup-map.md)
and the campaign novelty ledger before assigning overlapping work.

## Editable And Trusted Boundary

The ownership rule is:

> Candidate code may propose and execute the model, but the trusted parent owns
> correctness, measurement, and final ledger authority.

The editable session necessarily assembles target verify forwards, walks the
candidate-side accepted prefix, rolls state back, and produces evidence for the
parent. None of those self-reports is authoritative: the parent independently
checks the serial token stream, round structure, rows, and accounting.

`benchmark.json` `editablePaths` is the only machine-readable path authority.
Its current 89 entries form five practical groups:

- optional `mtp-head.manifest.json` and `mtp-head/` candidate-head content;
- `Sources/MLXFastModel/`, including the runtime and full MTP apparatus;
- `Sources/MLXFastTransform/` offline checkpoint transform;
- the exact listed Qwen model and `MLXLMCommon` support files;
- the exact listed MLX/Metal kernel sources and generated twins.

Do not infer that an adjacent file is editable. Check the manifest at
`BASE_SHA` before implementation. Submission archives use replace semantics
over `editablePaths`; only the two MTP-head paths are optional. A support-file
change outside the surface can make a local tree faster while uploading
nothing useful.

Trusted and not submitted include:

- target weights, transform contract, tokenizer, fixtures, and goldens;
- trusted driver, accept audit, row accounting, score emission, and worker
  protocol;
- timing, telemetry, thermal gate, workflows, and `.github/scripts/`;
- `benchmark.json`, `Package.swift`, and `Package.resolved`;
- tests, docs, local benchmark scripts, and other paths not named by the
  manifest.

`Sources/MLXFastModel/Qwen36MTPReferenceSession.swift` is an important mixed
case: it is editable through the directory-wide allowance even though it serves
rejected-tail reference replay. It runs after timing and cannot define emitted
tokens. Never route its reference work or any golden-derived information into
the timed path, proposal policy, or emitted stream.

The source budget is 3,000,000 bytes total, 524,288 bytes per file, and
262,144 bytes of candidate growth. `mtp-head/` is exempt from the source budget
and instead has a declared digest, byte count, and 2 GiB cap. Run the campaign
scope and budget checks before expensive validation.

## Correctness And Prohibited Strategies

Correctness is a hard gate. Every emitted token must equal the hidden serial
trajectory. The round journal must be structurally sound, effective draft
counts must match what occurred, and the reference-checked row ledger must
close. There is no near-tie allowance on this track.

The ranked pipeline also runs the public drift tripwire, hidden teacher-forced
base, anchor, free-run, behavior and GPQA gates, GPQA TTFT guardrail, semantic
judge, transform verification, static review, and integrity checks. Any failed
gate produces no promotable score.

The native MTP path inside the declared editable surface is the only sanctioned
future-token mechanism. Do not add:

- prompt lookup, n-gram, suffix, or token-history drafting;
- same-target lookahead or unsanctioned future-logit/KV computation;
- request-keyed replay caches whose only useful hit is a repeated benchmark
  request;
- cross-request future state, hidden-prompt specialization, or GPQA shortcuts;
- degraded or partial target forwards, unevaluated verify rows, omitted exact
  final-logit/top-two work for emitted rows, or fabricated top-two, row,
  acceptance, or ledger evidence;
- an undeclared auxiliary proposal model/head or candidate-selected target;
- phase, reference-leg, prompt-pool, or baseline detection used to change the
  work performed;
- logical rollback that leaves rejected cache/state rows reachable, or merely
  decrements an offset without trimming or overwriting rejected physical rows;
- timing shortcuts, protocol injection, network access, or filesystem
  exfiltration.

Input-independent weight/kernel/shape tables and ordinary within-request cache
reuse are legal when they preserve the contract. A candidate head may improve
proposal quality and cost; it cannot redefine the target answer.

Public fixtures were generated on M5. On another Apple GPU, compare an
unchanged `BASE_SHA` at the same token position before diagnosing a near-tie
local mismatch. That diagnostic does not relax official exactness.

## Kernel And Swift Rules

The vendored MLX package has two effective source forms:

- A family with an `mlx-generated/*.cpp` twin is JIT-compiled from the source
  string embedded in that generated C++ file. Update the readable
  `.metal`/`.h` source and the runtime-effective twin together.
- AOT-only families are loaded from `mlx.metallib`; rebuild it with
  `tools/build-mlx-metallib.sh` after relevant edits.
- `_nax` variants are selected on ranked M5 hardware and are first-class
  targets, not optional follow-up work.

Run `python3 research/twin_audit.py` after JIT-source edits. Before any kernel
experiment, prove the scored shape, dispatch family, source form, and M5
variant. Reduction order, precision, BF16 cast boundaries, packing, recurrent
state, cache layout, and width-dependent dispatch are correctness-sensitive.

For direct Swift commands, preserve the frozen dependency graph:

```bash
swift test --force-resolved-versions
MLXFAST_RUN_MLX_RUNTIME_TESTS=1 swift test --force-resolved-versions
swift build -c release --force-resolved-versions
tools/build-mlx-metallib.sh
```

A bare Swift build may write `.build/release` without rebuilding the scored
`.build-worker/release/mlxfast-runtime-worker`. Use the Qwen benchmark wrapper
for end-to-end work. Never run `swift package resolve` or `swift package
update`; if `Package.resolved` changes, restore it rather than accepting a new
dependency graph.

## Local Workflow And Measurement

Use only the Qwen entrypoints:

```bash
./setup.sh && ./setup-qwen-mtp.sh
./benchmark-qwen-mtp.sh --local-iterate
./benchmark-qwen-mtp.sh --local-submit
```

`--local-iterate` defaults to 64 decode tokens and `--local-submit` to 128.
They use one public fixture and cannot reproduce the ranked eight-prompt,
512-token result. Treat local scores as directional.

The local serial and candidate legs use the same local build. A general target
runtime or kernel improvement can speed both sides and cancel in the local
ratio, even though it counts officially because the ranked serial numerator
comes from a pinned baseline workspace. Compare absolute candidate
seconds/token against a fresh, same-host `BASE_SHA` run as well as the local
serial/MTP ratio.

Label every measurement and formula `harness=local` or `harness=ranked`. Use
matched absolute candidate MTP time as the primary local signal for broad
runtime and kernel changes; the local ratio may contain same-build cancellation
that does not exist officially.

The AWS Mac fleet assigns one physical Mac to each student. The advisor shares
only Edward's Mac; Alphonse, Thorfinn, and Askeladd each run on another Mac.
Locks, device occupancy, and thermal state are host-local. Run independent jobs
concurrently across distinct Macs and coordinate only co-located roles. Verify
the live placement rather than inferring a fleet-wide queue.

Run only one model-holding process at a time. The wrapper takes a per-user lock
and checks for orphaned workers; direct model commands do not. Inspect reported
PIDs before killing anything, and never disable the guard to work around a
legitimate concurrent run.

The wrapper cools the GPU before each resident measurement. Waiting at the 40C
gate is normal, not a hang, and the gate remains the default. For local timed
arms only, `MLXFAST_LOCAL_COOL_GATE=0` is permitted when all arms are
ABBA-counterbalanced within one session, every arm records entry and exit GPU
temperature with the entry spread reported beside the effect, and the result
retains `cool_gate_passed_real_gate=false` plus
`gate_qualified_for_timing=false`. Such evidence is explicitly ungated and
directional; compare it within that counterbalanced session, not as though it
were a gate-qualified or official result. Never fabricate a temperature source,
edit the gate constant, or bypass the process lock. Keep host, power, thermal
state, memory profile, head provenance, toolchain, token window, and frontier
SHA matched between base and candidate. Ranked authority is the self-hosted M5
runner labeled `m5-qwen38-27b-mtp`; non-M5 timing is directional only after
confirming the same kernel family executes.

## Experiment Discipline

One experiment should answer one causal question. For each arm:

1. Record an identity tuple: `BASE_SHA`, `UPSTREAM_SHA`, candidate commit and
   built-worker fingerprint, submitted-surface/twin/metallib digests, head
   digest, host/instance/chip, toolchain, memory and thermal mode, token window,
   fixture/reference source, exact cell/shape/dispatch, and harness.
2. State the official causal path, expected direction, minimum useful effect,
   and stop rule. Stress-test any score equation at zero and both signs.
3. Prove reachability from the current native-MTP worker and name the cost
   removed. Never transfer a constant or fitted model from another tree or cell
   without replay.
4. Check editable scope, byte budget, and the ranked score-boundary tripwire.
5. Measure an unchanged same-host baseline and save its artifact outside the
   submission surface.
6. Implement one mechanism. Before long timing, run the cheapest gate capable
   of falsifying the changed boundary. Numerical and reduction-order edits need
   actual floating-point touched-cell coverage, a positive control, and the
   shortest end-to-end exact-token/row-ledger run; integer-only or argmax checks
   are insufficient.
7. Measure the candidate under the same identity tuple. Start with the smallest
   decisive timing run; replicate only when noise could change the decision.
8. Compare absolute candidate time, paired ratio, draft/accept telemetry,
   rollback, and block latency where relevant. Cost-weight per-cell effects
   before aggregating them.
9. Reject, revise, or advance using the predeclared stop rule. Label every
   inference, interpolation, and extrapolation; never substitute M=8 for M=9,
   M4 for M5, local for ranked, or an abandoned dispatch table for the current
   tree.
10. Record positive, negative, invalid, and ambiguous results in the campaign
    ledger so later agents do not repeat them.

A microbenchmark is useful only when it bounds a named end-to-end cost.
Dispatch-count reduction is not a result unless it removes bytes, arithmetic,
or a dependency. A local pass on one easy prompt does not prove an adaptive
policy is robust across the hidden pool. Compose only independently measured
winners.

Promising current cost centers include draft scheduling, proposal-head cost and
quality, persistent head history, target verify width, GDN recurrence and
rollback, full-attention/GQA cache work, affine4 projection and vocabulary
readout, MLX scheduling/materialization, warmup, and transformed weight
metadata. Start from current profiles and the novelty ledger, not this list.

## Submission And Promotion

Yukon manages accounts and official submissions. It does not run the local
pre-submit command for you. In the maintained campaign checkout, use the guard:

```bash
export PATH="${HOME}/.local/bin:${PATH}"
yukon submissions --all
senpai/submit-official.sh "$BASE_SHA" \
  --model "senpai" \
  --note-file submission-note.md
yukon submissions
```

Use the exact lowercase campaign attribution `senpai`; record the underlying
models, effort levels, and agent harnesses in the public note body.

Immediately before submission, compare the highest-scoring live `promoted` row
with `senpai/frontier-state.json`. If the receipt, source ref, or score differs,
sync the organizer/promoted frontier, replay the candidate, and remeasure it.
Do not infer promotion from a `Validate submission` commit or a branch name.

Submit from a clean, committed candidate after `--local-submit`, exact diff
inspection, scope/budget checks, and generated-twin audit where relevant. Never
put credentials in notes, logs, commits, or agent messages. The ranked resource
is serialized; do not dispatch duplicate submissions to mine timing noise.

Only the official M5 result can promote a candidate. A green rejected receipt
may simply have failed to beat the current frontier; read its gates and metrics
before classifying it as incorrect.

## Historical Prior Art

Laguna work is useful only as a source of hypotheses about shared MLX concepts.
The curated transfer audit is
[`senpai/laguna-to-qwen-speedup-map.md`](senpai/laguna-to-qwen-speedup-map.md).
Do not run Laguna/DFlash setup or benchmark scripts, use their score formulas,
or copy their MoE/NVFP4 kernels without a Qwen-specific call-path and geometry
proof.
<!-- SENPAI-CAMPAIGN-END -->
").contains("whose only\npossible hit is the benchmark harness repeating an identical computation" → "whose only
possible hit is the benchmark harness repeating an identical computation")
✘ Test submissionStaticReviewPromptCoversMeasurementStructureExploitation() recorded an issue at BenchmarkScriptTests.swift:1204:9: Expectation failed: (normalizedRule → "<!-- senpai-campaign-begin -->
# qwen 3.8 native-mtp challenge agent guide

this repository has one ranked task: make qwen 3.8 27b native-mtp decode
faster on apple silicon without changing a single emitted token. the ranked
track is `qwen3.8-27b-mtp-v1`.

retained files from earlier tracks are historical prior art only. their
commands, model geometry, editable surface, correctness rules, scoring, and
runner configuration do not apply here. operate this repository only as the
qwen track named above.

## senpai campaign

for coordinated research in `morganmcg1/qwen38-challenge_senpai`, read
[`senpai/program.md`](senpai/program.md) and
[`senpai/experiment-runbook.md`](senpai/experiment-runbook.md) before assigning
or running experiments. the campaign ledger and exact frontier pins are in
[`senpai/campaign-ledger.md`](senpai/campaign-ledger.md) and
[`senpai/frontier-state.json`](senpai/frontier-state.json).

the intended remotes are:

```text
origin    https://github.com/morganmcg1/qwen38-challenge_senpai.git
upstream  https://github.com/layr-labs/qwen-3.8-mtp-challenge
```

`upstream` must have push url `disabled`. never run `yukon sync`,
`yukon sync --harness-only`, or `yukon reset` in this maintained checkout;
those commands are for plain solver checkouts and can reset campaign history or
repoint `origin`. use the repository-local `sync-organizer-frontier` skill for
organizer policy and promoted-snapshot refreshes.

record the exact campaign `base_sha` and organizer `upstream_sha` for every
experiment. preserve unrelated user work, campaign files, and dirty-worktree
changes. generated weights, caches, score files, downloaded artifacts, and
hidden material never belong in git.

## authority order

this repository still contains historical comments and bring-up prose. resolve
contract questions from the live enforcing sources in this order:

1. [`benchmark.json`](benchmark.json): track identity, commands, editable
   paths, optional paths, budget policy, and scoring fields.
2. [`fixtures/qwen3_8_27b_mtp_track.json`](fixtures/qwen3_8_27b_mtp_track.json):
   artifact pins, timed prompt pool, fidelity semantics, and calibration.
3. [`.github/workflows/qwen-mtp-ranked-benchmark.yml`](.github/workflows/qwen-mtp-ranked-benchmark.yml):
   ranked execution, runner label, and enforced environment values.
4. [`docs/qwen-mtp-editable-surface.md`](docs/qwen-mtp-editable-surface.md):
   rationale for the editable/trusted boundary.
5. [`task.md`](task.md): concise participant-facing summary.

for mutable solver behavior, inspect the current `base_sha` source and
`mtp-head.manifest.json`. narrative descriptions of the original starting tree
do not override the current implementation. if authorities disagree, stop and
identify the exact enforcing site before editing or assigning work.

## mission and score

the official score is a decode-only paired speedup anchored at serial `1.0`:

```text
for each hidden prompt p:
  raw_p = mean(pinned-baseline depth-0 serial seconds/token)
          / mean(candidate seconds/token)

published score = median(raw_1 ... raw_8)
```

the ranked legs come from different workspaces. the trusted, prebuilt baseline
workspace produces the serial numerator; the submitted candidate workspace
produces the mtp denominator. candidate-editable code cannot change the ranked
serial numerator, so any compliant reduction in candidate mtp time improves
the affected `raw_p`. never subtract a local serial-path share such as
`psi_serial` when pricing official value. the local same-build harness has a
different causal graph and must be labeled separately. run
`senpai/verify-ranked-score-boundary.sh` before using a derived score model.

all eight hidden prompts are measured. because eight is even, the median is the
mean of the two central ordered values. there is no no-op normalization and no
prompt lottery. the published median must be at least `0.90` and no greater
than the `5.0` plausibility ceiling.

each ranked leg processes a 512-token seed followed by 512 parent-counted
decode tokens, and the seed work is charged in the timed leg. prefill is not a
separate score, but work removed from the charged seed still improves the
result. the ranked run uses one thermally gated serial/candidate pair per
prompt unless the trusted contract changes.

the trusted parent offers a per-round ceiling. candidate policy may choose
from 0 through 8 drafts independently on every round. drafting nothing is
legal and degenerates to serial decode; it is a control, not the optimization
goal. acceptance rate alone is not the objective: deeper drafting wins only
when extra committed tokens repay head, target-verification, rejected-tail,
and state-management costs across varied prompts.

the organizer's original calibrated tree scored about `0.994`. that is a
historical starting-line measurement, not the current campaign frontier. read
the live yukon promoted row and `senpai/frontier-state.json` before comparing or
submitting work.

## target and current scored path

the fixed target is the organizer's mlx 4-bit affine/group-64 conversion of
qwen 3.8 27b:

```text
backbone  eigenlabs/qwen3.8-27b-4bit
          @ eda45ab47f465d08d6558f0353a2346e2eb9d5b3
upstream  qwen/qwen3.8-27b
          @ 1d4bf0f2ff6012fd82039f2fa52739d0dd7c60c0
```

the organizer-pinned default proposal head is the separate bf16 mtp
artifact. a candidate may instead declare an immutable remote or in-branch head
through `mtp-head.manifest.json`; inspect that file at `base_sha` for current
head provenance. the target checkpoint remains fixed regardless of the head.

the qwen tower is a dense hybrid architecture:

- 64 layers: 48 gated deltanet recurrent layers and 16 full-attention layers;
- hidden size 5,120 and vocabulary 248,320;
- full attention with 24 query heads, 4 kv heads, and head dimension 256;
- affine 4-bit/group-64 backbone weights and dense mlps.

the `qwen35*` source names and `qwen3_5_text` configuration label describe the
architecture family embedded in the qwen 3.8 artifact; they do not mean this
repository targets qwen 3.5 or 3.6.

the scored worker loads the transformed checkpoint through `llmmodelfactory`,
attaches the selected head, warms legal shapes, and serves rounds through
[`sources/mlxfastmodel/qwen36mtpblocksession.swift`](sources/mlxfastmodel/qwen36mtpblocksession.swift).
a round conceptually:

1. commits the pending primary token;
2. lets candidate policy choose 0...8 drafts within the offered maximum;
3. runs the mtp head chain to propose them;
4. verifies `[primary] + drafts` with the fixed target in a batched forward;
5. accepts the longest correct prefix;
6. keeps, restores, replays, or repairs target and head state as required;
7. reports actual rows and exact top-two evidence to the trusted parent.

the timed target model is the vendored `qwen35textmodel`/`qwen35model` in
[`vendor/mlx-swift-lm/libraries/mlxllm/models/qwen35.swift`](vendor/mlx-swift-lm/libraries/mlxllm/models/qwen35.swift),
together with the editable mtp session. directory-wide editability does not
prove reachability: for example, the separate `qwen35fastengine` path is not
the mtp worker's current target path. trace the call path before optimizing.

### current frontier is already optimized

do not treat historical defaults as current behavior or rediscover mechanisms
already in the promoted snapshot. at this guide's 2026-08-16 rewrite:

- `qwen36mtpblocksession.init` installs an adaptive cost-model policy; the
  nearby k=1 property initializer and constant-depth comments are historical
  fallback/bring-up text, not the instantiated policy;
- the campaign `mtp-head.manifest.json` declares a 4-bit/group-64 proposal
  head rather than using the organizer-pinned bf16 default;
- persistent head history, k/v-only history flush, device-resident drafting,
  exact rollback/replay, and scored-shape warmups are present;
- target verification has exact multi-row affine4 qmv sharing, fused gdn and
  full-attention preparation, and an exact two-stage top-two reducer;
- proposal selection already uses a compact vocabulary and a custom selector;
- the layer loop already carries an `asynceval` ladder for narrow widths.

these are mutable solver facts. recheck source after every frontier sync. see
[`senpai/laguna-to-qwen-speedup-map.md`](senpai/laguna-to-qwen-speedup-map.md)
and the campaign novelty ledger before assigning overlapping work.

## editable and trusted boundary

the ownership rule is:

> candidate code may propose and execute the model, but the trusted parent owns
> correctness, measurement, and final ledger authority.

the editable session necessarily assembles target verify forwards, walks the
candidate-side accepted prefix, rolls state back, and produces evidence for the
parent. none of those self-reports is authoritative: the parent independently
checks the serial token stream, round structure, rows, and accounting.

`benchmark.json` `editablepaths` is the only machine-readable path authority.
its current 89 entries form five practical groups:

- optional `mtp-head.manifest.json` and `mtp-head/` candidate-head content;
- `sources/mlxfastmodel/`, including the runtime and full mtp apparatus;
- `sources/mlxfasttransform/` offline checkpoint transform;
- the exact listed qwen model and `mlxlmcommon` support files;
- the exact listed mlx/metal kernel sources and generated twins.

do not infer that an adjacent file is editable. check the manifest at
`base_sha` before implementation. submission archives use replace semantics
over `editablepaths`; only the two mtp-head paths are optional. a support-file
change outside the surface can make a local tree faster while uploading
nothing useful.

trusted and not submitted include:

- target weights, transform contract, tokenizer, fixtures, and goldens;
- trusted driver, accept audit, row accounting, score emission, and worker
  protocol;
- timing, telemetry, thermal gate, workflows, and `.github/scripts/`;
- `benchmark.json`, `package.swift`, and `package.resolved`;
- tests, docs, local benchmark scripts, and other paths not named by the
  manifest.

`sources/mlxfastmodel/qwen36mtpreferencesession.swift` is an important mixed
case: it is editable through the directory-wide allowance even though it serves
rejected-tail reference replay. it runs after timing and cannot define emitted
tokens. never route its reference work or any golden-derived information into
the timed path, proposal policy, or emitted stream.

the source budget is 3,000,000 bytes total, 524,288 bytes per file, and
262,144 bytes of candidate growth. `mtp-head/` is exempt from the source budget
and instead has a declared digest, byte count, and 2 gib cap. run the campaign
scope and budget checks before expensive validation.

## correctness and prohibited strategies

correctness is a hard gate. every emitted token must equal the hidden serial
trajectory. the round journal must be structurally sound, effective draft
counts must match what occurred, and the reference-checked row ledger must
close. there is no near-tie allowance on this track.

the ranked pipeline also runs the public drift tripwire, hidden teacher-forced
base, anchor, free-run, behavior and gpqa gates, gpqa ttft guardrail, semantic
judge, transform verification, static review, and integrity checks. any failed
gate produces no promotable score.

the native mtp path inside the declared editable surface is the only sanctioned
future-token mechanism. do not add:

- prompt lookup, n-gram, suffix, or token-history drafting;
- same-target lookahead or unsanctioned future-logit/kv computation;
- request-keyed replay caches whose only useful hit is a repeated benchmark
  request;
- cross-request future state, hidden-prompt specialization, or gpqa shortcuts;
- degraded or partial target forwards, unevaluated verify rows, omitted exact
  final-logit/top-two work for emitted rows, or fabricated top-two, row,
  acceptance, or ledger evidence;
- an undeclared auxiliary proposal model/head or candidate-selected target;
- phase, reference-leg, prompt-pool, or baseline detection used to change the
  work performed;
- logical rollback that leaves rejected cache/state rows reachable, or merely
  decrements an offset without trimming or overwriting rejected physical rows;
- timing shortcuts, protocol injection, network access, or filesystem
  exfiltration.

input-independent weight/kernel/shape tables and ordinary within-request cache
reuse are legal when they preserve the contract. a candidate head may improve
proposal quality and cost; it cannot redefine the target answer.

public fixtures were generated on m5. on another apple gpu, compare an
unchanged `base_sha` at the same token position before diagnosing a near-tie
local mismatch. that diagnostic does not relax official exactness.

## kernel and swift rules

the vendored mlx package has two effective source forms:

- a family with an `mlx-generated/*.cpp` twin is jit-compiled from the source
  string embedded in that generated c++ file. update the readable
  `.metal`/`.h` source and the runtime-effective twin together.
- aot-only families are loaded from `mlx.metallib`; rebuild it with
  `tools/build-mlx-metallib.sh` after relevant edits.
- `_nax` variants are selected on ranked m5 hardware and are first-class
  targets, not optional follow-up work.

run `python3 research/twin_audit.py` after jit-source edits. before any kernel
experiment, prove the scored shape, dispatch family, source form, and m5
variant. reduction order, precision, bf16 cast boundaries, packing, recurrent
state, cache layout, and width-dependent dispatch are correctness-sensitive.

for direct swift commands, preserve the frozen dependency graph:

```bash
swift test --force-resolved-versions
mlxfast_run_mlx_runtime_tests=1 swift test --force-resolved-versions
swift build -c release --force-resolved-versions
tools/build-mlx-metallib.sh
```

a bare swift build may write `.build/release` without rebuilding the scored
`.build-worker/release/mlxfast-runtime-worker`. use the qwen benchmark wrapper
for end-to-end work. never run `swift package resolve` or `swift package
update`; if `package.resolved` changes, restore it rather than accepting a new
dependency graph.

## local workflow and measurement

use only the qwen entrypoints:

```bash
./setup.sh && ./setup-qwen-mtp.sh
./benchmark-qwen-mtp.sh --local-iterate
./benchmark-qwen-mtp.sh --local-submit
```

`--local-iterate` defaults to 64 decode tokens and `--local-submit` to 128.
they use one public fixture and cannot reproduce the ranked eight-prompt,
512-token result. treat local scores as directional.

the local serial and candidate legs use the same local build. a general target
runtime or kernel improvement can speed both sides and cancel in the local
ratio, even though it counts officially because the ranked serial numerator
comes from a pinned baseline workspace. compare absolute candidate
seconds/token against a fresh, same-host `base_sha` run as well as the local
serial/mtp ratio.

label every measurement and formula `harness=local` or `harness=ranked`. use
matched absolute candidate mtp time as the primary local signal for broad
runtime and kernel changes; the local ratio may contain same-build cancellation
that does not exist officially.

the aws mac fleet assigns one physical mac to each student. the advisor shares
only edward's mac; alphonse, thorfinn, and askeladd each run on another mac.
locks, device occupancy, and thermal state are host-local. run independent jobs
concurrently across distinct macs and coordinate only co-located roles. verify
the live placement rather than inferring a fleet-wide queue.

run only one model-holding process at a time. the wrapper takes a per-user lock
and checks for orphaned workers; direct model commands do not. inspect reported
pids before killing anything, and never disable the guard to work around a
legitimate concurrent run.

the wrapper cools the gpu before each resident measurement. waiting at the 40c
gate is normal, not a hang, and the gate remains the default. for local timed
arms only, `mlxfast_local_cool_gate=0` is permitted when all arms are
abba-counterbalanced within one session, every arm records entry and exit gpu
temperature with the entry spread reported beside the effect, and the result
retains `cool_gate_passed_real_gate=false` plus
`gate_qualified_for_timing=false`. such evidence is explicitly ungated and
directional; compare it within that counterbalanced session, not as though it
were a gate-qualified or official result. never fabricate a temperature source,
edit the gate constant, or bypass the process lock. keep host, power, thermal
state, memory profile, head provenance, toolchain, token window, and frontier
sha matched between base and candidate. ranked authority is the self-hosted m5
runner labeled `m5-qwen38-27b-mtp`; non-m5 timing is directional only after
confirming the same kernel family executes.

## experiment discipline

one experiment should answer one causal question. for each arm:

1. record an identity tuple: `base_sha`, `upstream_sha`, candidate commit and
   built-worker fingerprint, submitted-surface/twin/metallib digests, head
   digest, host/instance/chip, toolchain, memory and thermal mode, token window,
   fixture/reference source, exact cell/shape/dispatch, and harness.
2. state the official causal path, expected direction, minimum useful effect,
   and stop rule. stress-test any score equation at zero and both signs.
3. prove reachability from the current native-mtp worker and name the cost
   removed. never transfer a constant or fitted model from another tree or cell
   without replay.
4. check editable scope, byte budget, and the ranked score-boundary tripwire.
5. measure an unchanged same-host baseline and save its artifact outside the
   submission surface.
6. implement one mechanism. before long timing, run the cheapest gate capable
   of falsifying the changed boundary. numerical and reduction-order edits need
   actual floating-point touched-cell coverage, a positive control, and the
   shortest end-to-end exact-token/row-ledger run; integer-only or argmax checks
   are insufficient.
7. measure the candidate under the same identity tuple. start with the smallest
   decisive timing run; replicate only when noise could change the decision.
8. compare absolute candidate time, paired ratio, draft/accept telemetry,
   rollback, and block latency where relevant. cost-weight per-cell effects
   before aggregating them.
9. reject, revise, or advance using the predeclared stop rule. label every
   inference, interpolation, and extrapolation; never substitute m=8 for m=9,
   m4 for m5, local for ranked, or an abandoned dispatch table for the current
   tree.
10. record positive, negative, invalid, and ambiguous results in the campaign
    ledger so later agents do not repeat them.

a microbenchmark is useful only when it bounds a named end-to-end cost.
dispatch-count reduction is not a result unless it removes bytes, arithmetic,
or a dependency. a local pass on one easy prompt does not prove an adaptive
policy is robust across the hidden pool. compose only independently measured
winners.

promising current cost centers include draft scheduling, proposal-head cost and
quality, persistent head history, target verify width, gdn recurrence and
rollback, full-attention/gqa cache work, affine4 projection and vocabulary
readout, mlx scheduling/materialization, warmup, and transformed weight
metadata. start from current profiles and the novelty ledger, not this list.

## submission and promotion

yukon manages accounts and official submissions. it does not run the local
pre-submit command for you. in the maintained campaign checkout, use the guard:

```bash
export path="${home}/.local/bin:${path}"
yukon submissions --all
senpai/submit-official.sh "$base_sha" \
  --model "senpai" \
  --note-file submission-note.md
yukon submissions
```

use the exact lowercase campaign attribution `senpai`; record the underlying
models, effort levels, and agent harnesses in the public note body.

immediately before submission, compare the highest-scoring live `promoted` row
with `senpai/frontier-state.json`. if the receipt, source ref, or score differs,
sync the organizer/promoted frontier, replay the candidate, and remeasure it.
do not infer promotion from a `validate submission` commit or a branch name.

submit from a clean, committed candidate after `--local-submit`, exact diff
inspection, scope/budget checks, and generated-twin audit where relevant. never
put credentials in notes, logs, commits, or agent messages. the ranked resource
is serialized; do not dispatch duplicate submissions to mine timing noise.

only the official m5 result can promote a candidate. a green rejected receipt
may simply have failed to beat the current frontier; read its gates and metrics
before classifying it as incorrect.

## historical prior art

laguna work is useful only as a source of hypotheses about shared mlx concepts.
the curated transfer audit is
[`senpai/laguna-to-qwen-speedup-map.md`](senpai/laguna-to-qwen-speedup-map.md).
do not run laguna/dflash setup or benchmark scripts, use their score formulas,
or copy their moe/nvfp4 kernels without a qwen-specific call-path and geometry
proof.
<!-- senpai-campaign-end -->
").contains("serial non-speculative")
↳ AGENTS.md is missing the serial-track rule
✘ Test submissionStaticReviewPromptCoversMeasurementStructureExploitation() recorded an issue at BenchmarkScriptTests.swift:1208:9: Expectation failed: (normalizedRule → "<!-- senpai-campaign-begin -->
# qwen 3.8 native-mtp challenge agent guide

this repository has one ranked task: make qwen 3.8 27b native-mtp decode
faster on apple silicon without changing a single emitted token. the ranked
track is `qwen3.8-27b-mtp-v1`.

retained files from earlier tracks are historical prior art only. their
commands, model geometry, editable surface, correctness rules, scoring, and
runner configuration do not apply here. operate this repository only as the
qwen track named above.

## senpai campaign

for coordinated research in `morganmcg1/qwen38-challenge_senpai`, read
[`senpai/program.md`](senpai/program.md) and
[`senpai/experiment-runbook.md`](senpai/experiment-runbook.md) before assigning
or running experiments. the campaign ledger and exact frontier pins are in
[`senpai/campaign-ledger.md`](senpai/campaign-ledger.md) and
[`senpai/frontier-state.json`](senpai/frontier-state.json).

the intended remotes are:

```text
origin    https://github.com/morganmcg1/qwen38-challenge_senpai.git
upstream  https://github.com/layr-labs/qwen-3.8-mtp-challenge
```

`upstream` must have push url `disabled`. never run `yukon sync`,
`yukon sync --harness-only`, or `yukon reset` in this maintained checkout;
those commands are for plain solver checkouts and can reset campaign history or
repoint `origin`. use the repository-local `sync-organizer-frontier` skill for
organizer policy and promoted-snapshot refreshes.

record the exact campaign `base_sha` and organizer `upstream_sha` for every
experiment. preserve unrelated user work, campaign files, and dirty-worktree
changes. generated weights, caches, score files, downloaded artifacts, and
hidden material never belong in git.

## authority order

this repository still contains historical comments and bring-up prose. resolve
contract questions from the live enforcing sources in this order:

1. [`benchmark.json`](benchmark.json): track identity, commands, editable
   paths, optional paths, budget policy, and scoring fields.
2. [`fixtures/qwen3_8_27b_mtp_track.json`](fixtures/qwen3_8_27b_mtp_track.json):
   artifact pins, timed prompt pool, fidelity semantics, and calibration.
3. [`.github/workflows/qwen-mtp-ranked-benchmark.yml`](.github/workflows/qwen-mtp-ranked-benchmark.yml):
   ranked execution, runner label, and enforced environment values.
4. [`docs/qwen-mtp-editable-surface.md`](docs/qwen-mtp-editable-surface.md):
   rationale for the editable/trusted boundary.
5. [`task.md`](task.md): concise participant-facing summary.

for mutable solver behavior, inspect the current `base_sha` source and
`mtp-head.manifest.json`. narrative descriptions of the original starting tree
do not override the current implementation. if authorities disagree, stop and
identify the exact enforcing site before editing or assigning work.

## mission and score

the official score is a decode-only paired speedup anchored at serial `1.0`:

```text
for each hidden prompt p:
  raw_p = mean(pinned-baseline depth-0 serial seconds/token)
          / mean(candidate seconds/token)

published score = median(raw_1 ... raw_8)
```

the ranked legs come from different workspaces. the trusted, prebuilt baseline
workspace produces the serial numerator; the submitted candidate workspace
produces the mtp denominator. candidate-editable code cannot change the ranked
serial numerator, so any compliant reduction in candidate mtp time improves
the affected `raw_p`. never subtract a local serial-path share such as
`psi_serial` when pricing official value. the local same-build harness has a
different causal graph and must be labeled separately. run
`senpai/verify-ranked-score-boundary.sh` before using a derived score model.

all eight hidden prompts are measured. because eight is even, the median is the
mean of the two central ordered values. there is no no-op normalization and no
prompt lottery. the published median must be at least `0.90` and no greater
than the `5.0` plausibility ceiling.

each ranked leg processes a 512-token seed followed by 512 parent-counted
decode tokens, and the seed work is charged in the timed leg. prefill is not a
separate score, but work removed from the charged seed still improves the
result. the ranked run uses one thermally gated serial/candidate pair per
prompt unless the trusted contract changes.

the trusted parent offers a per-round ceiling. candidate policy may choose
from 0 through 8 drafts independently on every round. drafting nothing is
legal and degenerates to serial decode; it is a control, not the optimization
goal. acceptance rate alone is not the objective: deeper drafting wins only
when extra committed tokens repay head, target-verification, rejected-tail,
and state-management costs across varied prompts.

the organizer's original calibrated tree scored about `0.994`. that is a
historical starting-line measurement, not the current campaign frontier. read
the live yukon promoted row and `senpai/frontier-state.json` before comparing or
submitting work.

## target and current scored path

the fixed target is the organizer's mlx 4-bit affine/group-64 conversion of
qwen 3.8 27b:

```text
backbone  eigenlabs/qwen3.8-27b-4bit
          @ eda45ab47f465d08d6558f0353a2346e2eb9d5b3
upstream  qwen/qwen3.8-27b
          @ 1d4bf0f2ff6012fd82039f2fa52739d0dd7c60c0
```

the organizer-pinned default proposal head is the separate bf16 mtp
artifact. a candidate may instead declare an immutable remote or in-branch head
through `mtp-head.manifest.json`; inspect that file at `base_sha` for current
head provenance. the target checkpoint remains fixed regardless of the head.

the qwen tower is a dense hybrid architecture:

- 64 layers: 48 gated deltanet recurrent layers and 16 full-attention layers;
- hidden size 5,120 and vocabulary 248,320;
- full attention with 24 query heads, 4 kv heads, and head dimension 256;
- affine 4-bit/group-64 backbone weights and dense mlps.

the `qwen35*` source names and `qwen3_5_text` configuration label describe the
architecture family embedded in the qwen 3.8 artifact; they do not mean this
repository targets qwen 3.5 or 3.6.

the scored worker loads the transformed checkpoint through `llmmodelfactory`,
attaches the selected head, warms legal shapes, and serves rounds through
[`sources/mlxfastmodel/qwen36mtpblocksession.swift`](sources/mlxfastmodel/qwen36mtpblocksession.swift).
a round conceptually:

1. commits the pending primary token;
2. lets candidate policy choose 0...8 drafts within the offered maximum;
3. runs the mtp head chain to propose them;
4. verifies `[primary] + drafts` with the fixed target in a batched forward;
5. accepts the longest correct prefix;
6. keeps, restores, replays, or repairs target and head state as required;
7. reports actual rows and exact top-two evidence to the trusted parent.

the timed target model is the vendored `qwen35textmodel`/`qwen35model` in
[`vendor/mlx-swift-lm/libraries/mlxllm/models/qwen35.swift`](vendor/mlx-swift-lm/libraries/mlxllm/models/qwen35.swift),
together with the editable mtp session. directory-wide editability does not
prove reachability: for example, the separate `qwen35fastengine` path is not
the mtp worker's current target path. trace the call path before optimizing.

### current frontier is already optimized

do not treat historical defaults as current behavior or rediscover mechanisms
already in the promoted snapshot. at this guide's 2026-08-16 rewrite:

- `qwen36mtpblocksession.init` installs an adaptive cost-model policy; the
  nearby k=1 property initializer and constant-depth comments are historical
  fallback/bring-up text, not the instantiated policy;
- the campaign `mtp-head.manifest.json` declares a 4-bit/group-64 proposal
  head rather than using the organizer-pinned bf16 default;
- persistent head history, k/v-only history flush, device-resident drafting,
  exact rollback/replay, and scored-shape warmups are present;
- target verification has exact multi-row affine4 qmv sharing, fused gdn and
  full-attention preparation, and an exact two-stage top-two reducer;
- proposal selection already uses a compact vocabulary and a custom selector;
- the layer loop already carries an `asynceval` ladder for narrow widths.

these are mutable solver facts. recheck source after every frontier sync. see
[`senpai/laguna-to-qwen-speedup-map.md`](senpai/laguna-to-qwen-speedup-map.md)
and the campaign novelty ledger before assigning overlapping work.

## editable and trusted boundary

the ownership rule is:

> candidate code may propose and execute the model, but the trusted parent owns
> correctness, measurement, and final ledger authority.

the editable session necessarily assembles target verify forwards, walks the
candidate-side accepted prefix, rolls state back, and produces evidence for the
parent. none of those self-reports is authoritative: the parent independently
checks the serial token stream, round structure, rows, and accounting.

`benchmark.json` `editablepaths` is the only machine-readable path authority.
its current 89 entries form five practical groups:

- optional `mtp-head.manifest.json` and `mtp-head/` candidate-head content;
- `sources/mlxfastmodel/`, including the runtime and full mtp apparatus;
- `sources/mlxfasttransform/` offline checkpoint transform;
- the exact listed qwen model and `mlxlmcommon` support files;
- the exact listed mlx/metal kernel sources and generated twins.

do not infer that an adjacent file is editable. check the manifest at
`base_sha` before implementation. submission archives use replace semantics
over `editablepaths`; only the two mtp-head paths are optional. a support-file
change outside the surface can make a local tree faster while uploading
nothing useful.

trusted and not submitted include:

- target weights, transform contract, tokenizer, fixtures, and goldens;
- trusted driver, accept audit, row accounting, score emission, and worker
  protocol;
- timing, telemetry, thermal gate, workflows, and `.github/scripts/`;
- `benchmark.json`, `package.swift`, and `package.resolved`;
- tests, docs, local benchmark scripts, and other paths not named by the
  manifest.

`sources/mlxfastmodel/qwen36mtpreferencesession.swift` is an important mixed
case: it is editable through the directory-wide allowance even though it serves
rejected-tail reference replay. it runs after timing and cannot define emitted
tokens. never route its reference work or any golden-derived information into
the timed path, proposal policy, or emitted stream.

the source budget is 3,000,000 bytes total, 524,288 bytes per file, and
262,144 bytes of candidate growth. `mtp-head/` is exempt from the source budget
and instead has a declared digest, byte count, and 2 gib cap. run the campaign
scope and budget checks before expensive validation.

## correctness and prohibited strategies

correctness is a hard gate. every emitted token must equal the hidden serial
trajectory. the round journal must be structurally sound, effective draft
counts must match what occurred, and the reference-checked row ledger must
close. there is no near-tie allowance on this track.

the ranked pipeline also runs the public drift tripwire, hidden teacher-forced
base, anchor, free-run, behavior and gpqa gates, gpqa ttft guardrail, semantic
judge, transform verification, static review, and integrity checks. any failed
gate produces no promotable score.

the native mtp path inside the declared editable surface is the only sanctioned
future-token mechanism. do not add:

- prompt lookup, n-gram, suffix, or token-history drafting;
- same-target lookahead or unsanctioned future-logit/kv computation;
- request-keyed replay caches whose only useful hit is a repeated benchmark
  request;
- cross-request future state, hidden-prompt specialization, or gpqa shortcuts;
- degraded or partial target forwards, unevaluated verify rows, omitted exact
  final-logit/top-two work for emitted rows, or fabricated top-two, row,
  acceptance, or ledger evidence;
- an undeclared auxiliary proposal model/head or candidate-selected target;
- phase, reference-leg, prompt-pool, or baseline detection used to change the
  work performed;
- logical rollback that leaves rejected cache/state rows reachable, or merely
  decrements an offset without trimming or overwriting rejected physical rows;
- timing shortcuts, protocol injection, network access, or filesystem
  exfiltration.

input-independent weight/kernel/shape tables and ordinary within-request cache
reuse are legal when they preserve the contract. a candidate head may improve
proposal quality and cost; it cannot redefine the target answer.

public fixtures were generated on m5. on another apple gpu, compare an
unchanged `base_sha` at the same token position before diagnosing a near-tie
local mismatch. that diagnostic does not relax official exactness.

## kernel and swift rules

the vendored mlx package has two effective source forms:

- a family with an `mlx-generated/*.cpp` twin is jit-compiled from the source
  string embedded in that generated c++ file. update the readable
  `.metal`/`.h` source and the runtime-effective twin together.
- aot-only families are loaded from `mlx.metallib`; rebuild it with
  `tools/build-mlx-metallib.sh` after relevant edits.
- `_nax` variants are selected on ranked m5 hardware and are first-class
  targets, not optional follow-up work.

run `python3 research/twin_audit.py` after jit-source edits. before any kernel
experiment, prove the scored shape, dispatch family, source form, and m5
variant. reduction order, precision, bf16 cast boundaries, packing, recurrent
state, cache layout, and width-dependent dispatch are correctness-sensitive.

for direct swift commands, preserve the frozen dependency graph:

```bash
swift test --force-resolved-versions
mlxfast_run_mlx_runtime_tests=1 swift test --force-resolved-versions
swift build -c release --force-resolved-versions
tools/build-mlx-metallib.sh
```

a bare swift build may write `.build/release` without rebuilding the scored
`.build-worker/release/mlxfast-runtime-worker`. use the qwen benchmark wrapper
for end-to-end work. never run `swift package resolve` or `swift package
update`; if `package.resolved` changes, restore it rather than accepting a new
dependency graph.

## local workflow and measurement

use only the qwen entrypoints:

```bash
./setup.sh && ./setup-qwen-mtp.sh
./benchmark-qwen-mtp.sh --local-iterate
./benchmark-qwen-mtp.sh --local-submit
```

`--local-iterate` defaults to 64 decode tokens and `--local-submit` to 128.
they use one public fixture and cannot reproduce the ranked eight-prompt,
512-token result. treat local scores as directional.

the local serial and candidate legs use the same local build. a general target
runtime or kernel improvement can speed both sides and cancel in the local
ratio, even though it counts officially because the ranked serial numerator
comes from a pinned baseline workspace. compare absolute candidate
seconds/token against a fresh, same-host `base_sha` run as well as the local
serial/mtp ratio.

label every measurement and formula `harness=local` or `harness=ranked`. use
matched absolute candidate mtp time as the primary local signal for broad
runtime and kernel changes; the local ratio may contain same-build cancellation
that does not exist officially.

the aws mac fleet assigns one physical mac to each student. the advisor shares
only edward's mac; alphonse, thorfinn, and askeladd each run on another mac.
locks, device occupancy, and thermal state are host-local. run independent jobs
concurrently across distinct macs and coordinate only co-located roles. verify
the live placement rather than inferring a fleet-wide queue.

run only one model-holding process at a time. the wrapper takes a per-user lock
and checks for orphaned workers; direct model commands do not. inspect reported
pids before killing anything, and never disable the guard to work around a
legitimate concurrent run.

the wrapper cools the gpu before each resident measurement. waiting at the 40c
gate is normal, not a hang, and the gate remains the default. for local timed
arms only, `mlxfast_local_cool_gate=0` is permitted when all arms are
abba-counterbalanced within one session, every arm records entry and exit gpu
temperature with the entry spread reported beside the effect, and the result
retains `cool_gate_passed_real_gate=false` plus
`gate_qualified_for_timing=false`. such evidence is explicitly ungated and
directional; compare it within that counterbalanced session, not as though it
were a gate-qualified or official result. never fabricate a temperature source,
edit the gate constant, or bypass the process lock. keep host, power, thermal
state, memory profile, head provenance, toolchain, token window, and frontier
sha matched between base and candidate. ranked authority is the self-hosted m5
runner labeled `m5-qwen38-27b-mtp`; non-m5 timing is directional only after
confirming the same kernel family executes.

## experiment discipline

one experiment should answer one causal question. for each arm:

1. record an identity tuple: `base_sha`, `upstream_sha`, candidate commit and
   built-worker fingerprint, submitted-surface/twin/metallib digests, head
   digest, host/instance/chip, toolchain, memory and thermal mode, token window,
   fixture/reference source, exact cell/shape/dispatch, and harness.
2. state the official causal path, expected direction, minimum useful effect,
   and stop rule. stress-test any score equation at zero and both signs.
3. prove reachability from the current native-mtp worker and name the cost
   removed. never transfer a constant or fitted model from another tree or cell
   without replay.
4. check editable scope, byte budget, and the ranked score-boundary tripwire.
5. measure an unchanged same-host baseline and save its artifact outside the
   submission surface.
6. implement one mechanism. before long timing, run the cheapest gate capable
   of falsifying the changed boundary. numerical and reduction-order edits need
   actual floating-point touched-cell coverage, a positive control, and the
   shortest end-to-end exact-token/row-ledger run; integer-only or argmax checks
   are insufficient.
7. measure the candidate under the same identity tuple. start with the smallest
   decisive timing run; replicate only when noise could change the decision.
8. compare absolute candidate time, paired ratio, draft/accept telemetry,
   rollback, and block latency where relevant. cost-weight per-cell effects
   before aggregating them.
9. reject, revise, or advance using the predeclared stop rule. label every
   inference, interpolation, and extrapolation; never substitute m=8 for m=9,
   m4 for m5, local for ranked, or an abandoned dispatch table for the current
   tree.
10. record positive, negative, invalid, and ambiguous results in the campaign
    ledger so later agents do not repeat them.

a microbenchmark is useful only when it bounds a named end-to-end cost.
dispatch-count reduction is not a result unless it removes bytes, arithmetic,
or a dependency. a local pass on one easy prompt does not prove an adaptive
policy is robust across the hidden pool. compose only independently measured
winners.

promising current cost centers include draft scheduling, proposal-head cost and
quality, persistent head history, target verify width, gdn recurrence and
rollback, full-attention/gqa cache work, affine4 projection and vocabulary
readout, mlx scheduling/materialization, warmup, and transformed weight
metadata. start from current profiles and the novelty ledger, not this list.

## submission and promotion

yukon manages accounts and official submissions. it does not run the local
pre-submit command for you. in the maintained campaign checkout, use the guard:

```bash
export path="${home}/.local/bin:${path}"
yukon submissions --all
senpai/submit-official.sh "$base_sha" \
  --model "senpai" \
  --note-file submission-note.md
yukon submissions
```

use the exact lowercase campaign attribution `senpai`; record the underlying
models, effort levels, and agent harnesses in the public note body.

immediately before submission, compare the highest-scoring live `promoted` row
with `senpai/frontier-state.json`. if the receipt, source ref, or score differs,
sync the organizer/promoted frontier, replay the candidate, and remeasure it.
do not infer promotion from a `validate submission` commit or a branch name.

submit from a clean, committed candidate after `--local-submit`, exact diff
inspection, scope/budget checks, and generated-twin audit where relevant. never
put credentials in notes, logs, commits, or agent messages. the ranked resource
is serialized; do not dispatch duplicate submissions to mine timing noise.

only the official m5 result can promote a candidate. a green rejected receipt
may simply have failed to beat the current frontier; read its gates and metrics
before classifying it as incorrect.

## historical prior art

laguna work is useful only as a source of hypotheses about shared mlx concepts.
the curated transfer audit is
[`senpai/laguna-to-qwen-speedup-map.md`](senpai/laguna-to-qwen-speedup-map.md).
do not run laguna/dflash setup or benchmark scripts, use their score formulas,
or copy their moe/nvfp4 kernels without a qwen-specific call-path and geometry
proof.
<!-- senpai-campaign-end -->
").contains("prompt-lookup decoding")
◇ Test lagunaConfigRejectsUnsupportedRopeType() started.
◇ Test runtimeWorkerClientTimesOutWaitingForHello() started.
◇ Test checkpointValidatorAcceptsExactPublic912TensorInventory() started.
◇ Test denseTensorStoreLoadsMetadataAndReadsBytes() started.
◇ Test benchmarkPlacesHiddenGoldenInputsSymlinkSafely() started.
◇ Test nvfp4Group16SplitKMatmulMatchesDequantizedReferenceWhenRuntimeTestsAreEnabled() started.
◇ Test runtimeWorkerPinnedConfigurationPathRejectsUnsafeFilesystemEntries() started.
◇ Test setupFallsBackWhenPrimaryReferenceDownloadFails() started.
◇ Test "the fused kernel is bit-identical to the eager add + MLX rmsNorm chain" started.
◇ Test setupDFlashProvisionsTheSharedNVFP4ReferenceAndClaimsNoConversion() started.
✘ Test submissionStaticReviewPromptCoversMeasurementStructureExploitation() recorded an issue at BenchmarkScriptTests.swift:1210:9: Expectation failed: (normalizedRule → "<!-- senpai-campaign-begin -->
# qwen 3.8 native-mtp challenge agent guide

this repository has one ranked task: make qwen 3.8 27b native-mtp decode
faster on apple silicon without changing a single emitted token. the ranked
track is `qwen3.8-27b-mtp-v1`.

retained files from earlier tracks are historical prior art only. their
commands, model geometry, editable surface, correctness rules, scoring, and
runner configuration do not apply here. operate this repository only as the
qwen track named above.

## senpai campaign

for coordinated research in `morganmcg1/qwen38-challenge_senpai`, read
[`senpai/program.md`](senpai/program.md) and
[`senpai/experiment-runbook.md`](senpai/experiment-runbook.md) before assigning
or running experiments. the campaign ledger and exact frontier pins are in
[`senpai/campaign-ledger.md`](senpai/campaign-ledger.md) and
[`senpai/frontier-state.json`](senpai/frontier-state.json).

the intended remotes are:

```text
origin    https://github.com/morganmcg1/qwen38-challenge_senpai.git
upstream  https://github.com/layr-labs/qwen-3.8-mtp-challenge
```

`upstream` must have push url `disabled`. never run `yukon sync`,
`yukon sync --harness-only`, or `yukon reset` in this maintained checkout;
those commands are for plain solver checkouts and can reset campaign history or
repoint `origin`. use the repository-local `sync-organizer-frontier` skill for
organizer policy and promoted-snapshot refreshes.

record the exact campaign `base_sha` and organizer `upstream_sha` for every
experiment. preserve unrelated user work, campaign files, and dirty-worktree
changes. generated weights, caches, score files, downloaded artifacts, and
hidden material never belong in git.

## authority order

this repository still contains historical comments and bring-up prose. resolve
contract questions from the live enforcing sources in this order:

1. [`benchmark.json`](benchmark.json): track identity, commands, editable
   paths, optional paths, budget policy, and scoring fields.
2. [`fixtures/qwen3_8_27b_mtp_track.json`](fixtures/qwen3_8_27b_mtp_track.json):
   artifact pins, timed prompt pool, fidelity semantics, and calibration.
3. [`.github/workflows/qwen-mtp-ranked-benchmark.yml`](.github/workflows/qwen-mtp-ranked-benchmark.yml):
   ranked execution, runner label, and enforced environment values.
4. [`docs/qwen-mtp-editable-surface.md`](docs/qwen-mtp-editable-surface.md):
   rationale for the editable/trusted boundary.
5. [`task.md`](task.md): concise participant-facing summary.

for mutable solver behavior, inspect the current `base_sha` source and
`mtp-head.manifest.json`. narrative descriptions of the original starting tree
do not override the current implementation. if authorities disagree, stop and
identify the exact enforcing site before editing or assigning work.

## mission and score

the official score is a decode-only paired speedup anchored at serial `1.0`:

```text
for each hidden prompt p:
  raw_p = mean(pinned-baseline depth-0 serial seconds/token)
          / mean(candidate seconds/token)

published score = median(raw_1 ... raw_8)
```

the ranked legs come from different workspaces. the trusted, prebuilt baseline
workspace produces the serial numerator; the submitted candidate workspace
produces the mtp denominator. candidate-editable code cannot change the ranked
serial numerator, so any compliant reduction in candidate mtp time improves
the affected `raw_p`. never subtract a local serial-path share such as
`psi_serial` when pricing official value. the local same-build harness has a
different causal graph and must be labeled separately. run
`senpai/verify-ranked-score-boundary.sh` before using a derived score model.

all eight hidden prompts are measured. because eight is even, the median is the
mean of the two central ordered values. there is no no-op normalization and no
prompt lottery. the published median must be at least `0.90` and no greater
than the `5.0` plausibility ceiling.

each ranked leg processes a 512-token seed followed by 512 parent-counted
decode tokens, and the seed work is charged in the timed leg. prefill is not a
separate score, but work removed from the charged seed still improves the
result. the ranked run uses one thermally gated serial/candidate pair per
prompt unless the trusted contract changes.

the trusted parent offers a per-round ceiling. candidate policy may choose
from 0 through 8 drafts independently on every round. drafting nothing is
legal and degenerates to serial decode; it is a control, not the optimization
goal. acceptance rate alone is not the objective: deeper drafting wins only
when extra committed tokens repay head, target-verification, rejected-tail,
and state-management costs across varied prompts.

the organizer's original calibrated tree scored about `0.994`. that is a
historical starting-line measurement, not the current campaign frontier. read
the live yukon promoted row and `senpai/frontier-state.json` before comparing or
submitting work.

## target and current scored path

the fixed target is the organizer's mlx 4-bit affine/group-64 conversion of
qwen 3.8 27b:

```text
backbone  eigenlabs/qwen3.8-27b-4bit
          @ eda45ab47f465d08d6558f0353a2346e2eb9d5b3
upstream  qwen/qwen3.8-27b
          @ 1d4bf0f2ff6012fd82039f2fa52739d0dd7c60c0
```

the organizer-pinned default proposal head is the separate bf16 mtp
artifact. a candidate may instead declare an immutable remote or in-branch head
through `mtp-head.manifest.json`; inspect that file at `base_sha` for current
head provenance. the target checkpoint remains fixed regardless of the head.

the qwen tower is a dense hybrid architecture:

- 64 layers: 48 gated deltanet recurrent layers and 16 full-attention layers;
- hidden size 5,120 and vocabulary 248,320;
- full attention with 24 query heads, 4 kv heads, and head dimension 256;
- affine 4-bit/group-64 backbone weights and dense mlps.

the `qwen35*` source names and `qwen3_5_text` configuration label describe the
architecture family embedded in the qwen 3.8 artifact; they do not mean this
repository targets qwen 3.5 or 3.6.

the scored worker loads the transformed checkpoint through `llmmodelfactory`,
attaches the selected head, warms legal shapes, and serves rounds through
[`sources/mlxfastmodel/qwen36mtpblocksession.swift`](sources/mlxfastmodel/qwen36mtpblocksession.swift).
a round conceptually:

1. commits the pending primary token;
2. lets candidate policy choose 0...8 drafts within the offered maximum;
3. runs the mtp head chain to propose them;
4. verifies `[primary] + drafts` with the fixed target in a batched forward;
5. accepts the longest correct prefix;
6. keeps, restores, replays, or repairs target and head state as required;
7. reports actual rows and exact top-two evidence to the trusted parent.

the timed target model is the vendored `qwen35textmodel`/`qwen35model` in
[`vendor/mlx-swift-lm/libraries/mlxllm/models/qwen35.swift`](vendor/mlx-swift-lm/libraries/mlxllm/models/qwen35.swift),
together with the editable mtp session. directory-wide editability does not
prove reachability: for example, the separate `qwen35fastengine` path is not
the mtp worker's current target path. trace the call path before optimizing.

### current frontier is already optimized

do not treat historical defaults as current behavior or rediscover mechanisms
already in the promoted snapshot. at this guide's 2026-08-16 rewrite:

- `qwen36mtpblocksession.init` installs an adaptive cost-model policy; the
  nearby k=1 property initializer and constant-depth comments are historical
  fallback/bring-up text, not the instantiated policy;
- the campaign `mtp-head.manifest.json` declares a 4-bit/group-64 proposal
  head rather than using the organizer-pinned bf16 default;
- persistent head history, k/v-only history flush, device-resident drafting,
  exact rollback/replay, and scored-shape warmups are present;
- target verification has exact multi-row affine4 qmv sharing, fused gdn and
  full-attention preparation, and an exact two-stage top-two reducer;
- proposal selection already uses a compact vocabulary and a custom selector;
- the layer loop already carries an `asynceval` ladder for narrow widths.

these are mutable solver facts. recheck source after every frontier sync. see
[`senpai/laguna-to-qwen-speedup-map.md`](senpai/laguna-to-qwen-speedup-map.md)
and the campaign novelty ledger before assigning overlapping work.

## editable and trusted boundary

the ownership rule is:

> candidate code may propose and execute the model, but the trusted parent owns
> correctness, measurement, and final ledger authority.

the editable session necessarily assembles target verify forwards, walks the
candidate-side accepted prefix, rolls state back, and produces evidence for the
parent. none of those self-reports is authoritative: the parent independently
checks the serial token stream, round structure, rows, and accounting.

`benchmark.json` `editablepaths` is the only machine-readable path authority.
its current 89 entries form five practical groups:

- optional `mtp-head.manifest.json` and `mtp-head/` candidate-head content;
- `sources/mlxfastmodel/`, including the runtime and full mtp apparatus;
- `sources/mlxfasttransform/` offline checkpoint transform;
- the exact listed qwen model and `mlxlmcommon` support files;
- the exact listed mlx/metal kernel sources and generated twins.

do not infer that an adjacent file is editable. check the manifest at
`base_sha` before implementation. submission archives use replace semantics
over `editablepaths`; only the two mtp-head paths are optional. a support-file
change outside the surface can make a local tree faster while uploading
nothing useful.

trusted and not submitted include:

- target weights, transform contract, tokenizer, fixtures, and goldens;
- trusted driver, accept audit, row accounting, score emission, and worker
  protocol;
- timing, telemetry, thermal gate, workflows, and `.github/scripts/`;
- `benchmark.json`, `package.swift`, and `package.resolved`;
- tests, docs, local benchmark scripts, and other paths not named by the
  manifest.

`sources/mlxfastmodel/qwen36mtpreferencesession.swift` is an important mixed
case: it is editable through the directory-wide allowance even though it serves
rejected-tail reference replay. it runs after timing and cannot define emitted
tokens. never route its reference work or any golden-derived information into
the timed path, proposal policy, or emitted stream.

the source budget is 3,000,000 bytes total, 524,288 bytes per file, and
262,144 bytes of candidate growth. `mtp-head/` is exempt from the source budget
and instead has a declared digest, byte count, and 2 gib cap. run the campaign
scope and budget checks before expensive validation.

## correctness and prohibited strategies

correctness is a hard gate. every emitted token must equal the hidden serial
trajectory. the round journal must be structurally sound, effective draft
counts must match what occurred, and the reference-checked row ledger must
close. there is no near-tie allowance on this track.

the ranked pipeline also runs the public drift tripwire, hidden teacher-forced
base, anchor, free-run, behavior and gpqa gates, gpqa ttft guardrail, semantic
judge, transform verification, static review, and integrity checks. any failed
gate produces no promotable score.

the native mtp path inside the declared editable surface is the only sanctioned
future-token mechanism. do not add:

- prompt lookup, n-gram, suffix, or token-history drafting;
- same-target lookahead or unsanctioned future-logit/kv computation;
- request-keyed replay caches whose only useful hit is a repeated benchmark
  request;
- cross-request future state, hidden-prompt specialization, or gpqa shortcuts;
- degraded or partial target forwards, unevaluated verify rows, omitted exact
  final-logit/top-two work for emitted rows, or fabricated top-two, row,
  acceptance, or ledger evidence;
- an undeclared auxiliary proposal model/head or candidate-selected target;
- phase, reference-leg, prompt-pool, or baseline detection used to change the
  work performed;
- logical rollback that leaves rejected cache/state rows reachable, or merely
  decrements an offset without trimming or overwriting rejected physical rows;
- timing shortcuts, protocol injection, network access, or filesystem
  exfiltration.

input-independent weight/kernel/shape tables and ordinary within-request cache
reuse are legal when they preserve the contract. a candidate head may improve
proposal quality and cost; it cannot redefine the target answer.

public fixtures were generated on m5. on another apple gpu, compare an
unchanged `base_sha` at the same token position before diagnosing a near-tie
local mismatch. that diagnostic does not relax official exactness.

## kernel and swift rules

the vendored mlx package has two effective source forms:

- a family with an `mlx-generated/*.cpp` twin is jit-compiled from the source
  string embedded in that generated c++ file. update the readable
  `.metal`/`.h` source and the runtime-effective twin together.
- aot-only families are loaded from `mlx.metallib`; rebuild it with
  `tools/build-mlx-metallib.sh` after relevant edits.
- `_nax` variants are selected on ranked m5 hardware and are first-class
  targets, not optional follow-up work.

run `python3 research/twin_audit.py` after jit-source edits. before any kernel
experiment, prove the scored shape, dispatch family, source form, and m5
variant. reduction order, precision, bf16 cast boundaries, packing, recurrent
state, cache layout, and width-dependent dispatch are correctness-sensitive.

for direct swift commands, preserve the frozen dependency graph:

```bash
swift test --force-resolved-versions
mlxfast_run_mlx_runtime_tests=1 swift test --force-resolved-versions
swift build -c release --force-resolved-versions
tools/build-mlx-metallib.sh
```

a bare swift build may write `.build/release` without rebuilding the scored
`.build-worker/release/mlxfast-runtime-worker`. use the qwen benchmark wrapper
for end-to-end work. never run `swift package resolve` or `swift package
update`; if `package.resolved` changes, restore it rather than accepting a new
dependency graph.

## local workflow and measurement

use only the qwen entrypoints:

```bash
./setup.sh && ./setup-qwen-mtp.sh
./benchmark-qwen-mtp.sh --local-iterate
./benchmark-qwen-mtp.sh --local-submit
```

`--local-iterate` defaults to 64 decode tokens and `--local-submit` to 128.
they use one public fixture and cannot reproduce the ranked eight-prompt,
512-token result. treat local scores as directional.

the local serial and candidate legs use the same local build. a general target
runtime or kernel improvement can speed both sides and cancel in the local
ratio, even though it counts officially because the ranked serial numerator
comes from a pinned baseline workspace. compare absolute candidate
seconds/token against a fresh, same-host `base_sha` run as well as the local
serial/mtp ratio.

label every measurement and formula `harness=local` or `harness=ranked`. use
matched absolute candidate mtp time as the primary local signal for broad
runtime and kernel changes; the local ratio may contain same-build cancellation
that does not exist officially.

the aws mac fleet assigns one physical mac to each student. the advisor shares
only edward's mac; alphonse, thorfinn, and askeladd each run on another mac.
locks, device occupancy, and thermal state are host-local. run independent jobs
concurrently across distinct macs and coordinate only co-located roles. verify
the live placement rather than inferring a fleet-wide queue.

run only one model-holding process at a time. the wrapper takes a per-user lock
and checks for orphaned workers; direct model commands do not. inspect reported
pids before killing anything, and never disable the guard to work around a
legitimate concurrent run.

the wrapper cools the gpu before each resident measurement. waiting at the 40c
gate is normal, not a hang, and the gate remains the default. for local timed
arms only, `mlxfast_local_cool_gate=0` is permitted when all arms are
abba-counterbalanced within one session, every arm records entry and exit gpu
temperature with the entry spread reported beside the effect, and the result
retains `cool_gate_passed_real_gate=false` plus
`gate_qualified_for_timing=false`. such evidence is explicitly ungated and
directional; compare it within that counterbalanced session, not as though it
were a gate-qualified or official result. never fabricate a temperature source,
edit the gate constant, or bypass the process lock. keep host, power, thermal
state, memory profile, head provenance, toolchain, token window, and frontier
sha matched between base and candidate. ranked authority is the self-hosted m5
runner labeled `m5-qwen38-27b-mtp`; non-m5 timing is directional only after
confirming the same kernel family executes.

## experiment discipline

one experiment should answer one causal question. for each arm:

1. record an identity tuple: `base_sha`, `upstream_sha`, candidate commit and
   built-worker fingerprint, submitted-surface/twin/metallib digests, head
   digest, host/instance/chip, toolchain, memory and thermal mode, token window,
   fixture/reference source, exact cell/shape/dispatch, and harness.
2. state the official causal path, expected direction, minimum useful effect,
   and stop rule. stress-test any score equation at zero and both signs.
3. prove reachability from the current native-mtp worker and name the cost
   removed. never transfer a constant or fitted model from another tree or cell
   without replay.
4. check editable scope, byte budget, and the ranked score-boundary tripwire.
5. measure an unchanged same-host baseline and save its artifact outside the
   submission surface.
6. implement one mechanism. before long timing, run the cheapest gate capable
   of falsifying the changed boundary. numerical and reduction-order edits need
   actual floating-point touched-cell coverage, a positive control, and the
   shortest end-to-end exact-token/row-ledger run; integer-only or argmax checks
   are insufficient.
7. measure the candidate under the same identity tuple. start with the smallest
   decisive timing run; replicate only when noise could change the decision.
8. compare absolute candidate time, paired ratio, draft/accept telemetry,
   rollback, and block latency where relevant. cost-weight per-cell effects
   before aggregating them.
9. reject, revise, or advance using the predeclared stop rule. label every
   inference, interpolation, and extrapolation; never substitute m=8 for m=9,
   m4 for m5, local for ranked, or an abandoned dispatch table for the current
   tree.
10. record positive, negative, invalid, and ambiguous results in the campaign
    ledger so later agents do not repeat them.

a microbenchmark is useful only when it bounds a named end-to-end cost.
dispatch-count reduction is not a result unless it removes bytes, arithmetic,
or a dependency. a local pass on one easy prompt does not prove an adaptive
policy is robust across the hidden pool. compose only independently measured
winners.

promising current cost centers include draft scheduling, proposal-head cost and
quality, persistent head history, target verify width, gdn recurrence and
rollback, full-attention/gqa cache work, affine4 projection and vocabulary
readout, mlx scheduling/materialization, warmup, and transformed weight
metadata. start from current profiles and the novelty ledger, not this list.

## submission and promotion

yukon manages accounts and official submissions. it does not run the local
pre-submit command for you. in the maintained campaign checkout, use the guard:

```bash
export path="${home}/.local/bin:${path}"
yukon submissions --all
senpai/submit-official.sh "$base_sha" \
  --model "senpai" \
  --note-file submission-note.md
yukon submissions
```

use the exact lowercase campaign attribution `senpai`; record the underlying
models, effort levels, and agent harnesses in the public note body.

immediately before submission, compare the highest-scoring live `promoted` row
with `senpai/frontier-state.json`. if the receipt, source ref, or score differs,
sync the organizer/promoted frontier, replay the candidate, and remeasure it.
do not infer promotion from a `validate submission` commit or a branch name.

submit from a clean, committed candidate after `--local-submit`, exact diff
inspection, scope/budget checks, and generated-twin audit where relevant. never
put credentials in notes, logs, commits, or agent messages. the ranked resource
is serialized; do not dispatch duplicate submissions to mine timing noise.

only the official m5 result can promote a candidate. a green rejected receipt
may simply have failed to beat the current frontier; read its gates and metrics
before classifying it as incorrect.

## historical prior art

laguna work is useful only as a source of hypotheses about shared mlx concepts.
the curated transfer audit is
[`senpai/laguna-to-qwen-speedup-map.md`](senpai/laguna-to-qwen-speedup-map.md).
do not run laguna/dflash setup or benchmark scripts, use their score formulas,
or copy their moe/nvfp4 kernels without a qwen-specific call-path and geometry
proof.
<!-- senpai-campaign-end -->
").contains("deferred cache rows")
✘ Test submissionStaticReviewPromptCoversMeasurementStructureExploitation() recorded an issue at BenchmarkScriptTests.swift:1211:9: Expectation failed: (normalizedRule → "<!-- senpai-campaign-begin -->
# qwen 3.8 native-mtp challenge agent guide

this repository has one ranked task: make qwen 3.8 27b native-mtp decode
faster on apple silicon without changing a single emitted token. the ranked
track is `qwen3.8-27b-mtp-v1`.

retained files from earlier tracks are historical prior art only. their
commands, model geometry, editable surface, correctness rules, scoring, and
runner configuration do not apply here. operate this repository only as the
qwen track named above.

## senpai campaign

for coordinated research in `morganmcg1/qwen38-challenge_senpai`, read
[`senpai/program.md`](senpai/program.md) and
[`senpai/experiment-runbook.md`](senpai/experiment-runbook.md) before assigning
or running experiments. the campaign ledger and exact frontier pins are in
[`senpai/campaign-ledger.md`](senpai/campaign-ledger.md) and
[`senpai/frontier-state.json`](senpai/frontier-state.json).

the intended remotes are:

```text
origin    https://github.com/morganmcg1/qwen38-challenge_senpai.git
upstream  https://github.com/layr-labs/qwen-3.8-mtp-challenge
```

`upstream` must have push url `disabled`. never run `yukon sync`,
`yukon sync --harness-only`, or `yukon reset` in this maintained checkout;
those commands are for plain solver checkouts and can reset campaign history or
repoint `origin`. use the repository-local `sync-organizer-frontier` skill for
organizer policy and promoted-snapshot refreshes.

record the exact campaign `base_sha` and organizer `upstream_sha` for every
experiment. preserve unrelated user work, campaign files, and dirty-worktree
changes. generated weights, caches, score files, downloaded artifacts, and
hidden material never belong in git.

## authority order

this repository still contains historical comments and bring-up prose. resolve
contract questions from the live enforcing sources in this order:

1. [`benchmark.json`](benchmark.json): track identity, commands, editable
   paths, optional paths, budget policy, and scoring fields.
2. [`fixtures/qwen3_8_27b_mtp_track.json`](fixtures/qwen3_8_27b_mtp_track.json):
   artifact pins, timed prompt pool, fidelity semantics, and calibration.
3. [`.github/workflows/qwen-mtp-ranked-benchmark.yml`](.github/workflows/qwen-mtp-ranked-benchmark.yml):
   ranked execution, runner label, and enforced environment values.
4. [`docs/qwen-mtp-editable-surface.md`](docs/qwen-mtp-editable-surface.md):
   rationale for the editable/trusted boundary.
5. [`task.md`](task.md): concise participant-facing summary.

for mutable solver behavior, inspect the current `base_sha` source and
`mtp-head.manifest.json`. narrative descriptions of the original starting tree
do not override the current implementation. if authorities disagree, stop and
identify the exact enforcing site before editing or assigning work.

## mission and score

the official score is a decode-only paired speedup anchored at serial `1.0`:

```text
for each hidden prompt p:
  raw_p = mean(pinned-baseline depth-0 serial seconds/token)
          / mean(candidate seconds/token)

published score = median(raw_1 ... raw_8)
```

the ranked legs come from different workspaces. the trusted, prebuilt baseline
workspace produces the serial numerator; the submitted candidate workspace
produces the mtp denominator. candidate-editable code cannot change the ranked
serial numerator, so any compliant reduction in candidate mtp time improves
the affected `raw_p`. never subtract a local serial-path share such as
`psi_serial` when pricing official value. the local same-build harness has a
different causal graph and must be labeled separately. run
`senpai/verify-ranked-score-boundary.sh` before using a derived score model.

all eight hidden prompts are measured. because eight is even, the median is the
mean of the two central ordered values. there is no no-op normalization and no
prompt lottery. the published median must be at least `0.90` and no greater
than the `5.0` plausibility ceiling.

each ranked leg processes a 512-token seed followed by 512 parent-counted
decode tokens, and the seed work is charged in the timed leg. prefill is not a
separate score, but work removed from the charged seed still improves the
result. the ranked run uses one thermally gated serial/candidate pair per
prompt unless the trusted contract changes.

the trusted parent offers a per-round ceiling. candidate policy may choose
from 0 through 8 drafts independently on every round. drafting nothing is
legal and degenerates to serial decode; it is a control, not the optimization
goal. acceptance rate alone is not the objective: deeper drafting wins only
when extra committed tokens repay head, target-verification, rejected-tail,
and state-management costs across varied prompts.

the organizer's original calibrated tree scored about `0.994`. that is a
historical starting-line measurement, not the current campaign frontier. read
the live yukon promoted row and `senpai/frontier-state.json` before comparing or
submitting work.

## target and current scored path

the fixed target is the organizer's mlx 4-bit affine/group-64 conversion of
qwen 3.8 27b:

```text
backbone  eigenlabs/qwen3.8-27b-4bit
          @ eda45ab47f465d08d6558f0353a2346e2eb9d5b3
upstream  qwen/qwen3.8-27b
          @ 1d4bf0f2ff6012fd82039f2fa52739d0dd7c60c0
```

the organizer-pinned default proposal head is the separate bf16 mtp
artifact. a candidate may instead declare an immutable remote or in-branch head
through `mtp-head.manifest.json`; inspect that file at `base_sha` for current
head provenance. the target checkpoint remains fixed regardless of the head.

the qwen tower is a dense hybrid architecture:

- 64 layers: 48 gated deltanet recurrent layers and 16 full-attention layers;
- hidden size 5,120 and vocabulary 248,320;
- full attention with 24 query heads, 4 kv heads, and head dimension 256;
- affine 4-bit/group-64 backbone weights and dense mlps.

the `qwen35*` source names and `qwen3_5_text` configuration label describe the
architecture family embedded in the qwen 3.8 artifact; they do not mean this
repository targets qwen 3.5 or 3.6.

the scored worker loads the transformed checkpoint through `llmmodelfactory`,
attaches the selected head, warms legal shapes, and serves rounds through
[`sources/mlxfastmodel/qwen36mtpblocksession.swift`](sources/mlxfastmodel/qwen36mtpblocksession.swift).
a round conceptually:

1. commits the pending primary token;
2. lets candidate policy choose 0...8 drafts within the offered maximum;
3. runs the mtp head chain to propose them;
4. verifies `[primary] + drafts` with the fixed target in a batched forward;
5. accepts the longest correct prefix;
6. keeps, restores, replays, or repairs target and head state as required;
7. reports actual rows and exact top-two evidence to the trusted parent.

the timed target model is the vendored `qwen35textmodel`/`qwen35model` in
[`vendor/mlx-swift-lm/libraries/mlxllm/models/qwen35.swift`](vendor/mlx-swift-lm/libraries/mlxllm/models/qwen35.swift),
together with the editable mtp session. directory-wide editability does not
prove reachability: for example, the separate `qwen35fastengine` path is not
the mtp worker's current target path. trace the call path before optimizing.

### current frontier is already optimized

do not treat historical defaults as current behavior or rediscover mechanisms
already in the promoted snapshot. at this guide's 2026-08-16 rewrite:

- `qwen36mtpblocksession.init` installs an adaptive cost-model policy; the
  nearby k=1 property initializer and constant-depth comments are historical
  fallback/bring-up text, not the instantiated policy;
- the campaign `mtp-head.manifest.json` declares a 4-bit/group-64 proposal
  head rather than using the organizer-pinned bf16 default;
- persistent head history, k/v-only history flush, device-resident drafting,
  exact rollback/replay, and scored-shape warmups are present;
- target verification has exact multi-row affine4 qmv sharing, fused gdn and
  full-attention preparation, and an exact two-stage top-two reducer;
- proposal selection already uses a compact vocabulary and a custom selector;
- the layer loop already carries an `asynceval` ladder for narrow widths.

these are mutable solver facts. recheck source after every frontier sync. see
[`senpai/laguna-to-qwen-speedup-map.md`](senpai/laguna-to-qwen-speedup-map.md)
and the campaign novelty ledger before assigning overlapping work.

## editable and trusted boundary

the ownership rule is:

> candidate code may propose and execute the model, but the trusted parent owns
> correctness, measurement, and final ledger authority.

the editable session necessarily assembles target verify forwards, walks the
candidate-side accepted prefix, rolls state back, and produces evidence for the
parent. none of those self-reports is authoritative: the parent independently
checks the serial token stream, round structure, rows, and accounting.

`benchmark.json` `editablepaths` is the only machine-readable path authority.
its current 89 entries form five practical groups:

- optional `mtp-head.manifest.json` and `mtp-head/` candidate-head content;
- `sources/mlxfastmodel/`, including the runtime and full mtp apparatus;
- `sources/mlxfasttransform/` offline checkpoint transform;
- the exact listed qwen model and `mlxlmcommon` support files;
- the exact listed mlx/metal kernel sources and generated twins.

do not infer that an adjacent file is editable. check the manifest at
`base_sha` before implementation. submission archives use replace semantics
over `editablepaths`; only the two mtp-head paths are optional. a support-file
change outside the surface can make a local tree faster while uploading
nothing useful.

trusted and not submitted include:

- target weights, transform contract, tokenizer, fixtures, and goldens;
- trusted driver, accept audit, row accounting, score emission, and worker
  protocol;
- timing, telemetry, thermal gate, workflows, and `.github/scripts/`;
- `benchmark.json`, `package.swift`, and `package.resolved`;
- tests, docs, local benchmark scripts, and other paths not named by the
  manifest.

`sources/mlxfastmodel/qwen36mtpreferencesession.swift` is an important mixed
case: it is editable through the directory-wide allowance even though it serves
rejected-tail reference replay. it runs after timing and cannot define emitted
tokens. never route its reference work or any golden-derived information into
the timed path, proposal policy, or emitted stream.

the source budget is 3,000,000 bytes total, 524,288 bytes per file, and
262,144 bytes of candidate growth. `mtp-head/` is exempt from the source budget
and instead has a declared digest, byte count, and 2 gib cap. run the campaign
scope and budget checks before expensive validation.

## correctness and prohibited strategies

correctness is a hard gate. every emitted token must equal the hidden serial
trajectory. the round journal must be structurally sound, effective draft
counts must match what occurred, and the reference-checked row ledger must
close. there is no near-tie allowance on this track.

the ranked pipeline also runs the public drift tripwire, hidden teacher-forced
base, anchor, free-run, behavior and gpqa gates, gpqa ttft guardrail, semantic
judge, transform verification, static review, and integrity checks. any failed
gate produces no promotable score.

the native mtp path inside the declared editable surface is the only sanctioned
future-token mechanism. do not add:

- prompt lookup, n-gram, suffix, or token-history drafting;
- same-target lookahead or unsanctioned future-logit/kv computation;
- request-keyed replay caches whose only useful hit is a repeated benchmark
  request;
- cross-request future state, hidden-prompt specialization, or gpqa shortcuts;
- degraded or partial target forwards, unevaluated verify rows, omitted exact
  final-logit/top-two work for emitted rows, or fabricated top-two, row,
  acceptance, or ledger evidence;
- an undeclared auxiliary proposal model/head or candidate-selected target;
- phase, reference-leg, prompt-pool, or baseline detection used to change the
  work performed;
- logical rollback that leaves rejected cache/state rows reachable, or merely
  decrements an offset without trimming or overwriting rejected physical rows;
- timing shortcuts, protocol injection, network access, or filesystem
  exfiltration.

input-independent weight/kernel/shape tables and ordinary within-request cache
reuse are legal when they preserve the contract. a candidate head may improve
proposal quality and cost; it cannot redefine the target answer.

public fixtures were generated on m5. on another apple gpu, compare an
unchanged `base_sha` at the same token position before diagnosing a near-tie
local mismatch. that diagnostic does not relax official exactness.

## kernel and swift rules

the vendored mlx package has two effective source forms:

- a family with an `mlx-generated/*.cpp` twin is jit-compiled from the source
  string embedded in that generated c++ file. update the readable
  `.metal`/`.h` source and the runtime-effective twin together.
- aot-only families are loaded from `mlx.metallib`; rebuild it with
  `tools/build-mlx-metallib.sh` after relevant edits.
- `_nax` variants are selected on ranked m5 hardware and are first-class
  targets, not optional follow-up work.

run `python3 research/twin_audit.py` after jit-source edits. before any kernel
experiment, prove the scored shape, dispatch family, source form, and m5
variant. reduction order, precision, bf16 cast boundaries, packing, recurrent
state, cache layout, and width-dependent dispatch are correctness-sensitive.

for direct swift commands, preserve the frozen dependency graph:

```bash
swift test --force-resolved-versions
mlxfast_run_mlx_runtime_tests=1 swift test --force-resolved-versions
swift build -c release --force-resolved-versions
tools/build-mlx-metallib.sh
```

a bare swift build may write `.build/release` without rebuilding the scored
`.build-worker/release/mlxfast-runtime-worker`. use the qwen benchmark wrapper
for end-to-end work. never run `swift package resolve` or `swift package
update`; if `package.resolved` changes, restore it rather than accepting a new
dependency graph.

## local workflow and measurement

use only the qwen entrypoints:

```bash
./setup.sh && ./setup-qwen-mtp.sh
./benchmark-qwen-mtp.sh --local-iterate
./benchmark-qwen-mtp.sh --local-submit
```

`--local-iterate` defaults to 64 decode tokens and `--local-submit` to 128.
they use one public fixture and cannot reproduce the ranked eight-prompt,
512-token result. treat local scores as directional.

the local serial and candidate legs use the same local build. a general target
runtime or kernel improvement can speed both sides and cancel in the local
ratio, even though it counts officially because the ranked serial numerator
comes from a pinned baseline workspace. compare absolute candidate
seconds/token against a fresh, same-host `base_sha` run as well as the local
serial/mtp ratio.

label every measurement and formula `harness=local` or `harness=ranked`. use
matched absolute candidate mtp time as the primary local signal for broad
runtime and kernel changes; the local ratio may contain same-build cancellation
that does not exist officially.

the aws mac fleet assigns one physical mac to each student. the advisor shares
only edward's mac; alphonse, thorfinn, and askeladd each run on another mac.
locks, device occupancy, and thermal state are host-local. run independent jobs
concurrently across distinct macs and coordinate only co-located roles. verify
the live placement rather than inferring a fleet-wide queue.

run only one model-holding process at a time. the wrapper takes a per-user lock
and checks for orphaned workers; direct model commands do not. inspect reported
pids before killing anything, and never disable the guard to work around a
legitimate concurrent run.

the wrapper cools the gpu before each resident measurement. waiting at the 40c
gate is normal, not a hang, and the gate remains the default. for local timed
arms only, `mlxfast_local_cool_gate=0` is permitted when all arms are
abba-counterbalanced within one session, every arm records entry and exit gpu
temperature with the entry spread reported beside the effect, and the result
retains `cool_gate_passed_real_gate=false` plus
`gate_qualified_for_timing=false`. such evidence is explicitly ungated and
directional; compare it within that counterbalanced session, not as though it
were a gate-qualified or official result. never fabricate a temperature source,
edit the gate constant, or bypass the process lock. keep host, power, thermal
state, memory profile, head provenance, toolchain, token window, and frontier
sha matched between base and candidate. ranked authority is the self-hosted m5
runner labeled `m5-qwen38-27b-mtp`; non-m5 timing is directional only after
confirming the same kernel family executes.

## experiment discipline

one experiment should answer one causal question. for each arm:

1. record an identity tuple: `base_sha`, `upstream_sha`, candidate commit and
   built-worker fingerprint, submitted-surface/twin/metallib digests, head
   digest, host/instance/chip, toolchain, memory and thermal mode, token window,
   fixture/reference source, exact cell/shape/dispatch, and harness.
2. state the official causal path, expected direction, minimum useful effect,
   and stop rule. stress-test any score equation at zero and both signs.
3. prove reachability from the current native-mtp worker and name the cost
   removed. never transfer a constant or fitted model from another tree or cell
   without replay.
4. check editable scope, byte budget, and the ranked score-boundary tripwire.
5. measure an unchanged same-host baseline and save its artifact outside the
   submission surface.
6. implement one mechanism. before long timing, run the cheapest gate capable
   of falsifying the changed boundary. numerical and reduction-order edits need
   actual floating-point touched-cell coverage, a positive control, and the
   shortest end-to-end exact-token/row-ledger run; integer-only or argmax checks
   are insufficient.
7. measure the candidate under the same identity tuple. start with the smallest
   decisive timing run; replicate only when noise could change the decision.
8. compare absolute candidate time, paired ratio, draft/accept telemetry,
   rollback, and block latency where relevant. cost-weight per-cell effects
   before aggregating them.
9. reject, revise, or advance using the predeclared stop rule. label every
   inference, interpolation, and extrapolation; never substitute m=8 for m=9,
   m4 for m5, local for ranked, or an abandoned dispatch table for the current
   tree.
10. record positive, negative, invalid, and ambiguous results in the campaign
    ledger so later agents do not repeat them.

a microbenchmark is useful only when it bounds a named end-to-end cost.
dispatch-count reduction is not a result unless it removes bytes, arithmetic,
or a dependency. a local pass on one easy prompt does not prove an adaptive
policy is robust across the hidden pool. compose only independently measured
winners.

promising current cost centers include draft scheduling, proposal-head cost and
quality, persistent head history, target verify width, gdn recurrence and
rollback, full-attention/gqa cache work, affine4 projection and vocabulary
readout, mlx scheduling/materialization, warmup, and transformed weight
metadata. start from current profiles and the novelty ledger, not this list.

## submission and promotion

yukon manages accounts and official submissions. it does not run the local
pre-submit command for you. in the maintained campaign checkout, use the guard:

```bash
export path="${home}/.local/bin:${path}"
yukon submissions --all
senpai/submit-official.sh "$base_sha" \
  --model "senpai" \
  --note-file submission-note.md
yukon submissions
```

use the exact lowercase campaign attribution `senpai`; record the underlying
models, effort levels, and agent harnesses in the public note body.

immediately before submission, compare the highest-scoring live `promoted` row
with `senpai/frontier-state.json`. if the receipt, source ref, or score differs,
sync the organizer/promoted frontier, replay the candidate, and remeasure it.
do not infer promotion from a `validate submission` commit or a branch name.

submit from a clean, committed candidate after `--local-submit`, exact diff
inspection, scope/budget checks, and generated-twin audit where relevant. never
put credentials in notes, logs, commits, or agent messages. the ranked resource
is serialized; do not dispatch duplicate submissions to mine timing noise.

only the official m5 result can promote a candidate. a green rejected receipt
may simply have failed to beat the current frontier; read its gates and metrics
before classifying it as incorrect.

## historical prior art

laguna work is useful only as a source of hypotheses about shared mlx concepts.
the curated transfer audit is
[`senpai/laguna-to-qwen-speedup-map.md`](senpai/laguna-to-qwen-speedup-map.md).
do not run laguna/dflash setup or benchmark scripts, use their score formulas,
or copy their moe/nvfp4 kernels without a qwen-specific call-path and geometry
proof.
<!-- senpai-campaign-end -->
").contains("one position and leaves no pending future token")
◇ Test prefillFailsWhenMoreThan5PercentSlow() started.
◇ Test ngramSelfSimilarityNormalizesAndValidatesOrders() started.
◇ Test runtimeWorkerStartsTheOrphanReaperBeforeLoadingTheModel() started.
◇ Test benchmarkCorrectnessSupportsZeroStepBaseCaseSkipWithoutSelfCertifying() started.
◇ Test runtimeWorkerClientRejectsNonfiniteTimeoutsBeforeLaunch() started.
◇ Test decodeTimingPlanRejectsInvalidRanges() started.
◇ Test setupUsesOneCanonicalMutationLockForReferenceSymlinkAliases() started.
◇ Test startupMemoryPolicyProtectsDocumented36GiBLocalMachine() started.
◇ Test safetensorsCopySubsetAtomicallyReplacesExistingDestination() started.
◇ Test transformOnlyRefusesEveryMeasuringCombination() started.
◇ Test trustedTracePreservesLargeTopKAndOutOfSubsetExpectedDiagnostics() started.
◇ Test staticReviewFailsClosedOnSelfContradictoryPassedTrueWithHighSeverity() started.
◇ Test qwen36ConfigContractCarriesThePublicConfigAndItsPublishedDigest() started.
◇ Test qwenMTPWorkflowNamesTheTrackManifestAtEveryEditableSurfaceConsumer() started.
◇ Test downloadR2ObjectWithholdsBodyUnlessServerErrorStatus() started.
◇ Test poolsideNVFP4DistributionIdentityIsPinned() started.
◇ Test dflashResidentSetGetsTheDocumentedLowMemoryProfileBelow64GiB() started.
◇ Test qwen35CheckpointValidationInventoryMatchesThePublicHeaders() started.
◇ Test benchmarkScriptHidesPrivateDirectoryFromRuntimeWorker() started.
◇ Test transformBuildsPoolsideLagunaNVFP4RuntimeConfig() started.
◇ Test validateBenchmarkGoldenRejectsMalformedOracle() started.
◇ Test nvfp4ActualSharedExpertQMMShapesCoverDecodeAndSplitKPrefillWhenRuntimeTestsAreEnabled() started.
◇ Test benchmarkPreflightRejectsShortBenchmarkPrompt() started.
◇ Test transformRejectsPartialLagunaInventoryBeforePublishing() started.
◇ Test submissionStaticReviewQwenMTPArmSwapsThePolicyAndStillFailsClosed() started.
✘ Test submissionStaticReviewPromptCoversMeasurementStructureExploitation() recorded an issue at BenchmarkScriptTests.swift:1213:9: Expectation failed: (normalizedRule → "<!-- senpai-campaign-begin -->
# qwen 3.8 native-mtp challenge agent guide

this repository has one ranked task: make qwen 3.8 27b native-mtp decode
faster on apple silicon without changing a single emitted token. the ranked
track is `qwen3.8-27b-mtp-v1`.

retained files from earlier tracks are historical prior art only. their
commands, model geometry, editable surface, correctness rules, scoring, and
runner configuration do not apply here. operate this repository only as the
qwen track named above.

## senpai campaign

for coordinated research in `morganmcg1/qwen38-challenge_senpai`, read
[`senpai/program.md`](senpai/program.md) and
[`senpai/experiment-runbook.md`](senpai/experiment-runbook.md) before assigning
or running experiments. the campaign ledger and exact frontier pins are in
[`senpai/campaign-ledger.md`](senpai/campaign-ledger.md) and
[`senpai/frontier-state.json`](senpai/frontier-state.json).

the intended remotes are:

```text
origin    https://github.com/morganmcg1/qwen38-challenge_senpai.git
upstream  https://github.com/layr-labs/qwen-3.8-mtp-challenge
```

`upstream` must have push url `disabled`. never run `yukon sync`,
`yukon sync --harness-only`, or `yukon reset` in this maintained checkout;
those commands are for plain solver checkouts and can reset campaign history or
repoint `origin`. use the repository-local `sync-organizer-frontier` skill for
organizer policy and promoted-snapshot refreshes.

record the exact campaign `base_sha` and organizer `upstream_sha` for every
experiment. preserve unrelated user work, campaign files, and dirty-worktree
changes. generated weights, caches, score files, downloaded artifacts, and
hidden material never belong in git.

## authority order

this repository still contains historical comments and bring-up prose. resolve
contract questions from the live enforcing sources in this order:

1. [`benchmark.json`](benchmark.json): track identity, commands, editable
   paths, optional paths, budget policy, and scoring fields.
2. [`fixtures/qwen3_8_27b_mtp_track.json`](fixtures/qwen3_8_27b_mtp_track.json):
   artifact pins, timed prompt pool, fidelity semantics, and calibration.
3. [`.github/workflows/qwen-mtp-ranked-benchmark.yml`](.github/workflows/qwen-mtp-ranked-benchmark.yml):
   ranked execution, runner label, and enforced environment values.
4. [`docs/qwen-mtp-editable-surface.md`](docs/qwen-mtp-editable-surface.md):
   rationale for the editable/trusted boundary.
5. [`task.md`](task.md): concise participant-facing summary.

for mutable solver behavior, inspect the current `base_sha` source and
`mtp-head.manifest.json`. narrative descriptions of the original starting tree
do not override the current implementation. if authorities disagree, stop and
identify the exact enforcing site before editing or assigning work.

## mission and score

the official score is a decode-only paired speedup anchored at serial `1.0`:

```text
for each hidden prompt p:
  raw_p = mean(pinned-baseline depth-0 serial seconds/token)
          / mean(candidate seconds/token)

published score = median(raw_1 ... raw_8)
```

the ranked legs come from different workspaces. the trusted, prebuilt baseline
workspace produces the serial numerator; the submitted candidate workspace
produces the mtp denominator. candidate-editable code cannot change the ranked
serial numerator, so any compliant reduction in candidate mtp time improves
the affected `raw_p`. never subtract a local serial-path share such as
`psi_serial` when pricing official value. the local same-build harness has a
different causal graph and must be labeled separately. run
`senpai/verify-ranked-score-boundary.sh` before using a derived score model.

all eight hidden prompts are measured. because eight is even, the median is the
mean of the two central ordered values. there is no no-op normalization and no
prompt lottery. the published median must be at least `0.90` and no greater
than the `5.0` plausibility ceiling.

each ranked leg processes a 512-token seed followed by 512 parent-counted
decode tokens, and the seed work is charged in the timed leg. prefill is not a
separate score, but work removed from the charged seed still improves the
result. the ranked run uses one thermally gated serial/candidate pair per
prompt unless the trusted contract changes.

the trusted parent offers a per-round ceiling. candidate policy may choose
from 0 through 8 drafts independently on every round. drafting nothing is
legal and degenerates to serial decode; it is a control, not the optimization
goal. acceptance rate alone is not the objective: deeper drafting wins only
when extra committed tokens repay head, target-verification, rejected-tail,
and state-management costs across varied prompts.

the organizer's original calibrated tree scored about `0.994`. that is a
historical starting-line measurement, not the current campaign frontier. read
the live yukon promoted row and `senpai/frontier-state.json` before comparing or
submitting work.

## target and current scored path

the fixed target is the organizer's mlx 4-bit affine/group-64 conversion of
qwen 3.8 27b:

```text
backbone  eigenlabs/qwen3.8-27b-4bit
          @ eda45ab47f465d08d6558f0353a2346e2eb9d5b3
upstream  qwen/qwen3.8-27b
          @ 1d4bf0f2ff6012fd82039f2fa52739d0dd7c60c0
```

the organizer-pinned default proposal head is the separate bf16 mtp
artifact. a candidate may instead declare an immutable remote or in-branch head
through `mtp-head.manifest.json`; inspect that file at `base_sha` for current
head provenance. the target checkpoint remains fixed regardless of the head.

the qwen tower is a dense hybrid architecture:

- 64 layers: 48 gated deltanet recurrent layers and 16 full-attention layers;
- hidden size 5,120 and vocabulary 248,320;
- full attention with 24 query heads, 4 kv heads, and head dimension 256;
- affine 4-bit/group-64 backbone weights and dense mlps.

the `qwen35*` source names and `qwen3_5_text` configuration label describe the
architecture family embedded in the qwen 3.8 artifact; they do not mean this
repository targets qwen 3.5 or 3.6.

the scored worker loads the transformed checkpoint through `llmmodelfactory`,
attaches the selected head, warms legal shapes, and serves rounds through
[`sources/mlxfastmodel/qwen36mtpblocksession.swift`](sources/mlxfastmodel/qwen36mtpblocksession.swift).
a round conceptually:

1. commits the pending primary token;
2. lets candidate policy choose 0...8 drafts within the offered maximum;
3. runs the mtp head chain to propose them;
4. verifies `[primary] + drafts` with the fixed target in a batched forward;
5. accepts the longest correct prefix;
6. keeps, restores, replays, or repairs target and head state as required;
7. reports actual rows and exact top-two evidence to the trusted parent.

the timed target model is the vendored `qwen35textmodel`/`qwen35model` in
[`vendor/mlx-swift-lm/libraries/mlxllm/models/qwen35.swift`](vendor/mlx-swift-lm/libraries/mlxllm/models/qwen35.swift),
together with the editable mtp session. directory-wide editability does not
prove reachability: for example, the separate `qwen35fastengine` path is not
the mtp worker's current target path. trace the call path before optimizing.

### current frontier is already optimized

do not treat historical defaults as current behavior or rediscover mechanisms
already in the promoted snapshot. at this guide's 2026-08-16 rewrite:

- `qwen36mtpblocksession.init` installs an adaptive cost-model policy; the
  nearby k=1 property initializer and constant-depth comments are historical
  fallback/bring-up text, not the instantiated policy;
- the campaign `mtp-head.manifest.json` declares a 4-bit/group-64 proposal
  head rather than using the organizer-pinned bf16 default;
- persistent head history, k/v-only history flush, device-resident drafting,
  exact rollback/replay, and scored-shape warmups are present;
- target verification has exact multi-row affine4 qmv sharing, fused gdn and
  full-attention preparation, and an exact two-stage top-two reducer;
- proposal selection already uses a compact vocabulary and a custom selector;
- the layer loop already carries an `asynceval` ladder for narrow widths.

these are mutable solver facts. recheck source after every frontier sync. see
[`senpai/laguna-to-qwen-speedup-map.md`](senpai/laguna-to-qwen-speedup-map.md)
and the campaign novelty ledger before assigning overlapping work.

## editable and trusted boundary

the ownership rule is:

> candidate code may propose and execute the model, but the trusted parent owns
> correctness, measurement, and final ledger authority.

the editable session necessarily assembles target verify forwards, walks the
candidate-side accepted prefix, rolls state back, and produces evidence for the
parent. none of those self-reports is authoritative: the parent independently
checks the serial token stream, round structure, rows, and accounting.

`benchmark.json` `editablepaths` is the only machine-readable path authority.
its current 89 entries form five practical groups:

- optional `mtp-head.manifest.json` and `mtp-head/` candidate-head content;
- `sources/mlxfastmodel/`, including the runtime and full mtp apparatus;
- `sources/mlxfasttransform/` offline checkpoint transform;
- the exact listed qwen model and `mlxlmcommon` support files;
- the exact listed mlx/metal kernel sources and generated twins.

do not infer that an adjacent file is editable. check the manifest at
`base_sha` before implementation. submission archives use replace semantics
over `editablepaths`; only the two mtp-head paths are optional. a support-file
change outside the surface can make a local tree faster while uploading
nothing useful.

trusted and not submitted include:

- target weights, transform contract, tokenizer, fixtures, and goldens;
- trusted driver, accept audit, row accounting, score emission, and worker
  protocol;
- timing, telemetry, thermal gate, workflows, and `.github/scripts/`;
- `benchmark.json`, `package.swift`, and `package.resolved`;
- tests, docs, local benchmark scripts, and other paths not named by the
  manifest.

`sources/mlxfastmodel/qwen36mtpreferencesession.swift` is an important mixed
case: it is editable through the directory-wide allowance even though it serves
rejected-tail reference replay. it runs after timing and cannot define emitted
tokens. never route its reference work or any golden-derived information into
the timed path, proposal policy, or emitted stream.

the source budget is 3,000,000 bytes total, 524,288 bytes per file, and
262,144 bytes of candidate growth. `mtp-head/` is exempt from the source budget
and instead has a declared digest, byte count, and 2 gib cap. run the campaign
scope and budget checks before expensive validation.

## correctness and prohibited strategies

correctness is a hard gate. every emitted token must equal the hidden serial
trajectory. the round journal must be structurally sound, effective draft
counts must match what occurred, and the reference-checked row ledger must
close. there is no near-tie allowance on this track.

the ranked pipeline also runs the public drift tripwire, hidden teacher-forced
base, anchor, free-run, behavior and gpqa gates, gpqa ttft guardrail, semantic
judge, transform verification, static review, and integrity checks. any failed
gate produces no promotable score.

the native mtp path inside the declared editable surface is the only sanctioned
future-token mechanism. do not add:

- prompt lookup, n-gram, suffix, or token-history drafting;
- same-target lookahead or unsanctioned future-logit/kv computation;
- request-keyed replay caches whose only useful hit is a repeated benchmark
  request;
- cross-request future state, hidden-prompt specialization, or gpqa shortcuts;
- degraded or partial target forwards, unevaluated verify rows, omitted exact
  final-logit/top-two work for emitted rows, or fabricated top-two, row,
  acceptance, or ledger evidence;
- an undeclared auxiliary proposal model/head or candidate-selected target;
- phase, reference-leg, prompt-pool, or baseline detection used to change the
  work performed;
- logical rollback that leaves rejected cache/state rows reachable, or merely
  decrements an offset without trimming or overwriting rejected physical rows;
- timing shortcuts, protocol injection, network access, or filesystem
  exfiltration.

input-independent weight/kernel/shape tables and ordinary within-request cache
reuse are legal when they preserve the contract. a candidate head may improve
proposal quality and cost; it cannot redefine the target answer.

public fixtures were generated on m5. on another apple gpu, compare an
unchanged `base_sha` at the same token position before diagnosing a near-tie
local mismatch. that diagnostic does not relax official exactness.

## kernel and swift rules

the vendored mlx package has two effective source forms:

- a family with an `mlx-generated/*.cpp` twin is jit-compiled from the source
  string embedded in that generated c++ file. update the readable
  `.metal`/`.h` source and the runtime-effective twin together.
- aot-only families are loaded from `mlx.metallib`; rebuild it with
  `tools/build-mlx-metallib.sh` after relevant edits.
- `_nax` variants are selected on ranked m5 hardware and are first-class
  targets, not optional follow-up work.

run `python3 research/twin_audit.py` after jit-source edits. before any kernel
experiment, prove the scored shape, dispatch family, source form, and m5
variant. reduction order, precision, bf16 cast boundaries, packing, recurrent
state, cache layout, and width-dependent dispatch are correctness-sensitive.

for direct swift commands, preserve the frozen dependency graph:

```bash
swift test --force-resolved-versions
mlxfast_run_mlx_runtime_tests=1 swift test --force-resolved-versions
swift build -c release --force-resolved-versions
tools/build-mlx-metallib.sh
```

a bare swift build may write `.build/release` without rebuilding the scored
`.build-worker/release/mlxfast-runtime-worker`. use the qwen benchmark wrapper
for end-to-end work. never run `swift package resolve` or `swift package
update`; if `package.resolved` changes, restore it rather than accepting a new
dependency graph.

## local workflow and measurement

use only the qwen entrypoints:

```bash
./setup.sh && ./setup-qwen-mtp.sh
./benchmark-qwen-mtp.sh --local-iterate
./benchmark-qwen-mtp.sh --local-submit
```

`--local-iterate` defaults to 64 decode tokens and `--local-submit` to 128.
they use one public fixture and cannot reproduce the ranked eight-prompt,
512-token result. treat local scores as directional.

the local serial and candidate legs use the same local build. a general target
runtime or kernel improvement can speed both sides and cancel in the local
ratio, even though it counts officially because the ranked serial numerator
comes from a pinned baseline workspace. compare absolute candidate
seconds/token against a fresh, same-host `base_sha` run as well as the local
serial/mtp ratio.

label every measurement and formula `harness=local` or `harness=ranked`. use
matched absolute candidate mtp time as the primary local signal for broad
runtime and kernel changes; the local ratio may contain same-build cancellation
that does not exist officially.

the aws mac fleet assigns one physical mac to each student. the advisor shares
only edward's mac; alphonse, thorfinn, and askeladd each run on another mac.
locks, device occupancy, and thermal state are host-local. run independent jobs
concurrently across distinct macs and coordinate only co-located roles. verify
the live placement rather than inferring a fleet-wide queue.

run only one model-holding process at a time. the wrapper takes a per-user lock
and checks for orphaned workers; direct model commands do not. inspect reported
pids before killing anything, and never disable the guard to work around a
legitimate concurrent run.

the wrapper cools the gpu before each resident measurement. waiting at the 40c
gate is normal, not a hang, and the gate remains the default. for local timed
arms only, `mlxfast_local_cool_gate=0` is permitted when all arms are
abba-counterbalanced within one session, every arm records entry and exit gpu
temperature with the entry spread reported beside the effect, and the result
retains `cool_gate_passed_real_gate=false` plus
`gate_qualified_for_timing=false`. such evidence is explicitly ungated and
directional; compare it within that counterbalanced session, not as though it
were a gate-qualified or official result. never fabricate a temperature source,
edit the gate constant, or bypass the process lock. keep host, power, thermal
state, memory profile, head provenance, toolchain, token window, and frontier
sha matched between base and candidate. ranked authority is the self-hosted m5
runner labeled `m5-qwen38-27b-mtp`; non-m5 timing is directional only after
confirming the same kernel family executes.

## experiment discipline

one experiment should answer one causal question. for each arm:

1. record an identity tuple: `base_sha`, `upstream_sha`, candidate commit and
   built-worker fingerprint, submitted-surface/twin/metallib digests, head
   digest, host/instance/chip, toolchain, memory and thermal mode, token window,
   fixture/reference source, exact cell/shape/dispatch, and harness.
2. state the official causal path, expected direction, minimum useful effect,
   and stop rule. stress-test any score equation at zero and both signs.
3. prove reachability from the current native-mtp worker and name the cost
   removed. never transfer a constant or fitted model from another tree or cell
   without replay.
4. check editable scope, byte budget, and the ranked score-boundary tripwire.
5. measure an unchanged same-host baseline and save its artifact outside the
   submission surface.
6. implement one mechanism. before long timing, run the cheapest gate capable
   of falsifying the changed boundary. numerical and reduction-order edits need
   actual floating-point touched-cell coverage, a positive control, and the
   shortest end-to-end exact-token/row-ledger run; integer-only or argmax checks
   are insufficient.
7. measure the candidate under the same identity tuple. start with the smallest
   decisive timing run; replicate only when noise could change the decision.
8. compare absolute candidate time, paired ratio, draft/accept telemetry,
   rollback, and block latency where relevant. cost-weight per-cell effects
   before aggregating them.
9. reject, revise, or advance using the predeclared stop rule. label every
   inference, interpolation, and extrapolation; never substitute m=8 for m=9,
   m4 for m5, local for ranked, or an abandoned dispatch table for the current
   tree.
10. record positive, negative, invalid, and ambiguous results in the campaign
    ledger so later agents do not repeat them.

a microbenchmark is useful only when it bounds a named end-to-end cost.
dispatch-count reduction is not a result unless it removes bytes, arithmetic,
or a dependency. a local pass on one easy prompt does not prove an adaptive
policy is robust across the hidden pool. compose only independently measured
winners.

promising current cost centers include draft scheduling, proposal-head cost and
quality, persistent head history, target verify width, gdn recurrence and
rollback, full-attention/gqa cache work, affine4 projection and vocabulary
readout, mlx scheduling/materialization, warmup, and transformed weight
metadata. start from current profiles and the novelty ledger, not this list.

## submission and promotion

yukon manages accounts and official submissions. it does not run the local
pre-submit command for you. in the maintained campaign checkout, use the guard:

```bash
export path="${home}/.local/bin:${path}"
yukon submissions --all
senpai/submit-official.sh "$base_sha" \
  --model "senpai" \
  --note-file submission-note.md
yukon submissions
```

use the exact lowercase campaign attribution `senpai`; record the underlying
models, effort levels, and agent harnesses in the public note body.

immediately before submission, compare the highest-scoring live `promoted` row
with `senpai/frontier-state.json`. if the receipt, source ref, or score differs,
sync the organizer/promoted frontier, replay the candidate, and remeasure it.
do not infer promotion from a `validate submission` commit or a branch name.

submit from a clean, committed candidate after `--local-submit`, exact diff
inspection, scope/budget checks, and generated-twin audit where relevant. never
put credentials in notes, logs, commits, or agent messages. the ranked resource
is serialized; do not dispatch duplicate submissions to mine timing noise.

only the official m5 result can promote a candidate. a green rejected receipt
may simply have failed to beat the current frontier; read its gates and metrics
before classifying it as incorrect.

## historical prior art

laguna work is useful only as a source of hypotheses about shared mlx concepts.
the curated transfer audit is
[`senpai/laguna-to-qwen-speedup-map.md`](senpai/laguna-to-qwen-speedup-map.md).
do not run laguna/dflash setup or benchmark scripts, use their score formulas,
or copy their moe/nvfp4 kernels without a qwen-specific call-path and geometry
proof.
<!-- senpai-campaign-end -->
").contains("laguna xs 2.1")
◇ Test runtimeWorkerPinnedConfigurationAcceptsQwen35Architecture() started.
◇ Test cliSupportsPublicBaseGoldenGeneration() started.
◇ Test safetensorsHeaderEqualityIgnoresSourceFileIdentity() started.
◇ Test decodeTimingPlanStartsAfterSeedPrefill() started.
◇ Test gpqaCapturePathsFrameHiddenPromptsAsChatMLTurns() started.
◇ Test startupMemoryPolicyHonorsExplicitProfileRequest() started.
◇ Test aFailedSealedScoreIsWithheldFromStaging() started.
◇ Test runtimeWorkerClientCancelsSuccessfulRequestWatchdogs() started.
◇ Test aCleanSealedScoreIsStillStaged() started.
◇ Test benchmarkWorkflowFailsClosedWhenRunnerPrivateArtifactCleanupFails() started.
◇ Test aPublicGateReportCarryingWorkerOutputIsWithheldOnSubmissionBranches() started.
◇ Test mlxTensorBridgeMapsSafetensorsDTypes() started.
◇ Test benchmarkPreflightHonorsConfiguredWeightsByteLimit() started.
◇ Test failedScorePreservesExplicitPublicMismatchTokensAndRuntimeLabel() started.
◇ Test theInlineGateChecksEveryFieldTheValidationStepChecks() started.
◇ Test transformRejectsUnsupportedIndexShardBeforeCreatingOutput() started.
◇ Test contestantDocsCommandBlocksKeepTheDependencyGraphFrozen() started.
◇ Test transformAtomicallyReplacesExistingOutputAndRemovesStaleFiles() started.
◇ Test qwenMTPWorkflowAllowsConcurrentSubmissionBases() started.
◇ Test localIterateStreamsLiveNumbersDuringTheRun() started.
✘ Test submissionStaticReviewPromptCoversMeasurementStructureExploitation() recorded an issue at BenchmarkScriptTests.swift:1204:9: Expectation failed: (normalizedRule → "<!-- senpai-campaign-begin -->
# qwen 3.8 native-mtp challenge agent guide

this repository has one ranked task: make qwen 3.8 27b native-mtp decode
faster on apple silicon without changing a single emitted token. the ranked
track is `qwen3.8-27b-mtp-v1`.

retained files from earlier tracks are historical prior art only. their
commands, model geometry, editable surface, correctness rules, scoring, and
runner configuration do not apply here. operate this repository only as the
qwen track named above.

## senpai campaign

for coordinated research in `morganmcg1/qwen38-challenge_senpai`, read
[`senpai/program.md`](senpai/program.md) and
[`senpai/experiment-runbook.md`](senpai/experiment-runbook.md) before assigning
or running experiments. the campaign ledger and exact frontier pins are in
[`senpai/campaign-ledger.md`](senpai/campaign-ledger.md) and
[`senpai/frontier-state.json`](senpai/frontier-state.json).

the intended remotes are:

```text
origin    https://github.com/morganmcg1/qwen38-challenge_senpai.git
upstream  https://github.com/layr-labs/qwen-3.8-mtp-challenge
```

`upstream` must have push url `disabled`. never run `yukon sync`,
`yukon sync --harness-only`, or `yukon reset` in this maintained checkout;
those commands are for plain solver checkouts and can reset campaign history or
repoint `origin`. use the repository-local `sync-organizer-frontier` skill for
organizer policy and promoted-snapshot refreshes.

record the exact campaign `base_sha` and organizer `upstream_sha` for every
experiment. preserve unrelated user work, campaign files, and dirty-worktree
changes. generated weights, caches, score files, downloaded artifacts, and
hidden material never belong in git.

## authority order

this repository still contains historical comments and bring-up prose. resolve
contract questions from the live enforcing sources in this order:

1. [`benchmark.json`](benchmark.json): track identity, commands, editable
   paths, optional paths, budget policy, and scoring fields.
2. [`fixtures/qwen3_8_27b_mtp_track.json`](fixtures/qwen3_8_27b_mtp_track.json):
   artifact pins, timed prompt pool, fidelity semantics, and calibration.
3. [`.github/workflows/qwen-mtp-ranked-benchmark.yml`](.github/workflows/qwen-mtp-ranked-benchmark.yml):
   ranked execution, runner label, and enforced environment values.
4. [`docs/qwen-mtp-editable-surface.md`](docs/qwen-mtp-editable-surface.md):
   rationale for the editable/trusted boundary.
5. [`task.md`](task.md): concise participant-facing summary.

for mutable solver behavior, inspect the current `base_sha` source and
`mtp-head.manifest.json`. narrative descriptions of the original starting tree
do not override the current implementation. if authorities disagree, stop and
identify the exact enforcing site before editing or assigning work.

## mission and score

the official score is a decode-only paired speedup anchored at serial `1.0`:

```text
for each hidden prompt p:
  raw_p = mean(pinned-baseline depth-0 serial seconds/token)
          / mean(candidate seconds/token)

published score = median(raw_1 ... raw_8)
```

the ranked legs come from different workspaces. the trusted, prebuilt baseline
workspace produces the serial numerator; the submitted candidate workspace
produces the mtp denominator. candidate-editable code cannot change the ranked
serial numerator, so any compliant reduction in candidate mtp time improves
the affected `raw_p`. never subtract a local serial-path share such as
`psi_serial` when pricing official value. the local same-build harness has a
different causal graph and must be labeled separately. run
`senpai/verify-ranked-score-boundary.sh` before using a derived score model.

all eight hidden prompts are measured. because eight is even, the median is the
mean of the two central ordered values. there is no no-op normalization and no
prompt lottery. the published median must be at least `0.90` and no greater
than the `5.0` plausibility ceiling.

each ranked leg processes a 512-token seed followed by 512 parent-counted
decode tokens, and the seed work is charged in the timed leg. prefill is not a
separate score, but work removed from the charged seed still improves the
result. the ranked run uses one thermally gated serial/candidate pair per
prompt unless the trusted contract changes.

the trusted parent offers a per-round ceiling. candidate policy may choose
from 0 through 8 drafts independently on every round. drafting nothing is
legal and degenerates to serial decode; it is a control, not the optimization
goal. acceptance rate alone is not the objective: deeper drafting wins only
when extra committed tokens repay head, target-verification, rejected-tail,
and state-management costs across varied prompts.

the organizer's original calibrated tree scored about `0.994`. that is a
historical starting-line measurement, not the current campaign frontier. read
the live yukon promoted row and `senpai/frontier-state.json` before comparing or
submitting work.

## target and current scored path

the fixed target is the organizer's mlx 4-bit affine/group-64 conversion of
qwen 3.8 27b:

```text
backbone  eigenlabs/qwen3.8-27b-4bit
          @ eda45ab47f465d08d6558f0353a2346e2eb9d5b3
upstream  qwen/qwen3.8-27b
          @ 1d4bf0f2ff6012fd82039f2fa52739d0dd7c60c0
```

the organizer-pinned default proposal head is the separate bf16 mtp
artifact. a candidate may instead declare an immutable remote or in-branch head
through `mtp-head.manifest.json`; inspect that file at `base_sha` for current
head provenance. the target checkpoint remains fixed regardless of the head.

the qwen tower is a dense hybrid architecture:

- 64 layers: 48 gated deltanet recurrent layers and 16 full-attention layers;
- hidden size 5,120 and vocabulary 248,320;
- full attention with 24 query heads, 4 kv heads, and head dimension 256;
- affine 4-bit/group-64 backbone weights and dense mlps.

the `qwen35*` source names and `qwen3_5_text` configuration label describe the
architecture family embedded in the qwen 3.8 artifact; they do not mean this
repository targets qwen 3.5 or 3.6.

the scored worker loads the transformed checkpoint through `llmmodelfactory`,
attaches the selected head, warms legal shapes, and serves rounds through
[`sources/mlxfastmodel/qwen36mtpblocksession.swift`](sources/mlxfastmodel/qwen36mtpblocksession.swift).
a round conceptually:

1. commits the pending primary token;
2. lets candidate policy choose 0...8 drafts within the offered maximum;
3. runs the mtp head chain to propose them;
4. verifies `[primary] + drafts` with the fixed target in a batched forward;
5. accepts the longest correct prefix;
6. keeps, restores, replays, or repairs target and head state as required;
7. reports actual rows and exact top-two evidence to the trusted parent.

the timed target model is the vendored `qwen35textmodel`/`qwen35model` in
[`vendor/mlx-swift-lm/libraries/mlxllm/models/qwen35.swift`](vendor/mlx-swift-lm/libraries/mlxllm/models/qwen35.swift),
together with the editable mtp session. directory-wide editability does not
prove reachability: for example, the separate `qwen35fastengine` path is not
the mtp worker's current target path. trace the call path before optimizing.

### current frontier is already optimized

do not treat historical defaults as current behavior or rediscover mechanisms
already in the promoted snapshot. at this guide's 2026-08-16 rewrite:

- `qwen36mtpblocksession.init` installs an adaptive cost-model policy; the
  nearby k=1 property initializer and constant-depth comments are historical
  fallback/bring-up text, not the instantiated policy;
- the campaign `mtp-head.manifest.json` declares a 4-bit/group-64 proposal
  head rather than using the organizer-pinned bf16 default;
- persistent head history, k/v-only history flush, device-resident drafting,
  exact rollback/replay, and scored-shape warmups are present;
- target verification has exact multi-row affine4 qmv sharing, fused gdn and
  full-attention preparation, and an exact two-stage top-two reducer;
- proposal selection already uses a compact vocabulary and a custom selector;
- the layer loop already carries an `asynceval` ladder for narrow widths.

these are mutable solver facts. recheck source after every frontier sync. see
[`senpai/laguna-to-qwen-speedup-map.md`](senpai/laguna-to-qwen-speedup-map.md)
and the campaign novelty ledger before assigning overlapping work.

## editable and trusted boundary

the ownership rule is:

> candidate code may propose and execute the model, but the trusted parent owns
> correctness, measurement, and final ledger authority.

the editable session necessarily assembles target verify forwards, walks the
candidate-side accepted prefix, rolls state back, and produces evidence for the
parent. none of those self-reports is authoritative: the parent independently
checks the serial token stream, round structure, rows, and accounting.

`benchmark.json` `editablepaths` is the only machine-readable path authority.
its current 89 entries form five practical groups:

- optional `mtp-head.manifest.json` and `mtp-head/` candidate-head content;
- `sources/mlxfastmodel/`, including the runtime and full mtp apparatus;
- `sources/mlxfasttransform/` offline checkpoint transform;
- the exact listed qwen model and `mlxlmcommon` support files;
- the exact listed mlx/metal kernel sources and generated twins.

do not infer that an adjacent file is editable. check the manifest at
`base_sha` before implementation. submission archives use replace semantics
over `editablepaths`; only the two mtp-head paths are optional. a support-file
change outside the surface can make a local tree faster while uploading
nothing useful.

trusted and not submitted include:

- target weights, transform contract, tokenizer, fixtures, and goldens;
- trusted driver, accept audit, row accounting, score emission, and worker
  protocol;
- timing, telemetry, thermal gate, workflows, and `.github/scripts/`;
- `benchmark.json`, `package.swift`, and `package.resolved`;
- tests, docs, local benchmark scripts, and other paths not named by the
  manifest.

`sources/mlxfastmodel/qwen36mtpreferencesession.swift` is an important mixed
case: it is editable through the directory-wide allowance even though it serves
rejected-tail reference replay. it runs after timing and cannot define emitted
tokens. never route its reference work or any golden-derived information into
the timed path, proposal policy, or emitted stream.

the source budget is 3,000,000 bytes total, 524,288 bytes per file, and
262,144 bytes of candidate growth. `mtp-head/` is exempt from the source budget
and instead has a declared digest, byte count, and 2 gib cap. run the campaign
scope and budget checks before expensive validation.

## correctness and prohibited strategies

correctness is a hard gate. every emitted token must equal the hidden serial
trajectory. the round journal must be structurally sound, effective draft
counts must match what occurred, and the reference-checked row ledger must
close. there is no near-tie allowance on this track.

the ranked pipeline also runs the public drift tripwire, hidden teacher-forced
base, anchor, free-run, behavior and gpqa gates, gpqa ttft guardrail, semantic
judge, transform verification, static review, and integrity checks. any failed
gate produces no promotable score.

the native mtp path inside the declared editable surface is the only sanctioned
future-token mechanism. do not add:

- prompt lookup, n-gram, suffix, or token-history drafting;
- same-target lookahead or unsanctioned future-logit/kv computation;
- request-keyed replay caches whose only useful hit is a repeated benchmark
  request;
- cross-request future state, hidden-prompt specialization, or gpqa shortcuts;
- degraded or partial target forwards, unevaluated verify rows, omitted exact
  final-logit/top-two work for emitted rows, or fabricated top-two, row,
  acceptance, or ledger evidence;
- an undeclared auxiliary proposal model/head or candidate-selected target;
- phase, reference-leg, prompt-pool, or baseline detection used to change the
  work performed;
- logical rollback that leaves rejected cache/state rows reachable, or merely
  decrements an offset without trimming or overwriting rejected physical rows;
- timing shortcuts, protocol injection, network access, or filesystem
  exfiltration.

input-independent weight/kernel/shape tables and ordinary within-request cache
reuse are legal when they preserve the contract. a candidate head may improve
proposal quality and cost; it cannot redefine the target answer.

public fixtures were generated on m5. on another apple gpu, compare an
unchanged `base_sha` at the same token position before diagnosing a near-tie
local mismatch. that diagnostic does not relax official exactness.

## kernel and swift rules

the vendored mlx package has two effective source forms:

- a family with an `mlx-generated/*.cpp` twin is jit-compiled from the source
  string embedded in that generated c++ file. update the readable
  `.metal`/`.h` source and the runtime-effective twin together.
- aot-only families are loaded from `mlx.metallib`; rebuild it with
  `tools/build-mlx-metallib.sh` after relevant edits.
- `_nax` variants are selected on ranked m5 hardware and are first-class
  targets, not optional follow-up work.

run `python3 research/twin_audit.py` after jit-source edits. before any kernel
experiment, prove the scored shape, dispatch family, source form, and m5
variant. reduction order, precision, bf16 cast boundaries, packing, recurrent
state, cache layout, and width-dependent dispatch are correctness-sensitive.

for direct swift commands, preserve the frozen dependency graph:

```bash
swift test --force-resolved-versions
mlxfast_run_mlx_runtime_tests=1 swift test --force-resolved-versions
swift build -c release --force-resolved-versions
tools/build-mlx-metallib.sh
```

a bare swift build may write `.build/release` without rebuilding the scored
`.build-worker/release/mlxfast-runtime-worker`. use the qwen benchmark wrapper
for end-to-end work. never run `swift package resolve` or `swift package
update`; if `package.resolved` changes, restore it rather than accepting a new
dependency graph.

## local workflow and measurement

use only the qwen entrypoints:

```bash
./setup.sh && ./setup-qwen-mtp.sh
./benchmark-qwen-mtp.sh --local-iterate
./benchmark-qwen-mtp.sh --local-submit
```

`--local-iterate` defaults to 64 decode tokens and `--local-submit` to 128.
they use one public fixture and cannot reproduce the ranked eight-prompt,
512-token result. treat local scores as directional.

the local serial and candidate legs use the same local build. a general target
runtime or kernel improvement can speed both sides and cancel in the local
ratio, even though it counts officially because the ranked serial numerator
comes from a pinned baseline workspace. compare absolute candidate
seconds/token against a fresh, same-host `base_sha` run as well as the local
serial/mtp ratio.

label every measurement and formula `harness=local` or `harness=ranked`. use
matched absolute candidate mtp time as the primary local signal for broad
runtime and kernel changes; the local ratio may contain same-build cancellation
that does not exist officially.

the aws mac fleet assigns one physical mac to each student. the advisor shares
only edward's mac; alphonse, thorfinn, and askeladd each run on another mac.
locks, device occupancy, and thermal state are host-local. run independent jobs
concurrently across distinct macs and coordinate only co-located roles. verify
the live placement rather than inferring a fleet-wide queue.

run only one model-holding process at a time. the wrapper takes a per-user lock
and checks for orphaned workers; direct model commands do not. inspect reported
pids before killing anything, and never disable the guard to work around a
legitimate concurrent run.

the wrapper cools the gpu before each resident measurement. waiting at the 40c
gate is normal, not a hang, and the gate remains the default. for local timed
arms only, `mlxfast_local_cool_gate=0` is permitted when all arms are
abba-counterbalanced within one session, every arm records entry and exit gpu
temperature with the entry spread reported beside the effect, and the result
retains `cool_gate_passed_real_gate=false` plus
`gate_qualified_for_timing=false`. such evidence is explicitly ungated and
directional; compare it within that counterbalanced session, not as though it
were a gate-qualified or official result. never fabricate a temperature source,
edit the gate constant, or bypass the process lock. keep host, power, thermal
state, memory profile, head provenance, toolchain, token window, and frontier
sha matched between base and candidate. ranked authority is the self-hosted m5
runner labeled `m5-qwen38-27b-mtp`; non-m5 timing is directional only after
confirming the same kernel family executes.

## experiment discipline

one experiment should answer one causal question. for each arm:

1. record an identity tuple: `base_sha`, `upstream_sha`, candidate commit and
   built-worker fingerprint, submitted-surface/twin/metallib digests, head
   digest, host/instance/chip, toolchain, memory and thermal mode, token window,
   fixture/reference source, exact cell/shape/dispatch, and harness.
2. state the official causal path, expected direction, minimum useful effect,
   and stop rule. stress-test any score equation at zero and both signs.
3. prove reachability from the current native-mtp worker and name the cost
   removed. never transfer a constant or fitted model from another tree or cell
   without replay.
4. check editable scope, byte budget, and the ranked score-boundary tripwire.
5. measure an unchanged same-host baseline and save its artifact outside the
   submission surface.
6. implement one mechanism. before long timing, run the cheapest gate capable
   of falsifying the changed boundary. numerical and reduction-order edits need
   actual floating-point touched-cell coverage, a positive control, and the
   shortest end-to-end exact-token/row-ledger run; integer-only or argmax checks
   are insufficient.
7. measure the candidate under the same identity tuple. start with the smallest
   decisive timing run; replicate only when noise could change the decision.
8. compare absolute candidate time, paired ratio, draft/accept telemetry,
   rollback, and block latency where relevant. cost-weight per-cell effects
   before aggregating them.
9. reject, revise, or advance using the predeclared stop rule. label every
   inference, interpolation, and extrapolation; never substitute m=8 for m=9,
   m4 for m5, local for ranked, or an abandoned dispatch table for the current
   tree.
10. record positive, negative, invalid, and ambiguous results in the campaign
    ledger so later agents do not repeat them.

a microbenchmark is useful only when it bounds a named end-to-end cost.
dispatch-count reduction is not a result unless it removes bytes, arithmetic,
or a dependency. a local pass on one easy prompt does not prove an adaptive
policy is robust across the hidden pool. compose only independently measured
winners.

promising current cost centers include draft scheduling, proposal-head cost and
quality, persistent head history, target verify width, gdn recurrence and
rollback, full-attention/gqa cache work, affine4 projection and vocabulary
readout, mlx scheduling/materialization, warmup, and transformed weight
metadata. start from current profiles and the novelty ledger, not this list.

## submission and promotion

yukon manages accounts and official submissions. it does not run the local
pre-submit command for you. in the maintained campaign checkout, use the guard:

```bash
export path="${home}/.local/bin:${path}"
yukon submissions --all
senpai/submit-official.sh "$base_sha" \
  --model "senpai" \
  --note-file submission-note.md
yukon submissions
```

use the exact lowercase campaign attribution `senpai`; record the underlying
models, effort levels, and agent harnesses in the public note body.

immediately before submission, compare the highest-scoring live `promoted` row
with `senpai/frontier-state.json`. if the receipt, source ref, or score differs,
sync the organizer/promoted frontier, replay the candidate, and remeasure it.
do not infer promotion from a `validate submission` commit or a branch name.

submit from a clean, committed candidate after `--local-submit`, exact diff
inspection, scope/budget checks, and generated-twin audit where relevant. never
put credentials in notes, logs, commits, or agent messages. the ranked resource
is serialized; do not dispatch duplicate submissions to mine timing noise.

only the official m5 result can promote a candidate. a green rejected receipt
may simply have failed to beat the current frontier; read its gates and metrics
before classifying it as incorrect.

## historical prior art

laguna work is useful only as a source of hypotheses about shared mlx concepts.
the curated transfer audit is
[`senpai/laguna-to-qwen-speedup-map.md`](senpai/laguna-to-qwen-speedup-map.md).
do not run laguna/dflash setup or benchmark scripts, use their score formulas,
or copy their moe/nvfp4 kernels without a qwen-specific call-path and geometry
proof.
<!-- senpai-campaign-end -->
").contains("serial non-speculative")
↳ CLAUDE.md is missing the serial-track rule
✘ Test submissionStaticReviewPromptCoversMeasurementStructureExploitation() recorded an issue at BenchmarkScriptTests.swift:1208:9: Expectation failed: (normalizedRule → "<!-- senpai-campaign-begin -->
# qwen 3.8 native-mtp challenge agent guide

this repository has one ranked task: make qwen 3.8 27b native-mtp decode
faster on apple silicon without changing a single emitted token. the ranked
track is `qwen3.8-27b-mtp-v1`.

retained files from earlier tracks are historical prior art only. their
commands, model geometry, editable surface, correctness rules, scoring, and
runner configuration do not apply here. operate this repository only as the
qwen track named above.

## senpai campaign

for coordinated research in `morganmcg1/qwen38-challenge_senpai`, read
[`senpai/program.md`](senpai/program.md) and
[`senpai/experiment-runbook.md`](senpai/experiment-runbook.md) before assigning
or running experiments. the campaign ledger and exact frontier pins are in
[`senpai/campaign-ledger.md`](senpai/campaign-ledger.md) and
[`senpai/frontier-state.json`](senpai/frontier-state.json).

the intended remotes are:

```text
origin    https://github.com/morganmcg1/qwen38-challenge_senpai.git
upstream  https://github.com/layr-labs/qwen-3.8-mtp-challenge
```

`upstream` must have push url `disabled`. never run `yukon sync`,
`yukon sync --harness-only`, or `yukon reset` in this maintained checkout;
those commands are for plain solver checkouts and can reset campaign history or
repoint `origin`. use the repository-local `sync-organizer-frontier` skill for
organizer policy and promoted-snapshot refreshes.

record the exact campaign `base_sha` and organizer `upstream_sha` for every
experiment. preserve unrelated user work, campaign files, and dirty-worktree
changes. generated weights, caches, score files, downloaded artifacts, and
hidden material never belong in git.

## authority order

this repository still contains historical comments and bring-up prose. resolve
contract questions from the live enforcing sources in this order:

1. [`benchmark.json`](benchmark.json): track identity, commands, editable
   paths, optional paths, budget policy, and scoring fields.
2. [`fixtures/qwen3_8_27b_mtp_track.json`](fixtures/qwen3_8_27b_mtp_track.json):
   artifact pins, timed prompt pool, fidelity semantics, and calibration.
3. [`.github/workflows/qwen-mtp-ranked-benchmark.yml`](.github/workflows/qwen-mtp-ranked-benchmark.yml):
   ranked execution, runner label, and enforced environment values.
4. [`docs/qwen-mtp-editable-surface.md`](docs/qwen-mtp-editable-surface.md):
   rationale for the editable/trusted boundary.
5. [`task.md`](task.md): concise participant-facing summary.

for mutable solver behavior, inspect the current `base_sha` source and
`mtp-head.manifest.json`. narrative descriptions of the original starting tree
do not override the current implementation. if authorities disagree, stop and
identify the exact enforcing site before editing or assigning work.

## mission and score

the official score is a decode-only paired speedup anchored at serial `1.0`:

```text
for each hidden prompt p:
  raw_p = mean(pinned-baseline depth-0 serial seconds/token)
          / mean(candidate seconds/token)

published score = median(raw_1 ... raw_8)
```

the ranked legs come from different workspaces. the trusted, prebuilt baseline
workspace produces the serial numerator; the submitted candidate workspace
produces the mtp denominator. candidate-editable code cannot change the ranked
serial numerator, so any compliant reduction in candidate mtp time improves
the affected `raw_p`. never subtract a local serial-path share such as
`psi_serial` when pricing official value. the local same-build harness has a
different causal graph and must be labeled separately. run
`senpai/verify-ranked-score-boundary.sh` before using a derived score model.

all eight hidden prompts are measured. because eight is even, the median is the
mean of the two central ordered values. there is no no-op normalization and no
prompt lottery. the published median must be at least `0.90` and no greater
than the `5.0` plausibility ceiling.

each ranked leg processes a 512-token seed followed by 512 parent-counted
decode tokens, and the seed work is charged in the timed leg. prefill is not a
separate score, but work removed from the charged seed still improves the
result. the ranked run uses one thermally gated serial/candidate pair per
prompt unless the trusted contract changes.

the trusted parent offers a per-round ceiling. candidate policy may choose
from 0 through 8 drafts independently on every round. drafting nothing is
legal and degenerates to serial decode; it is a control, not the optimization
goal. acceptance rate alone is not the objective: deeper drafting wins only
when extra committed tokens repay head, target-verification, rejected-tail,
and state-management costs across varied prompts.

the organizer's original calibrated tree scored about `0.994`. that is a
historical starting-line measurement, not the current campaign frontier. read
the live yukon promoted row and `senpai/frontier-state.json` before comparing or
submitting work.

## target and current scored path

the fixed target is the organizer's mlx 4-bit affine/group-64 conversion of
qwen 3.8 27b:

```text
backbone  eigenlabs/qwen3.8-27b-4bit
          @ eda45ab47f465d08d6558f0353a2346e2eb9d5b3
upstream  qwen/qwen3.8-27b
          @ 1d4bf0f2ff6012fd82039f2fa52739d0dd7c60c0
```

the organizer-pinned default proposal head is the separate bf16 mtp
artifact. a candidate may instead declare an immutable remote or in-branch head
through `mtp-head.manifest.json`; inspect that file at `base_sha` for current
head provenance. the target checkpoint remains fixed regardless of the head.

the qwen tower is a dense hybrid architecture:

- 64 layers: 48 gated deltanet recurrent layers and 16 full-attention layers;
- hidden size 5,120 and vocabulary 248,320;
- full attention with 24 query heads, 4 kv heads, and head dimension 256;
- affine 4-bit/group-64 backbone weights and dense mlps.

the `qwen35*` source names and `qwen3_5_text` configuration label describe the
architecture family embedded in the qwen 3.8 artifact; they do not mean this
repository targets qwen 3.5 or 3.6.

the scored worker loads the transformed checkpoint through `llmmodelfactory`,
attaches the selected head, warms legal shapes, and serves rounds through
[`sources/mlxfastmodel/qwen36mtpblocksession.swift`](sources/mlxfastmodel/qwen36mtpblocksession.swift).
a round conceptually:

1. commits the pending primary token;
2. lets candidate policy choose 0...8 drafts within the offered maximum;
3. runs the mtp head chain to propose them;
4. verifies `[primary] + drafts` with the fixed target in a batched forward;
5. accepts the longest correct prefix;
6. keeps, restores, replays, or repairs target and head state as required;
7. reports actual rows and exact top-two evidence to the trusted parent.

the timed target model is the vendored `qwen35textmodel`/`qwen35model` in
[`vendor/mlx-swift-lm/libraries/mlxllm/models/qwen35.swift`](vendor/mlx-swift-lm/libraries/mlxllm/models/qwen35.swift),
together with the editable mtp session. directory-wide editability does not
prove reachability: for example, the separate `qwen35fastengine` path is not
the mtp worker's current target path. trace the call path before optimizing.

### current frontier is already optimized

do not treat historical defaults as current behavior or rediscover mechanisms
already in the promoted snapshot. at this guide's 2026-08-16 rewrite:

- `qwen36mtpblocksession.init` installs an adaptive cost-model policy; the
  nearby k=1 property initializer and constant-depth comments are historical
  fallback/bring-up text, not the instantiated policy;
- the campaign `mtp-head.manifest.json` declares a 4-bit/group-64 proposal
  head rather than using the organizer-pinned bf16 default;
- persistent head history, k/v-only history flush, device-resident drafting,
  exact rollback/replay, and scored-shape warmups are present;
- target verification has exact multi-row affine4 qmv sharing, fused gdn and
  full-attention preparation, and an exact two-stage top-two reducer;
- proposal selection already uses a compact vocabulary and a custom selector;
- the layer loop already carries an `asynceval` ladder for narrow widths.

these are mutable solver facts. recheck source after every frontier sync. see
[`senpai/laguna-to-qwen-speedup-map.md`](senpai/laguna-to-qwen-speedup-map.md)
and the campaign novelty ledger before assigning overlapping work.

## editable and trusted boundary

the ownership rule is:

> candidate code may propose and execute the model, but the trusted parent owns
> correctness, measurement, and final ledger authority.

the editable session necessarily assembles target verify forwards, walks the
candidate-side accepted prefix, rolls state back, and produces evidence for the
parent. none of those self-reports is authoritative: the parent independently
checks the serial token stream, round structure, rows, and accounting.

`benchmark.json` `editablepaths` is the only machine-readable path authority.
its current 89 entries form five practical groups:

- optional `mtp-head.manifest.json` and `mtp-head/` candidate-head content;
- `sources/mlxfastmodel/`, including the runtime and full mtp apparatus;
- `sources/mlxfasttransform/` offline checkpoint transform;
- the exact listed qwen model and `mlxlmcommon` support files;
- the exact listed mlx/metal kernel sources and generated twins.

do not infer that an adjacent file is editable. check the manifest at
`base_sha` before implementation. submission archives use replace semantics
over `editablepaths`; only the two mtp-head paths are optional. a support-file
change outside the surface can make a local tree faster while uploading
nothing useful.

trusted and not submitted include:

- target weights, transform contract, tokenizer, fixtures, and goldens;
- trusted driver, accept audit, row accounting, score emission, and worker
  protocol;
- timing, telemetry, thermal gate, workflows, and `.github/scripts/`;
- `benchmark.json`, `package.swift`, and `package.resolved`;
- tests, docs, local benchmark scripts, and other paths not named by the
  manifest.

`sources/mlxfastmodel/qwen36mtpreferencesession.swift` is an important mixed
case: it is editable through the directory-wide allowance even though it serves
rejected-tail reference replay. it runs after timing and cannot define emitted
tokens. never route its reference work or any golden-derived information into
the timed path, proposal policy, or emitted stream.

the source budget is 3,000,000 bytes total, 524,288 bytes per file, and
262,144 bytes of candidate growth. `mtp-head/` is exempt from the source budget
and instead has a declared digest, byte count, and 2 gib cap. run the campaign
scope and budget checks before expensive validation.

## correctness and prohibited strategies

correctness is a hard gate. every emitted token must equal the hidden serial
trajectory. the round journal must be structurally sound, effective draft
counts must match what occurred, and the reference-checked row ledger must
close. there is no near-tie allowance on this track.

the ranked pipeline also runs the public drift tripwire, hidden teacher-forced
base, anchor, free-run, behavior and gpqa gates, gpqa ttft guardrail, semantic
judge, transform verification, static review, and integrity checks. any failed
gate produces no promotable score.

the native mtp path inside the declared editable surface is the only sanctioned
future-token mechanism. do not add:

- prompt lookup, n-gram, suffix, or token-history drafting;
- same-target lookahead or unsanctioned future-logit/kv computation;
- request-keyed replay caches whose only useful hit is a repeated benchmark
  request;
- cross-request future state, hidden-prompt specialization, or gpqa shortcuts;
- degraded or partial target forwards, unevaluated verify rows, omitted exact
  final-logit/top-two work for emitted rows, or fabricated top-two, row,
  acceptance, or ledger evidence;
- an undeclared auxiliary proposal model/head or candidate-selected target;
- phase, reference-leg, prompt-pool, or baseline detection used to change the
  work performed;
- logical rollback that leaves rejected cache/state rows reachable, or merely
  decrements an offset without trimming or overwriting rejected physical rows;
- timing shortcuts, protocol injection, network access, or filesystem
  exfiltration.

input-independent weight/kernel/shape tables and ordinary within-request cache
reuse are legal when they preserve the contract. a candidate head may improve
proposal quality and cost; it cannot redefine the target answer.

public fixtures were generated on m5. on another apple gpu, compare an
unchanged `base_sha` at the same token position before diagnosing a near-tie
local mismatch. that diagnostic does not relax official exactness.

## kernel and swift rules

the vendored mlx package has two effective source forms:

- a family with an `mlx-generated/*.cpp` twin is jit-compiled from the source
  string embedded in that generated c++ file. update the readable
  `.metal`/`.h` source and the runtime-effective twin together.
- aot-only families are loaded from `mlx.metallib`; rebuild it with
  `tools/build-mlx-metallib.sh` after relevant edits.
- `_nax` variants are selected on ranked m5 hardware and are first-class
  targets, not optional follow-up work.

run `python3 research/twin_audit.py` after jit-source edits. before any kernel
experiment, prove the scored shape, dispatch family, source form, and m5
variant. reduction order, precision, bf16 cast boundaries, packing, recurrent
state, cache layout, and width-dependent dispatch are correctness-sensitive.

for direct swift commands, preserve the frozen dependency graph:

```bash
swift test --force-resolved-versions
mlxfast_run_mlx_runtime_tests=1 swift test --force-resolved-versions
swift build -c release --force-resolved-versions
tools/build-mlx-metallib.sh
```

a bare swift build may write `.build/release` without rebuilding the scored
`.build-worker/release/mlxfast-runtime-worker`. use the qwen benchmark wrapper
for end-to-end work. never run `swift package resolve` or `swift package
update`; if `package.resolved` changes, restore it rather than accepting a new
dependency graph.

## local workflow and measurement

use only the qwen entrypoints:

```bash
./setup.sh && ./setup-qwen-mtp.sh
./benchmark-qwen-mtp.sh --local-iterate
./benchmark-qwen-mtp.sh --local-submit
```

`--local-iterate` defaults to 64 decode tokens and `--local-submit` to 128.
they use one public fixture and cannot reproduce the ranked eight-prompt,
512-token result. treat local scores as directional.

the local serial and candidate legs use the same local build. a general target
runtime or kernel improvement can speed both sides and cancel in the local
ratio, even though it counts officially because the ranked serial numerator
comes from a pinned baseline workspace. compare absolute candidate
seconds/token against a fresh, same-host `base_sha` run as well as the local
serial/mtp ratio.

label every measurement and formula `harness=local` or `harness=ranked`. use
matched absolute candidate mtp time as the primary local signal for broad
runtime and kernel changes; the local ratio may contain same-build cancellation
that does not exist officially.

the aws mac fleet assigns one physical mac to each student. the advisor shares
only edward's mac; alphonse, thorfinn, and askeladd each run on another mac.
locks, device occupancy, and thermal state are host-local. run independent jobs
concurrently across distinct macs and coordinate only co-located roles. verify
the live placement rather than inferring a fleet-wide queue.

run only one model-holding process at a time. the wrapper takes a per-user lock
and checks for orphaned workers; direct model commands do not. inspect reported
pids before killing anything, and never disable the guard to work around a
legitimate concurrent run.

the wrapper cools the gpu before each resident measurement. waiting at the 40c
gate is normal, not a hang, and the gate remains the default. for local timed
arms only, `mlxfast_local_cool_gate=0` is permitted when all arms are
abba-counterbalanced within one session, every arm records entry and exit gpu
temperature with the entry spread reported beside the effect, and the result
retains `cool_gate_passed_real_gate=false` plus
`gate_qualified_for_timing=false`. such evidence is explicitly ungated and
directional; compare it within that counterbalanced session, not as though it
were a gate-qualified or official result. never fabricate a temperature source,
edit the gate constant, or bypass the process lock. keep host, power, thermal
state, memory profile, head provenance, toolchain, token window, and frontier
sha matched between base and candidate. ranked authority is the self-hosted m5
runner labeled `m5-qwen38-27b-mtp`; non-m5 timing is directional only after
confirming the same kernel family executes.

## experiment discipline

one experiment should answer one causal question. for each arm:

1. record an identity tuple: `base_sha`, `upstream_sha`, candidate commit and
   built-worker fingerprint, submitted-surface/twin/metallib digests, head
   digest, host/instance/chip, toolchain, memory and thermal mode, token window,
   fixture/reference source, exact cell/shape/dispatch, and harness.
2. state the official causal path, expected direction, minimum useful effect,
   and stop rule. stress-test any score equation at zero and both signs.
3. prove reachability from the current native-mtp worker and name the cost
   removed. never transfer a constant or fitted model from another tree or cell
   without replay.
4. check editable scope, byte budget, and the ranked score-boundary tripwire.
5. measure an unchanged same-host baseline and save its artifact outside the
   submission surface.
6. implement one mechanism. before long timing, run the cheapest gate capable
   of falsifying the changed boundary. numerical and reduction-order edits need
   actual floating-point touched-cell coverage, a positive control, and the
   shortest end-to-end exact-token/row-ledger run; integer-only or argmax checks
   are insufficient.
7. measure the candidate under the same identity tuple. start with the smallest
   decisive timing run; replicate only when noise could change the decision.
8. compare absolute candidate time, paired ratio, draft/accept telemetry,
   rollback, and block latency where relevant. cost-weight per-cell effects
   before aggregating them.
9. reject, revise, or advance using the predeclared stop rule. label every
   inference, interpolation, and extrapolation; never substitute m=8 for m=9,
   m4 for m5, local for ranked, or an abandoned dispatch table for the current
   tree.
10. record positive, negative, invalid, and ambiguous results in the campaign
    ledger so later agents do not repeat them.

a microbenchmark is useful only when it bounds a named end-to-end cost.
dispatch-count reduction is not a result unless it removes bytes, arithmetic,
or a dependency. a local pass on one easy prompt does not prove an adaptive
policy is robust across the hidden pool. compose only independently measured
winners.

promising current cost centers include draft scheduling, proposal-head cost and
quality, persistent head history, target verify width, gdn recurrence and
rollback, full-attention/gqa cache work, affine4 projection and vocabulary
readout, mlx scheduling/materialization, warmup, and transformed weight
metadata. start from current profiles and the novelty ledger, not this list.

## submission and promotion

yukon manages accounts and official submissions. it does not run the local
pre-submit command for you. in the maintained campaign checkout, use the guard:

```bash
export path="${home}/.local/bin:${path}"
yukon submissions --all
senpai/submit-official.sh "$base_sha" \
  --model "senpai" \
  --note-file submission-note.md
yukon submissions
```

use the exact lowercase campaign attribution `senpai`; record the underlying
models, effort levels, and agent harnesses in the public note body.

immediately before submission, compare the highest-scoring live `promoted` row
with `senpai/frontier-state.json`. if the receipt, source ref, or score differs,
sync the organizer/promoted frontier, replay the candidate, and remeasure it.
do not infer promotion from a `validate submission` commit or a branch name.

submit from a clean, committed candidate after `--local-submit`, exact diff
inspection, scope/budget checks, and generated-twin audit where relevant. never
put credentials in notes, logs, commits, or agent messages. the ranked resource
is serialized; do not dispatch duplicate submissions to mine timing noise.

only the official m5 result can promote a candidate. a green rejected receipt
may simply have failed to beat the current frontier; read its gates and metrics
before classifying it as incorrect.

## historical prior art

laguna work is useful only as a source of hypotheses about shared mlx concepts.
the curated transfer audit is
[`senpai/laguna-to-qwen-speedup-map.md`](senpai/laguna-to-qwen-speedup-map.md).
do not run laguna/dflash setup or benchmark scripts, use their score formulas,
or copy their moe/nvfp4 kernels without a qwen-specific call-path and geometry
proof.
<!-- senpai-campaign-end -->
").contains("prompt-lookup decoding")
✘ Test submissionStaticReviewPromptCoversMeasurementStructureExploitation() recorded an issue at BenchmarkScriptTests.swift:1210:9: Expectation failed: (normalizedRule → "<!-- senpai-campaign-begin -->
# qwen 3.8 native-mtp challenge agent guide

this repository has one ranked task: make qwen 3.8 27b native-mtp decode
faster on apple silicon without changing a single emitted token. the ranked
track is `qwen3.8-27b-mtp-v1`.

retained files from earlier tracks are historical prior art only. their
commands, model geometry, editable surface, correctness rules, scoring, and
runner configuration do not apply here. operate this repository only as the
qwen track named above.

## senpai campaign

for coordinated research in `morganmcg1/qwen38-challenge_senpai`, read
[`senpai/program.md`](senpai/program.md) and
[`senpai/experiment-runbook.md`](senpai/experiment-runbook.md) before assigning
or running experiments. the campaign ledger and exact frontier pins are in
[`senpai/campaign-ledger.md`](senpai/campaign-ledger.md) and
[`senpai/frontier-state.json`](senpai/frontier-state.json).

the intended remotes are:

```text
origin    https://github.com/morganmcg1/qwen38-challenge_senpai.git
upstream  https://github.com/layr-labs/qwen-3.8-mtp-challenge
```

`upstream` must have push url `disabled`. never run `yukon sync`,
`yukon sync --harness-only`, or `yukon reset` in this maintained checkout;
those commands are for plain solver checkouts and can reset campaign history or
repoint `origin`. use the repository-local `sync-organizer-frontier` skill for
organizer policy and promoted-snapshot refreshes.

record the exact campaign `base_sha` and organizer `upstream_sha` for every
experiment. preserve unrelated user work, campaign files, and dirty-worktree
changes. generated weights, caches, score files, downloaded artifacts, and
hidden material never belong in git.

## authority order

this repository still contains historical comments and bring-up prose. resolve
contract questions from the live enforcing sources in this order:

1. [`benchmark.json`](benchmark.json): track identity, commands, editable
   paths, optional paths, budget policy, and scoring fields.
2. [`fixtures/qwen3_8_27b_mtp_track.json`](fixtures/qwen3_8_27b_mtp_track.json):
   artifact pins, timed prompt pool, fidelity semantics, and calibration.
3. [`.github/workflows/qwen-mtp-ranked-benchmark.yml`](.github/workflows/qwen-mtp-ranked-benchmark.yml):
   ranked execution, runner label, and enforced environment values.
4. [`docs/qwen-mtp-editable-surface.md`](docs/qwen-mtp-editable-surface.md):
   rationale for the editable/trusted boundary.
5. [`task.md`](task.md): concise participant-facing summary.

for mutable solver behavior, inspect the current `base_sha` source and
`mtp-head.manifest.json`. narrative descriptions of the original starting tree
do not override the current implementation. if authorities disagree, stop and
identify the exact enforcing site before editing or assigning work.

## mission and score

the official score is a decode-only paired speedup anchored at serial `1.0`:

```text
for each hidden prompt p:
  raw_p = mean(pinned-baseline depth-0 serial seconds/token)
          / mean(candidate seconds/token)

published score = median(raw_1 ... raw_8)
```

the ranked legs come from different workspaces. the trusted, prebuilt baseline
workspace produces the serial numerator; the submitted candidate workspace
produces the mtp denominator. candidate-editable code cannot change the ranked
serial numerator, so any compliant reduction in candidate mtp time improves
the affected `raw_p`. never subtract a local serial-path share such as
`psi_serial` when pricing official value. the local same-build harness has a
different causal graph and must be labeled separately. run
`senpai/verify-ranked-score-boundary.sh` before using a derived score model.

all eight hidden prompts are measured. because eight is even, the median is the
mean of the two central ordered values. there is no no-op normalization and no
prompt lottery. the published median must be at least `0.90` and no greater
than the `5.0` plausibility ceiling.

each ranked leg processes a 512-token seed followed by 512 parent-counted
decode tokens, and the seed work is charged in the timed leg. prefill is not a
separate score, but work removed from the charged seed still improves the
result. the ranked run uses one thermally gated serial/candidate pair per
prompt unless the trusted contract changes.

the trusted parent offers a per-round ceiling. candidate policy may choose
from 0 through 8 drafts independently on every round. drafting nothing is
legal and degenerates to serial decode; it is a control, not the optimization
goal. acceptance rate alone is not the objective: deeper drafting wins only
when extra committed tokens repay head, target-verification, rejected-tail,
and state-management costs across varied prompts.

the organizer's original calibrated tree scored about `0.994`. that is a
historical starting-line measurement, not the current campaign frontier. read
the live yukon promoted row and `senpai/frontier-state.json` before comparing or
submitting work.

## target and current scored path

the fixed target is the organizer's mlx 4-bit affine/group-64 conversion of
qwen 3.8 27b:

```text
backbone  eigenlabs/qwen3.8-27b-4bit
          @ eda45ab47f465d08d6558f0353a2346e2eb9d5b3
upstream  qwen/qwen3.8-27b
          @ 1d4bf0f2ff6012fd82039f2fa52739d0dd7c60c0
```

the organizer-pinned default proposal head is the separate bf16 mtp
artifact. a candidate may instead declare an immutable remote or in-branch head
through `mtp-head.manifest.json`; inspect that file at `base_sha` for current
head provenance. the target checkpoint remains fixed regardless of the head.

the qwen tower is a dense hybrid architecture:

- 64 layers: 48 gated deltanet recurrent layers and 16 full-attention layers;
- hidden size 5,120 and vocabulary 248,320;
- full attention with 24 query heads, 4 kv heads, and head dimension 256;
- affine 4-bit/group-64 backbone weights and dense mlps.

the `qwen35*` source names and `qwen3_5_text` configuration label describe the
architecture family embedded in the qwen 3.8 artifact; they do not mean this
repository targets qwen 3.5 or 3.6.

the scored worker loads the transformed checkpoint through `llmmodelfactory`,
attaches the selected head, warms legal shapes, and serves rounds through
[`sources/mlxfastmodel/qwen36mtpblocksession.swift`](sources/mlxfastmodel/qwen36mtpblocksession.swift).
a round conceptually:

1. commits the pending primary token;
2. lets candidate policy choose 0...8 drafts within the offered maximum;
3. runs the mtp head chain to propose them;
4. verifies `[primary] + drafts` with the fixed target in a batched forward;
5. accepts the longest correct prefix;
6. keeps, restores, replays, or repairs target and head state as required;
7. reports actual rows and exact top-two evidence to the trusted parent.

the timed target model is the vendored `qwen35textmodel`/`qwen35model` in
[`vendor/mlx-swift-lm/libraries/mlxllm/models/qwen35.swift`](vendor/mlx-swift-lm/libraries/mlxllm/models/qwen35.swift),
together with the editable mtp session. directory-wide editability does not
prove reachability: for example, the separate `qwen35fastengine` path is not
the mtp worker's current target path. trace the call path before optimizing.

### current frontier is already optimized

do not treat historical defaults as current behavior or rediscover mechanisms
already in the promoted snapshot. at this guide's 2026-08-16 rewrite:

- `qwen36mtpblocksession.init` installs an adaptive cost-model policy; the
  nearby k=1 property initializer and constant-depth comments are historical
  fallback/bring-up text, not the instantiated policy;
- the campaign `mtp-head.manifest.json` declares a 4-bit/group-64 proposal
  head rather than using the organizer-pinned bf16 default;
- persistent head history, k/v-only history flush, device-resident drafting,
  exact rollback/replay, and scored-shape warmups are present;
- target verification has exact multi-row affine4 qmv sharing, fused gdn and
  full-attention preparation, and an exact two-stage top-two reducer;
- proposal selection already uses a compact vocabulary and a custom selector;
- the layer loop already carries an `asynceval` ladder for narrow widths.

these are mutable solver facts. recheck source after every frontier sync. see
[`senpai/laguna-to-qwen-speedup-map.md`](senpai/laguna-to-qwen-speedup-map.md)
and the campaign novelty ledger before assigning overlapping work.

## editable and trusted boundary

the ownership rule is:

> candidate code may propose and execute the model, but the trusted parent owns
> correctness, measurement, and final ledger authority.

the editable session necessarily assembles target verify forwards, walks the
candidate-side accepted prefix, rolls state back, and produces evidence for the
parent. none of those self-reports is authoritative: the parent independently
checks the serial token stream, round structure, rows, and accounting.

`benchmark.json` `editablepaths` is the only machine-readable path authority.
its current 89 entries form five practical groups:

- optional `mtp-head.manifest.json` and `mtp-head/` candidate-head content;
- `sources/mlxfastmodel/`, including the runtime and full mtp apparatus;
- `sources/mlxfasttransform/` offline checkpoint transform;
- the exact listed qwen model and `mlxlmcommon` support files;
- the exact listed mlx/metal kernel sources and generated twins.

do not infer that an adjacent file is editable. check the manifest at
`base_sha` before implementation. submission archives use replace semantics
over `editablepaths`; only the two mtp-head paths are optional. a support-file
change outside the surface can make a local tree faster while uploading
nothing useful.

trusted and not submitted include:

- target weights, transform contract, tokenizer, fixtures, and goldens;
- trusted driver, accept audit, row accounting, score emission, and worker
  protocol;
- timing, telemetry, thermal gate, workflows, and `.github/scripts/`;
- `benchmark.json`, `package.swift`, and `package.resolved`;
- tests, docs, local benchmark scripts, and other paths not named by the
  manifest.

`sources/mlxfastmodel/qwen36mtpreferencesession.swift` is an important mixed
case: it is editable through the directory-wide allowance even though it serves
rejected-tail reference replay. it runs after timing and cannot define emitted
tokens. never route its reference work or any golden-derived information into
the timed path, proposal policy, or emitted stream.

the source budget is 3,000,000 bytes total, 524,288 bytes per file, and
262,144 bytes of candidate growth. `mtp-head/` is exempt from the source budget
and instead has a declared digest, byte count, and 2 gib cap. run the campaign
scope and budget checks before expensive validation.

## correctness and prohibited strategies

correctness is a hard gate. every emitted token must equal the hidden serial
trajectory. the round journal must be structurally sound, effective draft
counts must match what occurred, and the reference-checked row ledger must
close. there is no near-tie allowance on this track.

the ranked pipeline also runs the public drift tripwire, hidden teacher-forced
base, anchor, free-run, behavior and gpqa gates, gpqa ttft guardrail, semantic
judge, transform verification, static review, and integrity checks. any failed
gate produces no promotable score.

the native mtp path inside the declared editable surface is the only sanctioned
future-token mechanism. do not add:

- prompt lookup, n-gram, suffix, or token-history drafting;
- same-target lookahead or unsanctioned future-logit/kv computation;
- request-keyed replay caches whose only useful hit is a repeated benchmark
  request;
- cross-request future state, hidden-prompt specialization, or gpqa shortcuts;
- degraded or partial target forwards, unevaluated verify rows, omitted exact
  final-logit/top-two work for emitted rows, or fabricated top-two, row,
  acceptance, or ledger evidence;
- an undeclared auxiliary proposal model/head or candidate-selected target;
- phase, reference-leg, prompt-pool, or baseline detection used to change the
  work performed;
- logical rollback that leaves rejected cache/state rows reachable, or merely
  decrements an offset without trimming or overwriting rejected physical rows;
- timing shortcuts, protocol injection, network access, or filesystem
  exfiltration.

input-independent weight/kernel/shape tables and ordinary within-request cache
reuse are legal when they preserve the contract. a candidate head may improve
proposal quality and cost; it cannot redefine the target answer.

public fixtures were generated on m5. on another apple gpu, compare an
unchanged `base_sha` at the same token position before diagnosing a near-tie
local mismatch. that diagnostic does not relax official exactness.

## kernel and swift rules

the vendored mlx package has two effective source forms:

- a family with an `mlx-generated/*.cpp` twin is jit-compiled from the source
  string embedded in that generated c++ file. update the readable
  `.metal`/`.h` source and the runtime-effective twin together.
- aot-only families are loaded from `mlx.metallib`; rebuild it with
  `tools/build-mlx-metallib.sh` after relevant edits.
- `_nax` variants are selected on ranked m5 hardware and are first-class
  targets, not optional follow-up work.

run `python3 research/twin_audit.py` after jit-source edits. before any kernel
experiment, prove the scored shape, dispatch family, source form, and m5
variant. reduction order, precision, bf16 cast boundaries, packing, recurrent
state, cache layout, and width-dependent dispatch are correctness-sensitive.

for direct swift commands, preserve the frozen dependency graph:

```bash
swift test --force-resolved-versions
mlxfast_run_mlx_runtime_tests=1 swift test --force-resolved-versions
swift build -c release --force-resolved-versions
tools/build-mlx-metallib.sh
```

a bare swift build may write `.build/release` without rebuilding the scored
`.build-worker/release/mlxfast-runtime-worker`. use the qwen benchmark wrapper
for end-to-end work. never run `swift package resolve` or `swift package
update`; if `package.resolved` changes, restore it rather than accepting a new
dependency graph.

## local workflow and measurement

use only the qwen entrypoints:

```bash
./setup.sh && ./setup-qwen-mtp.sh
./benchmark-qwen-mtp.sh --local-iterate
./benchmark-qwen-mtp.sh --local-submit
```

`--local-iterate` defaults to 64 decode tokens and `--local-submit` to 128.
they use one public fixture and cannot reproduce the ranked eight-prompt,
512-token result. treat local scores as directional.

the local serial and candidate legs use the same local build. a general target
runtime or kernel improvement can speed both sides and cancel in the local
ratio, even though it counts officially because the ranked serial numerator
comes from a pinned baseline workspace. compare absolute candidate
seconds/token against a fresh, same-host `base_sha` run as well as the local
serial/mtp ratio.

label every measurement and formula `harness=local` or `harness=ranked`. use
matched absolute candidate mtp time as the primary local signal for broad
runtime and kernel changes; the local ratio may contain same-build cancellation
that does not exist officially.

the aws mac fleet assigns one physical mac to each student. the advisor shares
only edward's mac; alphonse, thorfinn, and askeladd each run on another mac.
locks, device occupancy, and thermal state are host-local. run independent jobs
concurrently across distinct macs and coordinate only co-located roles. verify
the live placement rather than inferring a fleet-wide queue.

run only one model-holding process at a time. the wrapper takes a per-user lock
and checks for orphaned workers; direct model commands do not. inspect reported
pids before killing anything, and never disable the guard to work around a
legitimate concurrent run.

the wrapper cools the gpu before each resident measurement. waiting at the 40c
gate is normal, not a hang, and the gate remains the default. for local timed
arms only, `mlxfast_local_cool_gate=0` is permitted when all arms are
abba-counterbalanced within one session, every arm records entry and exit gpu
temperature with the entry spread reported beside the effect, and the result
retains `cool_gate_passed_real_gate=false` plus
`gate_qualified_for_timing=false`. such evidence is explicitly ungated and
directional; compare it within that counterbalanced session, not as though it
were a gate-qualified or official result. never fabricate a temperature source,
edit the gate constant, or bypass the process lock. keep host, power, thermal
state, memory profile, head provenance, toolchain, token window, and frontier
sha matched between base and candidate. ranked authority is the self-hosted m5
runner labeled `m5-qwen38-27b-mtp`; non-m5 timing is directional only after
confirming the same kernel family executes.

## experiment discipline

one experiment should answer one causal question. for each arm:

1. record an identity tuple: `base_sha`, `upstream_sha`, candidate commit and
   built-worker fingerprint, submitted-surface/twin/metallib digests, head
   digest, host/instance/chip, toolchain, memory and thermal mode, token window,
   fixture/reference source, exact cell/shape/dispatch, and harness.
2. state the official causal path, expected direction, minimum useful effect,
   and stop rule. stress-test any score equation at zero and both signs.
3. prove reachability from the current native-mtp worker and name the cost
   removed. never transfer a constant or fitted model from another tree or cell
   without replay.
4. check editable scope, byte budget, and the ranked score-boundary tripwire.
5. measure an unchanged same-host baseline and save its artifact outside the
   submission surface.
6. implement one mechanism. before long timing, run the cheapest gate capable
   of falsifying the changed boundary. numerical and reduction-order edits need
   actual floating-point touched-cell coverage, a positive control, and the
   shortest end-to-end exact-token/row-ledger run; integer-only or argmax checks
   are insufficient.
7. measure the candidate under the same identity tuple. start with the smallest
   decisive timing run; replicate only when noise could change the decision.
8. compare absolute candidate time, paired ratio, draft/accept telemetry,
   rollback, and block latency where relevant. cost-weight per-cell effects
   before aggregating them.
9. reject, revise, or advance using the predeclared stop rule. label every
   inference, interpolation, and extrapolation; never substitute m=8 for m=9,
   m4 for m5, local for ranked, or an abandoned dispatch table for the current
   tree.
10. record positive, negative, invalid, and ambiguous results in the campaign
    ledger so later agents do not repeat them.

a microbenchmark is useful only when it bounds a named end-to-end cost.
dispatch-count reduction is not a result unless it removes bytes, arithmetic,
or a dependency. a local pass on one easy prompt does not prove an adaptive
policy is robust across the hidden pool. compose only independently measured
winners.

promising current cost centers include draft scheduling, proposal-head cost and
quality, persistent head history, target verify width, gdn recurrence and
rollback, full-attention/gqa cache work, affine4 projection and vocabulary
readout, mlx scheduling/materialization, warmup, and transformed weight
metadata. start from current profiles and the novelty ledger, not this list.

## submission and promotion

yukon manages accounts and official submissions. it does not run the local
pre-submit command for you. in the maintained campaign checkout, use the guard:

```bash
export path="${home}/.local/bin:${path}"
yukon submissions --all
senpai/submit-official.sh "$base_sha" \
  --model "senpai" \
  --note-file submission-note.md
yukon submissions
```

use the exact lowercase campaign attribution `senpai`; record the underlying
models, effort levels, and agent harnesses in the public note body.

immediately before submission, compare the highest-scoring live `promoted` row
with `senpai/frontier-state.json`. if the receipt, source ref, or score differs,
sync the organizer/promoted frontier, replay the candidate, and remeasure it.
do not infer promotion from a `validate submission` commit or a branch name.

submit from a clean, committed candidate after `--local-submit`, exact diff
inspection, scope/budget checks, and generated-twin audit where relevant. never
put credentials in notes, logs, commits, or agent messages. the ranked resource
is serialized; do not dispatch duplicate submissions to mine timing noise.

only the official m5 result can promote a candidate. a green rejected receipt
may simply have failed to beat the current frontier; read its gates and metrics
before classifying it as incorrect.

## historical prior art

laguna work is useful only as a source of hypotheses about shared mlx concepts.
the curated transfer audit is
[`senpai/laguna-to-qwen-speedup-map.md`](senpai/laguna-to-qwen-speedup-map.md).
do not run laguna/dflash setup or benchmark scripts, use their score formulas,
or copy their moe/nvfp4 kernels without a qwen-specific call-path and geometry
proof.
<!-- senpai-campaign-end -->
").contains("deferred cache rows")
✘ Test submissionStaticReviewPromptCoversMeasurementStructureExploitation() recorded an issue at BenchmarkScriptTests.swift:1211:9: Expectation failed: (normalizedRule → "<!-- senpai-campaign-begin -->
# qwen 3.8 native-mtp challenge agent guide

this repository has one ranked task: make qwen 3.8 27b native-mtp decode
faster on apple silicon without changing a single emitted token. the ranked
track is `qwen3.8-27b-mtp-v1`.

retained files from earlier tracks are historical prior art only. their
commands, model geometry, editable surface, correctness rules, scoring, and
runner configuration do not apply here. operate this repository only as the
qwen track named above.

## senpai campaign

for coordinated research in `morganmcg1/qwen38-challenge_senpai`, read
[`senpai/program.md`](senpai/program.md) and
[`senpai/experiment-runbook.md`](senpai/experiment-runbook.md) before assigning
or running experiments. the campaign ledger and exact frontier pins are in
[`senpai/campaign-ledger.md`](senpai/campaign-ledger.md) and
[`senpai/frontier-state.json`](senpai/frontier-state.json).

the intended remotes are:

```text
origin    https://github.com/morganmcg1/qwen38-challenge_senpai.git
upstream  https://github.com/layr-labs/qwen-3.8-mtp-challenge
```

`upstream` must have push url `disabled`. never run `yukon sync`,
`yukon sync --harness-only`, or `yukon reset` in this maintained checkout;
those commands are for plain solver checkouts and can reset campaign history or
repoint `origin`. use the repository-local `sync-organizer-frontier` skill for
organizer policy and promoted-snapshot refreshes.

record the exact campaign `base_sha` and organizer `upstream_sha` for every
experiment. preserve unrelated user work, campaign files, and dirty-worktree
changes. generated weights, caches, score files, downloaded artifacts, and
hidden material never belong in git.

## authority order

this repository still contains historical comments and bring-up prose. resolve
contract questions from the live enforcing sources in this order:

1. [`benchmark.json`](benchmark.json): track identity, commands, editable
   paths, optional paths, budget policy, and scoring fields.
2. [`fixtures/qwen3_8_27b_mtp_track.json`](fixtures/qwen3_8_27b_mtp_track.json):
   artifact pins, timed prompt pool, fidelity semantics, and calibration.
3. [`.github/workflows/qwen-mtp-ranked-benchmark.yml`](.github/workflows/qwen-mtp-ranked-benchmark.yml):
   ranked execution, runner label, and enforced environment values.
4. [`docs/qwen-mtp-editable-surface.md`](docs/qwen-mtp-editable-surface.md):
   rationale for the editable/trusted boundary.
5. [`task.md`](task.md): concise participant-facing summary.

for mutable solver behavior, inspect the current `base_sha` source and
`mtp-head.manifest.json`. narrative descriptions of the original starting tree
do not override the current implementation. if authorities disagree, stop and
identify the exact enforcing site before editing or assigning work.

## mission and score

the official score is a decode-only paired speedup anchored at serial `1.0`:

```text
for each hidden prompt p:
  raw_p = mean(pinned-baseline depth-0 serial seconds/token)
          / mean(candidate seconds/token)

published score = median(raw_1 ... raw_8)
```

the ranked legs come from different workspaces. the trusted, prebuilt baseline
workspace produces the serial numerator; the submitted candidate workspace
produces the mtp denominator. candidate-editable code cannot change the ranked
serial numerator, so any compliant reduction in candidate mtp time improves
the affected `raw_p`. never subtract a local serial-path share such as
`psi_serial` when pricing official value. the local same-build harness has a
different causal graph and must be labeled separately. run
`senpai/verify-ranked-score-boundary.sh` before using a derived score model.

all eight hidden prompts are measured. because eight is even, the median is the
mean of the two central ordered values. there is no no-op normalization and no
prompt lottery. the published median must be at least `0.90` and no greater
than the `5.0` plausibility ceiling.

each ranked leg processes a 512-token seed followed by 512 parent-counted
decode tokens, and the seed work is charged in the timed leg. prefill is not a
separate score, but work removed from the charged seed still improves the
result. the ranked run uses one thermally gated serial/candidate pair per
prompt unless the trusted contract changes.

the trusted parent offers a per-round ceiling. candidate policy may choose
from 0 through 8 drafts independently on every round. drafting nothing is
legal and degenerates to serial decode; it is a control, not the optimization
goal. acceptance rate alone is not the objective: deeper drafting wins only
when extra committed tokens repay head, target-verification, rejected-tail,
and state-management costs across varied prompts.

the organizer's original calibrated tree scored about `0.994`. that is a
historical starting-line measurement, not the current campaign frontier. read
the live yukon promoted row and `senpai/frontier-state.json` before comparing or
submitting work.

## target and current scored path

the fixed target is the organizer's mlx 4-bit affine/group-64 conversion of
qwen 3.8 27b:

```text
backbone  eigenlabs/qwen3.8-27b-4bit
          @ eda45ab47f465d08d6558f0353a2346e2eb9d5b3
upstream  qwen/qwen3.8-27b
          @ 1d4bf0f2ff6012fd82039f2fa52739d0dd7c60c0
```

the organizer-pinned default proposal head is the separate bf16 mtp
artifact. a candidate may instead declare an immutable remote or in-branch head
through `mtp-head.manifest.json`; inspect that file at `base_sha` for current
head provenance. the target checkpoint remains fixed regardless of the head.

the qwen tower is a dense hybrid architecture:

- 64 layers: 48 gated deltanet recurrent layers and 16 full-attention layers;
- hidden size 5,120 and vocabulary 248,320;
- full attention with 24 query heads, 4 kv heads, and head dimension 256;
- affine 4-bit/group-64 backbone weights and dense mlps.

the `qwen35*` source names and `qwen3_5_text` configuration label describe the
architecture family embedded in the qwen 3.8 artifact; they do not mean this
repository targets qwen 3.5 or 3.6.

the scored worker loads the transformed checkpoint through `llmmodelfactory`,
attaches the selected head, warms legal shapes, and serves rounds through
[`sources/mlxfastmodel/qwen36mtpblocksession.swift`](sources/mlxfastmodel/qwen36mtpblocksession.swift).
a round conceptually:

1. commits the pending primary token;
2. lets candidate policy choose 0...8 drafts within the offered maximum;
3. runs the mtp head chain to propose them;
4. verifies `[primary] + drafts` with the fixed target in a batched forward;
5. accepts the longest correct prefix;
6. keeps, restores, replays, or repairs target and head state as required;
7. reports actual rows and exact top-two evidence to the trusted parent.

the timed target model is the vendored `qwen35textmodel`/`qwen35model` in
[`vendor/mlx-swift-lm/libraries/mlxllm/models/qwen35.swift`](vendor/mlx-swift-lm/libraries/mlxllm/models/qwen35.swift),
together with the editable mtp session. directory-wide editability does not
prove reachability: for example, the separate `qwen35fastengine` path is not
the mtp worker's current target path. trace the call path before optimizing.

### current frontier is already optimized

do not treat historical defaults as current behavior or rediscover mechanisms
already in the promoted snapshot. at this guide's 2026-08-16 rewrite:

- `qwen36mtpblocksession.init` installs an adaptive cost-model policy; the
  nearby k=1 property initializer and constant-depth comments are historical
  fallback/bring-up text, not the instantiated policy;
- the campaign `mtp-head.manifest.json` declares a 4-bit/group-64 proposal
  head rather than using the organizer-pinned bf16 default;
- persistent head history, k/v-only history flush, device-resident drafting,
  exact rollback/replay, and scored-shape warmups are present;
- target verification has exact multi-row affine4 qmv sharing, fused gdn and
  full-attention preparation, and an exact two-stage top-two reducer;
- proposal selection already uses a compact vocabulary and a custom selector;
- the layer loop already carries an `asynceval` ladder for narrow widths.

these are mutable solver facts. recheck source after every frontier sync. see
[`senpai/laguna-to-qwen-speedup-map.md`](senpai/laguna-to-qwen-speedup-map.md)
and the campaign novelty ledger before assigning overlapping work.

## editable and trusted boundary

the ownership rule is:

> candidate code may propose and execute the model, but the trusted parent owns
> correctness, measurement, and final ledger authority.

the editable session necessarily assembles target verify forwards, walks the
candidate-side accepted prefix, rolls state back, and produces evidence for the
parent. none of those self-reports is authoritative: the parent independently
checks the serial token stream, round structure, rows, and accounting.

`benchmark.json` `editablepaths` is the only machine-readable path authority.
its current 89 entries form five practical groups:

- optional `mtp-head.manifest.json` and `mtp-head/` candidate-head content;
- `sources/mlxfastmodel/`, including the runtime and full mtp apparatus;
- `sources/mlxfasttransform/` offline checkpoint transform;
- the exact listed qwen model and `mlxlmcommon` support files;
- the exact listed mlx/metal kernel sources and generated twins.

do not infer that an adjacent file is editable. check the manifest at
`base_sha` before implementation. submission archives use replace semantics
over `editablepaths`; only the two mtp-head paths are optional. a support-file
change outside the surface can make a local tree faster while uploading
nothing useful.

trusted and not submitted include:

- target weights, transform contract, tokenizer, fixtures, and goldens;
- trusted driver, accept audit, row accounting, score emission, and worker
  protocol;
- timing, telemetry, thermal gate, workflows, and `.github/scripts/`;
- `benchmark.json`, `package.swift`, and `package.resolved`;
- tests, docs, local benchmark scripts, and other paths not named by the
  manifest.

`sources/mlxfastmodel/qwen36mtpreferencesession.swift` is an important mixed
case: it is editable through the directory-wide allowance even though it serves
rejected-tail reference replay. it runs after timing and cannot define emitted
tokens. never route its reference work or any golden-derived information into
the timed path, proposal policy, or emitted stream.

the source budget is 3,000,000 bytes total, 524,288 bytes per file, and
262,144 bytes of candidate growth. `mtp-head/` is exempt from the source budget
and instead has a declared digest, byte count, and 2 gib cap. run the campaign
scope and budget checks before expensive validation.

## correctness and prohibited strategies

correctness is a hard gate. every emitted token must equal the hidden serial
trajectory. the round journal must be structurally sound, effective draft
counts must match what occurred, and the reference-checked row ledger must
close. there is no near-tie allowance on this track.

the ranked pipeline also runs the public drift tripwire, hidden teacher-forced
base, anchor, free-run, behavior and gpqa gates, gpqa ttft guardrail, semantic
judge, transform verification, static review, and integrity checks. any failed
gate produces no promotable score.

the native mtp path inside the declared editable surface is the only sanctioned
future-token mechanism. do not add:

- prompt lookup, n-gram, suffix, or token-history drafting;
- same-target lookahead or unsanctioned future-logit/kv computation;
- request-keyed replay caches whose only useful hit is a repeated benchmark
  request;
- cross-request future state, hidden-prompt specialization, or gpqa shortcuts;
- degraded or partial target forwards, unevaluated verify rows, omitted exact
  final-logit/top-two work for emitted rows, or fabricated top-two, row,
  acceptance, or ledger evidence;
- an undeclared auxiliary proposal model/head or candidate-selected target;
- phase, reference-leg, prompt-pool, or baseline detection used to change the
  work performed;
- logical rollback that leaves rejected cache/state rows reachable, or merely
  decrements an offset without trimming or overwriting rejected physical rows;
- timing shortcuts, protocol injection, network access, or filesystem
  exfiltration.

input-independent weight/kernel/shape tables and ordinary within-request cache
reuse are legal when they preserve the contract. a candidate head may improve
proposal quality and cost; it cannot redefine the target answer.

public fixtures were generated on m5. on another apple gpu, compare an
unchanged `base_sha` at the same token position before diagnosing a near-tie
local mismatch. that diagnostic does not relax official exactness.

## kernel and swift rules

the vendored mlx package has two effective source forms:

- a family with an `mlx-generated/*.cpp` twin is jit-compiled from the source
  string embedded in that generated c++ file. update the readable
  `.metal`/`.h` source and the runtime-effective twin together.
- aot-only families are loaded from `mlx.metallib`; rebuild it with
  `tools/build-mlx-metallib.sh` after relevant edits.
- `_nax` variants are selected on ranked m5 hardware and are first-class
  targets, not optional follow-up work.

run `python3 research/twin_audit.py` after jit-source edits. before any kernel
experiment, prove the scored shape, dispatch family, source form, and m5
variant. reduction order, precision, bf16 cast boundaries, packing, recurrent
state, cache layout, and width-dependent dispatch are correctness-sensitive.

for direct swift commands, preserve the frozen dependency graph:

```bash
swift test --force-resolved-versions
mlxfast_run_mlx_runtime_tests=1 swift test --force-resolved-versions
swift build -c release --force-resolved-versions
tools/build-mlx-metallib.sh
```

a bare swift build may write `.build/release` without rebuilding the scored
`.build-worker/release/mlxfast-runtime-worker`. use the qwen benchmark wrapper
for end-to-end work. never run `swift package resolve` or `swift package
update`; if `package.resolved` changes, restore it rather than accepting a new
dependency graph.

## local workflow and measurement

use only the qwen entrypoints:

```bash
./setup.sh && ./setup-qwen-mtp.sh
./benchmark-qwen-mtp.sh --local-iterate
./benchmark-qwen-mtp.sh --local-submit
```

`--local-iterate` defaults to 64 decode tokens and `--local-submit` to 128.
they use one public fixture and cannot reproduce the ranked eight-prompt,
512-token result. treat local scores as directional.

the local serial and candidate legs use the same local build. a general target
runtime or kernel improvement can speed both sides and cancel in the local
ratio, even though it counts officially because the ranked serial numerator
comes from a pinned baseline workspace. compare absolute candidate
seconds/token against a fresh, same-host `base_sha` run as well as the local
serial/mtp ratio.

label every measurement and formula `harness=local` or `harness=ranked`. use
matched absolute candidate mtp time as the primary local signal for broad
runtime and kernel changes; the local ratio may contain same-build cancellation
that does not exist officially.

the aws mac fleet assigns one physical mac to each student. the advisor shares
only edward's mac; alphonse, thorfinn, and askeladd each run on another mac.
locks, device occupancy, and thermal state are host-local. run independent jobs
concurrently across distinct macs and coordinate only co-located roles. verify
the live placement rather than inferring a fleet-wide queue.

run only one model-holding process at a time. the wrapper takes a per-user lock
and checks for orphaned workers; direct model commands do not. inspect reported
pids before killing anything, and never disable the guard to work around a
legitimate concurrent run.

the wrapper cools the gpu before each resident measurement. waiting at the 40c
gate is normal, not a hang, and the gate remains the default. for local timed
arms only, `mlxfast_local_cool_gate=0` is permitted when all arms are
abba-counterbalanced within one session, every arm records entry and exit gpu
temperature with the entry spread reported beside the effect, and the result
retains `cool_gate_passed_real_gate=false` plus
`gate_qualified_for_timing=false`. such evidence is explicitly ungated and
directional; compare it within that counterbalanced session, not as though it
were a gate-qualified or official result. never fabricate a temperature source,
edit the gate constant, or bypass the process lock. keep host, power, thermal
state, memory profile, head provenance, toolchain, token window, and frontier
sha matched between base and candidate. ranked authority is the self-hosted m5
runner labeled `m5-qwen38-27b-mtp`; non-m5 timing is directional only after
confirming the same kernel family executes.

## experiment discipline

one experiment should answer one causal question. for each arm:

1. record an identity tuple: `base_sha`, `upstream_sha`, candidate commit and
   built-worker fingerprint, submitted-surface/twin/metallib digests, head
   digest, host/instance/chip, toolchain, memory and thermal mode, token window,
   fixture/reference source, exact cell/shape/dispatch, and harness.
2. state the official causal path, expected direction, minimum useful effect,
   and stop rule. stress-test any score equation at zero and both signs.
3. prove reachability from the current native-mtp worker and name the cost
   removed. never transfer a constant or fitted model from another tree or cell
   without replay.
4. check editable scope, byte budget, and the ranked score-boundary tripwire.
5. measure an unchanged same-host baseline and save its artifact outside the
   submission surface.
6. implement one mechanism. before long timing, run the cheapest gate capable
   of falsifying the changed boundary. numerical and reduction-order edits need
   actual floating-point touched-cell coverage, a positive control, and the
   shortest end-to-end exact-token/row-ledger run; integer-only or argmax checks
   are insufficient.
7. measure the candidate under the same identity tuple. start with the smallest
   decisive timing run; replicate only when noise could change the decision.
8. compare absolute candidate time, paired ratio, draft/accept telemetry,
   rollback, and block latency where relevant. cost-weight per-cell effects
   before aggregating them.
9. reject, revise, or advance using the predeclared stop rule. label every
   inference, interpolation, and extrapolation; never substitute m=8 for m=9,
   m4 for m5, local for ranked, or an abandoned dispatch table for the current
   tree.
10. record positive, negative, invalid, and ambiguous results in the campaign
    ledger so later agents do not repeat them.

a microbenchmark is useful only when it bounds a named end-to-end cost.
dispatch-count reduction is not a result unless it removes bytes, arithmetic,
or a dependency. a local pass on one easy prompt does not prove an adaptive
policy is robust across the hidden pool. compose only independently measured
winners.

promising current cost centers include draft scheduling, proposal-head cost and
quality, persistent head history, target verify width, gdn recurrence and
rollback, full-attention/gqa cache work, affine4 projection and vocabulary
readout, mlx scheduling/materialization, warmup, and transformed weight
metadata. start from current profiles and the novelty ledger, not this list.

## submission and promotion

yukon manages accounts and official submissions. it does not run the local
pre-submit command for you. in the maintained campaign checkout, use the guard:

```bash
export path="${home}/.local/bin:${path}"
yukon submissions --all
senpai/submit-official.sh "$base_sha" \
  --model "senpai" \
  --note-file submission-note.md
yukon submissions
```

use the exact lowercase campaign attribution `senpai`; record the underlying
models, effort levels, and agent harnesses in the public note body.

immediately before submission, compare the highest-scoring live `promoted` row
with `senpai/frontier-state.json`. if the receipt, source ref, or score differs,
sync the organizer/promoted frontier, replay the candidate, and remeasure it.
do not infer promotion from a `validate submission` commit or a branch name.

submit from a clean, committed candidate after `--local-submit`, exact diff
inspection, scope/budget checks, and generated-twin audit where relevant. never
put credentials in notes, logs, commits, or agent messages. the ranked resource
is serialized; do not dispatch duplicate submissions to mine timing noise.

only the official m5 result can promote a candidate. a green rejected receipt
may simply have failed to beat the current frontier; read its gates and metrics
before classifying it as incorrect.

## historical prior art

laguna work is useful only as a source of hypotheses about shared mlx concepts.
the curated transfer audit is
[`senpai/laguna-to-qwen-speedup-map.md`](senpai/laguna-to-qwen-speedup-map.md).
do not run laguna/dflash setup or benchmark scripts, use their score formulas,
or copy their moe/nvfp4 kernels without a qwen-specific call-path and geometry
proof.
<!-- senpai-campaign-end -->
").contains("one position and leaves no pending future token")
✘ Test submissionStaticReviewPromptCoversMeasurementStructureExploitation() recorded an issue at BenchmarkScriptTests.swift:1213:9: Expectation failed: (normalizedRule → "<!-- senpai-campaign-begin -->
# qwen 3.8 native-mtp challenge agent guide

this repository has one ranked task: make qwen 3.8 27b native-mtp decode
faster on apple silicon without changing a single emitted token. the ranked
track is `qwen3.8-27b-mtp-v1`.

retained files from earlier tracks are historical prior art only. their
commands, model geometry, editable surface, correctness rules, scoring, and
runner configuration do not apply here. operate this repository only as the
qwen track named above.

## senpai campaign

for coordinated research in `morganmcg1/qwen38-challenge_senpai`, read
[`senpai/program.md`](senpai/program.md) and
[`senpai/experiment-runbook.md`](senpai/experiment-runbook.md) before assigning
or running experiments. the campaign ledger and exact frontier pins are in
[`senpai/campaign-ledger.md`](senpai/campaign-ledger.md) and
[`senpai/frontier-state.json`](senpai/frontier-state.json).

the intended remotes are:

```text
origin    https://github.com/morganmcg1/qwen38-challenge_senpai.git
upstream  https://github.com/layr-labs/qwen-3.8-mtp-challenge
```

`upstream` must have push url `disabled`. never run `yukon sync`,
`yukon sync --harness-only`, or `yukon reset` in this maintained checkout;
those commands are for plain solver checkouts and can reset campaign history or
repoint `origin`. use the repository-local `sync-organizer-frontier` skill for
organizer policy and promoted-snapshot refreshes.

record the exact campaign `base_sha` and organizer `upstream_sha` for every
experiment. preserve unrelated user work, campaign files, and dirty-worktree
changes. generated weights, caches, score files, downloaded artifacts, and
hidden material never belong in git.

## authority order

this repository still contains historical comments and bring-up prose. resolve
contract questions from the live enforcing sources in this order:

1. [`benchmark.json`](benchmark.json): track identity, commands, editable
   paths, optional paths, budget policy, and scoring fields.
2. [`fixtures/qwen3_8_27b_mtp_track.json`](fixtures/qwen3_8_27b_mtp_track.json):
   artifact pins, timed prompt pool, fidelity semantics, and calibration.
3. [`.github/workflows/qwen-mtp-ranked-benchmark.yml`](.github/workflows/qwen-mtp-ranked-benchmark.yml):
   ranked execution, runner label, and enforced environment values.
4. [`docs/qwen-mtp-editable-surface.md`](docs/qwen-mtp-editable-surface.md):
   rationale for the editable/trusted boundary.
5. [`task.md`](task.md): concise participant-facing summary.

for mutable solver behavior, inspect the current `base_sha` source and
`mtp-head.manifest.json`. narrative descriptions of the original starting tree
do not override the current implementation. if authorities disagree, stop and
identify the exact enforcing site before editing or assigning work.

## mission and score

the official score is a decode-only paired speedup anchored at serial `1.0`:

```text
for each hidden prompt p:
  raw_p = mean(pinned-baseline depth-0 serial seconds/token)
          / mean(candidate seconds/token)

published score = median(raw_1 ... raw_8)
```

the ranked legs come from different workspaces. the trusted, prebuilt baseline
workspace produces the serial numerator; the submitted candidate workspace
produces the mtp denominator. candidate-editable code cannot change the ranked
serial numerator, so any compliant reduction in candidate mtp time improves
the affected `raw_p`. never subtract a local serial-path share such as
`psi_serial` when pricing official value. the local same-build harness has a
different causal graph and must be labeled separately. run
`senpai/verify-ranked-score-boundary.sh` before using a derived score model.

all eight hidden prompts are measured. because eight is even, the median is the
mean of the two central ordered values. there is no no-op normalization and no
prompt lottery. the published median must be at least `0.90` and no greater
than the `5.0` plausibility ceiling.

each ranked leg processes a 512-token seed followed by 512 parent-counted
decode tokens, and the seed work is charged in the timed leg. prefill is not a
separate score, but work removed from the charged seed still improves the
result. the ranked run uses one thermally gated serial/candidate pair per
prompt unless the trusted contract changes.

the trusted parent offers a per-round ceiling. candidate policy may choose
from 0 through 8 drafts independently on every round. drafting nothing is
legal and degenerates to serial decode; it is a control, not the optimization
goal. acceptance rate alone is not the objective: deeper drafting wins only
when extra committed tokens repay head, target-verification, rejected-tail,
and state-management costs across varied prompts.

the organizer's original calibrated tree scored about `0.994`. that is a
historical starting-line measurement, not the current campaign frontier. read
the live yukon promoted row and `senpai/frontier-state.json` before comparing or
submitting work.

## target and current scored path

the fixed target is the organizer's mlx 4-bit affine/group-64 conversion of
qwen 3.8 27b:

```text
backbone  eigenlabs/qwen3.8-27b-4bit
          @ eda45ab47f465d08d6558f0353a2346e2eb9d5b3
upstream  qwen/qwen3.8-27b
          @ 1d4bf0f2ff6012fd82039f2fa52739d0dd7c60c0
```

the organizer-pinned default proposal head is the separate bf16 mtp
artifact. a candidate may instead declare an immutable remote or in-branch head
through `mtp-head.manifest.json`; inspect that file at `base_sha` for current
head provenance. the target checkpoint remains fixed regardless of the head.

the qwen tower is a dense hybrid architecture:

- 64 layers: 48 gated deltanet recurrent layers and 16 full-attention layers;
- hidden size 5,120 and vocabulary 248,320;
- full attention with 24 query heads, 4 kv heads, and head dimension 256;
- affine 4-bit/group-64 backbone weights and dense mlps.

the `qwen35*` source names and `qwen3_5_text` configuration label describe the
architecture family embedded in the qwen 3.8 artifact; they do not mean this
repository targets qwen 3.5 or 3.6.

the scored worker loads the transformed checkpoint through `llmmodelfactory`,
attaches the selected head, warms legal shapes, and serves rounds through
[`sources/mlxfastmodel/qwen36mtpblocksession.swift`](sources/mlxfastmodel/qwen36mtpblocksession.swift).
a round conceptually:

1. commits the pending primary token;
2. lets candidate policy choose 0...8 drafts within the offered maximum;
3. runs the mtp head chain to propose them;
4. verifies `[primary] + drafts` with the fixed target in a batched forward;
5. accepts the longest correct prefix;
6. keeps, restores, replays, or repairs target and head state as required;
7. reports actual rows and exact top-two evidence to the trusted parent.

the timed target model is the vendored `qwen35textmodel`/`qwen35model` in
[`vendor/mlx-swift-lm/libraries/mlxllm/models/qwen35.swift`](vendor/mlx-swift-lm/libraries/mlxllm/models/qwen35.swift),
together with the editable mtp session. directory-wide editability does not
prove reachability: for example, the separate `qwen35fastengine` path is not
the mtp worker's current target path. trace the call path before optimizing.

### current frontier is already optimized

do not treat historical defaults as current behavior or rediscover mechanisms
already in the promoted snapshot. at this guide's 2026-08-16 rewrite:

- `qwen36mtpblocksession.init` installs an adaptive cost-model policy; the
  nearby k=1 property initializer and constant-depth comments are historical
  fallback/bring-up text, not the instantiated policy;
- the campaign `mtp-head.manifest.json` declares a 4-bit/group-64 proposal
  head rather than using the organizer-pinned bf16 default;
- persistent head history, k/v-only history flush, device-resident drafting,
  exact rollback/replay, and scored-shape warmups are present;
- target verification has exact multi-row affine4 qmv sharing, fused gdn and
  full-attention preparation, and an exact two-stage top-two reducer;
- proposal selection already uses a compact vocabulary and a custom selector;
- the layer loop already carries an `asynceval` ladder for narrow widths.

these are mutable solver facts. recheck source after every frontier sync. see
[`senpai/laguna-to-qwen-speedup-map.md`](senpai/laguna-to-qwen-speedup-map.md)
and the campaign novelty ledger before assigning overlapping work.

## editable and trusted boundary

the ownership rule is:

> candidate code may propose and execute the model, but the trusted parent owns
> correctness, measurement, and final ledger authority.

the editable session necessarily assembles target verify forwards, walks the
candidate-side accepted prefix, rolls state back, and produces evidence for the
parent. none of those self-reports is authoritative: the parent independently
checks the serial token stream, round structure, rows, and accounting.

`benchmark.json` `editablepaths` is the only machine-readable path authority.
its current 89 entries form five practical groups:

- optional `mtp-head.manifest.json` and `mtp-head/` candidate-head content;
- `sources/mlxfastmodel/`, including the runtime and full mtp apparatus;
- `sources/mlxfasttransform/` offline checkpoint transform;
- the exact listed qwen model and `mlxlmcommon` support files;
- the exact listed mlx/metal kernel sources and generated twins.

do not infer that an adjacent file is editable. check the manifest at
`base_sha` before implementation. submission archives use replace semantics
over `editablepaths`; only the two mtp-head paths are optional. a support-file
change outside the surface can make a local tree faster while uploading
nothing useful.

trusted and not submitted include:

- target weights, transform contract, tokenizer, fixtures, and goldens;
- trusted driver, accept audit, row accounting, score emission, and worker
  protocol;
- timing, telemetry, thermal gate, workflows, and `.github/scripts/`;
- `benchmark.json`, `package.swift`, and `package.resolved`;
- tests, docs, local benchmark scripts, and other paths not named by the
  manifest.

`sources/mlxfastmodel/qwen36mtpreferencesession.swift` is an important mixed
case: it is editable through the directory-wide allowance even though it serves
rejected-tail reference replay. it runs after timing and cannot define emitted
tokens. never route its reference work or any golden-derived information into
the timed path, proposal policy, or emitted stream.

the source budget is 3,000,000 bytes total, 524,288 bytes per file, and
262,144 bytes of candidate growth. `mtp-head/` is exempt from the source budget
and instead has a declared digest, byte count, and 2 gib cap. run the campaign
scope and budget checks before expensive validation.

## correctness and prohibited strategies

correctness is a hard gate. every emitted token must equal the hidden serial
trajectory. the round journal must be structurally sound, effective draft
counts must match what occurred, and the reference-checked row ledger must
close. there is no near-tie allowance on this track.

the ranked pipeline also runs the public drift tripwire, hidden teacher-forced
base, anchor, free-run, behavior and gpqa gates, gpqa ttft guardrail, semantic
judge, transform verification, static review, and integrity checks. any failed
gate produces no promotable score.

the native mtp path inside the declared editable surface is the only sanctioned
future-token mechanism. do not add:

- prompt lookup, n-gram, suffix, or token-history drafting;
- same-target lookahead or unsanctioned future-logit/kv computation;
- request-keyed replay caches whose only useful hit is a repeated benchmark
  request;
- cross-request future state, hidden-prompt specialization, or gpqa shortcuts;
- degraded or partial target forwards, unevaluated verify rows, omitted exact
  final-logit/top-two work for emitted rows, or fabricated top-two, row,
  acceptance, or ledger evidence;
- an undeclared auxiliary proposal model/head or candidate-selected target;
- phase, reference-leg, prompt-pool, or baseline detection used to change the
  work performed;
- logical rollback that leaves rejected cache/state rows reachable, or merely
  decrements an offset without trimming or overwriting rejected physical rows;
- timing shortcuts, protocol injection, network access, or filesystem
  exfiltration.

input-independent weight/kernel/shape tables and ordinary within-request cache
reuse are legal when they preserve the contract. a candidate head may improve
proposal quality and cost; it cannot redefine the target answer.

public fixtures were generated on m5. on another apple gpu, compare an
unchanged `base_sha` at the same token position before diagnosing a near-tie
local mismatch. that diagnostic does not relax official exactness.

## kernel and swift rules

the vendored mlx package has two effective source forms:

- a family with an `mlx-generated/*.cpp` twin is jit-compiled from the source
  string embedded in that generated c++ file. update the readable
  `.metal`/`.h` source and the runtime-effective twin together.
- aot-only families are loaded from `mlx.metallib`; rebuild it with
  `tools/build-mlx-metallib.sh` after relevant edits.
- `_nax` variants are selected on ranked m5 hardware and are first-class
  targets, not optional follow-up work.

run `python3 research/twin_audit.py` after jit-source edits. before any kernel
experiment, prove the scored shape, dispatch family, source form, and m5
variant. reduction order, precision, bf16 cast boundaries, packing, recurrent
state, cache layout, and width-dependent dispatch are correctness-sensitive.

for direct swift commands, preserve the frozen dependency graph:

```bash
swift test --force-resolved-versions
mlxfast_run_mlx_runtime_tests=1 swift test --force-resolved-versions
swift build -c release --force-resolved-versions
tools/build-mlx-metallib.sh
```

a bare swift build may write `.build/release` without rebuilding the scored
`.build-worker/release/mlxfast-runtime-worker`. use the qwen benchmark wrapper
for end-to-end work. never run `swift package resolve` or `swift package
update`; if `package.resolved` changes, restore it rather than accepting a new
dependency graph.

## local workflow and measurement

use only the qwen entrypoints:

```bash
./setup.sh && ./setup-qwen-mtp.sh
./benchmark-qwen-mtp.sh --local-iterate
./benchmark-qwen-mtp.sh --local-submit
```

`--local-iterate` defaults to 64 decode tokens and `--local-submit` to 128.
they use one public fixture and cannot reproduce the ranked eight-prompt,
512-token result. treat local scores as directional.

the local serial and candidate legs use the same local build. a general target
runtime or kernel improvement can speed both sides and cancel in the local
ratio, even though it counts officially because the ranked serial numerator
comes from a pinned baseline workspace. compare absolute candidate
seconds/token against a fresh, same-host `base_sha` run as well as the local
serial/mtp ratio.

label every measurement and formula `harness=local` or `harness=ranked`. use
matched absolute candidate mtp time as the primary local signal for broad
runtime and kernel changes; the local ratio may contain same-build cancellation
that does not exist officially.

the aws mac fleet assigns one physical mac to each student. the advisor shares
only edward's mac; alphonse, thorfinn, and askeladd each run on another mac.
locks, device occupancy, and thermal state are host-local. run independent jobs
concurrently across distinct macs and coordinate only co-located roles. verify
the live placement rather than inferring a fleet-wide queue.

run only one model-holding process at a time. the wrapper takes a per-user lock
and checks for orphaned workers; direct model commands do not. inspect reported
pids before killing anything, and never disable the guard to work around a
legitimate concurrent run.

the wrapper cools the gpu before each resident measurement. waiting at the 40c
gate is normal, not a hang, and the gate remains the default. for local timed
arms only, `mlxfast_local_cool_gate=0` is permitted when all arms are
abba-counterbalanced within one session, every arm records entry and exit gpu
temperature with the entry spread reported beside the effect, and the result
retains `cool_gate_passed_real_gate=false` plus
`gate_qualified_for_timing=false`. such evidence is explicitly ungated and
directional; compare it within that counterbalanced session, not as though it
were a gate-qualified or official result. never fabricate a temperature source,
edit the gate constant, or bypass the process lock. keep host, power, thermal
state, memory profile, head provenance, toolchain, token window, and frontier
sha matched between base and candidate. ranked authority is the self-hosted m5
runner labeled `m5-qwen38-27b-mtp`; non-m5 timing is directional only after
confirming the same kernel family executes.

## experiment discipline

one experiment should answer one causal question. for each arm:

1. record an identity tuple: `base_sha`, `upstream_sha`, candidate commit and
   built-worker fingerprint, submitted-surface/twin/metallib digests, head
   digest, host/instance/chip, toolchain, memory and thermal mode, token window,
   fixture/reference source, exact cell/shape/dispatch, and harness.
2. state the official causal path, expected direction, minimum useful effect,
   and stop rule. stress-test any score equation at zero and both signs.
3. prove reachability from the current native-mtp worker and name the cost
   removed. never transfer a constant or fitted model from another tree or cell
   without replay.
4. check editable scope, byte budget, and the ranked score-boundary tripwire.
5. measure an unchanged same-host baseline and save its artifact outside the
   submission surface.
6. implement one mechanism. before long timing, run the cheapest gate capable
   of falsifying the changed boundary. numerical and reduction-order edits need
   actual floating-point touched-cell coverage, a positive control, and the
   shortest end-to-end exact-token/row-ledger run; integer-only or argmax checks
   are insufficient.
7. measure the candidate under the same identity tuple. start with the smallest
   decisive timing run; replicate only when noise could change the decision.
8. compare absolute candidate time, paired ratio, draft/accept telemetry,
   rollback, and block latency where relevant. cost-weight per-cell effects
   before aggregating them.
9. reject, revise, or advance using the predeclared stop rule. label every
   inference, interpolation, and extrapolation; never substitute m=8 for m=9,
   m4 for m5, local for ranked, or an abandoned dispatch table for the current
   tree.
10. record positive, negative, invalid, and ambiguous results in the campaign
    ledger so later agents do not repeat them.

a microbenchmark is useful only when it bounds a named end-to-end cost.
dispatch-count reduction is not a result unless it removes bytes, arithmetic,
or a dependency. a local pass on one easy prompt does not prove an adaptive
policy is robust across the hidden pool. compose only independently measured
winners.

promising current cost centers include draft scheduling, proposal-head cost and
quality, persistent head history, target verify width, gdn recurrence and
rollback, full-attention/gqa cache work, affine4 projection and vocabulary
readout, mlx scheduling/materialization, warmup, and transformed weight
metadata. start from current profiles and the novelty ledger, not this list.

## submission and promotion

yukon manages accounts and official submissions. it does not run the local
pre-submit command for you. in the maintained campaign checkout, use the guard:

```bash
export path="${home}/.local/bin:${path}"
yukon submissions --all
senpai/submit-official.sh "$base_sha" \
  --model "senpai" \
  --note-file submission-note.md
yukon submissions
```

use the exact lowercase campaign attribution `senpai`; record the underlying
models, effort levels, and agent harnesses in the public note body.

immediately before submission, compare the highest-scoring live `promoted` row
with `senpai/frontier-state.json`. if the receipt, source ref, or score differs,
sync the organizer/promoted frontier, replay the candidate, and remeasure it.
do not infer promotion from a `validate submission` commit or a branch name.

submit from a clean, committed candidate after `--local-submit`, exact diff
inspection, scope/budget checks, and generated-twin audit where relevant. never
put credentials in notes, logs, commits, or agent messages. the ranked resource
is serialized; do not dispatch duplicate submissions to mine timing noise.

only the official m5 result can promote a candidate. a green rejected receipt
may simply have failed to beat the current frontier; read its gates and metrics
before classifying it as incorrect.

## historical prior art

laguna work is useful only as a source of hypotheses about shared mlx concepts.
the curated transfer audit is
[`senpai/laguna-to-qwen-speedup-map.md`](senpai/laguna-to-qwen-speedup-map.md).
do not run laguna/dflash setup or benchmark scripts, use their score formulas,
or copy their moe/nvfp4 kernels without a qwen-specific call-path and geometry
proof.
<!-- senpai-campaign-end -->
").contains("laguna xs 2.1")
◇ Test qwen35TransformAcceptsTheExactPublicTextTower() started.
◇ Test runtimeWorkerPrivateDescriptorUsesPortableLowerBoundAndCloseOnExec() started.
◇ Test setupRefusesToReleaseMutationLockWithDifferentOwnerToken() started.
◇ Test loadGoldenFixtureRejectsNoopAnchorDelta() started.
◇ Test setupMutationLockWaiterRetriesWhenHolderReleasesDuringPublishAttempt() started.
◇ Test loadGoldenCasesRejectsWrongExpectedTokenCount() started.
◇ Test runtimeWorkerValidatesTransformedWeightsAtStartup() started.
◇ Test setupDownloadContractReportsProgressAndDetectsStalls() started.
◇ Test setupFailedDownloadPreservesPreviouslyPublishedFile() started.
◇ Test runtimeWorkerEnvironmentAllowlistPrefixDoesNotAdmitHarnessNames() started.
◇ Test mlxSwiftLMYaRNAuthorityKeepsDerivedMScale() started.
◇ Test fanBoostVerifiesWritesInspectsAllFansAndTracksRunRestoreState() started.
◇ Test benchmarkPreflightRejectsWeightsAboveDefaultByteLimit() started.
◇ Test checkpointValidatorRejectsForeignSchemasAndEveryInventoryMutation() started.
◇ Test privateArtifactGuardRejectsRenamedGoldenAndPromptFiles() started.
◇ Test setupParallelShardDownloadsEmitProgress() started.
◇ Test transformRejectsSymlinkAliasOfReferenceAsOutput() started.
◇ Test lagunaCorrectnessSelectsGreedyTokenWhenRuntimeTestsAreEnabled() started.
◇ Test firstTokenMismatchReportIncludesNonM5GoldenCaveat() started.
◇ Test benchmarkScriptPrintsNonM5CaveatOnlyForLocalTokenMismatchFailures() started.
◇ Test overlayScriptRejectsDangerousArtifactsAfterCopy() started.
◇ Test loadGoldenCasesRejectsOutOfRangeToken() started.
◇ Test safetensorsRejectsTensorRangePastEndOfFile() started.
◇ Test loadGoldenCasesRejectsNonPositiveRequiredPromptTokens() started.
◇ Test startupMemoryPolicyKeepsRanked128GiBProfile() started.
◇ Test runtimeWorkerProtocolDescriptorAllocationClosesInputWhenOutputFails() started.
◇ Test localRunAbortsWhenAModelHoldingProcessIsAlreadyResident() started.
◇ Test localCorrectnessFailureSurfacesCarryNonM5GoldenCaveat() started.
◇ Test lagunaCorrectnessTeacherForcedUsesGoldenPrefix() started.
◇ Test setupReleasesReferenceCacheMutationLockWithoutOverwritingOperationFailure() started.
◇ Test timedPrefillChargesOneValidatedColdForward() started.
◇ Test localModesForwardWorkerStderrLiveButOfficialRunsDoNot() started.
✘ Test participantDocsExposeDefaultCLIInstallDirectory() recorded an issue at BenchmarkScriptTests.swift:462:9: Expectation failed: (document → "<!-- SENPAI-CAMPAIGN-BEGIN -->
# Qwen 3.8 Native-MTP Challenge Agent Guide

This repository has one ranked task: make Qwen 3.8 27B native-MTP decode
faster on Apple Silicon without changing a single emitted token. The ranked
track is `qwen3.8-27b-mtp-v1`.

Retained files from earlier tracks are historical prior art only. Their
commands, model geometry, editable surface, correctness rules, scoring, and
runner configuration do not apply here. Operate this repository only as the
Qwen track named above.

## Senpai Campaign

For coordinated research in `morganmcg1/qwen38-challenge_senpai`, read
[`senpai/program.md`](senpai/program.md) and
[`senpai/experiment-runbook.md`](senpai/experiment-runbook.md) before assigning
or running experiments. The campaign ledger and exact frontier pins are in
[`senpai/campaign-ledger.md`](senpai/campaign-ledger.md) and
[`senpai/frontier-state.json`](senpai/frontier-state.json).

The intended remotes are:

```text
origin    https://github.com/morganmcg1/qwen38-challenge_senpai.git
upstream  https://github.com/Layr-Labs/qwen-3.8-mtp-challenge
```

`upstream` must have push URL `DISABLED`. Never run `yukon sync`,
`yukon sync --harness-only`, or `yukon reset` in this maintained checkout;
those commands are for plain solver checkouts and can reset campaign history or
repoint `origin`. Use the repository-local `sync-organizer-frontier` skill for
organizer policy and promoted-snapshot refreshes.

Record the exact campaign `BASE_SHA` and organizer `UPSTREAM_SHA` for every
experiment. Preserve unrelated user work, campaign files, and dirty-worktree
changes. Generated weights, caches, score files, downloaded artifacts, and
hidden material never belong in Git.

## Authority Order

This repository still contains historical comments and bring-up prose. Resolve
contract questions from the live enforcing sources in this order:

1. [`benchmark.json`](benchmark.json): track identity, commands, editable
   paths, optional paths, budget policy, and scoring fields.
2. [`fixtures/qwen3_8_27b_mtp_track.json`](fixtures/qwen3_8_27b_mtp_track.json):
   artifact pins, timed prompt pool, fidelity semantics, and calibration.
3. [`.github/workflows/qwen-mtp-ranked-benchmark.yml`](.github/workflows/qwen-mtp-ranked-benchmark.yml):
   ranked execution, runner label, and enforced environment values.
4. [`docs/qwen-mtp-editable-surface.md`](docs/qwen-mtp-editable-surface.md):
   rationale for the editable/trusted boundary.
5. [`TASK.md`](TASK.md): concise participant-facing summary.

For mutable solver behavior, inspect the current `BASE_SHA` source and
`mtp-head.manifest.json`. Narrative descriptions of the original starting tree
do not override the current implementation. If authorities disagree, stop and
identify the exact enforcing site before editing or assigning work.

## Mission And Score

The official score is a decode-only paired speedup anchored at serial `1.0`:

```text
for each hidden prompt p:
  raw_p = mean(pinned-baseline depth-0 serial seconds/token)
          / mean(candidate seconds/token)

published score = median(raw_1 ... raw_8)
```

The ranked legs come from different workspaces. The trusted, prebuilt baseline
workspace produces the serial numerator; the submitted candidate workspace
produces the MTP denominator. Candidate-editable code cannot change the ranked
serial numerator, so any compliant reduction in candidate MTP time improves
the affected `raw_p`. Never subtract a local serial-path share such as
`psi_serial` when pricing official value. The local same-build harness has a
different causal graph and must be labeled separately. Run
`senpai/verify-ranked-score-boundary.sh` before using a derived score model.

All eight hidden prompts are measured. Because eight is even, the median is the
mean of the two central ordered values. There is no no-op normalization and no
prompt lottery. The published median must be at least `0.90` and no greater
than the `5.0` plausibility ceiling.

Each ranked leg processes a 512-token seed followed by 512 parent-counted
decode tokens, and the seed work is charged in the timed leg. Prefill is not a
separate score, but work removed from the charged seed still improves the
result. The ranked run uses one thermally gated serial/candidate pair per
prompt unless the trusted contract changes.

The trusted parent offers a per-round ceiling. Candidate policy may choose
from 0 through 8 drafts independently on every round. Drafting nothing is
legal and degenerates to serial decode; it is a control, not the optimization
goal. Acceptance rate alone is not the objective: deeper drafting wins only
when extra committed tokens repay head, target-verification, rejected-tail,
and state-management costs across varied prompts.

The organizer's original calibrated tree scored about `0.994`. That is a
historical starting-line measurement, not the current campaign frontier. Read
the live Yukon promoted row and `senpai/frontier-state.json` before comparing or
submitting work.

## Target And Current Scored Path

The fixed target is the organizer's MLX 4-bit affine/group-64 conversion of
Qwen 3.8 27B:

```text
backbone  EigenLabs/Qwen3.8-27B-4bit
          @ eda45ab47f465d08d6558f0353a2346e2eb9d5b3
upstream  Qwen/Qwen3.8-27B
          @ 1d4bf0f2ff6012fd82039f2fa52739d0dd7c60c0
```

The organizer-pinned default proposal head is the separate bf16 MTP
artifact. A candidate may instead declare an immutable remote or in-branch head
through `mtp-head.manifest.json`; inspect that file at `BASE_SHA` for current
head provenance. The target checkpoint remains fixed regardless of the head.

The Qwen tower is a dense hybrid architecture:

- 64 layers: 48 Gated DeltaNet recurrent layers and 16 full-attention layers;
- hidden size 5,120 and vocabulary 248,320;
- full attention with 24 query heads, 4 KV heads, and head dimension 256;
- affine 4-bit/group-64 backbone weights and dense MLPs.

The `Qwen35*` source names and `qwen3_5_text` configuration label describe the
architecture family embedded in the Qwen 3.8 artifact; they do not mean this
repository targets Qwen 3.5 or 3.6.

The scored worker loads the transformed checkpoint through `LLMModelFactory`,
attaches the selected head, warms legal shapes, and serves rounds through
[`Sources/MLXFastModel/Qwen36MTPBlockSession.swift`](Sources/MLXFastModel/Qwen36MTPBlockSession.swift).
A round conceptually:

1. commits the pending primary token;
2. lets candidate policy choose 0...8 drafts within the offered maximum;
3. runs the MTP head chain to propose them;
4. verifies `[primary] + drafts` with the fixed target in a batched forward;
5. accepts the longest correct prefix;
6. keeps, restores, replays, or repairs target and head state as required;
7. reports actual rows and exact top-two evidence to the trusted parent.

The timed target model is the vendored `Qwen35TextModel`/`Qwen35Model` in
[`Vendor/mlx-swift-lm/Libraries/MLXLLM/Models/Qwen35.swift`](Vendor/mlx-swift-lm/Libraries/MLXLLM/Models/Qwen35.swift),
together with the editable MTP session. Directory-wide editability does not
prove reachability: for example, the separate `Qwen35FastEngine` path is not
the MTP worker's current target path. Trace the call path before optimizing.

### Current frontier is already optimized

Do not treat historical defaults as current behavior or rediscover mechanisms
already in the promoted snapshot. At this guide's 2026-08-16 rewrite:

- `Qwen36MTPBlockSession.init` installs an adaptive cost-model policy; the
  nearby K=1 property initializer and constant-depth comments are historical
  fallback/bring-up text, not the instantiated policy;
- the campaign `mtp-head.manifest.json` declares a 4-bit/group-64 proposal
  head rather than using the organizer-pinned bf16 default;
- persistent head history, K/V-only history flush, device-resident drafting,
  exact rollback/replay, and scored-shape warmups are present;
- target verification has exact multi-row affine4 QMV sharing, fused GDN and
  full-attention preparation, and an exact two-stage top-two reducer;
- proposal selection already uses a compact vocabulary and a custom selector;
- the layer loop already carries an `asyncEval` ladder for narrow widths.

These are mutable solver facts. Recheck source after every frontier sync. See
[`senpai/laguna-to-qwen-speedup-map.md`](senpai/laguna-to-qwen-speedup-map.md)
and the campaign novelty ledger before assigning overlapping work.

## Editable And Trusted Boundary

The ownership rule is:

> Candidate code may propose and execute the model, but the trusted parent owns
> correctness, measurement, and final ledger authority.

The editable session necessarily assembles target verify forwards, walks the
candidate-side accepted prefix, rolls state back, and produces evidence for the
parent. None of those self-reports is authoritative: the parent independently
checks the serial token stream, round structure, rows, and accounting.

`benchmark.json` `editablePaths` is the only machine-readable path authority.
Its current 89 entries form five practical groups:

- optional `mtp-head.manifest.json` and `mtp-head/` candidate-head content;
- `Sources/MLXFastModel/`, including the runtime and full MTP apparatus;
- `Sources/MLXFastTransform/` offline checkpoint transform;
- the exact listed Qwen model and `MLXLMCommon` support files;
- the exact listed MLX/Metal kernel sources and generated twins.

Do not infer that an adjacent file is editable. Check the manifest at
`BASE_SHA` before implementation. Submission archives use replace semantics
over `editablePaths`; only the two MTP-head paths are optional. A support-file
change outside the surface can make a local tree faster while uploading
nothing useful.

Trusted and not submitted include:

- target weights, transform contract, tokenizer, fixtures, and goldens;
- trusted driver, accept audit, row accounting, score emission, and worker
  protocol;
- timing, telemetry, thermal gate, workflows, and `.github/scripts/`;
- `benchmark.json`, `Package.swift`, and `Package.resolved`;
- tests, docs, local benchmark scripts, and other paths not named by the
  manifest.

`Sources/MLXFastModel/Qwen36MTPReferenceSession.swift` is an important mixed
case: it is editable through the directory-wide allowance even though it serves
rejected-tail reference replay. It runs after timing and cannot define emitted
tokens. Never route its reference work or any golden-derived information into
the timed path, proposal policy, or emitted stream.

The source budget is 3,000,000 bytes total, 524,288 bytes per file, and
262,144 bytes of candidate growth. `mtp-head/` is exempt from the source budget
and instead has a declared digest, byte count, and 2 GiB cap. Run the campaign
scope and budget checks before expensive validation.

## Correctness And Prohibited Strategies

Correctness is a hard gate. Every emitted token must equal the hidden serial
trajectory. The round journal must be structurally sound, effective draft
counts must match what occurred, and the reference-checked row ledger must
close. There is no near-tie allowance on this track.

The ranked pipeline also runs the public drift tripwire, hidden teacher-forced
base, anchor, free-run, behavior and GPQA gates, GPQA TTFT guardrail, semantic
judge, transform verification, static review, and integrity checks. Any failed
gate produces no promotable score.

The native MTP path inside the declared editable surface is the only sanctioned
future-token mechanism. Do not add:

- prompt lookup, n-gram, suffix, or token-history drafting;
- same-target lookahead or unsanctioned future-logit/KV computation;
- request-keyed replay caches whose only useful hit is a repeated benchmark
  request;
- cross-request future state, hidden-prompt specialization, or GPQA shortcuts;
- degraded or partial target forwards, unevaluated verify rows, omitted exact
  final-logit/top-two work for emitted rows, or fabricated top-two, row,
  acceptance, or ledger evidence;
- an undeclared auxiliary proposal model/head or candidate-selected target;
- phase, reference-leg, prompt-pool, or baseline detection used to change the
  work performed;
- logical rollback that leaves rejected cache/state rows reachable, or merely
  decrements an offset without trimming or overwriting rejected physical rows;
- timing shortcuts, protocol injection, network access, or filesystem
  exfiltration.

Input-independent weight/kernel/shape tables and ordinary within-request cache
reuse are legal when they preserve the contract. A candidate head may improve
proposal quality and cost; it cannot redefine the target answer.

Public fixtures were generated on M5. On another Apple GPU, compare an
unchanged `BASE_SHA` at the same token position before diagnosing a near-tie
local mismatch. That diagnostic does not relax official exactness.

## Kernel And Swift Rules

The vendored MLX package has two effective source forms:

- A family with an `mlx-generated/*.cpp` twin is JIT-compiled from the source
  string embedded in that generated C++ file. Update the readable
  `.metal`/`.h` source and the runtime-effective twin together.
- AOT-only families are loaded from `mlx.metallib`; rebuild it with
  `tools/build-mlx-metallib.sh` after relevant edits.
- `_nax` variants are selected on ranked M5 hardware and are first-class
  targets, not optional follow-up work.

Run `python3 research/twin_audit.py` after JIT-source edits. Before any kernel
experiment, prove the scored shape, dispatch family, source form, and M5
variant. Reduction order, precision, BF16 cast boundaries, packing, recurrent
state, cache layout, and width-dependent dispatch are correctness-sensitive.

For direct Swift commands, preserve the frozen dependency graph:

```bash
swift test --force-resolved-versions
MLXFAST_RUN_MLX_RUNTIME_TESTS=1 swift test --force-resolved-versions
swift build -c release --force-resolved-versions
tools/build-mlx-metallib.sh
```

A bare Swift build may write `.build/release` without rebuilding the scored
`.build-worker/release/mlxfast-runtime-worker`. Use the Qwen benchmark wrapper
for end-to-end work. Never run `swift package resolve` or `swift package
update`; if `Package.resolved` changes, restore it rather than accepting a new
dependency graph.

## Local Workflow And Measurement

Use only the Qwen entrypoints:

```bash
./setup.sh && ./setup-qwen-mtp.sh
./benchmark-qwen-mtp.sh --local-iterate
./benchmark-qwen-mtp.sh --local-submit
```

`--local-iterate` defaults to 64 decode tokens and `--local-submit` to 128.
They use one public fixture and cannot reproduce the ranked eight-prompt,
512-token result. Treat local scores as directional.

The local serial and candidate legs use the same local build. A general target
runtime or kernel improvement can speed both sides and cancel in the local
ratio, even though it counts officially because the ranked serial numerator
comes from a pinned baseline workspace. Compare absolute candidate
seconds/token against a fresh, same-host `BASE_SHA` run as well as the local
serial/MTP ratio.

Label every measurement and formula `harness=local` or `harness=ranked`. Use
matched absolute candidate MTP time as the primary local signal for broad
runtime and kernel changes; the local ratio may contain same-build cancellation
that does not exist officially.

The AWS Mac fleet assigns one physical Mac to each student. The advisor shares
only Edward's Mac; Alphonse, Thorfinn, and Askeladd each run on another Mac.
Locks, device occupancy, and thermal state are host-local. Run independent jobs
concurrently across distinct Macs and coordinate only co-located roles. Verify
the live placement rather than inferring a fleet-wide queue.

Run only one model-holding process at a time. The wrapper takes a per-user lock
and checks for orphaned workers; direct model commands do not. Inspect reported
PIDs before killing anything, and never disable the guard to work around a
legitimate concurrent run.

The wrapper cools the GPU before each resident measurement. Waiting at the 40C
gate is normal, not a hang, and the gate remains the default. For local timed
arms only, `MLXFAST_LOCAL_COOL_GATE=0` is permitted when all arms are
ABBA-counterbalanced within one session, every arm records entry and exit GPU
temperature with the entry spread reported beside the effect, and the result
retains `cool_gate_passed_real_gate=false` plus
`gate_qualified_for_timing=false`. Such evidence is explicitly ungated and
directional; compare it within that counterbalanced session, not as though it
were a gate-qualified or official result. Never fabricate a temperature source,
edit the gate constant, or bypass the process lock. Keep host, power, thermal
state, memory profile, head provenance, toolchain, token window, and frontier
SHA matched between base and candidate. Ranked authority is the self-hosted M5
runner labeled `m5-qwen38-27b-mtp`; non-M5 timing is directional only after
confirming the same kernel family executes.

## Experiment Discipline

One experiment should answer one causal question. For each arm:

1. Record an identity tuple: `BASE_SHA`, `UPSTREAM_SHA`, candidate commit and
   built-worker fingerprint, submitted-surface/twin/metallib digests, head
   digest, host/instance/chip, toolchain, memory and thermal mode, token window,
   fixture/reference source, exact cell/shape/dispatch, and harness.
2. State the official causal path, expected direction, minimum useful effect,
   and stop rule. Stress-test any score equation at zero and both signs.
3. Prove reachability from the current native-MTP worker and name the cost
   removed. Never transfer a constant or fitted model from another tree or cell
   without replay.
4. Check editable scope, byte budget, and the ranked score-boundary tripwire.
5. Measure an unchanged same-host baseline and save its artifact outside the
   submission surface.
6. Implement one mechanism. Before long timing, run the cheapest gate capable
   of falsifying the changed boundary. Numerical and reduction-order edits need
   actual floating-point touched-cell coverage, a positive control, and the
   shortest end-to-end exact-token/row-ledger run; integer-only or argmax checks
   are insufficient.
7. Measure the candidate under the same identity tuple. Start with the smallest
   decisive timing run; replicate only when noise could change the decision.
8. Compare absolute candidate time, paired ratio, draft/accept telemetry,
   rollback, and block latency where relevant. Cost-weight per-cell effects
   before aggregating them.
9. Reject, revise, or advance using the predeclared stop rule. Label every
   inference, interpolation, and extrapolation; never substitute M=8 for M=9,
   M4 for M5, local for ranked, or an abandoned dispatch table for the current
   tree.
10. Record positive, negative, invalid, and ambiguous results in the campaign
    ledger so later agents do not repeat them.

A microbenchmark is useful only when it bounds a named end-to-end cost.
Dispatch-count reduction is not a result unless it removes bytes, arithmetic,
or a dependency. A local pass on one easy prompt does not prove an adaptive
policy is robust across the hidden pool. Compose only independently measured
winners.

Promising current cost centers include draft scheduling, proposal-head cost and
quality, persistent head history, target verify width, GDN recurrence and
rollback, full-attention/GQA cache work, affine4 projection and vocabulary
readout, MLX scheduling/materialization, warmup, and transformed weight
metadata. Start from current profiles and the novelty ledger, not this list.

## Submission And Promotion

Yukon manages accounts and official submissions. It does not run the local
pre-submit command for you. In the maintained campaign checkout, use the guard:

```bash
export PATH="${HOME}/.local/bin:${PATH}"
yukon submissions --all
senpai/submit-official.sh "$BASE_SHA" \
  --model "senpai" \
  --note-file submission-note.md
yukon submissions
```

Use the exact lowercase campaign attribution `senpai`; record the underlying
models, effort levels, and agent harnesses in the public note body.

Immediately before submission, compare the highest-scoring live `promoted` row
with `senpai/frontier-state.json`. If the receipt, source ref, or score differs,
sync the organizer/promoted frontier, replay the candidate, and remeasure it.
Do not infer promotion from a `Validate submission` commit or a branch name.

Submit from a clean, committed candidate after `--local-submit`, exact diff
inspection, scope/budget checks, and generated-twin audit where relevant. Never
put credentials in notes, logs, commits, or agent messages. The ranked resource
is serialized; do not dispatch duplicate submissions to mine timing noise.

Only the official M5 result can promote a candidate. A green rejected receipt
may simply have failed to beat the current frontier; read its gates and metrics
before classifying it as incorrect.

## Historical Prior Art

Laguna work is useful only as a source of hypotheses about shared MLX concepts.
The curated transfer audit is
[`senpai/laguna-to-qwen-speedup-map.md`](senpai/laguna-to-qwen-speedup-map.md).
Do not run Laguna/DFlash setup or benchmark scripts, use their score formulas,
or copy their MoE/NVFP4 kernels without a Qwen-specific call-path and geometry
proof.
<!-- SENPAI-CAMPAIGN-END -->
").contains("Yukon CLI (`yukon`)")
↳ AGENTS.md must name the Yukon CLI explicitly
◇ Test runtimeWorkerPinnedConfigurationRejectsMissingQwen35Fields() started.
◇ Test ngramSelfSimilarityCountsEarlierMatchingFollower() started.
◇ Test trustedPreflightUsesOneShotWorkerProtocolAndPropagatesFailure() started.
◇ Test localFailedRunWithMeasuredTimingsStillPublishesNumericEstimatedScore() started.
◇ Test loadGoldenFixtureRejectsUnknownBenchmarkKeys() started.
◇ Test metallibFingerprintRecordVerificationFailsClosedOnStaleRecord() started.
◇ Test runtimeWorkerBenchmarkDecodeDoesNotReceiveBulkOracle() started.
✘ Test participantDocsExposeDefaultCLIInstallDirectory() recorded an issue at BenchmarkScriptTests.swift:462:9: Expectation failed: (document → "<!-- SENPAI-CAMPAIGN-BEGIN -->
# Qwen 3.8 Native-MTP Challenge Agent Guide

This repository has one ranked task: make Qwen 3.8 27B native-MTP decode
faster on Apple Silicon without changing a single emitted token. The ranked
track is `qwen3.8-27b-mtp-v1`.

Retained files from earlier tracks are historical prior art only. Their
commands, model geometry, editable surface, correctness rules, scoring, and
runner configuration do not apply here. Operate this repository only as the
Qwen track named above.

## Senpai Campaign

For coordinated research in `morganmcg1/qwen38-challenge_senpai`, read
[`senpai/program.md`](senpai/program.md) and
[`senpai/experiment-runbook.md`](senpai/experiment-runbook.md) before assigning
or running experiments. The campaign ledger and exact frontier pins are in
[`senpai/campaign-ledger.md`](senpai/campaign-ledger.md) and
[`senpai/frontier-state.json`](senpai/frontier-state.json).

The intended remotes are:

```text
origin    https://github.com/morganmcg1/qwen38-challenge_senpai.git
upstream  https://github.com/Layr-Labs/qwen-3.8-mtp-challenge
```

`upstream` must have push URL `DISABLED`. Never run `yukon sync`,
`yukon sync --harness-only`, or `yukon reset` in this maintained checkout;
those commands are for plain solver checkouts and can reset campaign history or
repoint `origin`. Use the repository-local `sync-organizer-frontier` skill for
organizer policy and promoted-snapshot refreshes.

Record the exact campaign `BASE_SHA` and organizer `UPSTREAM_SHA` for every
experiment. Preserve unrelated user work, campaign files, and dirty-worktree
changes. Generated weights, caches, score files, downloaded artifacts, and
hidden material never belong in Git.

## Authority Order

This repository still contains historical comments and bring-up prose. Resolve
contract questions from the live enforcing sources in this order:

1. [`benchmark.json`](benchmark.json): track identity, commands, editable
   paths, optional paths, budget policy, and scoring fields.
2. [`fixtures/qwen3_8_27b_mtp_track.json`](fixtures/qwen3_8_27b_mtp_track.json):
   artifact pins, timed prompt pool, fidelity semantics, and calibration.
3. [`.github/workflows/qwen-mtp-ranked-benchmark.yml`](.github/workflows/qwen-mtp-ranked-benchmark.yml):
   ranked execution, runner label, and enforced environment values.
4. [`docs/qwen-mtp-editable-surface.md`](docs/qwen-mtp-editable-surface.md):
   rationale for the editable/trusted boundary.
5. [`TASK.md`](TASK.md): concise participant-facing summary.

For mutable solver behavior, inspect the current `BASE_SHA` source and
`mtp-head.manifest.json`. Narrative descriptions of the original starting tree
do not override the current implementation. If authorities disagree, stop and
identify the exact enforcing site before editing or assigning work.

## Mission And Score

The official score is a decode-only paired speedup anchored at serial `1.0`:

```text
for each hidden prompt p:
  raw_p = mean(pinned-baseline depth-0 serial seconds/token)
          / mean(candidate seconds/token)

published score = median(raw_1 ... raw_8)
```

The ranked legs come from different workspaces. The trusted, prebuilt baseline
workspace produces the serial numerator; the submitted candidate workspace
produces the MTP denominator. Candidate-editable code cannot change the ranked
serial numerator, so any compliant reduction in candidate MTP time improves
the affected `raw_p`. Never subtract a local serial-path share such as
`psi_serial` when pricing official value. The local same-build harness has a
different causal graph and must be labeled separately. Run
`senpai/verify-ranked-score-boundary.sh` before using a derived score model.

All eight hidden prompts are measured. Because eight is even, the median is the
mean of the two central ordered values. There is no no-op normalization and no
prompt lottery. The published median must be at least `0.90` and no greater
than the `5.0` plausibility ceiling.

Each ranked leg processes a 512-token seed followed by 512 parent-counted
decode tokens, and the seed work is charged in the timed leg. Prefill is not a
separate score, but work removed from the charged seed still improves the
result. The ranked run uses one thermally gated serial/candidate pair per
prompt unless the trusted contract changes.

The trusted parent offers a per-round ceiling. Candidate policy may choose
from 0 through 8 drafts independently on every round. Drafting nothing is
legal and degenerates to serial decode; it is a control, not the optimization
goal. Acceptance rate alone is not the objective: deeper drafting wins only
when extra committed tokens repay head, target-verification, rejected-tail,
and state-management costs across varied prompts.

The organizer's original calibrated tree scored about `0.994`. That is a
historical starting-line measurement, not the current campaign frontier. Read
the live Yukon promoted row and `senpai/frontier-state.json` before comparing or
submitting work.

## Target And Current Scored Path

The fixed target is the organizer's MLX 4-bit affine/group-64 conversion of
Qwen 3.8 27B:

```text
backbone  EigenLabs/Qwen3.8-27B-4bit
          @ eda45ab47f465d08d6558f0353a2346e2eb9d5b3
upstream  Qwen/Qwen3.8-27B
          @ 1d4bf0f2ff6012fd82039f2fa52739d0dd7c60c0
```

The organizer-pinned default proposal head is the separate bf16 MTP
artifact. A candidate may instead declare an immutable remote or in-branch head
through `mtp-head.manifest.json`; inspect that file at `BASE_SHA` for current
head provenance. The target checkpoint remains fixed regardless of the head.

The Qwen tower is a dense hybrid architecture:

- 64 layers: 48 Gated DeltaNet recurrent layers and 16 full-attention layers;
- hidden size 5,120 and vocabulary 248,320;
- full attention with 24 query heads, 4 KV heads, and head dimension 256;
- affine 4-bit/group-64 backbone weights and dense MLPs.

The `Qwen35*` source names and `qwen3_5_text` configuration label describe the
architecture family embedded in the Qwen 3.8 artifact; they do not mean this
repository targets Qwen 3.5 or 3.6.

The scored worker loads the transformed checkpoint through `LLMModelFactory`,
attaches the selected head, warms legal shapes, and serves rounds through
[`Sources/MLXFastModel/Qwen36MTPBlockSession.swift`](Sources/MLXFastModel/Qwen36MTPBlockSession.swift).
A round conceptually:

1. commits the pending primary token;
2. lets candidate policy choose 0...8 drafts within the offered maximum;
3. runs the MTP head chain to propose them;
4. verifies `[primary] + drafts` with the fixed target in a batched forward;
5. accepts the longest correct prefix;
6. keeps, restores, replays, or repairs target and head state as required;
7. reports actual rows and exact top-two evidence to the trusted parent.

The timed target model is the vendored `Qwen35TextModel`/`Qwen35Model` in
[`Vendor/mlx-swift-lm/Libraries/MLXLLM/Models/Qwen35.swift`](Vendor/mlx-swift-lm/Libraries/MLXLLM/Models/Qwen35.swift),
together with the editable MTP session. Directory-wide editability does not
prove reachability: for example, the separate `Qwen35FastEngine` path is not
the MTP worker's current target path. Trace the call path before optimizing.

### Current frontier is already optimized

Do not treat historical defaults as current behavior or rediscover mechanisms
already in the promoted snapshot. At this guide's 2026-08-16 rewrite:

- `Qwen36MTPBlockSession.init` installs an adaptive cost-model policy; the
  nearby K=1 property initializer and constant-depth comments are historical
  fallback/bring-up text, not the instantiated policy;
- the campaign `mtp-head.manifest.json` declares a 4-bit/group-64 proposal
  head rather than using the organizer-pinned bf16 default;
- persistent head history, K/V-only history flush, device-resident drafting,
  exact rollback/replay, and scored-shape warmups are present;
- target verification has exact multi-row affine4 QMV sharing, fused GDN and
  full-attention preparation, and an exact two-stage top-two reducer;
- proposal selection already uses a compact vocabulary and a custom selector;
- the layer loop already carries an `asyncEval` ladder for narrow widths.

These are mutable solver facts. Recheck source after every frontier sync. See
[`senpai/laguna-to-qwen-speedup-map.md`](senpai/laguna-to-qwen-speedup-map.md)
and the campaign novelty ledger before assigning overlapping work.

## Editable And Trusted Boundary

The ownership rule is:

> Candidate code may propose and execute the model, but the trusted parent owns
> correctness, measurement, and final ledger authority.

The editable session necessarily assembles target verify forwards, walks the
candidate-side accepted prefix, rolls state back, and produces evidence for the
parent. None of those self-reports is authoritative: the parent independently
checks the serial token stream, round structure, rows, and accounting.

`benchmark.json` `editablePaths` is the only machine-readable path authority.
Its current 89 entries form five practical groups:

- optional `mtp-head.manifest.json` and `mtp-head/` candidate-head content;
- `Sources/MLXFastModel/`, including the runtime and full MTP apparatus;
- `Sources/MLXFastTransform/` offline checkpoint transform;
- the exact listed Qwen model and `MLXLMCommon` support files;
- the exact listed MLX/Metal kernel sources and generated twins.

Do not infer that an adjacent file is editable. Check the manifest at
`BASE_SHA` before implementation. Submission archives use replace semantics
over `editablePaths`; only the two MTP-head paths are optional. A support-file
change outside the surface can make a local tree faster while uploading
nothing useful.

Trusted and not submitted include:

- target weights, transform contract, tokenizer, fixtures, and goldens;
- trusted driver, accept audit, row accounting, score emission, and worker
  protocol;
- timing, telemetry, thermal gate, workflows, and `.github/scripts/`;
- `benchmark.json`, `Package.swift`, and `Package.resolved`;
- tests, docs, local benchmark scripts, and other paths not named by the
  manifest.

`Sources/MLXFastModel/Qwen36MTPReferenceSession.swift` is an important mixed
case: it is editable through the directory-wide allowance even though it serves
rejected-tail reference replay. It runs after timing and cannot define emitted
tokens. Never route its reference work or any golden-derived information into
the timed path, proposal policy, or emitted stream.

The source budget is 3,000,000 bytes total, 524,288 bytes per file, and
262,144 bytes of candidate growth. `mtp-head/` is exempt from the source budget
and instead has a declared digest, byte count, and 2 GiB cap. Run the campaign
scope and budget checks before expensive validation.

## Correctness And Prohibited Strategies

Correctness is a hard gate. Every emitted token must equal the hidden serial
trajectory. The round journal must be structurally sound, effective draft
counts must match what occurred, and the reference-checked row ledger must
close. There is no near-tie allowance on this track.

The ranked pipeline also runs the public drift tripwire, hidden teacher-forced
base, anchor, free-run, behavior and GPQA gates, GPQA TTFT guardrail, semantic
judge, transform verification, static review, and integrity checks. Any failed
gate produces no promotable score.

The native MTP path inside the declared editable surface is the only sanctioned
future-token mechanism. Do not add:

- prompt lookup, n-gram, suffix, or token-history drafting;
- same-target lookahead or unsanctioned future-logit/KV computation;
- request-keyed replay caches whose only useful hit is a repeated benchmark
  request;
- cross-request future state, hidden-prompt specialization, or GPQA shortcuts;
- degraded or partial target forwards, unevaluated verify rows, omitted exact
  final-logit/top-two work for emitted rows, or fabricated top-two, row,
  acceptance, or ledger evidence;
- an undeclared auxiliary proposal model/head or candidate-selected target;
- phase, reference-leg, prompt-pool, or baseline detection used to change the
  work performed;
- logical rollback that leaves rejected cache/state rows reachable, or merely
  decrements an offset without trimming or overwriting rejected physical rows;
- timing shortcuts, protocol injection, network access, or filesystem
  exfiltration.

Input-independent weight/kernel/shape tables and ordinary within-request cache
reuse are legal when they preserve the contract. A candidate head may improve
proposal quality and cost; it cannot redefine the target answer.

Public fixtures were generated on M5. On another Apple GPU, compare an
unchanged `BASE_SHA` at the same token position before diagnosing a near-tie
local mismatch. That diagnostic does not relax official exactness.

## Kernel And Swift Rules

The vendored MLX package has two effective source forms:

- A family with an `mlx-generated/*.cpp` twin is JIT-compiled from the source
  string embedded in that generated C++ file. Update the readable
  `.metal`/`.h` source and the runtime-effective twin together.
- AOT-only families are loaded from `mlx.metallib`; rebuild it with
  `tools/build-mlx-metallib.sh` after relevant edits.
- `_nax` variants are selected on ranked M5 hardware and are first-class
  targets, not optional follow-up work.

Run `python3 research/twin_audit.py` after JIT-source edits. Before any kernel
experiment, prove the scored shape, dispatch family, source form, and M5
variant. Reduction order, precision, BF16 cast boundaries, packing, recurrent
state, cache layout, and width-dependent dispatch are correctness-sensitive.

For direct Swift commands, preserve the frozen dependency graph:

```bash
swift test --force-resolved-versions
MLXFAST_RUN_MLX_RUNTIME_TESTS=1 swift test --force-resolved-versions
swift build -c release --force-resolved-versions
tools/build-mlx-metallib.sh
```

A bare Swift build may write `.build/release` without rebuilding the scored
`.build-worker/release/mlxfast-runtime-worker`. Use the Qwen benchmark wrapper
for end-to-end work. Never run `swift package resolve` or `swift package
update`; if `Package.resolved` changes, restore it rather than accepting a new
dependency graph.

## Local Workflow And Measurement

Use only the Qwen entrypoints:

```bash
./setup.sh && ./setup-qwen-mtp.sh
./benchmark-qwen-mtp.sh --local-iterate
./benchmark-qwen-mtp.sh --local-submit
```

`--local-iterate` defaults to 64 decode tokens and `--local-submit` to 128.
They use one public fixture and cannot reproduce the ranked eight-prompt,
512-token result. Treat local scores as directional.

The local serial and candidate legs use the same local build. A general target
runtime or kernel improvement can speed both sides and cancel in the local
ratio, even though it counts officially because the ranked serial numerator
comes from a pinned baseline workspace. Compare absolute candidate
seconds/token against a fresh, same-host `BASE_SHA` run as well as the local
serial/MTP ratio.

Label every measurement and formula `harness=local` or `harness=ranked`. Use
matched absolute candidate MTP time as the primary local signal for broad
runtime and kernel changes; the local ratio may contain same-build cancellation
that does not exist officially.

The AWS Mac fleet assigns one physical Mac to each student. The advisor shares
only Edward's Mac; Alphonse, Thorfinn, and Askeladd each run on another Mac.
Locks, device occupancy, and thermal state are host-local. Run independent jobs
concurrently across distinct Macs and coordinate only co-located roles. Verify
the live placement rather than inferring a fleet-wide queue.

Run only one model-holding process at a time. The wrapper takes a per-user lock
and checks for orphaned workers; direct model commands do not. Inspect reported
PIDs before killing anything, and never disable the guard to work around a
legitimate concurrent run.

The wrapper cools the GPU before each resident measurement. Waiting at the 40C
gate is normal, not a hang, and the gate remains the default. For local timed
arms only, `MLXFAST_LOCAL_COOL_GATE=0` is permitted when all arms are
ABBA-counterbalanced within one session, every arm records entry and exit GPU
temperature with the entry spread reported beside the effect, and the result
retains `cool_gate_passed_real_gate=false` plus
`gate_qualified_for_timing=false`. Such evidence is explicitly ungated and
directional; compare it within that counterbalanced session, not as though it
were a gate-qualified or official result. Never fabricate a temperature source,
edit the gate constant, or bypass the process lock. Keep host, power, thermal
state, memory profile, head provenance, toolchain, token window, and frontier
SHA matched between base and candidate. Ranked authority is the self-hosted M5
runner labeled `m5-qwen38-27b-mtp`; non-M5 timing is directional only after
confirming the same kernel family executes.

## Experiment Discipline

One experiment should answer one causal question. For each arm:

1. Record an identity tuple: `BASE_SHA`, `UPSTREAM_SHA`, candidate commit and
   built-worker fingerprint, submitted-surface/twin/metallib digests, head
   digest, host/instance/chip, toolchain, memory and thermal mode, token window,
   fixture/reference source, exact cell/shape/dispatch, and harness.
2. State the official causal path, expected direction, minimum useful effect,
   and stop rule. Stress-test any score equation at zero and both signs.
3. Prove reachability from the current native-MTP worker and name the cost
   removed. Never transfer a constant or fitted model from another tree or cell
   without replay.
4. Check editable scope, byte budget, and the ranked score-boundary tripwire.
5. Measure an unchanged same-host baseline and save its artifact outside the
   submission surface.
6. Implement one mechanism. Before long timing, run the cheapest gate capable
   of falsifying the changed boundary. Numerical and reduction-order edits need
   actual floating-point touched-cell coverage, a positive control, and the
   shortest end-to-end exact-token/row-ledger run; integer-only or argmax checks
   are insufficient.
7. Measure the candidate under the same identity tuple. Start with the smallest
   decisive timing run; replicate only when noise could change the decision.
8. Compare absolute candidate time, paired ratio, draft/accept telemetry,
   rollback, and block latency where relevant. Cost-weight per-cell effects
   before aggregating them.
9. Reject, revise, or advance using the predeclared stop rule. Label every
   inference, interpolation, and extrapolation; never substitute M=8 for M=9,
   M4 for M5, local for ranked, or an abandoned dispatch table for the current
   tree.
10. Record positive, negative, invalid, and ambiguous results in the campaign
    ledger so later agents do not repeat them.

A microbenchmark is useful only when it bounds a named end-to-end cost.
Dispatch-count reduction is not a result unless it removes bytes, arithmetic,
or a dependency. A local pass on one easy prompt does not prove an adaptive
policy is robust across the hidden pool. Compose only independently measured
winners.

Promising current cost centers include draft scheduling, proposal-head cost and
quality, persistent head history, target verify width, GDN recurrence and
rollback, full-attention/GQA cache work, affine4 projection and vocabulary
readout, MLX scheduling/materialization, warmup, and transformed weight
metadata. Start from current profiles and the novelty ledger, not this list.

## Submission And Promotion

Yukon manages accounts and official submissions. It does not run the local
pre-submit command for you. In the maintained campaign checkout, use the guard:

```bash
export PATH="${HOME}/.local/bin:${PATH}"
yukon submissions --all
senpai/submit-official.sh "$BASE_SHA" \
  --model "senpai" \
  --note-file submission-note.md
yukon submissions
```

Use the exact lowercase campaign attribution `senpai`; record the underlying
models, effort levels, and agent harnesses in the public note body.

Immediately before submission, compare the highest-scoring live `promoted` row
with `senpai/frontier-state.json`. If the receipt, source ref, or score differs,
sync the organizer/promoted frontier, replay the candidate, and remeasure it.
Do not infer promotion from a `Validate submission` commit or a branch name.

Submit from a clean, committed candidate after `--local-submit`, exact diff
inspection, scope/budget checks, and generated-twin audit where relevant. Never
put credentials in notes, logs, commits, or agent messages. The ranked resource
is serialized; do not dispatch duplicate submissions to mine timing noise.

Only the official M5 result can promote a candidate. A green rejected receipt
may simply have failed to beat the current frontier; read its gates and metrics
before classifying it as incorrect.

## Historical Prior Art

Laguna work is useful only as a source of hypotheses about shared MLX concepts.
The curated transfer audit is
[`senpai/laguna-to-qwen-speedup-map.md`](senpai/laguna-to-qwen-speedup-map.md).
Do not run Laguna/DFlash setup or benchmark scripts, use their score formulas,
or copy their MoE/NVFP4 kernels without a Qwen-specific call-path and geometry
proof.
<!-- SENPAI-CAMPAIGN-END -->
").contains("Yukon CLI (`yukon`)")
↳ CLAUDE.md must name the Yukon CLI explicitly
◇ Test correctnessReportEncodesStableFailureFields() started.
◇ Test lagunaYaRNIgnoresSerializedAttentionFactorLikeUpstreamMLX() started.
◇ Test loadGoldenFixtureRejectsNonPositiveBenchmarkBaselines() started.
◇ Test dflashDriverTwinsAreByteIdentical() started.
◇ Test benchmarkScriptSealsScoreFromStdoutDiscardingOnDiskTamper() started.
◇ Test benchmarkWorkspaceACLLocksTrustedSurfacesAgainstBench() started.
◇ Test goldenSequenceMatcherAcceptsExactAnswerSequences() started.
◇ Test loadGoldenFixtureRejectsMalformedLayeredCorrectnessGate() started.
◇ Test denseTensorStoreValidatesSparseShardLargerThanInt32() started.
◇ Test benchmarkPrintsFanRestoreReminderAfterBoostWithoutAutoRestoring() started.
◇ Test benchmarkScriptBareInvocationDefaultsToLocalIterate() started.
◇ Test loadGoldenFixtureRejectsEmptyCorrectnessGateSection() started.
◇ Test qwen36TransformOutputSatisfiesTheRuntimeWorkerPinnedConfigGate() started.
◇ Test decodeMeasurementRunsSingleUnmemoizableSeedForward() started.
◇ Test safetensorsCopySubsetRejectsOutputOffsetOverflow() started.
◇ Test runtimeWorkerOrphanReaperFiresOnceTheParentIsGone() started.
◇ Test setupReferenceVerificationRejectsManifestABA() started.
◇ Test robustReferenceDropsTheSlowestAndAverages() started.
◇ Test transformDetectsModelFamilyFromSourceConfig() started.
◇ Test setupDetectedMetalToolchainOverridesUnrelatedToolchainsEnvironment() started.
◇ Test buildEntrypointsAssertFrozenDependencyGraph() started.
◇ Test compressedTensorQuantizationSchemasAndWrongGroupSizeAreRejected() started.
◇ Test dflashReferenceRowsRequestCarriesTheVerifyBlockOnTheWire() started.
◇ Test transformAcceptsSparseShardLargerThanInt32() started.
◇ Test metallibBuilderRejectsAmbiguousCMakeOutputs() started.
◇ Test loadGoldenCasesRejectsWrongPromptTokenCount() started.
◇ Test loadGoldenCasesRejectsNonPositiveRequiredSteps() started.
◇ Test localRunLockRejectsOverlappingRunAndReclaimsStaleLock() started.
◇ Test loadGoldenFixtureRejectsNullBenchmarkObject() started.
◇ Test ngramSelfSimilarityAppliesPassThresholdToDraftHitRate() started.
◇ Test runtimeWorkerClientSkipsNonJSONStdoutLines() started.
◇ Test ngramSelfSimilarityUsesLongestRecurrentOrder() started.
◇ Test loadGoldenFixtureAcceptsPerPromptBenchmarkBaselines() started.
◇ Test nvfp4ActualRoutedGatherShapesCoverMultipleExpertsAndPrefillWhenRuntimeTestsAreEnabled() started.
◇ Test localIterateLiveDecodeStatusIncludesProjectedSpeedupAndScore() started.
◇ Test qwen36HeaderInventoryContractPinsTheShardIndependentDigest() started.
◇ Test endOfTurnTruncationCutsAtTheFirstEndOfTurnOnly() started.
◇ Test loadGoldenFixtureAcceptsBenchmarkOracle() started.
◇ Test safetensorsCopySubsetRejectsHardLinkToSourceWithoutMutatingIt() started.
◇ Test qwenChatTemplateEmitsNoDefaultSystemBlock() started.
◇ Test lagunaRuntimeMatchesVendoredUpstreamOnM5WhenEnabled() started.
◇ Test denseTensorStoreRejectsByteLengthMismatch() started.
◇ Test cliVerifiesMetallibFingerprintBeforeWorkerLaunch() started.
◇ Test naxSplitKJITFactoryUsesInputDType() started.
◇ Test runtimeWorkerEnvironmentDropsEverythingOutsideTheAllowlist() started.
◇ Test coolGateFanBoostIsOptInSudoSafeAndHardCappedAtSeventyPercent() started.
◇ Test lagunaRuntimeWiresOnlyExpertsToNVFP4WhenRuntimeTestsAreEnabled() started.
◇ Test bufferedFileLineReaderRejectsOversizedLine() started.
◇ Test qwenHybridCachePositionStillRejectsAStaleOrReusedCache() started.
◇ Test benchmarkOutputValidatorReportsTokenMismatches() started.
◇ Test lagunaWeightCacheConsultsStartupMemoryPolicy() started.
◇ Test overlayPairedTimingAcceptsTrustedCommitAndRejectsForgedOrMissingCommit() started.
◇ Test goldenSequenceMatcherAcceptsShortBehaviorPrefixes() started.
◇ Test startupMemoryPolicyIsAppliedBeforeModelLoadAndCleansWarmupCache() started.
◇ Test setupStartsMetallibBuildOnlyOnceInSynchronousAndParallelModes() started.
◇ Test benchmarkRebuildsTheMetallibWhenVendoredKernelSourcesAreNewer() started.
◇ Test lagunaWeightContractPinsExactXSHeaderInventory() started.
◇ Test submissionStaticReviewCapsFileSizeAndSurfaceGrowth() started.
◇ Test localIterateScoreUnderGoldenDriftStaysUsableButRecordsTheDivergence() started.
◇ Test commitIdentifierPrefersTrustedDispatchSHAOverGit() started.
◇ Test lagunaRuntimeCorrectnessReportsMissingArtifacts() started.
◇ Test checkedInPublicCorrectnessGoldenIsValid() started.
◇ Test headProvisioningDelegatesWithoutRebuildingOrMisadvising() started.
◇ Test runtimeWorkerClientDrainsLargeStderrBeforeHelloWhenForwardingIsOff() started.
◇ Test metadataDerivationFailsClosedExceptWhenHashVerificationIsOff() started.
◇ Test currentLongcopyFixtureDemonstratesHighSelfSimilarity() started.
◇ Test transformRejectsLagunaRouterAndStackedExpertCountMismatch() started.
◇ Test checkpointIndexToolsRejectsUnsafeShardPath() started.
◇ Test sealedStdoutScoreIsCoarsenedLikeTheWrittenFile() started.
◇ Test metallibBuilderStalledLockCreatorCannotPublishIntoReplacementLock() started.
mlxfast: benchmark elapsed=0.0s start correctness_steps=1 benchmark_decode_steps=1
mlxfast: benchmark elapsed=0.0s golden load start
mlxfast: benchmark elapsed=0.0s golden load complete cases=2 benchmark_oracle=present
mlxfast: benchmark elapsed=0.0s failed passed_correctness=false error=benchmark behavior and GPQA TTFT gates require runtime worker timing
◇ Test runtimeWorkerEnvironmentIsIdenticalAcrossPipelinePhases() started.
◇ Test transformVerifierRejectsOutputThatDiffersFromFreshTransformRun() started.
◇ Test safetensorsCopySubsetRejectsDirectoryDestinationWithoutRemovingIt() started.
◇ Test qwenHybridCachePositionRejectsRecurrentCachesThatCountPositions() started.
◇ Test loadGoldenCasesRejectsNamesWithSurroundingWhitespace() started.
◇ Test localFailedPayloadWithoutTimingsKeepsNullScore() started.
◇ Test poolsideLagunaTensorInventoryFixturePinsAllPublicHeaders() started.
◇ Test endOfTurnTruncationIsAnIdentityWhenTheTurnNeverEnds() started.
◇ Test benchmarkRejectsPartialSetupBeforeInvokingExistingBinary() started.
◇ Test onlyTheAllowlistedRefsMayDispatch() started.
◇ Test decodeAcceptsSmallRegressionAndUpTo5PercentGain() started.
◇ Test qwenHybridCachePositionRejectsWrongTopologyOrLayerCount() started.
◇ Test theGuardsAreExecutable() started.
◇ Test runtimeWorkerProtocolUsesAuthenticatedPrivateIO() started.
◇ Test onlyWorkflowDispatchMayDispatch() started.
◇ Test eachGuardIsPinnedToItsOwnWorkflowPath() started.
◇ Test lagunaCorrectnessComparesExpectedTokenSequences() started.
◇ Test bandTolerancesMatchConstants() started.
◇ Test eachWorkflowInvokesItsOwnGuard() started.
◇ Test transformVerifierRejectsOutputAboveConfiguredByteLimit() started.
◇ Test onlyTheTrustedRepositoryMayDispatch() started.
◇ Test benchmarkScriptForwardsLocalSubmitFlagToSwiftBenchmark() started.
◇ Test everyRequiredEnvironmentVariableIsRequired() started.
◇ Test theRankedWorkflowTimesEveryPoolPromptAndDrawsNothing() started.
◇ Test poolsideReferenceDownloadersUseSourceSpecificQueryHandling() started.
◇ Test transformRejectsOutputInsideReferenceDirectory() started.
◇ Test theManifestAndFixtureAgreeOnTheAggregationRule() started.
◇ Test lagunaWeightLoaderRejectsUnexpectedTensorInventory() started.
◇ Test poolsideLagunaConfigRejectsEveryBehaviorBearingMutation() started.
◇ Test benchmarkTransformCacheKeyIgnoresRuntimeModelSources() started.
◇ Test transformRejectsSymlinkedTokenizerMetadataAndPreservesOutput() started.
◇ Test metallibBuilderPreservesPublishedOutputWhenCopyFails() started.
◇ Test theEvenMedianRuleIsTheMeanOfTheTwoCentralValues() started.
◇ Test dflashWorkerTwinsDifferOnlyByTheTrustedHarnessGuard() started.
◇ Test setupStalledLockCreatorCannotPublishIntoReplacementLock() started.
◇ Test theFixtureDeclaresTheScoringSemanticsWithoutDisturbingThePool() started.
◇ Test transformWritesFlattenedRuntimeConfigFromTextConfig() started.
◇ Test benchmarkScriptAvoidsNestedSandboxWithRuntimeWorker() started.
◇ Test theWorkflowPinsThePairBudgetTheDocumentsDeclare() started.
◇ Test attachDerivedBenchmarkOracleRefusesToOverwriteAnExistingOracle() started.
◇ Test theScoringStepRecomputesTheMedianAndGatesTheBreakdownShape() started.
◇ Test theSeededCalibrationExpectationMatchesItsRecordedProvenance() started.
◇ Test submissionStaticReviewDiffModeFailsClosedAndSendsOnlyChangedFiles() started.
◇ Test lagunaRuntimeCorrectnessReportsGoldenMetadataWhenWeightsAreMissing() started.
◇ Test scoredBaselinesResolveFromGoldenWithConstantsFallback() started.
◇ Test checkRejectsNonFinite() started.
◇ Test goldenSequenceMatcherChecksExactPrefixes() started.
◇ Test loadGoldenFixtureRejectsUnknownCorrectnessGateKey() started.
◇ Test setupRecoversMutationLockOwnedByDeadLocalProcess() started.
◇ Test writeScorePayloadEmitsDarkbloomShape() started.
◇ Test setupAndMetallibLocksPublishWithBSDAndGNUCoreutils() started.
◇ Test compiledDecodeFlagsStayReadableByModelSources() started.
◇ Test unsafePrivateCalibrationToolingStaysExcluded() started.
◇ Test transformVerifierRejectsStaleExtraGeneratedFile() started.
◇ Test setupRecoversAgedMalformedMutationLockOwner() started.
✔ Test attachDerivedBenchmarkOracleRejectsABaseCaseShorterThanTheTimedDecodeWindow() passed after 0.288 seconds.
✔ Test defaultTransformedWeightsLimitIsTwentyFiveGiB() passed after 0.288 seconds.
benchmark: pinned trusted harness artifacts -> /var/folders/hg/g1_843ts0sv67jyk1mhhc83c0000gn/T/mlxfast-benchmark-script-tests-CF9CF1D8-EBE4-4016-893F-4DECC9F8D786/trusted-harness.sha256
benchmark: pinned worker harness artifacts -> /var/folders/hg/g1_843ts0sv67jyk1mhhc83c0000gn/T/mlxfast-benchmark-script-tests-CF9CF1D8-EBE4-4016-893F-4DECC9F8D786/participant-worker.sha256
benchmark: trusted harness artifacts verified against the post-build pin
✔ Test transformKeySelectionDropsRotaryInvFreqOnlyForLaguna() passed after 0.388 seconds.
benchmark: worker harness artifacts verified against the post-build pin
benchmark: worker harness artifacts verified against the post-build pin
benchmark.sh: reusing /var/folders/hg/g1_843ts0sv67jyk1mhhc83c0000gn/T/mlxfast-benchmark-script-tests-E865329C-5DBE-4073-AA8A-8CECF57BE4AC/weights/ because MLXFAST_SKIP_TRANSFORM=1
benchmark: trusted harness artifacts verified against the post-build pin
benchmark: trusted harness artifacts verified against the post-build pin
benchmark: worker harness artifacts verified against the post-build pin
benchmark.sh: reusing /var/folders/hg/g1_843ts0sv67jyk1mhhc83c0000gn/T/mlxfast-benchmark-script-tests-7441B72A-D709-47E0-A4B5-C8979F7B11EA/weights/ because MLXFAST_SKIP_TRANSFORM=1
benchmark.sh: reusing /var/folders/hg/g1_843ts0sv67jyk1mhhc83c0000gn/T/mlxfast-benchmark-script-tests-8EF835CA-1E19-49E8-BDDC-A8D22C9E5EEE/weights/ because MLXFAST_SKIP_TRANSFORM=1
benchmark: trusted workflow verified Layr-Labs/mlxfast-challenge-dev/.github/workflows/benchmark.yml@refs/heads/main
benchmark: trusted workflow verified Layr-Labs/mlxfast-challenge-dev/.github/workflows/benchmark.yml@refs/heads/submissions/example
benchmark: trusted workflow verified Layr-Labs/mlxfast-challenge-dev/.github/workflows/benchmark.yml@refs/heads/baseline/reference
benchmark: trusted workflow verified Layr-Labs/mlxfast-challenge-dev/.github/workflows/benchmark.yml@refs/heads/yukon/baseline/718528521cd7a7df341b750bc3ccb28478ff045b
✔ Test correctnessAcceptsOnlyExactTopLogitTies() passed after 0.750 seconds.
✔ Test safetensorsRejectsUInt64MaxHeaderLengthWithoutTrapping() passed after 0.764 seconds.
✔ Test loadGoldenCasesAcceptsValidFixture() passed after 0.765 seconds.
benchmark.sh: no mode given; defaulting to --local-iterate (use --official for the ranked entrypoint, which requires the private oracle)
✔ Test safetensorsCopySubsetRejectsForgedValidatedHeaderWithoutMutatingDestination() passed after 0.780 seconds.
✔ Test qwenHybridCachePositionAcceptsRecurrentCachesPinnedAtZero() passed after 0.795 seconds.
◇ Test everyPoolEntryCarriesItsMeasuredSpreadAndPairCount() started.
◇ Test theNoteRecordsTheRegimeDeltaAndTheRejectionRateFinding() started.
benchmark.sh: benchmark produced a failing score; see /var/folders/hg/g1_843ts0sv67jyk1mhhc83c0000gn/T/mlxfast-benchmark-script-tests-E865329C-5DBE-4073-AA8A-8CECF57BE4AC/score.json
◇ Test everyRetiredMTPNameStillTripsTheGuard() started.
◇ Test theWorkflowResolvesTheContractToTheFixtureThisSuiteReads() started.
◇ Test newQwenMTPSurfaceNamesCarryNoRetiredMTPName() started.
◇ Test aMalformedPoolEntryIsRefusedOnEveryDispatchNotOneInEight() started.
◇ Test anEnabledTrackDeclaresAnImplementedTokenFidelityGate() started.
◇ Test theQwenMTPTrackIsArmedOnQwen38() started.
◇ Test theTrackFixtureIsEnabledForGoLive() started.
◇ Test theDryRunArmAnnouncesThatNoScoreIsProduced() started.
◇ Test newQwenMTPFilesCarryNoRetiredMTPName() started.
◇ Test aRankedRunAgainstAnUnenabledTrackIsStillRefused() started.
{
  "score": null,
  "passed": true,
  "metrics": {
    "prefill_seconds_per_token": 0.1,
    "decode_seconds_per_token": 2.0,
    "prefill_speedup": 2.0,
    "decode_speedup": 2.0,
    "weights_hash": "fake-weights",
    "weights_file_count": 1,
    "weights_byte_count": 2
  }
}
◇ Test theRetiredMTPNameListDoesNotShrink() started.
◇ Test theInterlockAdmitsTheDryRunAndOnlyTheDryRun() started.
◇ Test nOnlyDimsAreNotSafeAsReductionDims() started.
◇ Test retiredNamesAreNotClearedByAQwenPrefix() started.
◇ Test aGatesOnlyDispatchIsExemptedByTheStepsOwnCondition() started.
◇ Test theFloorNamesTheCoordinateThatIsShort() started.
◇ Test scoredShapesStayOnTheQMVFastPath() started.
◇ Test theManifestAndWorkflowAgreeOnTrackIdentity() started.
◇ Test theEnumeratedNamesAreTheOnesTheFilesUse() started.
◇ Test theScoringStepIndependentlyRefusesAnUnenabledTrack() started.
➜ Test sweepCompactDraftReadoutOverBits() skipped.
➜ Test sweepQuantizedMatmulOverVerifyWidth() skipped.
◇ Test theManifestDeclaresTheDecodeFloorTheWorkflowEnforces() started.
◇ Test confirmTrackEnabledIsRequiredEvenForAGatesOnlyDryRun() started.
◇ Test goldenPinsAreRequiredEvenForAGatesOnlyDryRun() started.
◇ Test aRankedRunRequiresEightDistinctTimedTargets() started.
✔ Test goldenModelProvenanceIsStrictAndPinned() passed after 1.125 seconds.
✔ Test transformRejectsPoolsideLagunaRouterMissingCorrectionBias() passed after 1.127 seconds.
✔ Test offlineRunnerProvesNetworkIsBlockedBeforeRunningCommand() passed after 1.128 seconds.
✘ Test phaseStartAllocatorResetLeavesExactlyEmptyCacheWhenRuntimeTestsAreEnabled() recorded an issue at RuntimeWorkerSupportTests.swift:179:2: Caught error: runtime worker failed to clear the MLX allocator cache at phase start
✔ Test benchmarkWorkflowProbesAndEnforcesRuntimeWorkerSandbox() passed after 1.153 seconds.
✔ Test transformRejectsPoolsideQuantizationOverrides() passed after 1.153 seconds.
✔ Test dflashWorkerAppliesStartupMemoryPolicyBeforeItsModelLoads() passed after 1.153 seconds.
◇ Test theRenameMatchesTheEagerLoadersOwnRule() started.
◇ Test theStrippedPrefixIsTheTransformsOwnTextTowerPrefix() started.
◇ Test theWorkerAcceptsTheProtocolNotAConcreteClass() started.
◇ Test anUndeterminableQwenLayoutIsRefusedRatherThanGuessed() started.
◇ Test theStripRunsBeforeSanitizeAndQuantize() started.
◇ Test aNonQwenBackboneIsRefusedLoudly() started.
◇ Test theRawReferenceIsAWrappedModelAndItsPrefixIsKept() started.
◇ Test theHeadStagingInclusionIsDocumentedWhereItIsActionable() started.
◇ Test theTransformedTreeIsATextModelAndItsPrefixIsStripped() started.
◇ Test theNestedStructureDecidesRegardlessOfTheModelTypeSpelling() started.
◇ Test bothQwenClassesConformAndTheVendoredOneIsQualified() started.
✔ Test editableDecodeDelayHookIsFullyRemoved() passed after 1.217 seconds.
✔ Test safetensorsCopySubsetRebindsPublicHeaderToMatchingSource() passed after 1.216 seconds.
✔ Test theTransformCacheKeyCoversTheReferenceIdentity() passed after 1.221 seconds.
nax_splitk_case M=2 N=2048 K=6144 a_strides=[12288, 6144, 1] weight_strides=[6144, 1] max_abs_diff=0.001953125
✘ Test qwen36ConfigContractDigestMatchesTheReferenceManifest() recorded an issue at Qwen35ArtifactContractTests.swift:286:5: Expectation failed: (entry → "558cd605a6f1c16c73f4918534d122a943e12e754d38567b3b704acc93596965 4094 config.json") == nil
↳ the reference manifest has grown a config.json record. The 3.8 manifest body has landed: restore the digest comparison here against the 3.8 config.json, and re-scope the 3.6 config/inventory fixtures in the same commit.
✘ Test qwen36ConfigContractDigestMatchesTheReferenceManifest() recorded an issue at Qwen35ArtifactContractTests.swift:295:5: Expectation failed: (manifest → "# SHA256 manifest for EigenLabs/Qwen3.8-27B-4bit.
# Revision: eda45ab47f465d08d6558f0353a2346e2eb9d5b3
# Format: <sha256> <byte_count> <relative_path>
#
# GENERATED FROM THE PUBLISHED SNAPSHOT 2026-08-14. This file was a header-only
# stub from the identity swap until the upload landed; the body below is
# generated alongside the digests it has to agree with, from the published
# revision named above, which is what the stub's own note required.
#
# The backbone reference checkpoint is OUR OWN mlx-0.32.0 conversion (MLX 4-bit
# affine, group_size 64) of the official bf16 base Qwen/Qwen3.8-27B @
# 1d4bf0f2ff6012fd82039f2fa52739d0dd7c60c0. The shard split is three
# safetensors files, which is a property of our toolchain and was measured at
# publish rather than assumed.
#
# THE SHAPE PINS MOVE WITH THIS BODY, in this commit, because a count that is
# not summed from the digests it must agree with is not a pin:
#
#   MLXFAST_QWEN_MTP_TARGET_MANIFEST_RECORDS: 10
#   MLXFAST_QWEN_MTP_TARGET_MANIFEST_BYTES:   15153237117
#
# and so do the contract fixture's target.manifest_sha256 and
# target.expected_source_bytes.
#
# RECORD SCOPE -- OPEN ITEM. The published repository carries an ELEVENTH file,
# the `.gitattributes` Hugging Face generates for every repo
# (34448b82c17d60fec9b65b1f093c115ddbaadc04beb1b0140b6bfed2e012a930, 1570
# bytes). It is NOT pinned below, because the pin was published as a ten-record
# shape. The ranked workflow's `verify_cache` rejects any file in the cache
# that the manifest does not name -- run 31665285024 died on exactly that --
# so a stock `snapshot_download` of this revision will stage a file this
# manifest does not cover. Either stage the ten pinned files only, or add the
# record above and move the two shape pins to 11 / 15153238687 with it. This is
# recorded here rather than fixed silently because the shape pins are the
# operator's to set.
#
# WHAT THIS FILE NAMED BEFORE, and why it moved. On 2026-08-14 these header
# lines named a third-party personal-account conversion of the same bf16 base,
# adopted that morning. The adoption was TERMINATED the same day by the
# operator's validation kill-switch: the deterministic reconversion cross-check
# it had reserved was performed, and 994 of 1,847 tensors differ numerically
# from our own conversion. The upstream bf16 base did not move and is the
# lineage the records below descend from.
#
# THE 3.6 RECORDS WERE DELETED RATHER THAN CARRIED, one commit before this one:
# a manifest of 3.6 bytes checked against a 3.8 cache directory reports every
# file missing and reads as a corrupt download rather than as an unfinished
# pin. The stub window that reasoning opened is now closed by real records.
#
# THE TOWER, for readers who come here for it: 1,847 tensors, every one of them
# under the `language_model.` prefix and NONE under `mtp.`. The MTP head ships
# separately -- see fixtures/qwen3_8_27b_mtp_head.sha256 -- exactly as it did
# on 3.6.
748c964f9b7e5f2c3770ce013bbc0153be7c54311d9692c343b6188eefe77ac6 81 README.md
c3cf9e34abf4f9e36c2d72165aa9c132d3e2a725b6c2586aaa3a8af9d7a81041 8952 chat_template.jinja
558cd605a6f1c16c73f4918534d122a943e12e754d38567b3b704acc93596965 4094 config.json
e70c136c1b78ddc1fb0905bac8e733a4dc448d4f852a5dd75143fffc70be550e 202 generation_config.json
075eac5fbba3951bc4870c1ac65d684c32e0abb24284201bd27f49df56735963 5328325648 model-00001-of-00003.safetensors
6c99c446987a432beb1f6ef7d6fde6db02682f1ebe1a913760953140e0fa4e47 5354185130 model-00002-of-00003.safetensors
0e267246064a1e635077dd05e22181f5eda44a1fa8a67dcf46c903f858bbe35b 4450532735 model-00003-of-00003.safetensors
a2161f6a6c9f7c434145f97a4a4262a74eb3e16a95088e713754eb108b198511 189789 model.safetensors.index.json
06b9509352d2af50381ab2247e083b80d32d5c0aba91c272ca9ff729b6a0e523 19989325 tokenizer.json
95c557768e6b88a7128befc7bfd3c7de50e5d51af9b8b33a9f4dee0e04f99679 1161 tokenizer_config.json
").contains("QWEN38-PENDING-RELEASE")
◇ Test dflashOrphanScanMatchesThroughTheExtractedResidentPattern() started.
nax_splitk_case M=3 N=2048 K=6144 a_strides=[18432, 6144, 1] weight_strides=[6144, 1] max_abs_diff=0.001953125
◇ Test everyReusedDefinitionFailsClosedOnItsOwn(_:) started.
◇ Test dflashAcquiresAndReleasesTheLocalRunLock() started.
◇ Test dflashRefusesToStartAgainstAnOrphanedModelHoldingWorker() started.
◇ Test dflashImportsAndCallsTheResidentModelScan() started.
◇ Test extractedLockPathMatchesTheSerialLockPath() started.
◇ Test serialStillDefinesTheFunctionsDFlashExtracts() started.
nax_splitk_case M=2 N=1984 K=6144 a_strides=[12288, 6144, 1] weight_strides=[6144, 1] max_abs_diff=0.001953125
nax_splitk_case M=2 N=2048 K=6145 a_strides=[12290, 6145, 1] weight_strides=[6145, 1] max_abs_diff=0.001953125
◇ Test dflashReusesTheSerialLockDefinitionsRatherThanCopyingThem() started.
benchmark.sh: reusing /var/folders/hg/g1_843ts0sv67jyk1mhhc83c0000gn/T/mlxfast-benchmark-script-tests-4A6C33DD-79F8-4734-BFE9-4AB2CE8500FB/weights/ because MLXFAST_SKIP_TRANSFORM=1
benchmark.sh: reusing /var/folders/hg/g1_843ts0sv67jyk1mhhc83c0000gn/T/mlxfast-benchmark-script-tests-B4D5EEDF-CD47-4AAF-BB91-EFABD87FCBAD/weights/ because MLXFAST_SKIP_TRANSFORM=1
◇ Test dflashRunsPastTheGuardWhenNothingIsResident() started.
◇ Test aMissingResidentPatternFailsClosedRatherThanScanningForNothing() started.
benchmark.sh: reusing /var/folders/hg/g1_843ts0sv67jyk1mhhc83c0000gn/T/mlxfast-benchmark-script-tests-7441B72A-D709-47E0-A4B5-C8979F7B11EA/weights/ because MLXFAST_SKIP_TRANSFORM=1
{
  "score": null,
  "passed": true,
  "metrics": {
    "prefill_seconds_per_token": 0.1,
    "decode_seconds_per_token": 2.0,
    "prefill_speedup": 2.0,
    "decode_speedup": 2.0,
    "weights_hash": "fake-weights",
    "weights_file_count": 1,
    "weights_byte_count": 2
  }
}
◇ Test theCustomTop32KernelMatchesArgPartitionIncludingHeavilyTiedRows() started.
◇ Test theTwoDispatchTop32RunsAtItsAuditedSelectionGeometry() started.
➜ Test digestQuantizedMatmulOverVerifyWidth() skipped.
◇ Test theMetallibIsBesideTheTestBundleWhenGatedTestsAreRequested() started.
◇ Test directoryDigestPreservesLegacyDigestWithBoundedUncachedReads() started.
✔ Test benchmarkScoreChecksComponentFloors() passed after 1.643 seconds.
◇ Test theScriptsTheWorkflowInvokesAreExecutable() started.
benchmark.sh: warning: could not find origin/main for local-iterate baseline context.
benchmark.sh: run 'git fetch origin main' and measure the latest tip locally before comparing changes.
◇ Test theCleanupStepRemovesScratchTheStepShellDidNot() started.
◇ Test theCheckoutDoesNotPersistCredentials() started.
◇ Test theWorkflowDoesNotClaimHiddenBytesAreAlwaysRemoved() started.
◇ Test theProbeNeverEchoesObjectBytes() started.
◇ Test probeScratchIsRunScopedAndRemovedWhenTheStepEnds() started.
◇ Test aScratchPathWithoutTheSweptPrefixIsRefused() started.
◇ Test theProbeEndsWithAJanitorResetAndIntegrityAudit() started.
◇ Test theGuardRunsBeforeAnythingTouchesTheCredentials() started.
◇ Test theProbeSweepsAnEarlierRunsAbandonedScratch() started.
◇ Test onlyMainMayDispatchTheProbe() started.
◇ Test theSuppliedCountAgreesWithTheLoopOnEveryAdvertisedInputForm() started.
◇ Test aMissingKeyCountsAsProbed() started.
◇ Test theSuppliedCountIsDerivedFromTheInputAndCatchesADroppedKey() started.
◇ Test theProbeIsDispatchOnly() started.
◇ Test thePrimaryStripAndHeadMergeProduceTheTextModelNamespace() started.
◇ Test aTreeCarryingBothSpellingsIsRefused() started.
◇ Test theTrackScopedBaselineConstantsAreDistinctAndDocumented() started.
◇ Test theQwenWorkflowMirrorsTheTrackScopedGPQAFloor() started.
◇ Test theSharedGPQAFloorStaysAtTheDFlashCalibration() started.
◇ Test "the fused kernel still has the shape this audit measured" started.
◇ Test "the two arithmetic properties the eager chain depends on are still in the source" started.
✔ Test transformRejectsNonMLXCompressedAndGlobalScaleSchemas() passed after 1.747 seconds.
◇ Test theEditableSessionContinuesPastEosWithoutTheOverlay() started.
◇ Test theManifestStillDeclaresTheFixedWindow() started.
◇ Test theDocumentedCeilingMatchesTheManifest() started.
◇ Test theTrustedDriverStillOwnsTheWindowLengthByCount() started.
✔ Test rankedJobRunsPublicBehaviorGateBeforeHiddenGates() passed after 1.798 seconds.
➜ Test countsTheChunkSurcharge() skipped: "set MLXFAST_E57_DISPATCH_COUNT=1 to run the GPU probe"
◇ Test theHeadManifestNamesThePinnedRepositoryAndRevision() started.
◇ Test theHeadManifestIsWellFormed() started.
◇ Test theWorkflowShapePinsMatchTheCheckedInManifest() started.
◇ Test theHeadTensorCountIsOneConstant() started.
◇ Test forceSignedBypassesAWorkingAWSCLI() started.
◇ Test noAttemptFollowsRedirects() started.
◇ Test everyUploadRetryIsSignedAfresh() started.
◇ Test unsignableObjectKeysAreRefusedBeforeAnyRequest() started.
◇ Test everyRetryIsSignedAfresh() started.
◇ Test everyAttemptBoundsConnectAndTotalTime() started.
◇ Test theEmittedRequestMatchesWhatWasSigned() started.
◇ Test aStalledAttemptTimesOutAndIsRetriedRatherThanSucceedingEmpty() started.
◇ Test theOutputFileIsTruncatedBetweenAttempts() started.
✔ Test checkpointIndexToolsReturnsUniqueSortedSafetensorShards() passed after 1.859 seconds.
◇ Test noAttemptDelegatesRetryToCurl() started.
◇ Test thePayloadHashIsTheContentDigestForExoticInputPaths() started.
benchmark.sh: regenerating weights with Swift transform
◇ Test everyR2SecretReferenceLivesInTheEnvironmentBoundJob() started.
◇ Test dflashBindsThePrivateMaterialEnvironment() started.
◇ Test aGatesOnlyGoldenPreparationSkipsTheTimedGolden() started.
◇ Test aRankedSelectionSamplesFromAPopulatedPool() started.
◇ Test aRankedGoldenPreparationStillPinsTheTimedGolden() started.
benchmark.sh: reusing /var/folders/hg/g1_843ts0sv67jyk1mhhc83c0000gn/T/mlxfast-benchmark-script-tests-E3B62168-4A4E-4432-AA49-3552E44AF6CB/weights/ because MLXFAST_SKIP_TRANSFORM=1
benchmark.sh: reusing /var/folders/hg/g1_843ts0sv67jyk1mhhc83c0000gn/T/mlxfast-benchmark-script-tests-D525953A-A8BD-4653-85D3-733025FFF602/weights/ because MLXFAST_SKIP_TRANSFORM=1
◇ Test aRankedRunStillSelectsATimedTargetAndTimes() started.
◇ Test aCleanDryRunReachesTheParityGateAndSkipsPoolSelection() started.
◇ Test theCorrectnessGoldenIsPinnedIndependentlyOfThePool() started.
◇ Test aRankedSelectionStillFailsClosedOnAnUnusablePool() started.
◇ Test aRankedGoldenPreparationRefusesAnEmptySelection() started.
◇ Test theHeadOverrideReachesTheCandidateLegOnly() started.
◇ Test theHeadBooleansAreRecordedRatherThanAsserted() started.
◇ Test localDFlashScriptsNameNoRetiredMTPSurface() started.
◇ Test localDFlashRunnerReusesTheCorrectnessGateAndTheCoolDownEntrypoint() started.
◇ Test localDFlashScriptsInvokeOnlySubcommandsAndFlagsTheCLIDeclares() started.
◇ Test runbookExistsAndCoversTheStepsTheWorkflowsCite() started.
◇ Test runbookRecordsTheLimitsBeingAcceptedAtGoLive() started.
◇ Test runbookDescribesMedianOfEightScoring() started.
{
  "score": null,
  "passed": true,
  "metrics": {
    "weights_hash": "fake-weights",
    "weights_file_count": 1,
    "weights_byte_count": 2
  }
}
{
  "score": null,
  "passed": true,
  "metrics": {
    "weights_hash": "fake-weights",
    "weights_file_count": 1,
    "weights_byte_count": 2
  }
}
✘ Test contestantDocsCommandBlocksKeepTheDependencyGraphFrozen() recorded an issue at SetupScriptTests.swift:2386:9: Expectation failed: (doc → "<!-- SENPAI-CAMPAIGN-BEGIN -->
# Qwen 3.8 Native-MTP Challenge Agent Guide

This repository has one ranked task: make Qwen 3.8 27B native-MTP decode
faster on Apple Silicon without changing a single emitted token. The ranked
track is `qwen3.8-27b-mtp-v1`.

Retained files from earlier tracks are historical prior art only. Their
commands, model geometry, editable surface, correctness rules, scoring, and
runner configuration do not apply here. Operate this repository only as the
Qwen track named above.

## Senpai Campaign

For coordinated research in `morganmcg1/qwen38-challenge_senpai`, read
[`senpai/program.md`](senpai/program.md) and
[`senpai/experiment-runbook.md`](senpai/experiment-runbook.md) before assigning
or running experiments. The campaign ledger and exact frontier pins are in
[`senpai/campaign-ledger.md`](senpai/campaign-ledger.md) and
[`senpai/frontier-state.json`](senpai/frontier-state.json).

The intended remotes are:

```text
origin    https://github.com/morganmcg1/qwen38-challenge_senpai.git
upstream  https://github.com/Layr-Labs/qwen-3.8-mtp-challenge
```

`upstream` must have push URL `DISABLED`. Never run `yukon sync`,
`yukon sync --harness-only`, or `yukon reset` in this maintained checkout;
those commands are for plain solver checkouts and can reset campaign history or
repoint `origin`. Use the repository-local `sync-organizer-frontier` skill for
organizer policy and promoted-snapshot refreshes.

Record the exact campaign `BASE_SHA` and organizer `UPSTREAM_SHA` for every
experiment. Preserve unrelated user work, campaign files, and dirty-worktree
changes. Generated weights, caches, score files, downloaded artifacts, and
hidden material never belong in Git.

## Authority Order

This repository still contains historical comments and bring-up prose. Resolve
contract questions from the live enforcing sources in this order:

1. [`benchmark.json`](benchmark.json): track identity, commands, editable
   paths, optional paths, budget policy, and scoring fields.
2. [`fixtures/qwen3_8_27b_mtp_track.json`](fixtures/qwen3_8_27b_mtp_track.json):
   artifact pins, timed prompt pool, fidelity semantics, and calibration.
3. [`.github/workflows/qwen-mtp-ranked-benchmark.yml`](.github/workflows/qwen-mtp-ranked-benchmark.yml):
   ranked execution, runner label, and enforced environment values.
4. [`docs/qwen-mtp-editable-surface.md`](docs/qwen-mtp-editable-surface.md):
   rationale for the editable/trusted boundary.
5. [`TASK.md`](TASK.md): concise participant-facing summary.

For mutable solver behavior, inspect the current `BASE_SHA` source and
`mtp-head.manifest.json`. Narrative descriptions of the original starting tree
do not override the current implementation. If authorities disagree, stop and
identify the exact enforcing site before editing or assigning work.

## Mission And Score

The official score is a decode-only paired speedup anchored at serial `1.0`:

```text
for each hidden prompt p:
  raw_p = mean(pinned-baseline depth-0 serial seconds/token)
          / mean(candidate seconds/token)

published score = median(raw_1 ... raw_8)
```

The ranked legs come from different workspaces. The trusted, prebuilt baseline
workspace produces the serial numerator; the submitted candidate workspace
produces the MTP denominator. Candidate-editable code cannot change the ranked
serial numerator, so any compliant reduction in candidate MTP time improves
the affected `raw_p`. Never subtract a local serial-path share such as
`psi_serial` when pricing official value. The local same-build harness has a
different causal graph and must be labeled separately. Run
`senpai/verify-ranked-score-boundary.sh` before using a derived score model.

All eight hidden prompts are measured. Because eight is even, the median is the
mean of the two central ordered values. There is no no-op normalization and no
prompt lottery. The published median must be at least `0.90` and no greater
than the `5.0` plausibility ceiling.

Each ranked leg processes a 512-token seed followed by 512 parent-counted
decode tokens, and the seed work is charged in the timed leg. Prefill is not a
separate score, but work removed from the charged seed still improves the
result. The ranked run uses one thermally gated serial/candidate pair per
prompt unless the trusted contract changes.

The trusted parent offers a per-round ceiling. Candidate policy may choose
from 0 through 8 drafts independently on every round. Drafting nothing is
legal and degenerates to serial decode; it is a control, not the optimization
goal. Acceptance rate alone is not the objective: deeper drafting wins only
when extra committed tokens repay head, target-verification, rejected-tail,
and state-management costs across varied prompts.

The organizer's original calibrated tree scored about `0.994`. That is a
historical starting-line measurement, not the current campaign frontier. Read
the live Yukon promoted row and `senpai/frontier-state.json` before comparing or
submitting work.

## Target And Current Scored Path

The fixed target is the organizer's MLX 4-bit affine/group-64 conversion of
Qwen 3.8 27B:

```text
backbone  EigenLabs/Qwen3.8-27B-4bit
          @ eda45ab47f465d08d6558f0353a2346e2eb9d5b3
upstream  Qwen/Qwen3.8-27B
          @ 1d4bf0f2ff6012fd82039f2fa52739d0dd7c60c0
```

The organizer-pinned default proposal head is the separate bf16 MTP
artifact. A candidate may instead declare an immutable remote or in-branch head
through `mtp-head.manifest.json`; inspect that file at `BASE_SHA` for current
head provenance. The target checkpoint remains fixed regardless of the head.

The Qwen tower is a dense hybrid architecture:

- 64 layers: 48 Gated DeltaNet recurrent layers and 16 full-attention layers;
- hidden size 5,120 and vocabulary 248,320;
- full attention with 24 query heads, 4 KV heads, and head dimension 256;
- affine 4-bit/group-64 backbone weights and dense MLPs.

The `Qwen35*` source names and `qwen3_5_text` configuration label describe the
architecture family embedded in the Qwen 3.8 artifact; they do not mean this
repository targets Qwen 3.5 or 3.6.

The scored worker loads the transformed checkpoint through `LLMModelFactory`,
attaches the selected head, warms legal shapes, and serves rounds through
[`Sources/MLXFastModel/Qwen36MTPBlockSession.swift`](Sources/MLXFastModel/Qwen36MTPBlockSession.swift).
A round conceptually:

1. commits the pending primary token;
2. lets candidate policy choose 0...8 drafts within the offered maximum;
3. runs the MTP head chain to propose them;
4. verifies `[primary] + drafts` with the fixed target in a batched forward;
5. accepts the longest correct prefix;
6. keeps, restores, replays, or repairs target and head state as required;
7. reports actual rows and exact top-two evidence to the trusted parent.

The timed target model is the vendored `Qwen35TextModel`/`Qwen35Model` in
[`Vendor/mlx-swift-lm/Libraries/MLXLLM/Models/Qwen35.swift`](Vendor/mlx-swift-lm/Libraries/MLXLLM/Models/Qwen35.swift),
together with the editable MTP session. Directory-wide editability does not
prove reachability: for example, the separate `Qwen35FastEngine` path is not
the MTP worker's current target path. Trace the call path before optimizing.

### Current frontier is already optimized

Do not treat historical defaults as current behavior or rediscover mechanisms
already in the promoted snapshot. At this guide's 2026-08-16 rewrite:

- `Qwen36MTPBlockSession.init` installs an adaptive cost-model policy; the
  nearby K=1 property initializer and constant-depth comments are historical
  fallback/bring-up text, not the instantiated policy;
- the campaign `mtp-head.manifest.json` declares a 4-bit/group-64 proposal
  head rather than using the organizer-pinned bf16 default;
- persistent head history, K/V-only history flush, device-resident drafting,
  exact rollback/replay, and scored-shape warmups are present;
- target verification has exact multi-row affine4 QMV sharing, fused GDN and
  full-attention preparation, and an exact two-stage top-two reducer;
- proposal selection already uses a compact vocabulary and a custom selector;
- the layer loop already carries an `asyncEval` ladder for narrow widths.

These are mutable solver facts. Recheck source after every frontier sync. See
[`senpai/laguna-to-qwen-speedup-map.md`](senpai/laguna-to-qwen-speedup-map.md)
and the campaign novelty ledger before assigning overlapping work.

## Editable And Trusted Boundary

The ownership rule is:

> Candidate code may propose and execute the model, but the trusted parent owns
> correctness, measurement, and final ledger authority.

The editable session necessarily assembles target verify forwards, walks the
candidate-side accepted prefix, rolls state back, and produces evidence for the
parent. None of those self-reports is authoritative: the parent independently
checks the serial token stream, round structure, rows, and accounting.

`benchmark.json` `editablePaths` is the only machine-readable path authority.
Its current 89 entries form five practical groups:

- optional `mtp-head.manifest.json` and `mtp-head/` candidate-head content;
- `Sources/MLXFastModel/`, including the runtime and full MTP apparatus;
- `Sources/MLXFastTransform/` offline checkpoint transform;
- the exact listed Qwen model and `MLXLMCommon` support files;
- the exact listed MLX/Metal kernel sources and generated twins.

Do not infer that an adjacent file is editable. Check the manifest at
`BASE_SHA` before implementation. Submission archives use replace semantics
over `editablePaths`; only the two MTP-head paths are optional. A support-file
change outside the surface can make a local tree faster while uploading
nothing useful.

Trusted and not submitted include:

- target weights, transform contract, tokenizer, fixtures, and goldens;
- trusted driver, accept audit, row accounting, score emission, and worker
  protocol;
- timing, telemetry, thermal gate, workflows, and `.github/scripts/`;
- `benchmark.json`, `Package.swift`, and `Package.resolved`;
- tests, docs, local benchmark scripts, and other paths not named by the
  manifest.

`Sources/MLXFastModel/Qwen36MTPReferenceSession.swift` is an important mixed
case: it is editable through the directory-wide allowance even though it serves
rejected-tail reference replay. It runs after timing and cannot define emitted
tokens. Never route its reference work or any golden-derived information into
the timed path, proposal policy, or emitted stream.

The source budget is 3,000,000 bytes total, 524,288 bytes per file, and
262,144 bytes of candidate growth. `mtp-head/` is exempt from the source budget
and instead has a declared digest, byte count, and 2 GiB cap. Run the campaign
scope and budget checks before expensive validation.

## Correctness And Prohibited Strategies

Correctness is a hard gate. Every emitted token must equal the hidden serial
trajectory. The round journal must be structurally sound, effective draft
counts must match what occurred, and the reference-checked row ledger must
close. There is no near-tie allowance on this track.

The ranked pipeline also runs the public drift tripwire, hidden teacher-forced
base, anchor, free-run, behavior and GPQA gates, GPQA TTFT guardrail, semantic
judge, transform verification, static review, and integrity checks. Any failed
gate produces no promotable score.

The native MTP path inside the declared editable surface is the only sanctioned
future-token mechanism. Do not add:

- prompt lookup, n-gram, suffix, or token-history drafting;
- same-target lookahead or unsanctioned future-logit/KV computation;
- request-keyed replay caches whose only useful hit is a repeated benchmark
  request;
- cross-request future state, hidden-prompt specialization, or GPQA shortcuts;
- degraded or partial target forwards, unevaluated verify rows, omitted exact
  final-logit/top-two work for emitted rows, or fabricated top-two, row,
  acceptance, or ledger evidence;
- an undeclared auxiliary proposal model/head or candidate-selected target;
- phase, reference-leg, prompt-pool, or baseline detection used to change the
  work performed;
- logical rollback that leaves rejected cache/state rows reachable, or merely
  decrements an offset without trimming or overwriting rejected physical rows;
- timing shortcuts, protocol injection, network access, or filesystem
  exfiltration.

Input-independent weight/kernel/shape tables and ordinary within-request cache
reuse are legal when they preserve the contract. A candidate head may improve
proposal quality and cost; it cannot redefine the target answer.

Public fixtures were generated on M5. On another Apple GPU, compare an
unchanged `BASE_SHA` at the same token position before diagnosing a near-tie
local mismatch. That diagnostic does not relax official exactness.

## Kernel And Swift Rules

The vendored MLX package has two effective source forms:

- A family with an `mlx-generated/*.cpp` twin is JIT-compiled from the source
  string embedded in that generated C++ file. Update the readable
  `.metal`/`.h` source and the runtime-effective twin together.
- AOT-only families are loaded from `mlx.metallib`; rebuild it with
  `tools/build-mlx-metallib.sh` after relevant edits.
- `_nax` variants are selected on ranked M5 hardware and are first-class
  targets, not optional follow-up work.

Run `python3 research/twin_audit.py` after JIT-source edits. Before any kernel
experiment, prove the scored shape, dispatch family, source form, and M5
variant. Reduction order, precision, BF16 cast boundaries, packing, recurrent
state, cache layout, and width-dependent dispatch are correctness-sensitive.

For direct Swift commands, preserve the frozen dependency graph:

```bash
swift test --force-resolved-versions
MLXFAST_RUN_MLX_RUNTIME_TESTS=1 swift test --force-resolved-versions
swift build -c release --force-resolved-versions
tools/build-mlx-metallib.sh
```

A bare Swift build may write `.build/release` without rebuilding the scored
`.build-worker/release/mlxfast-runtime-worker`. Use the Qwen benchmark wrapper
for end-to-end work. Never run `swift package resolve` or `swift package
update`; if `Package.resolved` changes, restore it rather than accepting a new
dependency graph.

## Local Workflow And Measurement

Use only the Qwen entrypoints:

```bash
./setup.sh && ./setup-qwen-mtp.sh
./benchmark-qwen-mtp.sh --local-iterate
./benchmark-qwen-mtp.sh --local-submit
```

`--local-iterate` defaults to 64 decode tokens and `--local-submit` to 128.
They use one public fixture and cannot reproduce the ranked eight-prompt,
512-token result. Treat local scores as directional.

The local serial and candidate legs use the same local build. A general target
runtime or kernel improvement can speed both sides and cancel in the local
ratio, even though it counts officially because the ranked serial numerator
comes from a pinned baseline workspace. Compare absolute candidate
seconds/token against a fresh, same-host `BASE_SHA` run as well as the local
serial/MTP ratio.

Label every measurement and formula `harness=local` or `harness=ranked`. Use
matched absolute candidate MTP time as the primary local signal for broad
runtime and kernel changes; the local ratio may contain same-build cancellation
that does not exist officially.

The AWS Mac fleet assigns one physical Mac to each student. The advisor shares
only Edward's Mac; Alphonse, Thorfinn, and Askeladd each run on another Mac.
Locks, device occupancy, and thermal state are host-local. Run independent jobs
concurrently across distinct Macs and coordinate only co-located roles. Verify
the live placement rather than inferring a fleet-wide queue.

Run only one model-holding process at a time. The wrapper takes a per-user lock
and checks for orphaned workers; direct model commands do not. Inspect reported
PIDs before killing anything, and never disable the guard to work around a
legitimate concurrent run.

The wrapper cools the GPU before each resident measurement. Waiting at the 40C
gate is normal, not a hang, and the gate remains the default. For local timed
arms only, `MLXFAST_LOCAL_COOL_GATE=0` is permitted when all arms are
ABBA-counterbalanced within one session, every arm records entry and exit GPU
temperature with the entry spread reported beside the effect, and the result
retains `cool_gate_passed_real_gate=false` plus
`gate_qualified_for_timing=false`. Such evidence is explicitly ungated and
directional; compare it within that counterbalanced session, not as though it
were a gate-qualified or official result. Never fabricate a temperature source,
edit the gate constant, or bypass the process lock. Keep host, power, thermal
state, memory profile, head provenance, toolchain, token window, and frontier
SHA matched between base and candidate. Ranked authority is the self-hosted M5
runner labeled `m5-qwen38-27b-mtp`; non-M5 timing is directional only after
confirming the same kernel family executes.

## Experiment Discipline

One experiment should answer one causal question. For each arm:

1. Record an identity tuple: `BASE_SHA`, `UPSTREAM_SHA`, candidate commit and
   built-worker fingerprint, submitted-surface/twin/metallib digests, head
   digest, host/instance/chip, toolchain, memory and thermal mode, token window,
   fixture/reference source, exact cell/shape/dispatch, and harness.
2. State the official causal path, expected direction, minimum useful effect,
   and stop rule. Stress-test any score equation at zero and both signs.
3. Prove reachability from the current native-MTP worker and name the cost
   removed. Never transfer a constant or fitted model from another tree or cell
   without replay.
4. Check editable scope, byte budget, and the ranked score-boundary tripwire.
5. Measure an unchanged same-host baseline and save its artifact outside the
   submission surface.
6. Implement one mechanism. Before long timing, run the cheapest gate capable
   of falsifying the changed boundary. Numerical and reduction-order edits need
   actual floating-point touched-cell coverage, a positive control, and the
   shortest end-to-end exact-token/row-ledger run; integer-only or argmax checks
   are insufficient.
7. Measure the candidate under the same identity tuple. Start with the smallest
   decisive timing run; replicate only when noise could change the decision.
8. Compare absolute candidate time, paired ratio, draft/accept telemetry,
   rollback, and block latency where relevant. Cost-weight per-cell effects
   before aggregating them.
9. Reject, revise, or advance using the predeclared stop rule. Label every
   inference, interpolation, and extrapolation; never substitute M=8 for M=9,
   M4 for M5, local for ranked, or an abandoned dispatch table for the current
   tree.
10. Record positive, negative, invalid, and ambiguous results in the campaign
    ledger so later agents do not repeat them.

A microbenchmark is useful only when it bounds a named end-to-end cost.
Dispatch-count reduction is not a result unless it removes bytes, arithmetic,
or a dependency. A local pass on one easy prompt does not prove an adaptive
policy is robust across the hidden pool. Compose only independently measured
winners.

Promising current cost centers include draft scheduling, proposal-head cost and
quality, persistent head history, target verify width, GDN recurrence and
rollback, full-attention/GQA cache work, affine4 projection and vocabulary
readout, MLX scheduling/materialization, warmup, and transformed weight
metadata. Start from current profiles and the novelty ledger, not this list.

## Submission And Promotion

Yukon manages accounts and official submissions. It does not run the local
pre-submit command for you. In the maintained campaign checkout, use the guard:

```bash
export PATH="${HOME}/.local/bin:${PATH}"
yukon submissions --all
senpai/submit-official.sh "$BASE_SHA" \
  --model "senpai" \
  --note-file submission-note.md
yukon submissions
```

Use the exact lowercase campaign attribution `senpai`; record the underlying
models, effort levels, and agent harnesses in the public note body.

Immediately before submission, compare the highest-scoring live `promoted` row
with `senpai/frontier-state.json`. If the receipt, source ref, or score differs,
sync the organizer/promoted frontier, replay the candidate, and remeasure it.
Do not infer promotion from a `Validate submission` commit or a branch name.

Submit from a clean, committed candidate after `--local-submit`, exact diff
inspection, scope/budget checks, and generated-twin audit where relevant. Never
put credentials in notes, logs, commits, or agent messages. The ranked resource
is serialized; do not dispatch duplicate submissions to mine timing noise.

Only the official M5 result can promote a candidate. A green rejected receipt
may simply have failed to beat the current frontier; read its gates and metrics
before classifying it as incorrect.

## Historical Prior Art

Laguna work is useful only as a source of hypotheses about shared MLX concepts.
The curated transfer audit is
[`senpai/laguna-to-qwen-speedup-map.md`](senpai/laguna-to-qwen-speedup-map.md).
Do not run Laguna/DFlash setup or benchmark scripts, use their score formulas,
or copy their MoE/NVFP4 kernels without a Qwen-specific call-path and geometry
proof.
<!-- SENPAI-CAMPAIGN-END -->
").contains("git checkout -- Package.resolved")
✘ Test submissionStaticReviewPromptCoversMeasurementStructureExploitation() failed after 2.583 seconds with 11 issues.
↳ // Behavioral pin for run-submission-static-review.sh's diff-only mode, run
↳ // against a throwaway git repo. Every guard case exits before any network
↳ // call (a real curl attempt with the dummy key would fail the script); the
↳ // happy path shims curl on PATH and asserts the judge request contains only
↳ // the changed editable file, never the unchanged suspicious baseline file.
E28_FUSED_RMSNORM ["control_normed_differing_rate": 7.62939453125e-05, "axis_size": 5120, "control_normed_max_ulp": 1, "rows_per_family": 128, "control_lsize": 512, "declared_n_reads": 4, "control_normed_differing_cells": 50, "mlx_rms_looped_limit": 4096, "total_normed_differing_cells": 0, "control_residual_differing_cells": 0, "families": [["normed_differing_cells": 0, "family": "normal", "normed_max_ulp": 0, "residual_differing_cells": 0, "normed_differing_rate": 0.0, "cells": 655360], ["family": "outlier", "normed_differing_cells": 0, "normed_differing_rate": 0.0, "normed_max_ulp": 0, "residual_differing_cells": 0, "cells": 655360], ["residual_differing_cells": 0, "family": "tied", "normed_differing_rate": 0.0, "normed_differing_cells": 0, "normed_max_ulp": 0, "cells": 655360], ["normed_differing_cells": 0, "family": "large", "normed_differing_rate": 0.0, "normed_max_ulp": 0, "cells": 655360, "residual_differing_cells": 0]], "declared_lsize": 1024, "eps": 9.999999974752427e-07, "max_normed_ulp": 0, "total_residual_differing_cells": 0, "kernel": "qwen35_fused_residual_rms_norm", "schema": "e28.fused_residual_rmsnorm.v1"]
✔ Test ngramSelfSimilaritySeparatesRecentPredictionFromOptimisticUpperBound() passed after 2.709 seconds.
/var/folders/hg/g1_843ts0sv67jyk1mhhc83c0000gn/T/mlxfast-benchmark-script-tests-EAAA576D-9809-4713-9ED7-446FFCEA2B79/correctness_golden_512_2048.json: private prompt, golden, or model files must not be uploaded or cached
✔ Test loadGoldenFixtureRejectsDuplicateLayeredCorrectnessNames() passed after 2.721 seconds.
✔ Test setupDerivesTheMetadataDownloadListFromTheSelectedManifest() passed after 2.730 seconds.
✘ Test startupMemoryPolicyKeepsRanked128GiBProfile() recorded an issue at RuntimeStartupMemoryPolicyTests.swift:83:5: Expectation failed: (policy.maxMegabytesPerCommandBuffer → 512) == 320
✘ Test startupMemoryPolicyKeepsRanked128GiBProfile() recorded an issue at RuntimeStartupMemoryPolicyTests.swift:84:5: Expectation failed: (policy.maxOperationsPerCommandBuffer → 50) == 128
◇ Test theByteBudgetChargesExemptPathsSeparately() started.
◇ Test onlyTheHeadWeightsPathIsExemptFromTheSourceByteBudget() started.
◇ Test theHeadPathsAreOptionalAndEverySourcePathIsNot() started.
◇ Test noOpensslDigestParsesTheTextOutputFormat() started.
◇ Test theCanonicalRequestMatchesAnIndependentSigV4Implementation() started.
◇ Test theWholeSigningChainReproducesOnePinnedEndToEndSignature() started.
◇ Test theStringToSignMatchesTheAWSDocumentationWorkedExample() started.
✔ Test setupDownloadHeartbeatCountsInProgressPartialBytes() passed after 2.801 seconds.
◇ Test theScriptSigningChainReproducesThePinnedSignature() started.
◇ Test "real transformed Qwen checkpoint matches library and custom paths" started.
✔ Test mlxTensorBridgeCreatesArrayWhenRuntimeTestsAreEnabled() passed after 2.848 seconds.
✔ Test qwen35PreflightFixtureHasExactPinnedTensorInventory() passed after 2.863 seconds.
✔ Test safetensorsCopySubsetRejectsSourceAsDestinationWithoutMutatingIt() passed after 2.893 seconds.
✘ Test participantDocsExposeDefaultCLIInstallDirectory() failed after 2.917 seconds with 2 issues.
✔ Test quantizedSwitchLinearForwardsNVFP4GatherSemanticsWhenRuntimeTestsAreEnabled() passed after 2.919 seconds.
✔ Test failedScoreRedactsCorrectnessTokenMismatchByDefault() passed after 2.924 seconds.
benchmark.sh: reusing /var/folders/hg/g1_843ts0sv67jyk1mhhc83c0000gn/T/mlxfast-benchmark-script-tests-7AF38CA9-9BDC-479C-AFDA-F4969D8F1148/weights/ because MLXFAST_SKIP_TRANSFORM=1
benchmark.sh: warning: could not find origin/main for local-iterate baseline context.
benchmark.sh: run 'git fetch origin main' and measure the latest tip locally before comparing changes.
benchmark.sh: reusing /var/folders/hg/g1_843ts0sv67jyk1mhhc83c0000gn/T/mlxfast-benchmark-script-tests-CA4B8B28-D120-4107-8CEB-3DBF21AE8B6F/weights/ because MLXFAST_SKIP_TRANSFORM=1
✔ Test localIterateScorePublishesCLIUsableEstimatedScore() passed after 3.349 seconds.
✔ Test workerStderrDrainCapsUnterminatedLine() passed after 3.349 seconds.
benchmark.sh: reusing /var/folders/hg/g1_843ts0sv67jyk1mhhc83c0000gn/T/mlxfast-benchmark-script-tests-721995AB-4578-48D0-A68E-E10A2C4E6F70/weights/ because MLXFAST_SKIP_TRANSFORM=1
✔ Test cliSupportsHiddenGPQAGateAttachment() passed after 3.431 seconds.
◇ Test transformAndAttachRouteThroughTheParentToolSandbox() started.
◇ Test parentToolSandboxProfileDeniesNetworkForkExecAndResolver() started.
◇ Test parentToolSandboxIsArmedFailClosedAndReentryGuarded() started.
◇ Test runtimeWorkerAllowlistDropsSSHAgentSocket() started.
◇ Test runtimeWorkerFallbackProfileDeniesResolverMachLookup() started.
➜ Test theSinglerowAffine2ArmAgreesWithTheGenericArmOnTheDeclaredHead() skipped.
◇ Test theTrustedMaximumIsTheSameEightEverywhere() started.
◇ Test theLedgerClosesOnActualDraftsRatherThanTheOffer() started.
◇ Test theShippedDraftPolicyKeepsAnUnmodifiedTreeAtTwo() started.
◇ Test effectiveDepthIsSummarisedFromTheParentsOwnJournal() started.
◇ Test theNonDraftingRefusalIsGoneButItsKindSurvives() started.
◇ Test theSerialControlMayNeverDraft() started.
➜ Test pricesOneDispatch() skipped: "set MLXFAST_E58_DISPATCH_STORM=1 to run the GPU dispatch storm"
{
  "score": 0.998,
  "passed": false,
  "metrics": {
    "error": "local-iterate teacher-forced token mismatch",
    "weights_hash": "fake-weights",
    "weights_file_count": 1,
    "weights_byte_count": 2
  }
}
◇ Test offlineRunnerForwardsSignalReceivedBeforeCommandLaunch() started.
◇ Test theRowLedgerClosesOverAWholeWindow() started.
◇ Test fullAcceptanceAndZeroAcceptanceAreBothReachable() started.
◇ Test onlyATargetMismatchEndsTheAcceptedPrefix() started.
◇ Test theExtraVerifyRowNeverChangesTheCount() started.
◇ Test eosInsideAnAcceptedPrefixDoesNotEndTheWindow() started.
◇ Test theCountIsTheLongestCommonPrefixAndEosIsJustAToken() started.
◇ Test theSequencePositionComesFromATrimmableCache() started.
◇ Test rollbackClearsTheVendoredDepthOneRollbackState() started.
◇ Test rollbackDoesNothingToACacheAlreadyAtTheBase() started.
◇ Test rollbackTrimsTheWholeVerifyWindowAndNeverTrimsRecurrentCaches() started.
◇ Test theSnapshotSurvivesAnInPlaceWriteToTheLiveCache() started.
◇ Test aStackWithNoTrimmableCacheReportsAnImpossibleOffset() started.
{
  "score": null,
  "passed": true,
  "metrics": {
    "weights_hash": "fake-weights",
    "weights_file_count": 1,
    "weights_byte_count": 2
  }
}
◇ Test theDepthFlagHasACanonicalSpellingAndARefusedAmbiguity() started.
◇ Test theTwoHeadBooleansAreOnePredicate() started.
◇ Test thePayloadCarriesEveryFieldTheBoxWrapperReads() started.
◇ Test theArrayAndTheTrioAgreeIncludingOnEvenLengthWindows() started.
◇ Test rowsPerRoundMatchesTheWrapperEquation() started.
◇ Test theBlockArrayLengthIsTheRoundCount() started.
◇ Test theRankedGateAssertsOnlyFieldsTheReportEmits() started.
◇ Test aLedgerThatLeavesTheRejectedTailUnpricedIsRejected() started.
◇ Test aLedgerDeclaringMoreRowsThanTheTrustedMaximumAllowsIsRejected() started.
◇ Test excludingTheFirstBlockRescuesAWarmupAndStillCatchesAStall() started.
◇ Test parityAllOKRequiresBothExactnessAndACloseLedger() started.
◇ Test theTimedPayloadSatisfiesBothStallGuardrailRoutes() started.
◇ Test thePinnedReferenceDirectoryIsProvenanceNotAWeightsSource() started.
◇ Test theControlDepthIsOneConstantAcrossEveryConsumer() started.
◇ Test thePayloadKeyExtractorFindsRealKeys() started.
◇ Test bothVerbsAreDeclaredInTheShapeTheCallersGrepFor() started.
◇ Test theSerialControlIsDepthZeroAndItsLedgerCloses() started.
◇ Test theRowAccountingEquationsCloseOnAnHonestLedger() started.
✔ Test traceProtocolCarriesRequestedTopKAndExpectedTokenDiagnostics() passed after 4.028 seconds.
◇ Test redactedCategoryCarriesNoDetailFromTheViolation() started.
◇ Test everyViolationKindRedactsToItsOwnCategory() started.
✔ Test coolGateWarnsOnceWhenGpuTelemetryLooksImplausible() passed after 4.098 seconds.
◇ Test nvfp4DatatypeHelpersStayInAOTAndJITSources() started.
✔ Test residentModelGuardPatternCoversTheDFlashSubcommands() passed after 4.122 seconds.
✔ Test nonWorkerBenchmarkRejectsBehaviorGatesBecauseTTFTRequiresWorker() passed after 4.122 seconds.
◇ Test theStockTreeMustClearTheSubmissionFloor() started.
◇ Test theSeedFetchRefusesAnythingThatIsNotAProseSeedPlan() started.
◇ Test generationRunsFromThePinnedBaseline() started.
◇ Test theScriptsTheWorkflowInvokesAreExecutable() started.
◇ Test pinsComeFromTheReDownloadedObjectAndNotTheLocalFile() started.
◇ Test everyInterlockArmRefusesAndEmitsNoOutputs() started.
◇ Test onlyMainMayDispatchProvisioning() started.
◇ Test everyGoldenVerificationArmRefuses() started.
◇ Test provisioningBindsTheSameEnvironmentAsTheRankedJob() started.
◇ Test theGuardRefusesMaterialItCannotMeasure() started.
◇ Test theGuardAcceptsProse() started.
◇ Test theGuardRejectsAmendmentTensMeasuredDegenerateProfile() started.
◇ Test theGenerationArgvNeverCarriesSeedGenerate() started.
◇ Test theThresholdsAreNotEnvironmentOverridable() started.
◇ Test aMissingSeedNeverFallsBackToSeedGenerate() started.
◇ Test theProvisioningJobIsDispatchOnly() started.
◇ Test provisioningPublishesNoScoreAndTouchesNoRankedState() started.
benchmark.sh: reusing /var/folders/hg/g1_843ts0sv67jyk1mhhc83c0000gn/T/mlxfast-benchmark-script-tests-7AF38CA9-9BDC-479C-AFDA-F4969D8F1148/weights/ because MLXFAST_SKIP_TRANSFORM=1
✔ Test writeScorePayloadKeepsTokenStepSeparateFromLayerFailures() passed after 4.228 seconds.
benchmark.sh: reusing /var/folders/hg/g1_843ts0sv67jyk1mhhc83c0000gn/T/mlxfast-benchmark-script-tests-9183DAE5-08F8-4CF8-982F-37EF21DB2A4C/weights/ because MLXFAST_SKIP_TRANSFORM=1
✔ Test benchmarkGoldenOracleFieldsAreValidatedOnLoad() passed after 4.271 seconds.
✔ Test setupRetainsFailedMetallibStateInSynchronousAndParallelModes() passed after 4.272 seconds.
◇ Test trustedWorkerDoesNotCallTheEditableApply() started.
◇ Test startupPolicyApplyStaysInternal() started.
✔ Test transformRejectsReferenceAsOutputWithoutMutatingCheckpoint() passed after 4.293 seconds.
✘ Test theEvenMedianRuleIsTheMeanOfTheTwoCentralValues() recorded an issue at QwenMTPTrackNamingTests.swift:1237:9: Expectation failed: (unmodifiedRaw → 1.0) < 1.0
✘ Test theEvenMedianRuleIsTheMeanOfTheTwoCentralValues() recorded an issue at QwenMTPTrackNamingTests.swift:1238:9: Expectation failed: (abs(unmodifiedRaw - 0.9438) → 0.05620000000000003) < 0.01
✘ Test theEvenMedianRuleIsTheMeanOfTheTwoCentralValues() recorded an issue at QwenMTPTrackNamingTests.swift:1242:9: Expectation failed: (abs(expected - 0.9350424303) → 0.05899663420000001) < 1e-10
✔ Test setupReferenceCacheStampRejectsManifestSwapAfterVerification() passed after 4.306 seconds.
◇ Test theGateRejectsADeletedFixMarker(_:) started.
◇ Test theGateRejectsTheHistoricalOverlayBlob() started.
◇ Test theGatePassesOnTheWorkingTree() started.
◇ Test theGatePassesOnTheCommittedSession() started.
◇ Test theGateRejectsAReintroducedDefectMarker(_:) started.
◇ Test theSerialHiddenGoldenIsScrubbedBeforeTheDFlashGoldenIsInstalled() started.
◇ Test thePublicBehaviorGatePrecedesEveryHiddenDownload() started.
◇ Test theDFlashGoldenIsInstalledBeforeTheDFlashParityGate() started.
✔ Test prefillFailsWhenMoreThan5PercentFast() passed after 4.371 seconds.
✔ Test benchmarkKeepsZeroExpertStreamingSchemaWithoutStreamingMachinery() passed after 4.377 seconds.
✘ Test theSeededCalibrationExpectationMatchesItsRecordedProvenance() recorded an issue at QwenMTPTrackNamingTests.swift:1292:9: Expectation failed: (status → "measured_qwen38_cutover_2026_08_14").contains("seeded")
✘ Test theSeededCalibrationExpectationMatchesItsRecordedProvenance() recorded an issue at QwenMTPTrackNamingTests.swift:1293:9: Expectation failed: (status → "measured_qwen38_cutover_2026_08_14").contains("pending")
◇ Test theTwoBitSinglerowArmIsStillGatedOnExactlyTheDeclaredHeadWidth() started.
◇ Test rowSlicesOfTheHeadStillFallThroughToTheGenericArm() started.
✔ Test setupReferenceCacheStampRejectsMutationAfterHashBeforePublication() passed after 4.548 seconds.
✔ Test lagunaConfigRejectsBehaviorChangingXSMutations() passed after 4.556 seconds.
benchmark.sh: reusing /var/folders/hg/g1_843ts0sv67jyk1mhhc83c0000gn/T/mlxfast-benchmark-script-tests-D92042F7-B446-411E-926C-ED434511EC5C/weights/ because MLXFAST_SKIP_TRANSFORM=1
◇ Test everyPoolEntryKeyMatchesWhereItWasUploaded() started.
◇ Test theCorrectnessGoldenKeyMatchesWhereItWasUploaded() started.
◇ Test provenanceCarriesTheDeclaredSource() started.
◇ Test anAbsentDeclarationResolvesToThePinnedHead() started.
◇ Test theCheckedInDeclarationSelectsThePinnedHead() started.
◇ Test abrokenDeclarationIsARefusalAndNeverFallsBackToPinned() started.
◇ Test theHeadTreeDigestIsStableOrderIndependentAndContentSensitive() started.
◇ Test thelegalDeclarationShapesParse() started.
{
  "score": 0.998,
  "passed": false,
  "metrics": {
    "error": "weights digest failed",
    "weights_hash": "fake-weights",
    "weights_file_count": 1,
    "weights_byte_count": 2
  }
}
✔ Test hashWeightsDirectoryIsIndependentOfWeightsPathButSensitiveToContent() passed after 4.793 seconds.
✔ Test attachDerivedBenchmarkOracleReproducesTheSerialEraPrecedentRule() passed after 4.791 seconds.
✔ Test runtimeWorkerPinnedConfigurationRejectsAlternateQuantizationSchemas() passed after 4.791 seconds.
✔ Test runtimeWorkerClientCloseEscalatesPastIgnoredTerminate() passed after 4.792 seconds.
✔ Test runtimeWorkerPinnedConfigurationRejectsWrongQwen35Fields() passed after 4.791 seconds.
✔ Test materializedTensorRejectsMismatchedShapeAndBytes() passed after 4.791 seconds.
✔ Test setupCompatibilityLinkFailurePropagatesThroughConditionalCaller() passed after 4.791 seconds.
✔ Test pinTrustedHarnessScriptDetectsTamper() passed after 4.791 seconds.
✔ Test setupRejectsCommandLineToolsWithFullXcodeInstructions() passed after 4.793 seconds.
✔ Test metallibBuilderRegeneratesStaleCMakeCache() passed after 4.791 seconds.
✔ Test lagunaCorrectnessGeneratesGreedyTokensWithGrowingContext() passed after 4.791 seconds.
✔ Test trustedBenchmarkWorkflowGuardAllowsOnlyPermittedBranches() passed after 4.791 seconds.
✔ Test localIteratePhaseHeartbeatFiresWhileBlockedAndStopsAfterCancel() passed after 4.792 seconds.
✔ Test lagunaWeightLoaderExplicitlyRejectsNonMLXQuantizationSchemas() passed after 4.791 seconds.
✔ Test denseTensorStoreRejectsUnsafeShardPath() passed after 4.791 seconds.
✔ Test attachDerivedBenchmarkOracleLeavesEveryOtherSectionUntouched() passed after 4.791 seconds.
✔ Test attachDerivedBenchmarkOracleAcceptsTheExactTimedWindowBoundary() passed after 4.791 seconds.
✔ Test loadGoldenCasesRejectsMissingVersion() passed after 4.791 seconds.
✔ Test loadGoldenFixtureAcceptsLayeredCorrectnessGates() passed after 4.790 seconds.
✔ Test lagunaConfigRejectsChangedArchitecture() passed after 4.791 seconds.
✔ Test safetensorsCopySubsetRejectsDuplicateNamesWithoutMutatingDestination() passed after 4.790 seconds.
✔ Test transformRejectsIndexTensorMissingFromShardHeaderBeforeCreatingOutput() passed after 4.790 seconds.
✔ Test transformFailurePreservesExistingOutputAndRemovesStagingDirectory() passed after 4.790 seconds.
✔ Test localIterateSummaryEmitsSpeedupsAndEstimatedScore() passed after 4.790 seconds.
✔ Test loadGoldenFixtureRejectsHalfCalibratedBenchmarkBaselines() passed after 4.790 seconds.
✔ Test transformRuntimeConfigCaptureDoesNotRereadChangedSource() passed after 4.788 seconds.
✔ Test localIterateScoreSanitizesNonfiniteFailureMetricsForJSON() passed after 4.790 seconds.
✔ Test attachDerivedBenchmarkOracleOutputLoadsThroughTheStrictFixtureLoader() passed after 4.787 seconds.
✔ Test benchmarkScriptFailsFastWithGuidanceWhenGoldenIsMissing() passed after 4.788 seconds.
✔ Test safetensorsCopySubsetRejectsReplacedValidatedSourceAndPreservesDestination() passed after 4.787 seconds.
✔ Test theTransformCacheDefinitionsStayExtractable() passed after 4.787 seconds.
✔ Test runtimeWorkerClientTimesOutStalledRequest() passed after 4.793 seconds.
✔ Test benchmarkWorkflowsPinTrustedSourceScopeBeforeBuilding() passed after 4.787 seconds.
✔ Test setupVerifiedDownloadMarkerRejectsManifestABA() passed after 4.791 seconds.
✔ Test transformRejectsQuantizedLagunaExpertStackMissingScales() passed after 4.788 seconds.
✔ Test qwenChatTemplateMatchesTheCheckpointSidecarPlainPath() passed after 4.788 seconds.
✔ Test benchmarkPreflightRejectsMissingBenchmarkOracle() passed after 4.788 seconds.
✔ Test editableSurfaceByteBudgetEnforcesCapsAtWorkerLaunch() passed after 4.788 seconds.
✔ Test trustedSandboxRebindsExecPermissionToTheRuntimeWorker() passed after 4.788 seconds.
✔ Test lowMemoryPlanWritesNoFeatureDefaultsAndAnnouncesRankedParity() passed after 4.788 seconds.
✔ Test benchmarkPreflightRejectsMissingSemanticTensor() passed after 4.788 seconds.
✔ Test checkpointIndexToolsRejectsUnsupportedShard() passed after 4.788 seconds.
✔ Test benchmarkScriptFailsWhenScorePayloadFails() passed after 4.794 seconds.
✔ Test attachDerivedBenchmarkOracleCarriesNoPerPromptBaselines() passed after 4.788 seconds.
✔ Test cliSupportsFreeRunGateAttachmentCoveringTimedDecodeOffsets() passed after 4.788 seconds.
✔ Test benchmarkScriptResolvesThePinnedQwenReference() passed after 4.788 seconds.
✔ Test transformRejectsExistingRegularFileOutputWithoutReplacingIt() passed after 4.806 seconds.
✔ Test runtimeWorkerWatchdogAtomicCancellationDisarmsTimer() passed after 4.806 seconds.
✘ Test theQwenMTPTrackIsArmedOnQwen38() recorded an issue at QwenMTPTrackNamingTests.swift:667:13: Expectation failed: (value → "10").contains(releaseMarker → "QWEN38-PENDING-RELEASE")
↳ MLXFAST_QWEN_MTP_TARGET_MANIFEST_RECORDS is '10', not QWEN38-PENDING-RELEASE. Every measured or uploaded pin must stay pending until it is re-measured against the Qwen 3.8 tower; a resolved-looking 3.6 value here is a 3.6 artifact about to be scored under the 3.8 track id.
✘ Test theQwenMTPTrackIsArmedOnQwen38() recorded an issue at QwenMTPTrackNamingTests.swift:667:13: Expectation failed: (value → "15153237117").contains(releaseMarker → "QWEN38-PENDING-RELEASE")
↳ MLXFAST_QWEN_MTP_TARGET_MANIFEST_BYTES is '15153237117', not QWEN38-PENDING-RELEASE. Every measured or uploaded pin must stay pending until it is re-measured against the Qwen 3.8 tower; a resolved-looking 3.6 value here is a 3.6 artifact about to be scored under the 3.8 track id.
✘ Test theQwenMTPTrackIsArmedOnQwen38() recorded an issue at QwenMTPTrackNamingTests.swift:667:13: Expectation failed: (value → "175b02add3fe36e557ffcc3b4187615c763aba03b1da483c534db23c9d2cbd19").contains(releaseMarker → "QWEN38-PENDING-RELEASE")
↳ MLXFAST_QWEN_MTP_CORRECTNESS_GOLDEN_SHA256 is '175b02add3fe36e557ffcc3b4187615c763aba03b1da483c534db23c9d2cbd19', not QWEN38-PENDING-RELEASE. Every measured or uploaded pin must stay pending until it is re-measured against the Qwen 3.8 tower; a resolved-looking 3.6 value here is a 3.6 artifact about to be scored under the 3.8 track id.
✘ Test theQwenMTPTrackIsArmedOnQwen38() recorded an issue at QwenMTPTrackNamingTests.swift:667:13: Expectation failed: (value → "109743").contains(releaseMarker → "QWEN38-PENDING-RELEASE")
↳ MLXFAST_QWEN_MTP_CORRECTNESS_GOLDEN_BYTES is '109743', not QWEN38-PENDING-RELEASE. Every measured or uploaded pin must stay pending until it is re-measured against the Qwen 3.8 tower; a resolved-looking 3.6 value here is a 3.6 artifact about to be scored under the 3.8 track id.
✘ Test theQwenMTPTrackIsArmedOnQwen38() recorded an issue at QwenMTPTrackNamingTests.swift:667:13: Expectation failed: (value → "3758ca9237f3f312198a3c7ab53cff506975332d191ff4ffe28c098336de0187").contains(releaseMarker → "QWEN38-PENDING-RELEASE")
↳ MLXFAST_RAW_CORRECTNESS_GOLDEN_SHA256 is '3758ca9237f3f312198a3c7ab53cff506975332d191ff4ffe28c098336de0187', not QWEN38-PENDING-RELEASE. Every measured or uploaded pin must stay pending until it is re-measured against the Qwen 3.8 tower; a resolved-looking 3.6 value here is a 3.6 artifact about to be scored under the 3.8 track id.
✘ Test theQwenMTPTrackIsArmedOnQwen38() recorded an issue at QwenMTPTrackNamingTests.swift:667:13: Expectation failed: (value → "36215").contains(releaseMarker → "QWEN38-PENDING-RELEASE")
↳ MLXFAST_RAW_CORRECTNESS_GOLDEN_BYTES is '36215', not QWEN38-PENDING-RELEASE. Every measured or uploaded pin must stay pending until it is re-measured against the Qwen 3.8 tower; a resolved-looking 3.6 value here is a 3.6 artifact about to be scored under the 3.8 track id.
✘ Test theQwenMTPTrackIsArmedOnQwen38() recorded an issue at QwenMTPTrackNamingTests.swift:667:13: Expectation failed: (value → "40e17c81180eda91186408c72cf6fef097bd2e635424427668eaa69472a12f22").contains(releaseMarker → "QWEN38-PENDING-RELEASE")
↳ MLXFAST_GPQA_REFERENCE_SHA256 is '40e17c81180eda91186408c72cf6fef097bd2e635424427668eaa69472a12f22', not QWEN38-PENDING-RELEASE. Every measured or uploaded pin must stay pending until it is re-measured against the Qwen 3.8 tower; a resolved-looking 3.6 value here is a 3.6 artifact about to be scored under the 3.8 track id.
✘ Test theQwenMTPTrackIsArmedOnQwen38() recorded an issue at QwenMTPTrackNamingTests.swift:667:13: Expectation failed: (value → "10143").contains(releaseMarker → "QWEN38-PENDING-RELEASE")
↳ MLXFAST_GPQA_REFERENCE_BYTES is '10143', not QWEN38-PENDING-RELEASE. Every measured or uploaded pin must stay pending until it is re-measured against the Qwen 3.8 tower; a resolved-looking 3.6 value here is a 3.6 artifact about to be scored under the 3.8 track id.
✘ Test theQwenMTPTrackIsArmedOnQwen38() recorded an issue at QwenMTPTrackNamingTests.swift:828:17: Expectation failed: (value → "correctness_prompts/qwen3.8-27b-mtp-v1/qwen3.8-27b-hidden-correctness-golden-3758ca9237f3f312198a3c7ab53cff506975332d191ff4ffe28c098336de0187.json").contains(releaseMarker → "QWEN38-PENDING-RELEASE")
↳ MLXFAST_CORRECTNESS_GOLDEN_R2_PATH is 'correctness_prompts/qwen3.8-27b-mtp-v1/qwen3.8-27b-hidden-correctness-golden-3758ca9237f3f312198a3c7ab53cff506975332d191ff4ffe28c098336de0187.json', not QWEN38-PENDING-RELEASE; it must stay pending
✘ Test theQwenMTPTrackIsArmedOnQwen38() recorded an issue at QwenMTPTrackNamingTests.swift:828:17: Expectation failed: (value → "correctness_prompts/qwen3.8-27b-mtp-v1/qwen3.8-27b-gpqa-reference-cases-40e17c81180eda91186408c72cf6fef097bd2e635424427668eaa69472a12f22.json").contains(releaseMarker → "QWEN38-PENDING-RELEASE")
↳ MLXFAST_GPQA_R2_PATH is 'correctness_prompts/qwen3.8-27b-mtp-v1/qwen3.8-27b-gpqa-reference-cases-40e17c81180eda91186408c72cf6fef097bd2e635424427668eaa69472a12f22.json', not QWEN38-PENDING-RELEASE; it must stay pending
✘ Test theQwenMTPTrackIsArmedOnQwen38() recorded an issue at QwenMTPTrackNamingTests.swift:828:17: Expectation failed: (value → "correctness_prompts/qwen3.8-27b-mtp-v1/qwen3.8-27b-mtp-correctness-golden-hidden-175b02add3fe36e557ffcc3b4187615c763aba03b1da483c534db23c9d2cbd19.json").contains(releaseMarker → "QWEN38-PENDING-RELEASE")
↳ MLXFAST_QWEN_MTP_CORRECTNESS_GOLDEN_R2_PATH is 'correctness_prompts/qwen3.8-27b-mtp-v1/qwen3.8-27b-mtp-correctness-golden-hidden-175b02add3fe36e557ffcc3b4187615c763aba03b1da483c534db23c9d2cbd19.json', not QWEN38-PENDING-RELEASE; it must stay pending
✔ Test scoreMetricsDecodeOlderPayloadWithoutWeightsIntegrityFields() passed after 4.845 seconds.
✔ Test setupAllowsOnlyOneCompetingStaleLockRecoverer() passed after 4.847 seconds.
{
  "score": null,
  "passed": true,
  "metrics": {
    "weights_hash": "fake-weights",
    "weights_file_count": 1,
    "weights_byte_count": 2
  }
}
✔ Test setupReferenceCacheStampRejectsSameSizeImmediateMutation() passed after 4.915 seconds.
✔ Test qwen35TransformRejectsAlternateQuantizationSpecs() passed after 4.983 seconds.
✔ Test checkpointIndexBuildRejectsDuplicateTensorNamesAcrossShards() passed after 4.984 seconds.
✔ Test setupReferenceCacheStampRejectsManifestSwapDuringFastValidation() passed after 4.991 seconds.
✔ Test loadGoldenCasesRejectsNamesWithControlCharacters() passed after 4.984 seconds.
✔ Test setupUsesDownloadedMetalToolchainIdentifierForCompilerProbe() passed after 4.984 seconds.
✔ Test transformRejectsUnsafeIndexShardBeforeCreatingOutput() passed after 4.984 seconds.
✔ Test setupReferenceCacheStampDoesNotModifyReadOnlyCheckpoint() passed after 4.990 seconds.
✔ Test transformSelectsTextTowerTensorsAndDropsVisionTensors() passed after 4.984 seconds.
✔ Test timedDecodeChargesOneValidatedSeedForward() passed after 4.984 seconds.
✔ Test benchmarkWorkflowPinsTrustedLayerCountForFinalValidation() passed after 4.984 seconds.
✔ Test safetensorsCopySubsetUsesThrowingDestinationCreate() passed after 4.984 seconds.
✔ Test transformRequiresMatchingExplicitPoolsideQuantizationBlocks() passed after 4.984 seconds.
✔ Test transformRejectsCheckpointWithoutTextTowerTensorsBeforeCreatingOutput() passed after 4.984 seconds.
✔ Test prefillAcceptsAtReferenceAndModestlyFaster() passed after 4.984 seconds.
✔ Test compareTeacherForcedWithWorkerUsesSerialTeacherForcedSteps() passed after 4.984 seconds.
✔ Test workerPhaseStartsResetTrustedAllocatorBeforeForwardSetup() passed after 4.984 seconds.
✔ Test terminatedLocalRunReapsTheModelHoldingProcessTree() passed after 4.990 seconds.
✔ Test metallibBuilderReclaimsOnlyDeadOrBrokenAtomicLockGenerations() passed after 4.985 seconds.
✔ Test semanticBehaviorGateRequiresPromptAndReferenceAnswer() passed after 4.984 seconds.
✔ Test benchmarkScoreUsesWeightedBaselineSpeedups() passed after 4.984 seconds.
✔ Test safetensorsCopySubsetPreservesBytesForUnsortedTensorNames() passed after 4.984 seconds.
✔ Test qwen36PublicConfigFixturePinsExactArtifactSemantics() passed after 4.984 seconds.
✔ Test endOfTurnTruncationCutsTheFinishedTurnTail() passed after 4.984 seconds.
✔ Test loadGoldenFixtureStaleBenchmarkOracleErrorMentionsPrecomputedFixture() passed after 4.984 seconds.
✔ Test setupScriptDefaultsToFastReferenceMirror() passed after 4.984 seconds.
✔ Test safetensorsRejectsBooleanAndFloatingPointIntegerFields() passed after 4.984 seconds.
✔ Test benchmarkPreflightRejectsMalformedGolden() passed after 4.984 seconds.
✔ Test ngramSelfSimilarityReportsNoHitsForUniqueContinuation() passed after 4.979 seconds.
✔ Test localDecodeProgressIntervalUsesExpectedCadence() passed after 4.979 seconds.
✔ Test participantDocsDescribeTheirOwnTrackScoreAndFloor() passed after 4.978 seconds.
✔ Test workerStderrDrainForwardsRedactedLinesAndKeepsTailForDiagnostics() passed after 4.978 seconds.
✔ Test startupMemoryPolicyUses64GiBAsFullProfileBoundary() passed after 4.978 seconds.
✔ Test safetensorsCopySubsetRejectsSourceMutationBeforePublishAndRemovesTemporaryFile() passed after 4.978 seconds.
✔ Test runtimeWorkerOrphanReaperStaysQuietWhileTheParentIsAlive() passed after 4.985 seconds.
✔ Test qwenChatTemplatePreClosesTheThinkingBlock() passed after 4.978 seconds.
✘ Test phaseStartAllocatorResetLeavesExactlyEmptyCacheWhenRuntimeTestsAreEnabled() failed after 4.978 seconds with 1 issue.
✔ Test nvfp4NibbleOrderAndE4M3ScaleBytesMatchMLXContractWhenRuntimeTestsAreEnabled() passed after 4.978 seconds.
✔ Test referenceCacheProbeWorkflowIsManualAndExperimental() passed after 4.978 seconds.
✔ Test attachDerivedBenchmarkOracleRejectsAGoldenWithNoBaseCases() passed after 4.970 seconds.
✔ Test correctnessCheckGatesFalseSkipsGateLoopsAndReportsBaseCaseCountAlone() passed after 4.966 seconds.
✔ Test transformRejectsSourceMetadataMutationsBeforePublishingOutput() passed after 4.980 seconds.
✔ Test correctnessPreflightHonorsConfiguredWeightsByteLimit() passed after 4.978 seconds.
✔ Test poolsideLagunaPublicConfigFixturePinsExactArtifactSemantics() passed after 4.966 seconds.
✔ Test hashWeightsDirectoryRejectsHardlinks() passed after 4.966 seconds.
✔ Test setupAndMetallibStatFallbackDiscardsFailedProbeOutput() passed after 4.990 seconds.
✔ Test decodeFailsGainBeyond5Percent() passed after 4.969 seconds.
✔ Test metallibFingerprintScriptAndSwiftTwinAgree() passed after 4.990 seconds.
✔ Test transformVerifierAcceptsFreshSubmittedTransformOutputAndIgnoresLocalCacheMarkers() passed after 4.967 seconds.
✘ Test qwen36ConfigContractDigestMatchesTheReferenceManifest() failed after 4.965 seconds with 2 issues.
↳ /// The `config_sha256` both fixtures pin is the digest of the checkpoint's own
↳ /// `config.json` bytes, so it must equal the line the checked-in reference
↳ /// manifest carries for that file.
↳ ///
↳ /// HALF-SUSPENDED SINCE 2026-08-14. The reference manifest was repointed at the
↳ /// adopted Qwen 3.8 backbone and its per-file body is a stub pending
↳ /// generation, so it carries no `config.json` record to compare against. The
↳ /// two THREE-SIX fixtures (fixtures/qwen3_6_27b_config.json and
↳ /// fixtures/qwen3_6_27b_tensor_inventory.json) keep their 3.6 names and their
↳ /// 3.6 content on purpose -- they describe the checkpoint they were captured
↳ /// from -- so the half of this check that compares them to each other still
↳ /// binds and is kept. Only the manifest leg waits.
↳ ///
↳ /// The stub is asserted rather than tolerated: if the 3.8 body lands, this test
↳ /// must be restored to a real three-way comparison against the 3.8
↳ /// `config.json` digest, and the two 3.6 fixtures re-captured or explicitly
↳ /// re-scoped in the same commit.
✔ Test transformRejectsOutputThatContainsWorkingDirectory() passed after 4.965 seconds.
✔ Test qwenHybridCachePositionRejectsDivergentFullAttentionOffsets() passed after 4.965 seconds.
✔ Test setupAcceptsLegacyReferenceDirectoryAsItsOwnCompatibilityPath() passed after 4.965 seconds.
✔ Test benchmarkLocalIterateModeUsesPublicFixtureAndNonOfficialScore() passed after 4.966 seconds.
✔ Test benchmarkGatesOnlyRunSkipsSpuriousSemanticCaptureFailure() passed after 4.966 seconds.
◇ Test case passing 1 argument definition → "local_run_lock_path" to everyReusedDefinitionFailsClosedOnItsOwn(_:) started.
◇ Test case passing 1 argument definition → "acquire_local_run_lock" to everyReusedDefinitionFailsClosedOnItsOwn(_:) started.
◇ Test case passing 1 argument definition → "release_local_run_lock" to everyReusedDefinitionFailsClosedOnItsOwn(_:) started.
◇ Test case passing 1 argument definition → "list_resident_model_processes" to everyReusedDefinitionFailsClosedOnItsOwn(_:) started.
◇ Test case passing 1 argument definition → "abort_if_model_already_resident" to everyReusedDefinitionFailsClosedOnItsOwn(_:) started.
{
  "score": 1,
  "passed": true,
  "metrics": {
    "weights_hash": "fake-weights",
    "weights_file_count": 1,
    "weights_byte_count": 2
  }
}
✔ Test naxSplitKBF16MatchesRegularNAXWhenRuntimeTestsAreEnabled() passed after 5.651 seconds.
✔ Test setupScriptCoordinatesCacheAndMetallibState() passed after 5.647 seconds.
✔ Test workerComputesExpectedTokenDiagnosticsOutsideReturnedTopSubset() passed after 5.713 seconds.
✔ Test localIterateProjectedDecodeSecondsPerTokenConvergesToChargedMean() passed after 5.725 seconds.
✔ Test setupReclaimsOnlyDeadOrBrokenAtomicLockGenerations() passed after 5.731 seconds.
✔ Test setupDownloadPublishesVerifiedPartialAtomically() passed after 5.725 seconds.
✔ Test trustedParentStartsPhaseTimerBeforeWorkerResetRequest() passed after 5.725 seconds.
✔ Test loadGoldenFixtureRejectsMalformedBenchmarkOracle() passed after 5.725 seconds.
✔ Test pairedBaselineOverrideParsesTrustedEnvironmentFailClosed() passed after 5.725 seconds.
✔ Test setupRejectsZeroMutationLockStaleThreshold() passed after 5.725 seconds.
✔ Test materializedTensorDecodesSupportedValues() passed after 5.725 seconds.
✔ Test benchmarkScriptPrintsLocalSummaryWithBaselineComparison() passed after 5.756 seconds.
✔ Test localIteratePrefillStatusReportsPerTokenAndSpeedup() passed after 5.725 seconds.
✔ Test publicDiagnosticsAreCoarsenedWhileRankingStaysPrecise() passed after 5.725 seconds.
✔ Test qwen36TensorInventoryFixturePinsAllPublicHeaders() passed after 5.792 seconds.
✔ Test evaluateTimedRunMatchesSeparateScoreSpeedupAndBandChecks() passed after 5.792 seconds.
✔ Test theQwenMTPRunnerRefreshesTheTransformAndGatesTheModelFamily() passed after 5.792 seconds.
✔ Test benchmarkScriptRejectsMultipleScoreObjectsOnStdout() passed after 5.817 seconds.
✔ Test benchmarkCliSupportsSkippableBenchmarkCorrectness() passed after 5.792 seconds.
✔ Test benchmarkFailsFastWhenSetupArtifactsAreMissing() passed after 5.792 seconds.
✔ Test cliSupportsBenchmarkOracleAttachmentForRankedGoldens() passed after 5.792 seconds.
✔ Test lagunaConfigRejectsAnyQuantizationOverride() passed after 5.792 seconds.
✔ Test robustReferenceRejectsTooFewOrInvalid() passed after 5.796 seconds.
✔ Test frozenDependencyGraphAssertionDetectsManifestDrift() passed after 5.797 seconds.
✔ Test setupFailureCleanupTerminatesMetallibProcessGroup() passed after 5.797 seconds.
✔ Test bufferedFileLineReaderPreservesBufferedLinesAndEOFFragment() passed after 5.796 seconds.
✔ Test benchmarkWindowConstantsAreFrozen() passed after 5.796 seconds.
✔ Test qwenChatTemplateEmbedsThePromptVerbatim() passed after 5.796 seconds.
✔ Test lagunaConfigRequiresMatchingExplicitQuantizationBlocks() passed after 5.810 seconds.
✔ Test localModesGuardAgainstDoubleModelResidency() passed after 5.821 seconds.
✔ Test decodeMeasurementInvokesNoPhaseVaryingEditableHook() passed after 5.821 seconds.
✔ Test correctnessPreflightAcceptsPublicGoldenWithoutBenchmarkOracle() passed after 5.829 seconds.
✔ Test benchmarkScriptRejectsPathFlagsBeforeForwardingToSwiftBenchmark() passed after 6.173 seconds.
✔ Test lagunaConfigLoadsPinnedPoolsideNVFP4Geometry() passed after 6.179 seconds.
E28_DRAFT_TOP32 bench ["tiles": 64, "entry_point": "qwen35BenchDraftTop32", "iters": 200, "two_dispatch_us": 1079.925298690796, "arg_partition_us": 1182.1049451828003, "per_thread": 7, "schema": "e28.draft_top32_bench.v1", "speedup": 1.0946173282687959]
✔ Test endOfTurnTruncationNeverReturnsAnEmptyCapture() passed after 6.239 seconds.
✔ Test decodeFailsRegressionBeyond2Percent() passed after 6.262 seconds.
✔ Test safetensorsCopySubsetRejectsMissingRequestedTensor() passed after 6.262 seconds.
✔ Test lagunaConfigAcceptsAbsentOrExplicitNullNullableArtifactFields() passed after 6.263 seconds.
✔ Test benchmarkRebuildsWhenParticipantSourcesAreNewerThanTheBuiltWorker() passed after 6.263 seconds.
E28_DRAFT_TOP32 verify ["entry_point": "qwen35VerifyDraftTop32", "trials": 256, "seed": 1, "tied_trials": 64, "first_bad_trial": -1, "schema": "e28.draft_top32_verify.v1", "mismatches": 0]
✔ Test workerStderrDrainCapsRetainedTail() passed after 6.266 seconds.
✔ Test qwenFullAttentionIntervalMatchesThePinnedLayerSchedule() passed after 6.266 seconds.
✔ Test loadGoldenCasesRejectsDuplicateCaseNames() passed after 6.266 seconds.
✔ Test benchmarkWindowFreezeDocMatchesConstants() passed after 6.267 seconds.
✔ Test runtimeWorkerRejectsPublicContractFieldsPreviouslyIgnoredByDecoder() passed after 6.270 seconds.
✔ Test benchmarkRestoresFansWhenAbortedAfterBoost() passed after 6.297 seconds.
✔ Test rankedScoreSemanticsAreUnchangedByLocalEstimatedScore() passed after 6.273 seconds.
✔ Test qwen35TransformRejectsInventoryAndPackingDrift() passed after 6.273 seconds.
✔ Test localIterateScoreFailsForUnusableTimings() passed after 6.273 seconds.
✔ Test lagunaConfigRejectsUnsupportedRopeType() passed after 6.266 seconds.
✔ Test benchmarkTimingChargesDecodeSetupAndSeparatesWorkers() passed after 6.273 seconds.
✔ Test submissionStaticReviewOversizeFailureExplainsStaleCloneDeletions() passed after 6.280 seconds.
✔ Test denseTensorStoreLoadsMetadataAndReadsBytes() passed after 6.268 seconds.
✔ Test benchmarkPreflightAcceptsRequiredArtifacts() passed after 6.278 seconds.
✔ Test checkpointValidatorAcceptsExactPublic912TensorInventory() passed after 6.271 seconds.
✔ Test benchmarkPlacesHiddenGoldenInputsSymlinkSafely() passed after 6.271 seconds.
✔ Test nvfp4Group16SplitKMatmulMatchesDequantizedReferenceWhenRuntimeTestsAreEnabled() passed after 6.271 seconds.
✔ Test runtimeWorkerPinnedConfigurationPathRejectsUnsafeFilesystemEntries() passed after 6.271 seconds.
✔ Test setupDFlashProvisionsTheSharedNVFP4ReferenceAndClaimsNoConversion() passed after 6.742 seconds.
✔ Test prefillFailsWhenMoreThan5PercentSlow() passed after 7.163 seconds.
✔ Test ngramSelfSimilarityNormalizesAndValidatesOrders() passed after 7.395 seconds.
✔ Test runtimeWorkerStartsTheOrphanReaperBeforeLoadingTheModel() passed after 7.395 seconds.
✔ Test benchmarkCorrectnessSupportsZeroStepBaseCaseSkipWithoutSelfCertifying() passed after 7.395 seconds.
✔ Test runtimeWorkerClientRejectsNonfiniteTimeoutsBeforeLaunch() passed after 7.395 seconds.
✔ Test decodeTimingPlanRejectsInvalidRanges() passed after 7.395 seconds.
✔ Test setupUsesOneCanonicalMutationLockForReferenceSymlinkAliases() passed after 7.395 seconds.
✔ Test runtimeWorkerClientTimesOutWaitingForHello() passed after 7.397 seconds.
✔ Test startupMemoryPolicyProtectsDocumented36GiBLocalMachine() passed after 7.400 seconds.
✔ Test transformOnlyRefusesEveryMeasuringCombination() passed after 7.400 seconds.
✔ Test safetensorsCopySubsetAtomicallyReplacesExistingDestination() passed after 7.400 seconds.
✔ Test staticReviewFailsClosedOnSelfContradictoryPassedTrueWithHighSeverity() passed after 7.400 seconds.
✔ Test qwen36ConfigContractCarriesThePublicConfigAndItsPublishedDigest() passed after 7.400 seconds.
✔ Test qwenMTPWorkflowNamesTheTrackManifestAtEveryEditableSurfaceConsumer() passed after 7.690 seconds.
✔ Test setupReferenceCacheStampUsesUniqueAtomicTemporaryFiles() passed after 8.273 seconds.
✔ Test poolsideNVFP4DistributionIdentityIsPinned() passed after 8.326 seconds.
✔ Test dflashResidentSetGetsTheDocumentedLowMemoryProfileBelow64GiB() passed after 8.651 seconds.
✔ Test qwen35CheckpointValidationInventoryMatchesThePublicHeaders() passed after 8.651 seconds.
✔ Test benchmarkScriptHidesPrivateDirectoryFromRuntimeWorker() passed after 8.651 seconds.
✔ Test transformBuildsPoolsideLagunaNVFP4RuntimeConfig() passed after 8.651 seconds.
✔ Test validateBenchmarkGoldenRejectsMalformedOracle() passed after 8.651 seconds.
✔ Test nvfp4ActualSharedExpertQMMShapesCoverDecodeAndSplitKPrefillWhenRuntimeTestsAreEnabled() passed after 8.651 seconds.
✔ Test semanticGPQAGateRetriesTransportFailuresAndFailsInfraNotSemantic() passed after 8.694 seconds.
✔ Test transformRejectsPartialLagunaInventoryBeforePublishing() passed after 8.651 seconds.
✔ Test benchmarkPreflightRejectsShortBenchmarkPrompt() passed after 8.651 seconds.
✔ Test runtimeWorkerPinnedConfigurationAcceptsQwen35Architecture() passed after 8.664 seconds.
✔ Test setupFallsBackWhenPrimaryReferenceDownloadFails() passed after 8.667 seconds.
✔ Test safetensorsHeaderEqualityIgnoresSourceFileIdentity() passed after 8.664 seconds.
✔ Test decodeTimingPlanStartsAfterSeedPrefill() passed after 8.664 seconds.
✔ Test cliSupportsPublicBaseGoldenGeneration() passed after 8.686 seconds.
✔ Test startupMemoryPolicyHonorsExplicitProfileRequest() passed after 8.685 seconds.
✔ Test gpqaCapturePathsFrameHiddenPromptsAsChatMLTurns() passed after 8.685 seconds.
✔ Test trustedTracePreservesLargeTopKAndOutOfSubsetExpectedDiagnostics() passed after 8.686 seconds.
✔ Test benchmarkScriptFallsBackToCacheWhenReferenceSymlinkIsBroken() passed after 8.704 seconds.
✔ Test benchmarkWorkflowFailsClosedWhenRunnerPrivateArtifactCleanupFails() passed after 8.693 seconds.
✔ Test benchmarkScriptForwardsLocalIterateDefaultsToSwiftBenchmark() passed after 8.704 seconds.
✔ Test mlxTensorBridgeMapsSafetensorsDTypes() passed after 8.693 seconds.
✔ Test benchmarkPreflightHonorsConfiguredWeightsByteLimit() passed after 8.693 seconds.
✔ Test failedScorePreservesExplicitPublicMismatchTokensAndRuntimeLabel() passed after 8.693 seconds.
✔ Test theInlineGateChecksEveryFieldTheValidationStepChecks() passed after 8.693 seconds.
✔ Test transformRejectsUnsupportedIndexShardBeforeCreatingOutput() passed after 8.693 seconds.
✘ Test contestantDocsCommandBlocksKeepTheDependencyGraphFrozen() failed after 8.693 seconds with 1 issue.
↳ /// The contestant docs' own command blocks must not instruct a bare
↳ /// `swift build` / `swift test` / `swift package resolve`: a bare invocation
↳ /// can silently rewrite the frozen Package.resolved (SwiftPM re-resolves the
↳ /// ranged transitive pins on toolchain drift), after which setup.sh and
↳ /// benchmark.sh refuse to run via assert_frozen_dependency_graph — blaming
↳ /// the contestant for following the docs.
✔ Test transformAtomicallyReplacesExistingOutputAndRemovesStaleFiles() passed after 8.693 seconds.
✔ Test benchmarkScriptLocalSummarySkipsQuietlyWithoutTimingMetrics() passed after 8.705 seconds.
✔ Test qwenMTPWorkflowAllowsConcurrentSubmissionBases() passed after 8.693 seconds.
✔ Test "the fused kernel is bit-identical to the eager add + MLX rmsNorm chain" passed after 8.696 seconds.
✔ Test runtimeWorkerPrivateDescriptorUsesPortableLowerBoundAndCloseOnExec() passed after 8.689 seconds.
✔ Test localIterateStreamsLiveNumbersDuringTheRun() passed after 8.693 seconds.
✔ Test loadGoldenFixtureRejectsNoopAnchorDelta() passed after 8.689 seconds.
✔ Test qwen35TransformAcceptsTheExactPublicTextTower() passed after 8.689 seconds.
✔ Test loadGoldenCasesRejectsWrongExpectedTokenCount() passed after 8.689 seconds.
✔ Test runtimeWorkerValidatesTransformedWeightsAtStartup() passed after 8.689 seconds.
✔ Test setupDownloadContractReportsProgressAndDetectsStalls() passed after 8.689 seconds.
✔ Test setupRefusesToReleaseMutationLockWithDifferentOwnerToken() passed after 8.689 seconds.
✔ Test runtimeWorkerEnvironmentAllowlistPrefixDoesNotAdmitHarnessNames() passed after 8.689 seconds.
✔ Test mlxSwiftLMYaRNAuthorityKeepsDerivedMScale() passed after 8.689 seconds.
✔ Test fanBoostVerifiesWritesInspectsAllFansAndTracksRunRestoreState() passed after 8.689 seconds.
✔ Test setupFailedDownloadPreservesPreviouslyPublishedFile() passed after 8.689 seconds.
✔ Test verifyTrustedSourceScopeScriptDetectsScopeTamper() passed after 8.735 seconds.
✔ Test privateArtifactGuardRejectsRenamedGoldenAndPromptFiles() passed after 8.689 seconds.
✔ Test benchmarkPreflightRejectsWeightsAboveDefaultByteLimit() passed after 8.689 seconds.
✔ Test transformRejectsSymlinkAliasOfReferenceAsOutput() passed after 8.687 seconds.
✔ Test lagunaCorrectnessSelectsGreedyTokenWhenRuntimeTestsAreEnabled() passed after 8.687 seconds.
✔ Test firstTokenMismatchReportIncludesNonM5GoldenCaveat() passed after 8.687 seconds.
✔ Test checkpointValidatorRejectsForeignSchemasAndEveryInventoryMutation() passed after 8.689 seconds.
✔ Test overlayScriptRejectsDangerousArtifactsAfterCopy() passed after 8.678 seconds.
✔ Test loadGoldenCasesRejectsOutOfRangeToken() passed after 8.678 seconds.
✔ Test safetensorsRejectsTensorRangePastEndOfFile() passed after 8.678 seconds.
✔ Test loadGoldenCasesRejectsNonPositiveRequiredPromptTokens() passed after 8.678 seconds.
✘ Test startupMemoryPolicyKeepsRanked128GiBProfile() failed after 8.678 seconds with 2 issues.
✔ Test runtimeWorkerProtocolDescriptorAllocationClosesInputWhenOutputFails() passed after 8.678 seconds.
✔ Test benchmarkScriptTransformOnlyStopsBeforeAnyMeasurement() passed after 8.713 seconds.
✔ Test localCorrectnessFailureSurfacesCarryNonM5GoldenCaveat() passed after 8.794 seconds.
✔ Test lagunaCorrectnessTeacherForcedUsesGoldenPrefix() passed after 8.876 seconds.
✔ Test metallibBuilderSerializesConcurrentProcesses() passed after 8.933 seconds.
✔ Test timedPrefillChargesOneValidatedColdForward() passed after 8.876 seconds.
SKIP Qwen reference parity: set MLXFAST_RUN_QWEN_REFERENCE_PARITY=1 and MLXFAST_QWEN_REFERENCE_WEIGHTS_PATH=<transformed-weights>
✔ Test localModesForwardWorkerStderrLiveButOfficialRunsDoNot() passed after 8.876 seconds.
✔ Test runtimeWorkerPinnedConfigurationRejectsMissingQwen35Fields() passed after 8.872 seconds.
✔ Test ngramSelfSimilarityCountsEarlierMatchingFollower() passed after 8.872 seconds.
✔ Test runtimeWorkerClientCancelsSuccessfulRequestWatchdogs() passed after 8.897 seconds.
✔ Test setupReleasesReferenceCacheMutationLockWithoutOverwritingOperationFailure() passed after 8.877 seconds.
✔ Test loadGoldenFixtureRejectsUnknownBenchmarkKeys() passed after 8.870 seconds.
✔ Test localFailedRunWithMeasuredTimingsStillPublishesNumericEstimatedScore() passed after 8.870 seconds.
✔ Test metallibFingerprintRecordVerificationFailsClosedOnStaleRecord() passed after 8.870 seconds.
✔ Test correctnessReportEncodesStableFailureFields() passed after 8.868 seconds.
✔ Test lagunaYaRNIgnoresSerializedAttentionFactorLikeUpstreamMLX() passed after 8.868 seconds.
✔ Test loadGoldenFixtureRejectsNonPositiveBenchmarkBaselines() passed after 8.866 seconds.
✔ Test dflashDriverTwinsAreByteIdentical() passed after 8.822 seconds.
✔ Test localRunAbortsWhenAModelHoldingProcessIsAlreadyResident() passed after 8.881 seconds.
✔ Test benchmarkWorkspaceACLLocksTrustedSurfacesAgainstBench() passed after 8.822 seconds.
✔ Test goldenSequenceMatcherAcceptsExactAnswerSequences() passed after 8.822 seconds.
✔ Test loadGoldenFixtureRejectsMalformedLayeredCorrectnessGate() passed after 8.822 seconds.
✔ Test denseTensorStoreValidatesSparseShardLargerThanInt32() passed after 8.822 seconds.
✔ Test runtimeWorkerBenchmarkDecodeDoesNotReceiveBulkOracle() passed after 8.870 seconds.
✔ Test aFailedSealedScoreIsWithheldFromStaging() passed after 8.897 seconds.
✔ Test loadGoldenFixtureRejectsEmptyCorrectnessGateSection() passed after 8.822 seconds.
✔ Test qwen36TransformOutputSatisfiesTheRuntimeWorkerPinnedConfigGate() passed after 8.822 seconds.
✔ Test decodeMeasurementRunsSingleUnmemoizableSeedForward() passed after 8.812 seconds.
✔ Test safetensorsCopySubsetRejectsOutputOffsetOverflow() passed after 8.812 seconds.
✔ Test runtimeWorkerOrphanReaperFiresOnceTheParentIsGone() passed after 8.812 seconds.
✔ Test setupSerializesSharedReferenceCacheMutationAndRechecksAfterWaiting() passed after 8.933 seconds.
✔ Test robustReferenceDropsTheSlowestAndAverages() passed after 8.812 seconds.
✔ Test transformDetectsModelFamilyFromSourceConfig() passed after 8.808 seconds.
✔ Test setupDetectedMetalToolchainOverridesUnrelatedToolchainsEnvironment() passed after 8.808 seconds.
✔ Test setupReferenceVerificationRejectsManifestABA() passed after 8.812 seconds.
✔ Test compressedTensorQuantizationSchemasAndWrongGroupSizeAreRejected() passed after 8.807 seconds.
✔ Test buildEntrypointsAssertFrozenDependencyGraph() passed after 8.807 seconds.
✔ Test transformAcceptsSparseShardLargerThanInt32() passed after 8.807 seconds.
✔ Test aCleanSealedScoreIsStillStaged() passed after 8.900 seconds.
✔ Test loadGoldenCasesRejectsWrongPromptTokenCount() passed after 8.814 seconds.
✔ Test loadGoldenCasesRejectsNonPositiveRequiredSteps() passed after 8.814 seconds.
✔ Test downloadR2ObjectWithholdsBodyUnlessServerErrorStatus() passed after 8.905 seconds.
✔ Test loadGoldenFixtureRejectsNullBenchmarkObject() passed after 8.814 seconds.
✔ Test ngramSelfSimilarityAppliesPassThresholdToDraftHitRate() passed after 8.823 seconds.
✔ Test runtimeWorkerClientSkipsNonJSONStdoutLines() passed after 8.823 seconds.
✔ Test ngramSelfSimilarityUsesLongestRecurrentOrder() passed after 8.823 seconds.
✔ Test loadGoldenFixtureAcceptsPerPromptBenchmarkBaselines() passed after 8.823 seconds.
✔ Test setupMutationLockWaiterRetriesWhenHolderReleasesDuringPublishAttempt() passed after 8.909 seconds.
✔ Test localIterateLiveDecodeStatusIncludesProjectedSpeedupAndScore() passed after 8.823 seconds.
✔ Test qwen36HeaderInventoryContractPinsTheShardIndependentDigest() passed after 8.823 seconds.
✔ Test endOfTurnTruncationCutsAtTheFirstEndOfTurnOnly() passed after 8.823 seconds.
✔ Test loadGoldenFixtureAcceptsBenchmarkOracle() passed after 8.823 seconds.
✔ Test safetensorsCopySubsetRejectsHardLinkToSourceWithoutMutatingIt() passed after 8.823 seconds.
✔ Test qwenChatTemplateEmitsNoDefaultSystemBlock() passed after 8.823 seconds.
✔ Test lagunaRuntimeMatchesVendoredUpstreamOnM5WhenEnabled() passed after 8.823 seconds.
✔ Test denseTensorStoreRejectsByteLengthMismatch() passed after 8.823 seconds.
✔ Test cliVerifiesMetallibFingerprintBeforeWorkerLaunch() passed after 8.830 seconds.
✔ Test naxSplitKJITFactoryUsesInputDType() passed after 8.830 seconds.
✔ Test runtimeWorkerEnvironmentDropsEverythingOutsideTheAllowlist() passed after 8.835 seconds.
✔ Test coolGateFanBoostIsOptInSudoSafeAndHardCappedAtSeventyPercent() passed after 8.835 seconds.
✔ Test nvfp4ActualRoutedGatherShapesCoverMultipleExpertsAndPrefillWhenRuntimeTestsAreEnabled() passed after 8.835 seconds.
✔ Test bufferedFileLineReaderRejectsOversizedLine() passed after 8.835 seconds.
✔ Test qwenHybridCachePositionStillRejectsAStaleOrReusedCache() passed after 8.835 seconds.
✔ Test benchmarkOutputValidatorReportsTokenMismatches() passed after 8.835 seconds.
✔ Test lagunaWeightCacheConsultsStartupMemoryPolicy() passed after 8.835 seconds.
✔ Test lagunaRuntimeWiresOnlyExpertsToNVFP4WhenRuntimeTestsAreEnabled() passed after 8.835 seconds.
✔ Test goldenSequenceMatcherAcceptsShortBehaviorPrefixes() passed after 8.835 seconds.
✔ Test startupMemoryPolicyIsAppliedBeforeModelLoadAndCleansWarmupCache() passed after 8.835 seconds.
✔ Test benchmarkScriptSealsScoreFromStdoutDiscardingOnDiskTamper() passed after 8.850 seconds.
✔ Test benchmarkRebuildsTheMetallibWhenVendoredKernelSourcesAreNewer() passed after 8.835 seconds.
✔ Test lagunaWeightContractPinsExactXSHeaderInventory() passed after 8.835 seconds.
✔ Test overlayPairedTimingAcceptsTrustedCommitAndRejectsForgedOrMissingCommit() passed after 8.835 seconds.
✔ Test localIterateScoreUnderGoldenDriftStaysUsableButRecordsTheDivergence() passed after 8.835 seconds.
✔ Test benchmarkPrintsFanRestoreReminderAfterBoostWithoutAutoRestoring() passed after 8.892 seconds.
✔ Test lagunaRuntimeCorrectnessReportsMissingArtifacts() passed after 8.877 seconds.
✔ Test checkedInPublicCorrectnessGoldenIsValid() passed after 8.877 seconds.
✔ Test headProvisioningDelegatesWithoutRebuildingOrMisadvising() passed after 8.877 seconds.
✔ Test commitIdentifierPrefersTrustedDispatchSHAOverGit() passed after 8.877 seconds.
✔ Test metadataDerivationFailsClosedExceptWhenHashVerificationIsOff() passed after 8.876 seconds.
✔ Test currentLongcopyFixtureDemonstratesHighSelfSimilarity() passed after 8.876 seconds.
✔ Test transformRejectsLagunaRouterAndStackedExpertCountMismatch() passed after 8.876 seconds.
✔ Test checkpointIndexToolsRejectsUnsafeShardPath() passed after 8.918 seconds.
✔ Test sealedStdoutScoreIsCoarsenedLikeTheWrittenFile() passed after 8.918 seconds.
✔ Test submissionStaticReviewQwenMTPArmSwapsThePolicyAndStillFailsClosed() passed after 9.010 seconds.
✔ Test runtimeWorkerEnvironmentIsIdenticalAcrossPipelinePhases() passed after 8.894 seconds.
✔ Test transformVerifierRejectsOutputThatDiffersFromFreshTransformRun() passed after 8.895 seconds.
✔ Test safetensorsCopySubsetRejectsDirectoryDestinationWithoutRemovingIt() passed after 8.895 seconds.
✔ Test qwenHybridCachePositionRejectsRecurrentCachesThatCountPositions() passed after 8.894 seconds.
✔ Test loadGoldenCasesRejectsNamesWithSurroundingWhitespace() passed after 8.892 seconds.
✔ Test benchmarkScriptBareInvocationDefaultsToLocalIterate() passed after 8.935 seconds.
✔ Test setupStartsMetallibBuildOnlyOnceInSynchronousAndParallelModes() passed after 8.920 seconds.
✔ Test endOfTurnTruncationIsAnIdentityWhenTheTurnNeverEnds() passed after 8.892 seconds.
✔ Test poolsideLagunaTensorInventoryFixturePinsAllPublicHeaders() passed after 8.892 seconds.
✔ Test localFailedPayloadWithoutTimingsKeepsNullScore() passed after 8.894 seconds.
✔ Test decodeAcceptsSmallRegressionAndUpTo5PercentGain() passed after 8.894 seconds.
✔ Test qwenHybridCachePositionRejectsWrongTopologyOrLayerCount() passed after 8.894 seconds.
✔ Test trustedPreflightUsesOneShotWorkerProtocolAndPropagatesFailure() passed after 8.988 seconds.
✔ Test theGuardsAreExecutable() passed after 8.894 seconds.
✔ Test runtimeWorkerProtocolUsesAuthenticatedPrivateIO() passed after 8.894 seconds.
✔ Test benchmarkRejectsPartialSetupBeforeInvokingExistingBinary() passed after 8.894 seconds.
✔ Test lagunaCorrectnessComparesExpectedTokenSequences() passed after 8.894 seconds.
✔ Test bandTolerancesMatchConstants() passed after 9.350 seconds.
✔ Test onlyWorkflowDispatchMayDispatch() passed after 9.351 seconds.
✔ Test eachWorkflowInvokesItsOwnGuard() passed after 9.350 seconds.
✔ Test onlyTheAllowlistedRefsMayDispatch() passed after 9.351 seconds.
✔ Test onlyTheTrustedRepositoryMayDispatch() passed after 9.350 seconds.
✔ Test transformVerifierRejectsOutputAboveConfiguredByteLimit() passed after 9.350 seconds.
✔ Test theRankedWorkflowTimesEveryPoolPromptAndDrawsNothing() passed after 9.348 seconds.
✔ Test eachGuardIsPinnedToItsOwnWorkflowPath() passed after 9.351 seconds.
✔ Test transformRejectsOutputInsideReferenceDirectory() passed after 9.348 seconds.
✔ Test theManifestAndFixtureAgreeOnTheAggregationRule() passed after 9.348 seconds.
✔ Test everyRequiredEnvironmentVariableIsRequired() passed after 9.351 seconds.
✔ Test dflashReferenceRowsRequestCarriesTheVerifyBlockOnTheWire() passed after 9.382 seconds.
✔ Test lagunaWeightLoaderRejectsUnexpectedTensorInventory() passed after 9.349 seconds.
✔ Test benchmarkTransformCacheKeyIgnoresRuntimeModelSources() passed after 9.345 seconds.
✔ Test transformRejectsSymlinkedTokenizerMetadataAndPreservesOutput() passed after 9.345 seconds.
✘ Test theEvenMedianRuleIsTheMeanOfTheTwoCentralValues() failed after 9.345 seconds with 3 issues.
↳ /// THE RULE ITSELF, executed. 8 is even, so "the median" is ambiguous until
↳ /// the tie-break is named, and the two candidate rules disagree on every
↳ /// even-length population. The wrapper and the workflow both implement the
↳ /// mean-of-two-central rule; this pins what that means so a future
↳ /// "simplification" onto the lower-median rule used by the per-pair
↳ /// diagnostic is a red test rather than a quiet score shift.
✔ Test dflashWorkerTwinsDifferOnlyByTheTrustedHarnessGuard() passed after 9.306 seconds.
✔ Test poolsideLagunaConfigRejectsEveryBehaviorBearingMutation() passed after 9.349 seconds.
✔ Test theFixtureDeclaresTheScoringSemanticsWithoutDisturbingThePool() passed after 9.306 seconds.
✔ Test transformWritesFlattenedRuntimeConfigFromTextConfig() passed after 9.306 seconds.
✔ Test benchmarkScriptAvoidsNestedSandboxWithRuntimeWorker() passed after 9.305 seconds.
✔ Test theWorkflowPinsThePairBudgetTheDocumentsDeclare() passed after 9.305 seconds.
◇ Test case passing 1 argument marker → "stopTokens.contains(drafts[" to theGateRejectsADeletedFixMarker(_:) started.
◇ Test case passing 1 argument marker → "pendingPrimary" to theGateRejectsADeletedFixMarker(_:) started.
◇ Test case passing 1 argument marker → "if  self.stopTokens .contains( primary ) {" to theGateRejectsAReintroducedDefectMarker(_:) started.
◇ Test case passing 1 argument marker → "public private(set) var reachedStopToken = false" to theGateRejectsAReintroducedDefectMarker(_:) started.
◇ Test case passing 1 argument marker → "if let i = committed.firstIndex(where: {
  stopTokens.contains($0)
}) {" to theGateRejectsAReintroducedDefectMarker(_:) started.
✔ Test attachDerivedBenchmarkOracleRefusesToOverwriteAnExistingOracle() passed after 9.624 seconds.
✔ Test theScoringStepRecomputesTheMedianAndGatesTheBreakdownShape() passed after 9.624 seconds.
✘ Test theSeededCalibrationExpectationMatchesItsRecordedProvenance() failed after 9.611 seconds with 2 issues.
↳ /// THE SEEDED CALIBRATION EXPECTATION, checked against its own provenance.
↳ ///
↳ /// `expected_raw_median` is not a measurement taken under these semantics --
↳ /// no dispatch has run under them, because the runner for this repository is
↳ /// not registered yet. It is RE-DERIVED from the k2 reference dispatch's own
↳ /// sealed per-prompt raw ratios, and the fixture carries those eight numbers
↳ /// so the derivation can be checked rather than believed. This test IS that
↳ /// check: recompute the median from the recorded inputs and require it to
↳ /// equal the recorded output.
✔ Test submissionStaticReviewCapsFileSizeAndSurfaceGrowth() passed after 9.700 seconds.
✔ Test lagunaRuntimeCorrectnessReportsGoldenMetadataWhenWeightsAreMissing() passed after 9.611 seconds.
✔ Test scoredBaselinesResolveFromGoldenWithConstantsFallback() passed after 9.641 seconds.
✔ Test checkRejectsNonFinite() passed after 9.641 seconds.
✔ Test goldenSequenceMatcherChecksExactPrefixes() passed after 9.641 seconds.
✔ Test loadGoldenFixtureRejectsUnknownCorrectnessGateKey() passed after 9.641 seconds.
✔ Test runtimeWorkerClientDrainsLargeStderrBeforeHelloWhenForwardingIsOff() passed after 9.736 seconds.
✔ Test writeScorePayloadEmitsDarkbloomShape() passed after 9.639 seconds.
✔ Test setupRecoversMutationLockOwnedByDeadLocalProcess() passed after 9.639 seconds.
✔ Test compiledDecodeFlagsStayReadableByModelSources() passed after 9.639 seconds.
✔ Test unsafePrivateCalibrationToolingStaysExcluded() passed after 9.639 seconds.
✘ Test theCheckedInDeclarationSelectsThePinnedHead() recorded an issue at QwenMTPOpenSurfaceTests.swift:54:9: Expectation failed: (declaration.source → .remote) == .pinned
✘ Test theCheckedInDeclarationSelectsThePinnedHead() recorded an issue at QwenMTPOpenSurfaceTests.swift:71:9: Expectation failed: (declared["repository"] as? String → nil) == "EigenLabs/Qwen3.8-27B-MTP-bf16"
✘ Test theCheckedInDeclarationSelectsThePinnedHead() recorded an issue at QwenMTPOpenSurfaceTests.swift:74:9: Expectation failed: (declared["revision"] as? String → nil) == "26a328e070875b0314d652a039b6b59902690f03"
✘ Test theCheckedInDeclarationSelectsThePinnedHead() recorded an issue at QwenMTPOpenSurfaceTests.swift:77:9: Expectation failed: ((declared["tensor_count"] as? Int) → nil) == (MLXFastConstants.qwenMTPHeadTensorCount → 15)
✘ Test theCheckedInDeclarationSelectsThePinnedHead() recorded an issue at QwenMTPOpenSurfaceTests.swift:87:9: Expectation failed: (declaration.sha256 → "559b24ebca354018e4402fdb1f5af1afe5a0721bd2ebf04133500d846f7d5f71") == (weightsRecord[0] → "8fceddc664f3ea96d02e304463aa1319213ff52cdf1f3401d4bce64e7075c349")
✘ Test theCheckedInDeclarationSelectsThePinnedHead() recorded an issue at QwenMTPOpenSurfaceTests.swift:88:9: Expectation failed: (declaration.bytes → 427742600) == (Int(weightsRecord[1]) → 849400347)
✔ Test transformVerifierRejectsStaleExtraGeneratedFile() passed after 9.649 seconds.
✔ Test semanticGPQAGateParsesOpusVerdictShapesAndFailsUnparseableCaseClosed() passed after 9.883 seconds.
✔ Test everyPoolEntryCarriesItsMeasuredSpreadAndPairCount() passed after 9.068 seconds.
✔ Test theNoteRecordsTheRegimeDeltaAndTheRejectionRateFinding() passed after 9.068 seconds.
✔ Test everyRetiredMTPNameStillTripsTheGuard() passed after 9.032 seconds.
✔ Test theWorkflowResolvesTheContractToTheFixtureThisSuiteReads() passed after 9.032 seconds.
✔ Test newQwenMTPSurfaceNamesCarryNoRetiredMTPName() passed after 9.032 seconds.
✔ Test benchmarkScriptPrintsNonM5CaveatOnlyForLocalTokenMismatchFailures() passed after 9.825 seconds.
✔ Test anEnabledTrackDeclaresAnImplementedTokenFidelityGate() passed after 9.032 seconds.
✘ Test theQwenMTPTrackIsArmedOnQwen38() failed after 8.988 seconds with 11 issues.
↳ /// The track is ARMED on Qwen 3.8.
↳ ///
↳ /// This assertion has now pointed both ways more than once, which is the whole
↳ /// point of it: it pinned the inert posture through the 3.6 draft, was inverted
↳ /// to pin the ARMED posture at the 2026-08-13 go-live, was inverted back for the
↳ /// 3.6 -> 3.8 model cutover, and is inverted again here now that the 3.8
↳ /// bring-up has completed. Each switch below is a one-character edit away from
↳ /// flipping, so DISARMING the track means editing this test in the same commit
↳ /// as the flags -- deliberate friction, in both directions
↳ /// (NEW-MODEL-BRINGUP 7.5).
✔ Test theTrackFixtureIsEnabledForGoLive() passed after 8.988 seconds.
✔ Test theDryRunArmAnnouncesThatNoScoreIsProduced() passed after 8.988 seconds.
✔ Test setupRecoversAgedMalformedMutationLockOwner() passed after 9.650 seconds.
✔ Test aRankedRunAgainstAnUnenabledTrackIsStillRefused() passed after 8.988 seconds.
✔ Test theRetiredMTPNameListDoesNotShrink() passed after 8.918 seconds.
✔ Test localRunLockRejectsOverlappingRunAndReclaimsStaleLock() passed after 9.751 seconds.
✔ Test nOnlyDimsAreNotSafeAsReductionDims() passed after 8.896 seconds.
✔ Test retiredNamesAreNotClearedByAQwenPrefix() passed after 8.896 seconds.
✔ Test theInterlockAdmitsTheDryRunAndOnlyTheDryRun() passed after 8.918 seconds.
✔ Test newQwenMTPFilesCarryNoRetiredMTPName() passed after 8.988 seconds.
✔ Test scoredShapesStayOnTheQMVFastPath() passed after 8.896 seconds.
✔ Test theManifestAndWorkflowAgreeOnTrackIdentity() passed after 8.896 seconds.
✔ Test theFloorNamesTheCoordinateThatIsShort() passed after 8.896 seconds.
✔ Test theScoringStepIndependentlyRefusesAnUnenabledTrack() passed after 8.896 seconds.
✔ Test aGatesOnlyDispatchIsExemptedByTheStepsOwnCondition() passed after 8.897 seconds.
✔ Test confirmTrackEnabledIsRequiredEvenForAGatesOnlyDryRun() passed after 8.896 seconds.
✔ Test goldenPinsAreRequiredEvenForAGatesOnlyDryRun() passed after 8.895 seconds.
✔ Test theManifestDeclaresTheDecodeFloorTheWorkflowEnforces() passed after 8.896 seconds.
✔ Test theRenameMatchesTheEagerLoadersOwnRule() passed after 8.684 seconds.
✔ Test theStrippedPrefixIsTheTransformsOwnTextTowerPrefix() passed after 8.684 seconds.
✔ Test theWorkerAcceptsTheProtocolNotAConcreteClass() passed after 8.684 seconds.
✔ Test anUndeterminableQwenLayoutIsRefusedRatherThanGuessed() passed after 8.684 seconds.
✔ Test theStripRunsBeforeSanitizeAndQuantize() passed after 8.684 seconds.
✔ Test aNonQwenBackboneIsRefusedLoudly() passed after 8.684 seconds.
✔ Test theRawReferenceIsAWrappedModelAndItsPrefixIsKept() passed after 8.684 seconds.
✔ Test theHeadStagingInclusionIsDocumentedWhereItIsActionable() passed after 8.684 seconds.
✔ Test theTransformedTreeIsATextModelAndItsPrefixIsStripped() passed after 8.684 seconds.
✔ Test theNestedStructureDecidesRegardlessOfTheModelTypeSpelling() passed after 8.684 seconds.
✔ Test bothQwenClassesConformAndTheVendoredOneIsQualified() passed after 8.684 seconds.
✔ Test theEnumeratedNamesAreTheOnesTheFilesUse() passed after 8.897 seconds.
✔ Test poolsideReferenceDownloadersUseSourceSpecificQueryHandling() passed after 9.720 seconds.
✔ Test aRankedRunRequiresEightDistinctTimedTargets() passed after 8.895 seconds.
✔ Test aPublicGateReportCarryingWorkerOutputIsWithheldOnSubmissionBranches() passed after 9.842 seconds.
✔ Test aMalformedPoolEntryIsRefusedOnEveryDispatchNotOneInEight() passed after 9.032 seconds.
✔ Test benchmarkScriptForwardsLocalSubmitFlagToSwiftBenchmark() passed after 9.720 seconds.
✔ Test dflashAcquiresAndReleasesTheLocalRunLock() passed after 8.609 seconds.
✔ Test metallibBuilderStalledLockCreatorCannotPublishIntoReplacementLock() passed after 9.750 seconds.
✔ Test dflashImportsAndCallsTheResidentModelScan() passed after 8.609 seconds.
✔ Test extractedLockPathMatchesTheSerialLockPath() passed after 8.609 seconds.
✔ Test serialStillDefinesTheFunctionsDFlashExtracts() passed after 8.609 seconds.
✔ Test dflashReusesTheSerialLockDefinitionsRatherThanCopyingThem() passed after 8.578 seconds.
✔ Test setupAndMetallibLocksPublishWithBSDAndGNUCoreutils() passed after 9.651 seconds.
✔ Test everyReusedDefinitionFailsClosedOnItsOwn(_:) with 5 test cases passed after 8.609 seconds.
✔ Test setupStalledLockCreatorCannotPublishIntoReplacementLock() passed after 9.675 seconds.
✔ Test setupParallelShardDownloadsEmitProgress() passed after 9.835 seconds.
✔ Suite QwenQMVParityTests passed after 9.885 seconds.
✔ Test theMetallibIsBesideTheTestBundleWhenGatedTestsAreRequested() passed after 8.312 seconds.
✔ Test directoryDigestPreservesLegacyDigestWithBoundedUncachedReads() passed after 8.298 seconds.
✔ Test theCleanupStepRemovesScratchTheStepShellDidNot() passed after 8.236 seconds.
✔ Test theCheckoutDoesNotPersistCredentials() passed after 8.236 seconds.
✔ Test theWorkflowDoesNotClaimHiddenBytesAreAlwaysRemoved() passed after 8.233 seconds.
✔ Test theScriptsTheWorkflowInvokesAreExecutable() passed after 8.241 seconds.
✔ Test metallibBuilderRejectsAmbiguousCMakeOutputs() passed after 9.752 seconds.
✔ Test aScratchPathWithoutTheSweptPrefixIsRefused() passed after 8.180 seconds.
✔ Test theProbeEndsWithAJanitorResetAndIntegrityAudit() passed after 8.180 seconds.
✔ Test theGuardRunsBeforeAnythingTouchesTheCredentials() passed after 8.180 seconds.
✔ Test dflashOrphanScanMatchesThroughTheExtractedResidentPattern() passed after 8.636 seconds.
✔ Test theTwoDispatchTop32RunsAtItsAuditedSelectionGeometry() passed after 8.338 seconds.
✔ Test onlyMainMayDispatchTheProbe() passed after 8.175 seconds.
✔ Test theProbeIsDispatchOnly() passed after 8.173 seconds.
✔ Test thePrimaryStripAndHeadMergeProduceTheTextModelNamespace() passed after 8.173 seconds.
✔ Test aTreeCarryingBothSpellingsIsRefused() passed after 8.171 seconds.
✔ Test theTrackScopedBaselineConstantsAreDistinctAndDocumented() passed after 8.168 seconds.
✔ Test theCustomTop32KernelMatchesArgPartitionIncludingHeavilyTiedRows() passed after 8.338 seconds.
✔ Test theSharedGPQAFloorStaysAtTheDFlashCalibration() passed after 8.168 seconds.
✔ Test theQwenWorkflowMirrorsTheTrackScopedGPQAFloor() passed after 8.168 seconds.
✔ Test "the fused kernel still has the shape this audit measured" passed after 8.141 seconds.
✔ Test "the two arithmetic properties the eager chain depends on are still in the source" passed after 8.138 seconds.
✔ Test theManifestStillDeclaresTheFixedWindow() passed after 8.127 seconds.
✔ Test theDocumentedCeilingMatchesTheManifest() passed after 8.103 seconds.
✔ Test theTrustedDriverStillOwnsTheWindowLengthByCount() passed after 8.103 seconds.
✔ Test theHeadManifestNamesThePinnedRepositoryAndRevision() passed after 8.046 seconds.
✔ Test theHeadManifestIsWellFormed() passed after 8.046 seconds.
✔ Test theEditableSessionContinuesPastEosWithoutTheOverlay() passed after 8.127 seconds.
✔ Test theWorkflowShapePinsMatchTheCheckedInManifest() passed after 8.043 seconds.
✔ Test theHeadTensorCountIsOneConstant() passed after 8.043 seconds.
✔ Test dflashRefusesToStartAgainstAnOrphanedModelHoldingWorker() passed after 8.610 seconds.
✔ Test unsignableObjectKeysAreRefusedBeforeAnyRequest() passed after 8.030 seconds.
✔ Test submissionStaticReviewDiffModeFailsClosedAndSendsOnlyChangedFiles() passed after 9.662 seconds.
✔ Test theProbeNeverEchoesObjectBytes() passed after 8.221 seconds.
✔ Test probeScratchIsRunScopedAndRemovedWhenTheStepEnds() passed after 8.221 seconds.
✔ Test theProbeSweepsAnEarlierRunsAbandonedScratch() passed after 8.180 seconds.
✔ Test aMissingResidentPatternFailsClosedRatherThanScanningForNothing() passed after 8.552 seconds.
✔ Test aMissingKeyCountsAsProbed() passed after 8.173 seconds.
✔ Test theSuppliedCountIsDerivedFromTheInputAndCatchesADroppedKey() passed after 8.173 seconds.
✔ Test everyR2SecretReferenceLivesInTheEnvironmentBoundJob() passed after 7.981 seconds.
✔ Test dflashBindsThePrivateMaterialEnvironment() passed after 7.981 seconds.
✔ Test metallibBuilderPreservesPublishedOutputWhenCopyFails() passed after 9.715 seconds.
✔ Test theSuppliedCountAgreesWithTheLoopOnEveryAdvertisedInputForm() passed after 8.173 seconds.
✔ Test aRankedSelectionSamplesFromAPopulatedPool() passed after 7.957 seconds.
✔ Test aRankedRunStillSelectsATimedTargetAndTimes() passed after 7.889 seconds.
✔ Test aCleanDryRunReachesTheParityGateAndSkipsPoolSelection() passed after 7.889 seconds.
✔ Test theCorrectnessGoldenIsPinnedIndependentlyOfThePool() passed after 7.889 seconds.
✔ Test aGatesOnlyGoldenPreparationSkipsTheTimedGolden() passed after 7.973 seconds.
✔ Test aRankedSelectionStillFailsClosedOnAnUnusablePool() passed after 7.889 seconds.
✔ Test theHeadOverrideReachesTheCandidateLegOnly() passed after 7.771 seconds.
✔ Test theHeadBooleansAreRecordedRatherThanAsserted() passed after 7.771 seconds.
✔ Test localDFlashScriptsNameNoRetiredMTPSurface() passed after 7.766 seconds.
✔ Test localDFlashRunnerReusesTheCorrectnessGateAndTheCoolDownEntrypoint() passed after 7.766 seconds.
✔ Test everyUploadRetryIsSignedAfresh() passed after 8.030 seconds.
✔ Test runbookExistsAndCoversTheStepsTheWorkflowsCite() passed after 7.561 seconds.
✔ Test runbookRecordsTheLimitsBeingAcceptedAtGoLive() passed after 7.561 seconds.
✔ Test runbookDescribesMedianOfEightScoring() passed after 7.561 seconds.
✔ Test theByteBudgetChargesExemptPathsSeparately() passed after 7.116 seconds.
✔ Test onlyTheHeadWeightsPathIsExemptFromTheSourceByteBudget() passed after 7.116 seconds.
✔ Test theHeadPathsAreOptionalAndEverySourcePathIsNot() passed after 7.116 seconds.
✔ Test noOpensslDigestParsesTheTextOutputFormat() passed after 7.094 seconds.
✔ Test localDFlashScriptsInvokeOnlySubcommandsAndFlagsTheCLIDeclares() passed after 7.767 seconds.
✔ Test theWholeSigningChainReproducesOnePinnedEndToEndSignature() passed after 7.094 seconds.
✔ Test theCanonicalRequestMatchesAnIndependentSigV4Implementation() passed after 7.094 seconds.
✔ Test everyRetryIsSignedAfresh() passed after 8.031 seconds.
✔ Test "real transformed Qwen checkpoint matches library and custom paths" passed after 7.038 seconds.
✔ Test transformAndAttachRouteThroughTheParentToolSandbox() passed after 6.402 seconds.
✔ Test theStringToSignMatchesTheAWSDocumentationWorkedExample() passed after 7.085 seconds.
✔ Test parentToolSandboxProfileDeniesNetworkForkExecAndResolver() passed after 6.402 seconds.
✔ Test runtimeWorkerAllowlistDropsSSHAgentSocket() passed after 6.272 seconds.
✔ Test runtimeWorkerFallbackProfileDeniesResolverMachLookup() passed after 6.272 seconds.
✔ Suite QwenDraftReadoutExactnessTests passed after 9.886 seconds.
✔ Test theTrustedMaximumIsTheSameEightEverywhere() passed after 6.170 seconds.
✔ Test theLedgerClosesOnActualDraftsRatherThanTheOffer() passed after 6.170 seconds.
✔ Test theShippedDraftPolicyKeepsAnUnmodifiedTreeAtTwo() passed after 6.170 seconds.
✔ Test parentToolSandboxIsArmedFailClosedAndReentryGuarded() passed after 6.402 seconds.
✔ Test theNonDraftingRefusalIsGoneButItsKindSurvives() passed after 6.170 seconds.
✔ Test theSerialControlMayNeverDraft() passed after 6.170 seconds.
✔ Suite E58DispatchStormTests passed after 9.886 seconds.
✔ Test effectiveDepthIsSummarisedFromTheParentsOwnJournal() passed after 6.170 seconds.
✔ Test theRowLedgerClosesOverAWholeWindow() passed after 6.112 seconds.
✔ Test fullAcceptanceAndZeroAcceptanceAreBothReachable() passed after 6.110 seconds.
✔ Test onlyATargetMismatchEndsTheAcceptedPrefix() passed after 6.110 seconds.
✔ Test theExtraVerifyRowNeverChangesTheCount() passed after 6.110 seconds.
✔ Test eosInsideAnAcceptedPrefixDoesNotEndTheWindow() passed after 6.110 seconds.
✔ Test theCountIsTheLongestCommonPrefixAndEosIsJustAToken() passed after 6.110 seconds.
✔ Test theSequencePositionComesFromATrimmableCache() passed after 5.984 seconds.
✔ Test rollbackClearsTheVendoredDepthOneRollbackState() passed after 5.984 seconds.
✔ Test rollbackDoesNothingToACacheAlreadyAtTheBase() passed after 5.984 seconds.
✔ Test rollbackTrimsTheWholeVerifyWindowAndNeverTrimsRecurrentCaches() passed after 5.981 seconds.
✔ Test theSnapshotSurvivesAnInPlaceWriteToTheLiveCache() passed after 5.978 seconds.
✔ Test aStackWithNoTrimmableCacheReportsAnImpossibleOffset() passed after 5.978 seconds.
✔ Test theDepthFlagHasACanonicalSpellingAndARefusedAmbiguity() passed after 5.906 seconds.
✔ Test theTwoHeadBooleansAreOnePredicate() passed after 5.906 seconds.
✔ Test thePayloadCarriesEveryFieldTheBoxWrapperReads() passed after 5.906 seconds.
✔ Test theArrayAndTheTrioAgreeIncludingOnEvenLengthWindows() passed after 5.906 seconds.
✔ Test rowsPerRoundMatchesTheWrapperEquation() passed after 5.906 seconds.
✔ Test theBlockArrayLengthIsTheRoundCount() passed after 5.906 seconds.
✔ Test theRankedGateAssertsOnlyFieldsTheReportEmits() passed after 5.906 seconds.
✔ Test aLedgerThatLeavesTheRejectedTailUnpricedIsRejected() passed after 5.906 seconds.
✔ Test aLedgerDeclaringMoreRowsThanTheTrustedMaximumAllowsIsRejected() passed after 5.906 seconds.
✔ Test excludingTheFirstBlockRescuesAWarmupAndStillCatchesAStall() passed after 5.906 seconds.
✔ Test parityAllOKRequiresBothExactnessAndACloseLedger() passed after 5.906 seconds.
✔ Test theTimedPayloadSatisfiesBothStallGuardrailRoutes() passed after 5.906 seconds.
✔ Test thePinnedReferenceDirectoryIsProvenanceNotAWeightsSource() passed after 5.906 seconds.
✔ Test theControlDepthIsOneConstantAcrossEveryConsumer() passed after 5.906 seconds.
✔ Test dflashRunsPastTheGuardWhenNothingIsResident() passed after 8.554 seconds.
✔ Test bothVerbsAreDeclaredInTheShapeTheCallersGrepFor() passed after 5.906 seconds.
✔ Test theSerialControlIsDepthZeroAndItsLedgerCloses() passed after 5.906 seconds.
✔ Test theRowAccountingEquationsCloseOnAnHonestLedger() passed after 5.906 seconds.
✔ Test thePayloadKeyExtractorFindsRealKeys() passed after 5.906 seconds.
✔ Test redactedCategoryCarriesNoDetailFromTheViolation() passed after 5.828 seconds.
✔ Test nvfp4DatatypeHelpersStayInAOTAndJITSources() passed after 5.789 seconds.
✔ Test theScriptSigningChainReproducesThePinnedSignature() passed after 7.086 seconds.
✔ Test theStockTreeMustClearTheSubmissionFloor() passed after 5.755 seconds.
✔ Test generationRunsFromThePinnedBaseline() passed after 5.670 seconds.
✔ Test theEmittedRequestMatchesWhatWasSigned() passed after 8.028 seconds.
✔ Test theScriptsTheWorkflowInvokesAreExecutable() passed after 5.670 seconds.
✔ Test everyInterlockArmRefusesAndEmitsNoOutputs() passed after 5.670 seconds.
✔ Test onlyMainMayDispatchProvisioning() passed after 5.670 seconds.
✔ Test aStalledAttemptTimesOutAndIsRetriedRatherThanSucceedingEmpty() passed after 8.028 seconds.
✔ Test provisioningBindsTheSameEnvironmentAsTheRankedJob() passed after 5.670 seconds.
✔ Test theOutputFileIsTruncatedBetweenAttempts() passed after 8.028 seconds.
✔ Test theGuardRefusesMaterialItCannotMeasure() passed after 5.670 seconds.
✔ Test noAttemptDelegatesRetryToCurl() passed after 8.028 seconds.
✔ Test theGuardRejectsAmendmentTensMeasuredDegenerateProfile() passed after 5.670 seconds.
✔ Test theGuardAcceptsProse() passed after 5.670 seconds.
✔ Test aMissingSeedNeverFallsBackToSeedGenerate() passed after 5.670 seconds.
✔ Test theProvisioningJobIsDispatchOnly() passed after 5.670 seconds.
✔ Test provisioningPublishesNoScoreAndTouchesNoRankedState() passed after 5.670 seconds.
✔ Test trustedWorkerDoesNotCallTheEditableApply() passed after 5.614 seconds.
✔ Test startupPolicyApplyStaysInternal() passed after 5.614 seconds.
✔ Test everyViolationKindRedactsToItsOwnCategory() passed after 5.828 seconds.
✔ Test theThresholdsAreNotEnvironmentOverridable() passed after 5.670 seconds.
✔ Test aRankedGoldenPreparationRefusesAnEmptySelection() passed after 7.891 seconds.
✔ Test theGateRejectsADeletedFixMarker(_:) with 2 test cases passed after 5.515 seconds.
✔ Test theGatePassesOnTheWorkingTree() passed after 5.515 seconds.
✔ Test theGateRejectsTheHistoricalOverlayBlob() passed after 5.515 seconds.
✔ Test theGatePassesOnTheCommittedSession() passed after 5.515 seconds.
✔ Test theSerialHiddenGoldenIsScrubbedBeforeTheDFlashGoldenIsInstalled() passed after 5.515 seconds.
✔ Test thePublicBehaviorGatePrecedesEveryHiddenDownload() passed after 5.515 seconds.
✔ Test theDFlashGoldenIsInstalledBeforeTheDFlashParityGate() passed after 5.515 seconds.
✔ Test theTwoBitSinglerowArmIsStillGatedOnExactlyTheDeclaredHeadWidth() passed after 5.339 seconds.
✔ Test rowSlicesOfTheHeadStillFallThroughToTheGenericArm() passed after 5.339 seconds.
✔ Test everyPoolEntryKeyMatchesWhereItWasUploaded() passed after 5.214 seconds.
✔ Test theCorrectnessGoldenKeyMatchesWhereItWasUploaded() passed after 5.214 seconds.
✔ Test provenanceCarriesTheDeclaredSource() passed after 5.193 seconds.
✔ Test anAbsentDeclarationResolvesToThePinnedHead() passed after 5.193 seconds.
✘ Test theCheckedInDeclarationSelectsThePinnedHead() failed after 5.193 seconds with 6 issues.
↳ /// The checked-in declaration is the DEFAULT, and the default is pinned.
↳ ///
↳ /// The file itself was MISSING until 2026-08-14 -- the contract change
↳ /// added `mtp-head.manifest.json` to `editablePaths` and to
↳ /// `optionalEditablePaths`, and named it in the README, the runbook and the
↳ /// static-review prompt, but never authored it. That is not a cosmetic gap:
↳ /// this test read it through `parse(contentsOf:)`, which treats an
↳ /// unreadable path as a REFUSAL rather than as absence, so the one test
↳ /// asserting the shipped default threw instead of checking it. Absence
↳ /// would still have resolved to `pinned` through `resolve(contractRoot:)`
↳ /// -- asserted separately below -- but the default a participant reads
↳ /// before editing has to exist to be read.
✔ Test abrokenDeclarationIsARefusalAndNeverFallsBackToPinned() passed after 5.193 seconds.
✔ Test theHeadTreeDigestIsStableOrderIndependentAndContentSensitive() passed after 5.193 seconds.
✔ Test thelegalDeclarationShapesParse() passed after 5.193 seconds.
✔ Suite SetupDFlashTargetIdentityTests passed after 9.889 seconds.
✔ Suite "DFlash gates-only artifact leak" passed after 9.889 seconds.
✔ Suite "Fused residual RMSNorm bit-identity" passed after 9.889 seconds.
✔ Suite "Qwen-MTP trusted-context guards" passed after 9.889 seconds.
✘ Suite QwenMTPScoringSemanticsTests failed after 9.889 seconds with 5 issues.
↳ /// The operator-ratified scoring change of 2026-08-14: a ranked run times ALL
↳ /// eight pool prompts and publishes the MEDIAN of the eight RAW
↳ /// serial-relative speedups. SERIAL = 1.0.
↳ ///
↳ /// This supersedes the 2026-08-13 normalised rule, which divided each prompt by
↳ /// its pinned no-op reference and therefore made an UNMODIFIED candidate score
↳ /// exactly 1.0 by construction -- declaring the shipped depth-2 speculative
↳ /// decoder to be the zero point when it is measurably a ~6.5% regression
↳ /// against true serial decode. The no-op references survive as informational
↳ /// diagnostics and divide nothing.
↳ ///
↳ /// Everything here reads checked-in text or runs pure arithmetic. The point of
↳ /// the suite is that the rule is stated identically in the four places that
↳ /// have to agree -- the contract fixture, the track manifest, the ranked
↳ /// workflow and the operator runbook -- because the previous single-sample rule
↳ /// was also stated in all four, and a change that updates three of them leaves
↳ /// the fourth as a confident lie.
✔ Suite QwenMTPNoopReferenceProvenanceTests passed after 9.889 seconds.
✘ Suite QwenMTPTrackNamingTests failed after 9.889 seconds with 11 issues.
↳ // NAMING LAW FOR THE QWEN 3.8 NATIVE-MTP TRACK (qwen3.8-27b-mtp-v1).
↳ //
↳ // The organizer's rule is that no retired Gemma/Laguna MTP surface name may
↳ // survive anywhere in the repository, as a substring, in any new name. The
↳ // existing guard is BenchmarkScriptTests'
↳ // `localDFlashScriptsNameNoRetiredMTPSurface` (the local DFlash scripts do not
↳ // NAME them); its companion `retiredMTPNamesStayRetired` (the retired FILES
↳ // stay deleted) went with DFlashTrackTests at the 2026-08-13 repo split. Both
↳ // were written for the DFlash track and neither knows about this one.
↳ //
↳ // Renaming must be TESTED, not just done, and the test has to run in BOTH
↳ // directions:
↳ //
↳ //   NEGATIVE  every entry of `retiredMTPNames` must still trip the guard's own
↳ //             substring check. A track that "passes the guard" because the
↳ //             guard was quietly narrowed has proven nothing.
↳ //   POSITIVE  every name this track introduces must be proven free of all of
↳ //             them -- enumerated here by name, so a future rename is caught by
↳ //             this file rather than by a reviewer reading a diff.
↳ //
↳ // THE TRAP THIS FILE EXISTS FOR. The retired names are matched as SUBSTRINGS,
↳ // so a "qwen-" prefix does NOT make a retired name new: the natural spellings
↳ // for this track's workflow, its probe verb and its reference-row artifacts all
↳ // reproduce a retired string with a prefix in front of it. Only changing what
↳ // follows "mtp-" clears the rule. `retiredNamesAreNotClearedByAQwenPrefix`
↳ // below pins exactly that, so the reasoning survives even if everyone who
↳ // remembers it has moved on.
↳ //
↳ // Nothing here loads a model, runs a benchmark or needs a network: every
↳ // assertion reads checked-in text.
✔ Suite "DFlash enablement interlock" passed after 9.889 seconds.
✔ Suite QwenQMVCostCurveTests passed after 9.889 seconds.
✔ Suite QwenMTPBackboneLayoutTests passed after 9.889 seconds.
✔ Suite "DFlash local run lock" passed after 9.889 seconds.
✔ Suite QwenMTPMetallibAvailabilityTests passed after 9.889 seconds.
✔ Suite "DFlash R2 key probe workflow" passed after 9.889 seconds.
✔ Suite QwenDraftTop32SelectionTests passed after 9.889 seconds.
✔ Suite QwenMTPWeightRenameTests passed after 9.889 seconds.
✔ Suite QwenMTPTrackScopedConstantsTests passed after 9.889 seconds.
✔ Suite "Fused residual RMSNorm source gate" passed after 9.889 seconds.
✔ Suite QwenMTPFixedWindowSourceGuardTests passed after 9.889 seconds.
➜ Test chunkChangesTheAttentionOutputBitwise() skipped: "set MLXFAST_E57_DISPATCH_COUNT=1 to run the GPU probe"
✔ Suite QwenMTPHeadManifestTests passed after 9.889 seconds.
✔ Suite "DFlash private material binding" passed after 9.889 seconds.
✔ Suite QwenMTPHeadProvenanceGateTests passed after 9.889 seconds.
✔ Suite LocalDFlashScriptSurfaceTests passed after 9.889 seconds.
✔ Suite QwenMTPGoLiveRunbookTests passed after 9.889 seconds.
✔ Suite QwenMTPOptionalEditablePathTests passed after 9.889 seconds.
✔ Suite "R2 SigV4 signing" passed after 9.889 seconds.
✔ Suite Qwen35ReferenceParityTests passed after 9.889 seconds.
✔ Suite ParentToolSandboxTests passed after 9.888 seconds.
✔ Suite QwenMTPDepthFreedomTests passed after 9.888 seconds.
✔ Suite QwenMTPFixedWindowTests passed after 9.888 seconds.
✔ Suite QwenMTPRollbackContractTests passed after 9.888 seconds.
✔ Suite QwenMTPPayloadSchemaTests passed after 9.888 seconds.
✔ Suite "DFlash redactor kind coverage" passed after 9.888 seconds.
✔ Suite QwenMTPLaunchInvariantTests passed after 9.889 seconds.
✔ Suite DFlashTrustedEditableCouplingTests passed after 9.889 seconds.
✔ Suite DFlashHiddenGoldenOccupancyTests passed after 9.888 seconds.
✔ Suite QwenDraftReadoutGatePremiseTests passed after 9.888 seconds.
✔ Suite "DFlash golden R2 keys" passed after 9.888 seconds.
✘ Suite QwenMTPHeadDeclarationTests failed after 9.888 seconds with 6 issues.
◇ Test localThermalGateRunsAtEveryPhaseBoundary() started.
✔ Suite E57SdpaChunkDispatchCountTests passed after 9.889 seconds.
◇ Test nvfp4Group16SplitKUsesKernelTileAlignment() started.
✔ Test aRankedGoldenPreparationStillPinsTheTimedGolden() passed after 7.992 seconds.
✔ Test nvfp4Group16SplitKUsesKernelTileAlignment() passed after 0.031 seconds.
✔ Suite "DFlash dry run reaches its own gate" passed after 9.920 seconds.
◇ Test nvfp4NAXGatherFactoryUsesDeclaredTemplateNames() started.
✔ Test nvfp4NAXGatherFactoryUsesDeclaredTemplateNames() passed after 0.004 seconds.
◇ Test nvfp4PackedNibblesDecodeLowNibbleFirstWhenRuntimeTestsAreEnabled() started.
✔ Test nvfp4PackedNibblesDecodeLowNibbleFirstWhenRuntimeTestsAreEnabled() passed after 0.002 seconds.
◇ Test nvfp4U8E4M3ScalesDecodePerGroupWhenRuntimeTestsAreEnabled() started.
✔ Test nvfp4U8E4M3ScalesDecodePerGroupWhenRuntimeTestsAreEnabled() passed after 0.001 seconds.
◇ Test nvfp4Group16SplitKMatchesNonSplitReferenceWhenRuntimeTestsAreEnabled() started.
✔ Test nvfp4Group16SplitKMatchesNonSplitReferenceWhenRuntimeTestsAreEnabled() passed after 0.009 seconds.
◇ Test affineGroup32SplitKRemainsUnchangedWhenRuntimeTestsAreEnabled() started.
✔ Test offlineRunnerForwardsSignalReceivedBeforeCommandLaunch() passed after 6.265 seconds.
◇ Test metallibLockReleaseRefusesOwnerAndPublishedLinkReplacement() started.
✔ Test affineGroup32SplitKRemainsUnchangedWhenRuntimeTestsAreEnabled() passed after 0.198 seconds.
◇ Test nvfp4ActualSharedProjectionShapesMatchNonSplitReferenceWhenRuntimeTestsAreEnabled() started.
✔ Test theGateRejectsAReintroducedDefectMarker(_:) with 3 test cases passed after 5.789 seconds.
✔ Suite QwenMTPSyncContentGateTests passed after 10.160 seconds.
✔ Test metallibLockReleaseRefusesOwnerAndPublishedLinkReplacement() passed after 0.168 seconds.
◇ Test benchmarkRejectsMalformedStagedShardWithoutReplacingWeights(caseName:expectedError:) started.
◇ Test case passing 2 arguments caseName → "tensor-inventory", expectedError → "tensor inventory disagrees with the index" to benchmarkRejectsMalformedStagedShardWithoutReplacingWeights(caseName:expectedError:) started.
✔ Test nvfp4ActualSharedProjectionShapesMatchNonSplitReferenceWhenRuntimeTestsAreEnabled() passed after 0.172 seconds.
◇ Test nvfp4GatherRoutedDecodeAndPrefillShapesMatchNonGatherReferenceWhenRuntimeTestsAreEnabled() started.
✔ Test theGenerationArgvNeverCarriesSeedGenerate() passed after 6.188 seconds.
✔ Test noAttemptFollowsRedirects() passed after 8.710 seconds.
✔ Test everyAttemptBoundsConnectAndTotalTime() passed after 8.911 seconds.
✔ Test localThermalGateRunsAtEveryPhaseBoundary() passed after 0.934 seconds.
◇ Test benchmarkScriptPreservesCompactJSONIntegrityMetrics() started.
benchmark.sh: reusing /var/folders/hg/g1_843ts0sv67jyk1mhhc83c0000gn/T/2FCC7E67-1405-4479-9DA1-00CEC141EA34/weights/ because MLXFAST_SKIP_TRANSFORM=1
◇ Test case passing 2 arguments caseName → "unindexed-shard", expectedError → "contains unindexed shard" to benchmarkRejectsMalformedStagedShardWithoutReplacingWeights(caseName:expectedError:) started.
✔ Test nvfp4GatherRoutedDecodeAndPrefillShapesMatchNonGatherReferenceWhenRuntimeTestsAreEnabled() passed after 0.955 seconds.
✔ Suite NVFP4QuantizedMMTests passed after 11.263 seconds.
{"score":null,"passed":true,"metrics":{"weights_hash":"compact-hash","weights_file_count":7,"weights_byte_count":11}}
✔ Test benchmarkScriptPreservesCompactJSONIntegrityMetrics() passed after 0.743 seconds.
◇ Test benchmarkScriptRejectsWeightsPathsThatCanEraseWorkspace(weightsPath:) started.
◇ Test case passing 1 argument weightsPath → "." to benchmarkScriptRejectsWeightsPathsThatCanEraseWorkspace(weightsPath:) started.
◇ Test case passing 1 argument weightsPath → ".." to benchmarkScriptRejectsWeightsPathsThatCanEraseWorkspace(weightsPath:) started.
✔ Test benchmarkScriptRejectsWeightsPathsThatCanEraseWorkspace(weightsPath:) with 2 test cases passed after 0.144 seconds.
◇ Test benchmarkScriptRejectsTrackedSourcesAsWeightsPath() started.
✔ Test everyGoldenVerificationArmRefuses() passed after 7.622 seconds.
✔ Test forceSignedBypassesAWorkingAWSCLI() passed after 10.005 seconds.
✔ Test benchmarkScriptRejectsTrackedSourcesAsWeightsPath() passed after 0.241 seconds.
◇ Test benchmarkScriptPreservesUnmanagedExternalWeightsDirectory() started.
◇ Test case passing 2 arguments caseName → "range-hole", expectedError → "staged safetensors shard has an invalid header: model.safetensors" to benchmarkRejectsMalformedStagedShardWithoutReplacingWeights(caseName:expectedError:) started.
benchmark.sh: regenerating weights with Swift transform
✔ Test benchmarkScriptPreservesUnmanagedExternalWeightsDirectory() passed after 0.199 seconds.
◇ Test benchmarkScriptCanInstallIntoEmptyExternalWeightsDirectory() started.
benchmark.sh: regenerating weights with Swift transform
◇ Test case passing 2 arguments caseName → "trailing-byte", expectedError → "staged safetensors shard has an invalid header: model.safetensors" to benchmarkRejectsMalformedStagedShardWithoutReplacingWeights(caseName:expectedError:) started.
{"score":null,"passed":true,"metrics":{"weights_hash":"hash","weights_file_count":1,"weights_byte_count":12}}
✔ Test pinsComeFromTheReDownloadedObjectAndNotTheLocalFile() passed after 8.596 seconds.
✔ Test benchmarkScriptCanInstallIntoEmptyExternalWeightsDirectory() passed after 0.665 seconds.
◇ Test benchmarkScriptRejectsReservedTransformMetadataSymlink() started.
✔ Test thePayloadHashIsTheContentDigestForExoticInputPaths() passed after 11.072 seconds.
✔ Suite "R2 request execution" passed after 12.931 seconds.
benchmark.sh: regenerating weights with Swift transform
✔ Test benchmarkRejectsMalformedStagedShardWithoutReplacingWeights(caseName:expectedError:) with 4 test cases passed after 2.881 seconds.
◇ Test hashWeightsRejectsSymlinkRootAndNonregularEntryWithoutWritingOutput() started.
✔ Test hashWeightsRejectsSymlinkRootAndNonregularEntryWithoutWritingOutput() passed after 0.010 seconds.
✔ Suite ShellGapRegressionTests passed after 13.085 seconds.
✔ Test benchmarkScriptRejectsReservedTransformMetadataSymlink() passed after 0.384 seconds.
◇ Test benchmarkScriptPreservesWeightsWhenTransformOutputIsIncomplete() started.
benchmark.sh: regenerating weights with Swift transform
✔ Test theSeedFetchRefusesAnythingThatIsNotAProseSeedPlan() passed after 9.204 seconds.
✔ Suite "DFlash golden provisioning" passed after 13.421 seconds.
✔ Test benchmarkScriptPreservesWeightsWhenTransformOutputIsIncomplete() passed after 0.346 seconds.
◇ Test benchmarkScriptValidatesStagedJSONIndexAndSafetensorsBeforeReplacement() started.
benchmark.sh: regenerating weights with Swift transform
benchmark.sh: regenerating weights with Swift transform
benchmark.sh: regenerating weights with Swift transform
benchmark.sh: regenerating weights with Swift transform
benchmark.sh: regenerating weights with Swift transform
benchmark.sh: regenerating weights with Swift transform
✔ Test benchmarkScriptValidatesStagedJSONIndexAndSafetensorsBeforeReplacement() passed after 2.399 seconds.
◇ Test benchmarkScriptRejectsReservedTransformRollbackSymlink() started.
benchmark.sh: regenerating weights with Swift transform
✔ Test benchmarkScriptRejectsReservedTransformRollbackSymlink() passed after 0.433 seconds.
◇ Test benchmarkScriptRejectsTrackedScorePath() started.
✔ Test benchmarkScriptRejectsTrackedScorePath() passed after 0.058 seconds.
◇ Test benchmarkScriptRejectsWorkspaceAsVerificationTemporaryDirectory() started.
benchmark.sh: reusing /var/folders/hg/g1_843ts0sv67jyk1mhhc83c0000gn/T/59593471-6F50-4EAA-91DB-EC1C6A4E7E03/weights/ because MLXFAST_SKIP_TRANSFORM=1
✔ Test benchmarkScriptRejectsWorkspaceAsVerificationTemporaryDirectory() passed after 0.154 seconds.
◇ Test benchmarkScriptPreservesVerificationTemporaryRootContents() started.
benchmark.sh: reusing /var/folders/hg/g1_843ts0sv67jyk1mhhc83c0000gn/T/C6D223BB-B4F9-4AAA-BF7F-E63D861540D9/weights/ because MLXFAST_SKIP_TRANSFORM=1
benchmark.sh: verifying weights match a fresh run of the submitted Swift transform
{"score":null,"passed":true,"metrics":{"weights_hash":"hash","weights_file_count":1,"weights_byte_count":12}}
✔ Test benchmarkScriptPreservesVerificationTemporaryRootContents() passed after 0.417 seconds.
◇ Test artifactStagingRejectsTraversalWithoutDeletingDestination() started.
✔ Test artifactStagingRejectsTraversalWithoutDeletingDestination() passed after 0.004 seconds.
◇ Test artifactStagingRejectsSymlinkedRunRoot() started.
✔ Test artifactStagingRejectsSymlinkedRunRoot() passed after 0.004 seconds.
◇ Test artifactStagingCreatesOwnedRunRootAndCopiesFiles() started.
benchmark: staged artifacts in /var/folders/hg/g1_843ts0sv67jyk1mhhc83c0000gn/T/3DA07792-B29D-443C-B3BD-412BE21E4487/mlxfast-artifacts-run/benchmark-results
✔ Test artifactStagingCreatesOwnedRunRootAndCopiesFiles() passed after 0.025 seconds.
◇ Test redactedBenchmarkFailureCategorizesPublicGateWorkerCrashWithoutQuotingWorkerStderr() started.
✔ Test redactedBenchmarkFailureCategorizesPublicGateWorkerCrashWithoutQuotingWorkerStderr() passed after 0.014 seconds.
◇ Test redactedBenchmarkFailureCategorizesPublicGateTokenMismatchWithoutTokenValues() started.
✔ Test redactedBenchmarkFailureCategorizesPublicGateTokenMismatchWithoutTokenValues() passed after 0.014 seconds.
◇ Test redactedBenchmarkFailureCollapsesUnknownPublicGateErrorToOpaqueCategory() started.
✔ Test redactedBenchmarkFailureCollapsesUnknownPublicGateErrorToOpaqueCategory() passed after 0.014 seconds.
◇ Test redactedBenchmarkFailureKeepsNoScoreUnlessPublicGateReportShowsFailure() started.
✔ Test redactedBenchmarkFailureKeepsNoScoreUnlessPublicGateReportShowsFailure() passed after 0.028 seconds.
◇ Test redactedBenchmarkFailurePrefersScoreDerivedCategoryOverPublicGateReport() started.
✔ Test redactedBenchmarkFailurePrefersScoreDerivedCategoryOverPublicGateReport() passed after 0.034 seconds.
◇ Test redactedBenchmarkFailureReportsAcceptanceBandSeparatelyFromCorrectness() started.
✔ Test redactedBenchmarkFailureReportsAcceptanceBandSeparatelyFromCorrectness() passed after 0.028 seconds.
◇ Test offlineRunnerRemovesProfilesAndPreservesCommandFailure() started.
run-offline.sh: network egress and child process execution are blocked; running: /private/var/folders/hg/g1_843ts0sv67jyk1mhhc83c0000gn/T/A8B7BBFA-AAEC-4E95-90F4-5F34377E08B8/command.sh 
✔ Test offlineRunnerRemovesProfilesAndPreservesCommandFailure() passed after 0.305 seconds.
◇ Test offlineRunnerForwardsTerminationAndRemovesProfiles() started.
run-offline.sh: network egress and child process execution are blocked; running: /private/var/folders/hg/g1_843ts0sv67jyk1mhhc83c0000gn/T/5051ABC8-6BCB-481A-9BF6-F6C8FC15FCC3/command.sh 
✔ Test offlineRunnerForwardsTerminationAndRemovesProfiles() passed after 0.263 seconds.
◇ Test pairedTimingOverlayRejectsWeightsChangedAfterGates() started.
✔ Test pairedTimingOverlayRejectsWeightsChangedAfterGates() passed after 0.018 seconds.
◇ Test pairedTimingOverlayRejectsHarnessChangedAfterGates() started.
✔ Test pairedTimingOverlayRejectsHarnessChangedAfterGates() passed after 0.015 seconds.
◇ Test pairedTimingOverlayAcceptsMatchingHarnessAndWeightsIdentity() started.
overlay-paired-timing: passed=true score=1 decode_speedup=1 prefill_speedup=1
✔ Test pairedTimingOverlayAcceptsMatchingHarnessAndWeightsIdentity() passed after 0.045 seconds.
◇ Test finalArtifactValidatorEnforcesTimingAndIntegrityContracts() started.
✔ Test finalArtifactValidatorEnforcesTimingAndIntegrityContracts() passed after 0.567 seconds.
◇ Test benchmarkScriptStagesTransformInItsOnlySandboxWritableDirectory() started.
benchmark.sh: regenerating weights with Swift transform
{"score":null,"passed":true,"metrics":{"weights_hash":"hash","weights_file_count":1,"weights_byte_count":12}}
✔ Test benchmarkScriptStagesTransformInItsOnlySandboxWritableDirectory() passed after 0.620 seconds.
◇ Test benchmarkScriptPropagatesUnsandboxedTransformVerificationFailure() started.
benchmark.sh: reusing /var/folders/hg/g1_843ts0sv67jyk1mhhc83c0000gn/T/43D28347-66DF-422C-91EE-33CF89AA8D97/weights/ because MLXFAST_SKIP_TRANSFORM=1
benchmark.sh: verifying weights match a fresh run of the submitted Swift transform
✔ Test benchmarkScriptPropagatesUnsandboxedTransformVerificationFailure() passed after 0.374 seconds.
◇ Test benchmarkScriptRejectsScoreInsideSymlinkedReferenceDirectory() started.
✔ Test benchmarkScriptRejectsScoreInsideSymlinkedReferenceDirectory() passed after 0.049 seconds.
◇ Test benchmarkScriptFailsWhenTransformSourceGitInspectionFails() started.
✔ Test benchmarkScriptFailsWhenTransformSourceGitInspectionFails() passed after 0.265 seconds.
◇ Test benchmarkScriptRejectsConflictingLocalModesBeforeInvokingSwift() started.
✔ Test benchmarkScriptRejectsConflictingLocalModesBeforeInvokingSwift() passed after 0.013 seconds.
◇ Test benchmarkScriptCleansOwnedTemporaryFilesWithoutMaskingFailureStatus() started.
benchmark.sh: reusing /var/folders/hg/g1_843ts0sv67jyk1mhhc83c0000gn/T/2ECA7F4A-B3F6-445F-9E96-C9A1B2D9187A/weights/ because MLXFAST_SKIP_TRANSFORM=1
✔ Test benchmarkScriptCleansOwnedTemporaryFilesWithoutMaskingFailureStatus() passed after 0.329 seconds.
◇ Test scoredWorkerResponsesExcludeDiagnostics() started.
✔ Test scoredWorkerResponsesExcludeDiagnostics() passed after 0.011 seconds.
◇ Test semanticGPQACaptureIsWrittenOnlyAfterCorrectnessWorkerCloses() started.
✔ Test semanticGPQACaptureIsWrittenOnlyAfterCorrectnessWorkerCloses() passed after 0.002 seconds.
✔ Suite BenchmarkSafetyTests passed after 20.066 seconds.
✘ Test run with 688 tests in 49 suites failed after 20.066 seconds with 41 issues.
